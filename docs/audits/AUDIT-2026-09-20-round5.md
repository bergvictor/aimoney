# Audit 2026-09-20 round 5 — aimoney (read-only, track B)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r5`. HEAD `b70c707` (night round 4); live rev `2511e5d` (night round 3, one commit behind). No git writes, no network, no installs, no edits to existing files. Only this file created at the root. Never read `.env`, secrets or tokens. One single-stage shell command per call, no inline code, no redirection. No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` at the root; `README.md` read first, then the live snapshots in `_night/live/`, then source.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests (`README.md:3-15`, `worker/src/index.js:4-5`).
- Entry points: `public/index.html` + `public/app.js` (1709 lines, dashboard), `functions/api/[[path]].js` (740 lines, D1 REST API), `worker/src/index.js` (743 lines, research agent) + `worker/src/lib.js` (79 lines, shared pure helpers).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `/` + `POST /run`; live snapshot `_night/live/aimoney_pages_dev_api_health.html:1` at `2026-09-21T20:57:42Z`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + up to 2 briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:728-738`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema `:27` + shared migrations `:33-34` + worker `:38`) or manual `deploy/deploy.sh`, gated by `deploy/verify.sh` (public reads only).

## 2. End-to-end walk-through

Signal to decision. The hop where it most often goes silent is hop 12 (human Vet), with hop 14 (money probes) null alongside it.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:81` `redditSignals`, `:110` `githubSignals` via `timedJson` (`:37-47`, 6s timeout covering the body read). Per-query throws counted; all-failed names the source in `src_fail`, partial in `src_partial` (`:76-77`, `:105-106`, `:134-135`).
2. Store: `:363-368` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:369-372` (`phase:collected` / `phase:collected stale:N`).
3. Sweep: `:380-384` unprocessed signals older than 30d become noise (`state.stale`); a failed sweep is named (`staleFailed`, finish `:652`).
4. Take: `:386-388` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:399-426` via pure `exactUrlTarget` (`:173-183`); bounded `IN` selects (`:408-413`, failures named `prepass_select:failed`); a match links as supports (notes newest-kept `:422`, no status move); failed links skipped and counted (`prepass:N_failed`, `:420`). The notes append at `:422` is silent (F5).
6. Gates, read before classify: `:439-442` bare-without-brief > 10 caps inserts to 1/tick; `:443-446` unreviewed > 10 pauses inflow to 0/tick (`maxNewThisRun = 0`). Both count failures fall through silently (F4). Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:457-469` `buildClassifyPrompt(opps, fresh)` (`:281`) + `aiComplete` (`:141`, budget `MAX_AI_CALLS = 4`, `:19`), parsed by `parseJsonLines` (`worker/src/lib.js:64`, NDJSON + `repairJson` for `\_`). Inflow-paused ticks skip classify entirely and still brief (finish line carries `inflow:paused`, `:652`).
8. Verdict loop `:482-550`: strict action enum (`:488`), supports-without-id becomes noise (`:495-497`), overflow stays unprocessed (`:499`). `new` INSERTs (`:510-524`) with an `UNREVIEWED` note (`:520`) and capped `effectiveScore` (`worker/src/lib.js:30`, cap 6000 `:23`); slug collision links as supports (`:525-537`, select at `:529` silent — F5).
9. Verdict flush: `:551` `flushVerdictWrites` (`:300-312`); failures as `verdict:N_failed`.
10. Briefs: main pass (`:553-607`, top-scored live-only or oldest-unreviewed-first past 48h via `oldestForBrief` `:568` — silent, F5 — with `selectBareRow` `:262` failures named `bare_select:failed`) plus extra pass (`:614-649`) when the oldest unreviewed exceeds 48h (live oldest 188.6h, so both fire), via `buildBriefPrompt` (`:207`), `parseBriefJson` (`:218`), `insertBrief` (`:229`, never stores empty); poison rotation via `rememberBriefSkip` (`:256`) and `brief:skipped` (`:604`,`:644`); failures `brief:failed`. Manual runs skip briefs by deadline (`:350`, `briefDeadline = -1`).
11. Finish: `:652` single `finish("ok", …)` carrying mode/pause/stale/src/verdict/prepass markers; errors finish `error` plus `console.error("cron-failed", …)` (`:660`, also `:672`, `:736`). `scheduled` `:666-674`, `POST /run` `:728-738`.
12. Vet — the ONLY path from unreviewed to vetted, one sentence: a manual dashboard press — `public/app.js:562` `vetOpportunity` → `:538` `fetchDetailForWrite` (token-gated detail fetch, warms N+1 at `:546`) → `:567` inner → `:485` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`:574-577`) with auto-advance (`:582` via `:741-744`, focus warms N+2 at `:738` — off by one, F7). Starter variant `:629-665` (vet + planned starter + flip to `testing`, resume-safe) and Kill `:670-693` (`[date killed]` + post-mortem at `:682`); `V` vets at `:757`; drawer Vet at `:1126`. One human verdict per keypress, no bulk path.
13. API writes: `updateOpportunity` (`functions/api/[[path]].js:237`, text bounds, status/range guards, tagless-clear 400 `:271-274` against `VERDICT_TAG` `:90`, killed-clear post-mortem guard `:282-288`, `effectiveScore` `:290`); `createExperiment` (`:378`, closure gate `:392-393`, stamps `:398-399`, `revenue_source` retry `:421-429` with `dropped` `:429`) / `updateExperiment` (`:441`, stamps `:467`, gate `:471-473`, retry `:498-508`, `dropped` `:508-510`, ledger via shared `outcomeLedgerLine` `:103-108`); any other missing column 503s naming only it (`:737`, `missingColumnOf` `:122-126`).
14. Health: `functions/api/[[path]].js:526-709` — 12 probes (`:546-559`) in one batch (`:560`) with per-probe isolation fallback (`:569-598`, failures named in `health_probe_failures` + column-only `health_probe_detail`), lazy self-migration (`:607-623` via `ensureMoneyColumns` `:36-77`), `last_vetted`/`last_verdict` parsed in JS (`:657-671`) from 500-char tails (`:558`, `LIMIT 2000`), `inflow_paused` derived from the unreviewed probe (`:700`); payload `:709` (`ok:false` + nulls only on total outage; `db` is `up` when any probe answers).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 3) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (188.6h). Second: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F2) — and until it runs, money writes 503 too (F6). Third, code-silent: inflow-gate count failures (F4), three more worker failure shapes (F5), and a prefetch chain that warms the wrong row (F7).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:8`) → exit 0 this session, run once. TAP summary: `# tests 481`, `# suites 150`, `# pass 481`, `# fail 0`, `# duration_ms 90564.757708` (runner baseline `_night/tests-baseline.log`: 481/481 in 90307.1158 — same shape). Node `v22.22.3`.
- Parse/compile: `python3 -m compileall -q functions worker d1 deploy scripts public` → exit 0, no output — there are no Python sources (glob `**/*.py` matches nothing; positive control: the same tool+mode matches `UNREVIEWED` dozens of times across `worker/src/index.js` + `public/app.js` + `worker/src/lib.js`, so the zero is real). `node --check public/app.js` → exit 0 (only the sandbox `UNDICI-EHPA` proxy warning). JS parse is also pinned by the green suite itself (every file imported) plus the `shipped-JS parse gate` suite (`public/dashboard.test.mjs:1124-1151`, `node --check` every shipped JS + a broken-syntax positive control) and the deploy `dashboard-appjs parse gate` suite (green).
- Dead code: none material in shipped source. `appendKeepNewest` (`worker/src/lib.js:51-54`) is test-only by documented design (pinned JS spec of the SQL `substr(notes || ?, -8000)` idiom, `:49-50`), not dead; every worker helper (`exactUrlTarget`, `agentMoneyEstimates`, `buildBriefPrompt`, `parseBriefJson`, `insertBrief`, `selectBareRow`, `buildClassifyPrompt`, `flushVerdictWrites`) has 2+ call sites. `console.*` in shipped source is only the three deliberate `cron-failed` tail markers (`worker/src/index.js:660,672,736`) plus the design comment (`:658`). Control: the same search matches those lines, so no hidden logging.
- Duplicated logic: shared singles verified — `effectiveScore`/`tokensMatch`/`clamp10` (`worker/src/lib.js`, API import `functions/api/[[path]].js:10`, worker import `worker/src/index.js:160`), `vettedNotes`/`fetchDetailForWrite`, `buildBriefPrompt`/`parseBriefJson`/`insertBrief`, `buildClassifyPrompt`, `flushVerdictWrites`, `outcomeLedgerLine` (`:103-108`), one migration script (`deploy/apply-d1-migrations.sh`, called by `deploy/deploy.sh:40` and CI `:33-34`). Remaining mirrors are documented-deliberate: `centsDollars` ↔ dashboard `moneyCents` (`:97-99` comment), SQL `substr(notes || ?, -8000)` ↔ `appendKeepNewest`, `VERDICT_TAG` boolean gate (`:90`) vs health `matchAll` date-capture (`:663-668`, gate vs capture).
- TODO density: zero in shipped source. Regex `TODO|FIXME|HACK|XXX` over the repo hits only prose in `docs/audits/*.md`. Control: the same tool+mode matches `UNREVIEWED` across `worker/src/index.js` + `public/app.js` + `worker/src/lib.js`, so the zero is real.
- Matcher honesty (the brief's check-that-matches-nothing rule): every absence above carries its positive control in the same bullet (py→`UNREVIEWED` hits, TODO→`UNREVIEWED`, dead-code→traced importers, console→`cron-failed` hits). The suite reports its denominator (`# tests 481`, `# pass 481`) — a zero-test pass would read as `tests 0`, not success.
- Surface rules: all pass, no finding. Light-only (`public/styles.css:4` `color-scheme: light`, ink-on-paper palette; only media queries are `prefers-reduced-motion` and `max-width`, `:210-218`); favicon linked with theme-color (`public/index.html:9-10`, byte-identical in `_night/live/aimoney_pages_dev.html:9-10`); `public/favicon.svg` 284 bytes (`wc -c`), 32×32 `$` on `#0d6b3f`; brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suite `public/tab-icon.test.mjs` green inside the exit-0 run. First five seconds: the static shell (5595 bytes live, `wc -c` matches repo) renders brand + `Every way to make money with AI, ranked.` + lede with zero JS; counts fill from `…` placeholders, then the last-good snapshot, then live refresh.
- Tree: HEAD `b70c707`; live rev `2511e5d` is its parent (`git diff 2511e5d HEAD --stat`: 8 files, 399 insertions, 110 deletions — exactly the round-4 commit). `git ls-files --eol` shows all 40 tracked files `i/lf w/lf` with `eol=lf` — the CRLF warning in the brief does not apply to this checkout. Sizes (`wc -l`): `public/app.js` 1709, `functions/api/[[path]].js` 740, `worker/src/index.js` 743, `worker/src/lib.js` 79.
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (time `2026-09-21T20:57:42Z`, rev `2511e5d…`, one commit behind HEAD, F3): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:3, oldest_unreviewed_age_h:188.6, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…` — and no `inflow_paused` key (it ships in HEAD `:700` but live trails). The brief's 08:45 measurement read the same 11 unreviewed at 174.4h — the queue has not moved all day; age rises ~1d/d while cron stays healthy.

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 188.6h / 7.9d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `last_verdict` null, `hours_since_last_ok_run` 3. Was at 08:45: same 11 unreviewed at 174.4h — the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none — no `won`/`lost` row exists (`experiments_by_status:{planned:2}`, `decisions_last_7d:0`), and the money legs read null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter, run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment a human records the first verdict. The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.9 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:562` `vetOpportunity` → shared `vettedNotes` (`:485-488`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id` (`:574-577`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:520`: `— UNREVIEWED, scores capped until a human vets it`) and every other `UNREVIEWED` site in `index.js` is a create/count/select site — zero clearing paths and zero `vetted` strings in the worker. So `vetted_last_7d` 0 with `hours_since_last_ok_run` 3 is human-gated-by-design, not a broken automation. Vet-round report as requested: path `public/app.js:562`; cause human-gated-by-design — there is no failing test because nothing is broken, and the passing real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (green inside the exit-0 run) is the positive proof; a single run of the path (one Vet press) produces exactly unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today. Health already dates the stall without being told: `last_vetted`/`last_verdict` (`functions/api/[[path]].js:657-671`, `:709`), both null live.

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:443-446`, classify skipped `:457-458`), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change for the verdict itself — the two-minute path (`V`/`K` + auto-advance, one verdict per keypress, token-gated, N+1 prefetch `public/app.js:546`, detail-before-write `:538-560`) already ships with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines (`docs/OWNER-ACTIONS.md:7-20`). Touch no repo files for this finding. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live: `revenue_last_7d/revenue_total/spent_total` null with `health_probe_failures:[revenue_week,revenue_lifetime]`, `health_probe_detail:{…:revenue_cents}`, `schema_missing_columns:[revenue_cents,spent_cents]`, `schema_migration:disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)`. The two failing probes are the only ones reading money (`functions/api/[[path]].js:553-554`, `SUM(revenue_cents)` / `SUM(revenue_cents/spent_cents)`). `d1/schema.sql:5` is `CREATE TABLE IF NOT EXISTS` (no-op on the existing table); the ADD COLUMNs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied after `schema.sql` by both CI (`.github/workflows/deploy-worker.yml:27` then `:33-34` `Apply D1 migrations in sorted order` via `./deploy/apply-d1-migrations.sh`) and `deploy/deploy.sh:36` then `:40` — one shared loop + tolerate-list (`deploy/apply-d1-migrations.sh:13-28`, duplicate-column no-op, anything else exits 1). The in-worker fallback is deliberately off: `functions/api/[[path]].js:58` (`if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` → report missing + disabled, change nothing). Migration-ask results, all green this session in the `self-migration switch` suite (9/9): old schema + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers on the same call (`:63-76`, one ADD at a time, duplicate-column tolerated); var unset leaves the table unchanged and names the missing columns plus the disabled line; second run is a clean no-op (decided outcomes cached `:33-34`, transient PRAGMA/empty answers never cached `:44-55`). Exact variable name: `ALLOW_SCHEMA_MIGRATION`. Exact CI step (already in place, no change needed): `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: earned-to-date is unknown rather than zero, so the money-first question cannot be answered from the project's own data — and until the columns land, money writes 503 too (F6). (The decisions COUNT split, `functions/api/[[path]].js:552`, already saved `decisions_last_7d` = 0 — the money legs are the only remaining nulls.)

Fix: no code change; the path is already correct. The owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var — the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, and re-runs the failed money probes on the same call — or adds the two Cloudflare repo secrets and redeploys so CI applies the migrations. One variable, one decision; see §5 for the exact lines. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F3. Live still serves the round-3 revision — one shipped round is committed but not deployed

Evidence. Live `/api/health` reports `rev: 2511e5d…`, which equals the night-round-3 commit `2511e5d` (`git log`: `Night round 3 (aimoney)`), while the clone HEAD is `b70c707` (`Night round 4 (aimoney): vet-path proof, inflow-paused bit, worker failure markers`). `git diff 2511e5d HEAD --stat` is exactly that one commit (8 files, 399 insertions, 110 deletions). Proof on the wire: live health carries no `inflow_paused` key, while HEAD computes it at `functions/api/[[path]].js:700` with green tests (`health inflow-paused bit`, 4/4 in this run).

Why it costs: the round-4 changes — the paused-triage bit and the silent-failure markers — are merged but unserved, so the live surface (and the owner's phone) does not reflect the repository: a paused triage still reads as clean on live. Trust cost, not a code defect: the code is right, it is just not running.

Fix: no code change. The owner merges/deploys the branch (or opens the Pages dashboard and checks the latest deployment status) — one line in §5. The next audit should confirm live rev == HEAD and `inflow_paused:true` present. Touch no repo files. Size S. Risk none (deploy-only; `deploy/verify.sh` gates the rollout on public reads).

### F4. Inflow-gate count failures finish `ok` with no marker — a D1 blip silently unpauses the funnel

Evidence. `worker/src/index.js:439-446`: `try { const bareRow = await …; if (…) maxNewThisRun = 1; } catch { /* bare count failed; keep the default cap */ }` and `try { const unRow = await …; if (…) maxNewThisRun = 0; } catch { /* unreviewed count failed; keep the bare-gated cap */ }`. No count, no marker, no log — and the finish line (`:652`) carries no gate marker (it names `stale:failed`, `prepass_select:failed`, `bare_select:failed` since round 4, but nothing for these two). On live (unreviewed 11, bare 19) a failed unreviewed count keeps the cap at 1 instead of 0 — the paused funnel silently inserts — and a failed bare count keeps 2 instead of 1. The tick finishes `ok` with a finish line byte-identical to a healthy paused tick.

Why it costs: the overnight-reliability shape on the machine's input gate — the exact check-that-reads-as-success the brief bans. Debugging "why did 2 proposals land while the queue was at 11" starts from a green board and a clean run log.

Fix: count each gate failure and carry a compact conditional marker on the existing finish line next to the round-4 markers (e.g. `bare_count:failed`, `unreviewed_count:failed`), keeping every existing marker byte-identical and the fallback caps exactly as today (marker-only, no behavior change). Each shape gets a stubbed-D1 test that throws on exactly that count: run still finishes `ok`, marker present, caps fall back as today. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk low (log-string-only; no signal, verdict, brief, cap, or AI-budget changes).

### F5. Three more worker failure shapes finish `ok` with no marker and quietly lose work

Evidence. (1) Pre-pass notes append: `worker/src/index.js:422` — `…UPDATE opportunities SET notes = substr(notes || ?, -8000)…` `.run().catch(() => null)` after `state.updated++` (`:421`): the link is counted but the corroborating evidence is dropped, so the count lies. (2) Slug-collision select: `:529` — `SELECT id … WHERE slug = ?` `.first().catch(() => null)`; on failure `existing` is null and the signal is marked `processed=1` with no parent (`:535`) — a D1 blip turns would-be supports into noise, inflating `noise_24h` and burying the evidence. (3) Brief-age select: `:568` — `SELECT created_at … UNREVIEWED …` `.first().catch(() => null)`; on failure `oldestForBrief` is null, `ageMsForBrief` is NaN, the main brief falls back to top-scored (`:571-574`) and the extra brief is skipped (`:616`, NaN is never > 48h) — on the live 188h backlog the wrong row is briefed and a slot is lost. All three finish `ok` with a finish line (`:652`) indistinguishable from a healthy tick.

Why it costs: failure visibility on evidence and brief slots — each shape degrades the pass for that tick (lost corroboration, misclassified signals, misdirected briefs) while every surface reports green.

Fix: name each with a compact conditional marker beside the existing ones (e.g. `prepass_notes:N_failed`, `slug_select:failed`, `brief_age:failed`); for the slug select, leave the signal unprocessed on select failure so a later tick retries it (instead of noise-ing it) — failure-path-only, reversible, and a persistently failing select still ages honestly toward the 30d sweep. One stubbed-D1 test per shape: run finishes `ok`, marker present, verdict/brief behavior otherwise unchanged; the slug test asserts the signal stays `processed = 0`. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk low (two markers are log-only; the slug change touches only the select-failure path, never the happy path).

### F6. Two planned experiments, zero running, zero decisions — the money path stalls at planned, then 503s

Evidence. Live `experiments_by_status:{planned:2}`, `decisions_last_7d:0`; no `running`/`won`/`lost` keys. The one-tap moves exist and are human-pressed: planned→`running` stamps `started_at` (`functions/api/[[path]].js:467`), Win/Lose closes with human-entered cents + optional source; the API closure gate holds (won/lost require `result` + `post_mortem`, `:471-473`, `ended_at` stamped `:473`, outcome ledger appended `:480` via shared `outcomeLedgerLine` `:103-108`). But every experiment write carries the money columns (`:487-488` args, `:493-494` SQL), the retry covers only `revenue_source` (`:498-508`), and any other missing column 503s naming only it (`:737`, `missingColumnOf` `:122-126`). The live table lacks the cents columns (F2), so Start and Win/Lose on live 503 today with the switch one-liner — the outage blocks the lane metric, not just the ledger.

Why it costs: vetted → experiment → decision is the lane metric, and it reads 0/0/0 with the first step blocked. Signals seen and proposals added are attempts, not outcomes; without one running row there is no number within a week and no amount/date/source to make real.

Fix: first the owner enables F2 (one variable — Start works on the very next call after the columns land). Then, after F1 vets the parent row, the owner presses Start on the $0-capital starter, runs the smallest real-world test inside a week, then closes via Win/Lose with a human-entered $ amount and a one-line post-mortem (`docs/OWNER-ACTIONS.md:43-54`). No code change; the dashboard, API gate, stamps, ledger, and 503 hint already enforce the shape. Touch no repo files. Size S (one human decision + the sprint). Risk none to the repo (token-gated PATCHes; no auto-transitions; never mark decided without evidence).

### F7. The review prefetch warms the wrong row — every verdict pays a detail GET it should not

Evidence. Verdicting row N prefetches N+1 (`public/app.js:546`: `prefetchReviewDetail(nextReviewId(id)); // verdicting N warms N+1; never awaited`), but the post-verdict `refreshTargets` (`:579`) wipes the whole detail cache (`:1682`: `if (opps) { …; invalidateDetailCache(); }`), and the auto-advance then focuses N+1, which warms N+2 (`:738`: `prefetchReviewDetail(nextReviewId(id)); // focusing N warms N+1; never awaited`). Net: after every verdict the next target (N+1) is cold and its prefetch bytes were discarded whenever they resolved before the wipe (the common case — the prefetch starts first with the smaller payload), while N+2 sits warm a verdict too early. The next `fetchDetailForWrite` (`:547-551`) misses and pays a full detail GET. The existing prefetch tests (round-3 suite, green) drive prefetch→verdict in isolation and never pass through `refreshTargets`, which is why this shipped. Waste per queue clear: 11 discarded prefetches + 11 avoidable GETs.

Why it costs: waste on the bottleneck path (human-bottleneck rule #1 is sub-minute phone verdicts) — each V/K costs a needless round trip, and the discarded prefetch doubles the bytes. On a phone connection this is the difference between instant and laggy auto-advance.

Fix: warm the focused row itself in `focusReviewRow` (prefetch `id` alongside `nextReviewId(id)`), so the post-refresh focus re-arms exactly the row the next verdict will need; keep the wholesale invalidation on refresh untouched (it is the documented safety). Pin with a dashboard test that drives focus→verdict→focus→verdict against a counted stub api and asserts the second verdict issues zero detail GETs. Touch `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk low (prefetch-only; the verdict write path and the invalidation safety are unchanged).

### F8. Ten ledger columns on a 360px phone — score and $/mo compete with meters nobody acts on in list view

Evidence. The ledger renders 10 columns (`_night/live/aimoney_pages_dev.html:59-70` thead: #, Opportunity, Status, Score, Value, Effort, Conf., Fit, $/mo, First $; rows at `public/app.js:190-202` with four `meter()` cells). The wrap scrolls horizontally (`public/styles.css:107`: `.table-wrap { overflow-x: auto; … }`) and the only narrow-screen rule shrinks the masthead (`:213-218` `@media (max-width: 760px)`), hiding nothing in the table. On the owner's phone the most important truth per row (title, score, $/mo) shares 360px with four 1–10 meter columns he never acts on without opening the drawer (which keeps the full meters).

Why it costs: first-five-seconds on the surface he actually opens — the verdict call needs title/score/$/mo/capital/next, and horizontal scrolling per row slows the 11-verdict clear F1 asks for.

Fix: hide the four meter columns (Value, Effort, Conf., Fit) under the existing 760px breakpoint via a small CSS rule (add classes to the four `th` cells and the four `meter()` `td` cells, or nth-child), keeping rank/title/status/score/$/mo/first-$ visible; desktop renders byte-identical and the drawer keeps every meter. Pin with a dashboard/tab-icon-style test asserting the hiding rule exists and no dark-mode payload ships with it. Touch `public/index.html`, `public/app.js`, `public/styles.css`, `public/dashboard.test.mjs` (pin only). Size S. Risk low (CSS-only effect below 760px; no data, order, or score changes).

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, or auto-testing without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless. `functions/api/[[path]].js:16-27` (switch docblock), `:58` (unset → report + change nothing), `:63-76` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate for decided outcomes. Do not default it on, do not add a second trigger, and do not add a migration endpoint: `a URL that alters the schema is a liability even when it is authenticated.`
- Never a data-losing migration here: ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- `revenue_source` arrives only via CI/manual migration, never via the self-migration (probe-read cents only, `README.md:83`). Writes against a table missing it retry once without it and name the drop (`dropped: ["revenue_source"]`, `functions/api/[[path]].js:429,508`); any other missing column 503s naming only it (`:737`). Do not widen the retry to cents — a dropped $ figure is worse than a loud 503.
- Manual `POST /run` skips briefs by design (30s `waitUntil` cap, `worker/src/index.js:7-11,350`); the 202 discloses it (`:738`, `briefs_skipped:true`, `reason: manual skips brief pass`) and the dashboard button is labeled `Run triage now (briefs on cron)` (`public/index.html:26`). Do not move briefs onto the manual path.
- The detail cache is dropped wholesale on every opportunities refresh by design (`public/app.js:510-514`, enforced at `:1682`), `so verdicts never write from replaced rows`. F7 adds a post-refresh prefetch only — do not weaken the invalidation.
- Owner actions (human-only, one line each): (1) clear Needs-review oldest-first with V/K, 11 rows, ~1 min/row (`docs/OWNER-ACTIONS.md:7-20`); (2) set `ALLOW_SCHEMA_MIGRATION=1` as a Pages production env var and retry the latest deployment (`docs/OWNER-ACTIONS.md:22-33`); (3) add `CF_API_TOKEN` + `CF_ACCOUNT_ID` repo secrets and redeploy so CI applies `revenue_source` (`docs/OWNER-ACTIONS.md:35-41`; workflow fails fast without them, `.github/workflows/deploy-worker.yml:20-26`); (4) deploy HEAD so live stops trailing at `2511e5d` (F3); (5) Start one $0 starter and drive it to won/lost with a human-entered $ + post-mortem (`docs/OWNER-ACTIONS.md:43-54`). Never spend, create accounts, enter payment/personal details, send anything outward, accept terms, or submit forms — draft, queue, and leave the decision to him.

## 6. Proposed next tasks (2–4)

The brief's vet-nothing, migration-switch, deploy-converge, health-isolation, and last-verdict rounds are shipped — F1–F3 carry the file:line proof and the remaining work is owner actions (§5), not code. The four tasks below carry the still-open explicit angles instead (human-bottleneck #1 phone clear, reliability, failure visibility, waste, first-five-seconds), most valuable first. Every task runs offline against stubbed D1/static files — no network, installs, credentials, logins, or owner questions.

- Task 1: Prefetch the focused review row so each verdict costs zero detail GETs
  - Owned files: `public/app.js`, `public/dashboard.test.mjs` (pin only).
  - Acceptance: `focusReviewRow` warms the focused `id` itself (post-refresh, so the wholesale invalidation at `:1682` cannot wipe it); a counted-stub test drives focus→verdict→focus→verdict and asserts the second verdict issues zero detail GETs; wholesale invalidation on refresh unchanged; `npm test` green.
- Task 2: Name inflow-gate count failures instead of finishing ok silently
  - Owned files: `worker/src/index.js`, `worker/src/index.test.js`.
  - Acceptance: each gate catch (`:439-442` bare, `:443-446` unreviewed) appends a compact conditional marker to the finish line beside the round-4 markers; fallback caps byte-identical (marker-only); two stubbed-D1 tests (one per count throwing, incl. unreviewed 11) finish `ok` with the marker present; `npm test` green.
- Task 3: Name the three remaining silent worker failures with markers
  - Owned files: `worker/src/index.js`, `worker/src/index.test.js`.
  - Acceptance: pre-pass notes append (`:422`), slug-collision select (`:529`), and brief-age select (`:568`) each carry a compact conditional marker on the finish line; slug-select failure leaves the signal unprocessed for retry instead of noise-ing it; three stubbed-D1 tests (one per shape) finish `ok` with the marker present; quiet ticks byte-identical; `npm test` green.
- Task 4: Hide value/effort/conf/fit meters on narrow phone screens
  - Owned files: `public/index.html`, `public/app.js`, `public/styles.css`, `public/dashboard.test.mjs` (pin only).
  - Acceptance: under the 760px breakpoint the four meter columns hide while rank/title/status/score/$/mo/first-$ stay visible; desktop renders unchanged; meters remain in the drawer; a static test pins the hiding rule and the tab-icon suite stays green; `npm test` green.
