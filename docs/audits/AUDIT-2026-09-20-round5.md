# Audit 2026-09-20 round 5 — aimoney (read-only)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r5`. HEAD `812ba25`, tree clean before this file (`git status --short` empty); no network, no installs, no edits to existing files. Only this file created. Never read `.env`/secrets/tokens.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests.
- Entry points: `public/index.html` + `public/app.js` (dashboard), `functions/api/[[path]].js` (D1-backed REST API), `worker/src/index.js` (research agent).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `https://aimoney-research.levitinvlad.workers.dev/`; live rev `d4262cd`, one commit behind HEAD `812ba25`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:710-721`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema + shared migrations + worker) or manual `deploy/deploy.sh`, gated by `deploy/verify.sh`.

## 2. End-to-end walk-through

Signal to decision; the hop where it most often goes silent is hop 12.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:78` `redditSignals`, `:106` `githubSignals` via `timedJson` (`:37`, 6s timeout covering the body read). All-queries-failed names the source in `src_fail`.
2. Store: `:354-359` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:360-363` (`phase:collected`).
3. Sweep: `:367-372` unprocessed signals older than 30d become noise (`stale:N`).
4. Take: `:374-376` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:385-413` via pure `exactUrlTarget` (`:168`); bounded `IN` selects (`:391-401`); a match links as supports (notes newest-kept, no status move); a failed link is skipped and counted (`prepass:N_failed`), never thrown.
6. Gates, read before classify: `:425-429` bare-without-brief > 10 caps inserts to 1/tick; `:430-433` unreviewed > 10 pauses inflow to 0/tick. Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:444-457` runs only with fresh signals AND `maxNewThisRun > 0`; `buildClassifyPrompt` (`:272`) + `aiComplete` (`:136`), parsed by `parseJsonLines` (`worker/src/lib.js:64`). Paused ticks skip classify, log `inflow:paused`, and still brief.
8. Verdict loop `:469-537`: first-wins validation, strict action enum, numeric-or-null id. `new` INSERTs (`:496-508`) with an `UNREVIEWED` note (`:507`) and capped `effectiveScore` (`worker/src/lib.js:30`, cap 6000 `:23`); overflow stays unprocessed (`:486`); slug collision links as supports (`:516-523`).
9. Verdict flush: `:538` `flushVerdictWrites` (`:291`, one batch with per-write fallback; failures as `verdict:N_failed`).
10. Briefs: main `:550-589` (top-scored live-only, or oldest-unreviewed-first past 48h) plus extra `:596-631` when the oldest unreviewed exceeds 48h, via `buildBriefPrompt` (`:202`), `parseBriefJson` (`:213`), `insertBrief` (`:224`, never stores empty); poison rotation `:248-265`; failures `brief:failed`.
11. Finish: `:634` single `finish("ok", …)` carrying mode/pause/stale/src_fail markers; errors `:636-644` finish `error` plus the `console.error("cron-failed", …)` tail marker (`:642`, also `:653`, `:717`).
12. Vet — the path that moves unreviewed to vetted: `public/app.js:545` `vetOpportunity` → `:526` `fetchDetailForWrite` (token-gated detail fetch, N+1 prefetch `:529`) → `:550` inner → `:473` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id`. Starter variant `:599` adds a planned experiment and flips the row to `testing` (retry-safe resume `:592-626`). Kill (`:638`, drawer `:1080`) writes `[date killed]` + post-mortem. `V`/`K` keys (`:715-730`) with auto-advance (`:710`): one human verdict per keypress, no bulk path.
13. API writes: `updateOpportunity` (`functions/api/[[path]].js:227`, text bounds, status/range guards, tagless-clear 400 `:261-265` with calendar-checked `VERDICT_TAG` `:87`, `effectiveScore` `:267`); `createExperiment` (`:355`) / `updateExperiment` (`:416`, won/lost require `result` + `post_mortem`, stamps, shared `outcomeLedgerLine` `:95`, cents clamps, `revenue_source` retry `:396-405`/`:471-480`).
14. Health: `functions/api/[[path]].js:497-675` — 12 probes (`:513-530`) in one batch (`:531`) with per-probe isolation fallback (`:540-569`, failures named in `health_probe_failures` + column-only `health_probe_detail` `:557`), lazy self-migration (`:578-594`), `last_vetted`/`last_verdict` parsed in JS (`:628-641`) from bounded tails; payload `:675` (`ok:false` + nulls only on total outage).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet: cron ticks succeed (`hours_since_last_ok_run` 2.8) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (182.4h and rising ~1d/d). Second: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F2). Third, new this round: two dashboard honesty gaps that mislead exactly while the owner clears the queue (F4, F5).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:8`) → exit 0 this session, every suite in the visible window `ok` (full TAP ~120KB, tool window truncates; exit code observed). Totals pinned by the same-week Windows baseline `_night/tests-baseline.log:287-294`: 426 tests / 136 suites, 426 pass, 0 fail, 0 skipped, `duration_ms 90277.8687`. Positive control against a zero-test pass: dozens of observed `ok N - …` lines plus exit 0 (a 0-test run prints no suites).
- Parse: `node --check public/app.js` → exit 0 (only the sandbox `UNDICI-EHPA` proxy warning on stderr). `python3 -m compileall -q functions worker d1 deploy scripts public` → exit 0, no output (JS repo, no Python inputs; JS parse is pinned by the shipped-JS parse-gate suite with a broken-syntax positive control inside the green run).
- Dead code: none in shipped source. `console.` over `functions/api/[[path]].js`, `worker/src/index.js`, `worker/src/lib.js`, `public/app.js` hits only the three deliberate `console.error("cron-failed", …)` markers (`worker/src/index.js:642,654,718`) plus one comment mentioning `console.*` (`:640`); `debugger` matches nothing. Control: the `console.` tool+mode is the same one matching `UNREVIEWED` 70+ times across those files, so the three is real.
- Duplicated logic: one trivial case — `reviewBacklogBit` (`public/app.js:329-337`) inlines the `fmtAgeH` (`:300-301`) age buckets instead of calling it. Everything else is shared: `effectiveScore`/`tokensMatch` (`worker/src/lib.js:30,39`, API import `functions/api/[[path]].js:10`), `vettedNotes`, `fetchDetailForWrite`, brief/classify/insert helpers, `flushVerdictWrites`, `outcomeLedgerLine`, one migration script.
- TODO density: zero in shipped source. `TODO|FIXME|HACK|XXX` over the repo matches only two prose lines in `docs/audits/*.md`, zero in shipped source. Control: the same tool+mode matches `UNREVIEWED` 70+ times in shipped files, so the zero is real.
- Surface rules: light-only (`public/styles.css:4` `color-scheme: light`; `prefers-color-scheme` and `data-theme` match nothing in `public/index.html`, `public/styles.css`, `public/app.js`). Favicon linked (`public/index.html:9-10`); `public/favicon.svg` is 284 bytes (`wc -c`), 32×32 `$` on `#0d6b3f`; brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suites green inside the exit-0 run. No surface findings. Live dashboard shell is byte-identical to the repo (`cmp _night/live/aimoney_pages_dev.html public/index.html`, exit 0, no output).
- Tree: `git status --short` empty before this file; HEAD `812ba25` (`Night round 4 … retry-safe starter, calendar-checked verdict tags, guarded drawer skills parse`), parent `d4262cd` = live rev; `.gitattributes:4` pins `eol=lf`.
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1`: `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:2.8, oldest_unreviewed_age_h:182.4, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. Prior snapshots: same 11 unreviewed at 178.7–181.9h — the queue has not moved across rounds; the age rises ~1d/d.

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 182.4h / 7.6d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted`/`last_verdict` null, `hours_since_last_ok_run` 2.8. Was: 0 vetted/week, 0 decisions/week, same 11 unreviewed — seven rounds shipped and the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none measured — `revenue_total`, `revenue_last_7d`, `spent_total` all null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter, run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment a human records the first verdict. The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.6 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:545` `vetOpportunity` → shared `vettedNotes` (`:473`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id` (`functions/api/[[path]].js:227`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:507`: `` `… — UNREVIEWED, scores capped until a human vets it …` ``) and every other `UNREVIEWED` site in `index.js` is a create/count/select site — zero clearing paths. So `vetted_last_7d` 0 with `hours_since_last_ok_run` 2.8 is design, not failure: no guard misfires, no budget exhausts, no query returns wrong candidates, no exception is swallowed — cron triage + briefs run and verdicts stay human-gated. What a single run of the path produces is certified by the real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs:2003`, green): one Vet press yields unreviewed −1, `vetted_last_7d` +1, `last_vetted` = today. Health already dates the stall: `last_vetted` + `last_verdict` (`functions/api/[[path]].js:628-641`, payload `:675`; live shows both null).

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:430-433`, classify skipped `:444-457`, `inflow:paused` on the finish line `:634`), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change — the 2-minute path (`V`/`K` + auto-advance, `public/app.js:715-730`, one verdict per keypress, token-gated; N+1 prefetch `:512-529`, so each verdict costs one round trip; inline decision + brief lines `:205-222`, no drawer round-trip) already shipped with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row. Touch no repo files. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live: `revenue_last_7d/revenue_total/spent_total` null with `health_probe_failures:[revenue_week,revenue_lifetime]`, `health_probe_detail:{…:revenue_cents}`, `schema_missing_columns:[revenue_cents,spent_cents]`, `schema_migration:disabled…`. The two failing probes are the only ones reading money (`functions/api/[[path]].js:524-525`, `SUM(revenue_cents)` / `SUM(revenue_cents/spent_cents)`). `d1/schema.sql` is all `CREATE TABLE IF NOT EXISTS` (no-op on the existing table); the ADD COLUMNs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied after `schema.sql` by both CI (`.github/workflows/deploy-worker.yml:27` then `:33-34` `Apply D1 migrations in sorted order` via `./deploy/apply-d1-migrations.sh`) and `deploy/deploy.sh:36` then `:40` — one shared loop + tolerate-list (`deploy/apply-d1-migrations.sh:13-28`, duplicate-column no-op, anything else exits 1), pinned by the convergence tests (incl. the withhold-one-file positive control). The in-worker fallback is deliberately off: `functions/api/[[path]].js:53` (`if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` → report missing + disabled, change nothing). Migration-ask results, all green in this session's exit-0 run: old schema + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers on the same call; var unset leaves the table unchanged and names the missing columns; second run is a clean no-op (`self-migration switch` suite). Exact variable name: `ALLOW_SCHEMA_MIGRATION`. Exact CI step: `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: earned-to-date is unknown rather than zero, so the money-first question cannot be answered from the project's own data. (The decisions COUNT split, `:523`, already saved `decisions_last_7d` = 0 — the money legs are the only remaining nulls.)

Fix: no code change; the path is already correct. The owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var — the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, and re-runs the failed money probes on the same call — or adds the two Cloudflare repo secrets and redeploys so CI applies the migrations. One variable, one decision; see §5 for the exact lines. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F3. Live serves the previous commit — round-4 vet-path fixes are shipped but not serving

Evidence. Live `/api/health` `rev` is `d4262cd3b3a446b234a591ef8ad96859803a5738`; HEAD is `812ba25` (`Night round 4 … retry-safe starter, calendar-checked verdict tags, guarded drawer skills parse`), whose parent is `d4262cd`. HEAD adds the Vet-&-starter resume (`public/app.js:592-626`), the calendar-checked verdict guard (`functions/api/[[path]].js:87`, health parse `:634-640`), and the guarded drawer skills parse (`public/app.js:1033-1038`); live predates all three. `cmp` on the dashboard shell is byte-identical (exit 0) — the drift is in `app.js`/API, not `index.html`, exactly matching round 4's file list.

Why it costs: the owner clears 11 rows without the retry-safe starter (a failed tap can still duplicate the planned starter live), with the loose verdict-tag guard live (an impossible tag can still poison `last_verdict`), and with the unguarded drawer parse live (one malformed row still bricks its own drawer).

Fix: deploy HEAD — Pages auto-deploys on push to `master` (`scripts/stamp-release.sh` stamps the full SHA + release), then run `deploy/verify.sh` and require PASSED: the live-revision gate (`deploy/verify.sh:66-106`) plus agent-freshness (`:116-124`) fail closed. The worker leg needs the two repo secrets (§5); without them ship Pages-only and say so. Touch no repo files. Size S. Risk low (426 green + the verify gate).

### F4. Review filter prints a false all-clear during a list outage

Evidence. On an `/api/opportunities` failure, `refreshTargets` records the failure and substitutes an empty list (`public/app.js:1641`: `.catch(() => { state.apiFailures.push("/api/opportunities"); return { opportunities: [] }; })`), then wipes memory (`:1646`: `state.opportunities = opps.opportunities || []`), so `refreshReview` derives an empty queue (`:281-288`). The chip count/age are guarded (`:292-296` keep the last-good values), and the sibling renders have failure branches (`renderLedger` `:193-194`: `Could not load the priority list — see the banner above and retry.`; `renderExperiments` `:883-884` likewise) — but `renderReview` (`:224-247`) has none: its empty branch (`:247`) prints `Review queue empty — every agent proposal has been vetted or killed.` Clicking Needs-review mid-outage therefore declares all 11 rows vetted.

Why it costs: the one sentence the owner reads while clearing the queue becomes a lie at exactly the moment he acts on it — a failure-visibility bug on the lane's critical path, in the class the standing rules call out (a check whose nothing-matched output is indistinguishable from success).

Fix: mirror the `renderLedger` failure branch in `renderReview`: when `state.apiFailures` includes `/api/opportunities`, render the `Could not load…` row instead of the empty-queue line. Pin in `public/dashboard.test.mjs`: failed opps fetch + review filter asserts the failure row and never the all-clear text; healthy empty queue still prints the all-clear. Touch `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk low (render-only; refresh, cache, and PATCH shapes untouched).

### F5. Cached-strip Vet/Kill before the first refresh is a silent no-op

Evidence. On a cold boot with a last-good cache, `paintLastGood` renders tappable Vet/Kill for the cached top pick (`public/app.js:1605-1609`) while `state.opportunities`/`state.reviewList` are still `[]`. All three verdict entries gate on `fetchDetailForWrite` (`:546`, `:600`, `:639`: `if (!await fetchDetailForWrite(id, …)) return;`), which resolves the row from the in-memory lists (`:528`) and returns `o || null` on both the cache-hit (`:533`) and fetch (`:541`) paths — `null` when the row is not loaded yet, after burning one detail GET. The caller then returns with no toast, no modal, nothing: the owner tapped Vet on the first paint, the tap vanished.

Why it costs: the Start-here strip is the first-five-seconds surface on a phone, and its Vet button is dead-but-tappable for the whole first-load window (up to the 15s read timeout on a slow network) — the exact tap F1 needs him to make, eaten silently.

Fix: in `fetchDetailForWrite`, when the row is absent from the loaded lists, toast `List still loading — try again in a moment.` (mirroring the existing `<Action> failed` toast shape) and return null before the wasted GET; keep the token gate first. Pin in `public/dashboard.test.mjs`: empty lists + cached id asserts the toast and zero PATCH/GET; loaded lists behave exactly as today. Touch `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk low (pre-write guard only; verdict, prefetch, and refresh paths untouched).

### F6. Decisions fallback counts a failed experiments fetch as zero

Evidence. `renderExperiments` (`public/app.js:880`): `const decisions = healthDecisions !== null ? healthDecisions : fallbackDecisions(exps);` — but on an `/api/experiments` failure `state.experiments` is `[]` (`:1642-1647`), so `fallbackDecisions([])` returns 0. When the decisions probe is also null (double failure: the D1 decisions probe failing while `/api/experiments` fails), the summary line (`:886-892`) prints `0 decisions this week` from an empty failed list — unknown presented as zero, the shape the money directive bans. (Single failures are fine: health wins whenever present, and a healthy empty list is a true 0.)

Why it costs: narrow but principled — the lane-metric line can fabricate a 0 the same way `revenue_total: 0` once did, and the stall nudge (`:896`) keys off the same value.

Fix: at the call site, use the fallback only when `/api/experiments` is not in `state.apiFailures`, else `decisions = null` (the `:886` guard and the `:896` nudge already handle null). Pin in `public/dashboard.test.mjs`: decisions-probe-null + failed exps fetch asserts no `0 decisions` text and no nudge; probe-null + loaded list still falls back. Touch `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk low (header line only; API, probes, and fallback helper untouched).

### F7. Two planned experiments, zero running, zero decisions — the money path stalls at planned

Evidence. Live `experiments_by_status:{planned:2}`, `decisions_last_7d:0`; no `running`/`won`/`lost` keys. The seed starter is still planned by name: `d1/seed.sql:96-105` (`Spec-ad sprint: 10 brands, 10 free ads`, `planned`, `metric: replies; pilots closed`, `target: >=3 replies; >=1 pilot at >=$500`). The one-tap moves already exist and are human-pressed: `startExperiment` (`public/app.js:806`, PATCH `running`, API stamps `started_at`), `loseExperiment` (`:823`) and `winExperiment` (`:850`, human-entered $ → `revenue_cents` + optional spend/source), all token-gated. The API closure gate holds: won/lost require `result` + `post_mortem` (`functions/api/[[path]].js:443-448`), `ended_at` stamped, outcome ledger appended (`:453-459`, shared `outcomeLedgerLine` `:95`).

Why it costs: vetted → experiment → decision is the lane metric, and it reads 0/0/0. Signals seen and proposals added are attempts, not outcomes; without one running row there is no number within a week and no amount/date/source to make real.

Fix: no code change. After F1 vets the parent row, the owner presses Start on the $0-capital starter, runs the 10-brand sprint outside the repo, then closes via Win (human-entered $ + optional spend + `via {source}`) or Lose — both require a one-line result + post-mortem. Touch no repo files. Size S (one human decision + the sprint). Risk none (token-gated PATCHes; no auto-transitions; never mark decided without evidence).

### F8. Nineteen of 27 rows have no brief — the evidence gap behind each slow vet

Evidence. Live `bare_without_brief:19` with `opportunities:27`. Brief capacity is 1 + 1 per cron tick: main (`worker/src/index.js:550-589`, top-scored live-only `:566-568`, or oldest-unreviewed-first past 48h `:553-561`) plus extra (`:596-631`) when the oldest unreviewed exceeds 48h (live oldest 182.4h, so both fire). Each costs 1 of 4 AI calls (`MAX_AI_CALLS`, `:19`); poison rows rotate (`:248-265`) and failures land as `brief:failed`. Inflow is already paused at 0/tick while unreviewed > 10 (`:430-433`), so the bare queue drains 2/tick with no new bare rows arriving.

Why it costs: a vet without a brief is a guess, not a sub-minute decision — the reviewer opens the drawer, reads raw notes, and slows the 11-row clear that unpauses inflow. The brief backlog is the second queue behind the review queue.

Fix: no code change; the 48h oldest-first flip + extra brief already prioritize the review queue, and the pause already stops new bare inflow. The owner vets from the inline decision lines ($/mo range, capital, next action, summary excerpt) while cron drains 2 bare rows per 6h tick; the next audit should confirm `bare_without_brief` falling. Touch no repo files. Size S. Risk none (read-only for the agent; cron already paces within its AI budget).

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, or auto-testing without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless. `functions/api/[[path]].js:16-25` (switch docblock), `:53` (unset → report + change nothing), `:59-67` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate (`:35,:69-71`). Do not default it on, do not add a second trigger, and do not add a migration endpoint: `a URL that alters the schema is a liability even when it is authenticated.`
- Never a data-losing migration here: ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change); unset behaviour is byte-identical to today. His one line: Pages Settings → Environment variables → add `ALLOW_SCHEMA_MIGRATION=1` to production, retry the deployment, reload `/api/health` once — the two money columns appear and the money legs fill on the same call.
- Clearing the 11 rows is the owner's (needs `ADMIN_TOKEN` + human judgment per row; an offline implementer has neither). His one line: open Priority → Needs review oldest-first → press V or K on each of the 11 rows (~2 min); `unreviewed` → 0 reopens inflow.
- Starting an experiment to a decision is the owner's. His one line: `Zero spend` chip → `Vet & log starter` → Experiments `Start` → run the smallest real test within a week → `Win` (human-entered $ + optional spend + `via {source}`) or `Lose`, both with result + post-mortem.
- Deploying HEAD is the owner's (needs network + push). His one line: push `812ba25` to `master` (or let the pending branch merge), then `./deploy/verify.sh` must print PASSED.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action.
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment. Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure.
- Never, whatever the ranking says: no spending, no account or identity creation, no outward sends, no accepting terms, no captcha solving, no fabricated reviews/receipts/testimonials, no scraping or messaging a platform's terms forbid, no financial advice or implied return.
- House rules honored by this audit: never read `.env`/secrets/tokens; no edits to existing files (this findings file only); CRLF care unneeded (nothing edited; `.gitattributes:4` pins `eol=lf`).

## 6. Proposed next tasks (2–4)

Focus compliance: every focus-named build item resolves to already-shipped green — the vet ROUND (path = `public/app.js:545` manual button, cause = no automation was ever supposed to vet, real-path test at `functions/api/api.test.mjs:2003`, `last_vetted`/`last_verdict` at `functions/api/[[path]].js:628-641`), the migration switch (`ALLOW_SCHEMA_MIGRATION`, 3/3 results green), the deploy-path convergence (`.github/workflows/deploy-worker.yml:33-34` + `deploy/apply-d1-migrations.sh`), and the health isolation fallback. What remains of the focus is owner actions (vet the rows, set the var, deploy HEAD), which need network/credentials/human judgment and sit in §5 per the completable-task rule. The tasks below are the remaining unsafe edges on the 11-verdict path itself, worded standalone, each completable offline with no network, installs, credentials, or logins.

### Task 1. Show a load-failure row in the review queue instead of the false all-clear

Owns: `public/app.js`, `public/dashboard.test.mjs`. Context (F4): `renderReview` (`public/app.js:224-247`) has no `apiFailures` branch, so an `/api/opportunities` outage prints `Review queue empty — every agent proposal has been vetted or killed.` (`:247`) while the siblings (`renderLedger` `:193-194`, `renderExperiments` `:883-884`) print a failure row. Mirror the `renderLedger` branch: when `state.apiFailures` includes `/api/opportunities`, render the `Could not load…` row.

Acceptance: a test with a failed opps fetch + review filter asserts the failure row and never the all-clear text; a healthy empty queue still prints the all-clear; full `npm test` green.

### Task 2. Toast when a cached-strip verdict taps before the list loads

Owns: `public/app.js`, `public/dashboard.test.mjs`. Context (F5): the last-good strip renders tappable Vet/Kill (`public/app.js:1605-1609`) while the lists are still `[]`, and `fetchDetailForWrite` (`:526-543`) returns null for unloaded rows while all three callers (`:546`, `:600`, `:639`) return silently — the tap vanishes with no feedback. Toast `List still loading — try again in a moment.` and return before the wasted GET when the row is absent from the loaded lists; keep the token gate first.

Acceptance: a test with empty lists + cached id asserts the toast and zero GET/PATCH; loaded lists behave exactly as today (existing verdict suites unchanged); full `npm test` green.

### Task 3. Keep decisions unknown when the experiments fetch fails

Owns: `public/app.js`, `public/dashboard.test.mjs`. Context (F6): `renderExperiments` (`public/app.js:880`) falls back to `fallbackDecisions(exps)` when the decisions probe is null, but a failed fetch leaves `state.experiments` as `[]`, so the double failure prints `0 decisions this week` from an empty failed list. Use the fallback only when `/api/experiments` is not in `state.apiFailures`, else `decisions = null` (the `:886` guard and `:896` nudge already handle null).

Acceptance: a test with decisions-probe-null + failed exps fetch asserts no `0 decisions` text and no nudge; probe-null + loaded list still falls back to the list count; full `npm test` green.
