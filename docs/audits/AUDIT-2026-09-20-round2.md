# AUDIT-2026-09-20-round2 — aimoney (round)

Clone: `/root/coding-projects/_workspace/night3-aimoney-r2` at `c9ffba9` (read-only; this file is the only write).
Live snapshots read: `_night/live/aimoney_pages_dev.html` (HTTP 200, 5390 bytes, 119 lines), `_night/live/aimoney_pages_dev_api_health.html` (HTTP 200, 269 bytes, 1 line).
No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` in clone; `README.md` used as governing doc. `.env`/secrets never read. No git writes, no network, no installs.
HEAD `c9ffba9` ("Round 3 tasks 1-3 done: create guards, stall nudge with vet handoff, outcome ledger") verified present on master; the parked branch `origin/night/aimoney-round2` is still absent (`git branch -a` shows only `master`, `remotes/origin/master`) and fetch is forbidden, so its delta stays unverified — the hand-port in `c9ffba9` is what master carries.

Lane metric (aimoney row): experiments reaching a decision (`won`/`lost` with post-mortem) per week, plus vetted proposals becoming experiments, with the unreviewed backlog drained. Current value from the repo's own live data: **0 decisions and 0 vetted in 7d** (`decisions_last_7d:0`, `vetted_last_7d:0`), **11 unreviewed of 27 (oldest 149.7h ≈ 6.2d)**, **19/27 bare without brief**, `experiments_by_status:{"planned":2}` with zero `running`/`won`/`lost`. Live rev is still `b86d2b0` — one deploy behind master — and the counts are identical to the prior round's fetch (oldest aged 149.4h → 149.7h): another fresh cron cycle (`hours_since_last_ok_run:0`) with zero queue movement.

## 1. What this project is and how it runs

- Purpose: Cloudflare-native AIMoney Lab — ranked AI-money opportunities with scores, research briefs, experiments, and a cron research agent.
- Entry points: dashboard `public/index.html` + `public/app.js`; API `functions/api/[[path]].js`; agent `worker/src/index.js` + `worker/src/lib.js`; DB `d1/schema.sql`.
- Live surfaces: `https://aimoney.pages.dev`, `https://aimoney.pages.dev/api/health`, worker `https://aimoney-research.<account>.workers.dev`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`) — collect signals, AI triage, 1–2 briefs per cron tick, log to `agent_runs`.
- How it deploys: `./deploy/deploy.sh` (D1 schema + seed-if-empty, stamp `public/release.json`, Pages deploy, worker deploy); Pages Git-connected rebuild + `.github/workflows/deploy-worker.yml` on `worker/**` push; `./deploy/verify.sh` checks markers + 12h freshness.

## 2. End-to-end walk-through

Main loop: signal found → agent proposal → human vet → experiment → close with learning → rescore.

1. Cron/manual triggers pass: `worker/src/index.js:380-382` (`scheduled` → `runResearch(env,"cron")`) and `worker/src/index.js:436-445` (`POST /run` → `waitUntil(runResearch(env,"manual"))`, 202 `{"status":"accepted","briefs_skipped":true}`).
2. Collect signals in parallel: `worker/src/index.js:176` (`Promise.allSettled([hnSignals(), redditSignals(), githubSignals()])`); sources `worker/src/index.js:47-69` (HN ×4), `worker/src/index.js:71-93` (Reddit ×3), `worker/src/index.js:95-116` (GitHub, `slice(0,2)` of 3 queries at `:115`); fetch helper `worker/src/index.js:35-45` (`timedJson`, 6s abort covering body read).
3. Batch-insert signals + heartbeat: `worker/src/index.js:181-186` (`INSERT OR IGNORE INTO signals`, one `DB.batch`); `worker/src/index.js:187-191` (`phase:collected/classify-ai/...` heartbeat); reap stuck runs `worker/src/index.js:145-150` (`running` older than 30 min → `error`).
4. Classify fresh vs live list: `worker/src/index.js:192-194` (`SELECT * FROM signals WHERE processed=0 ORDER BY id DESC LIMIT 6`); `worker/src/index.js:201-203` (top-60 opps); `worker/src/index.js:217-221` (`aiComplete` Mistral NDJSON); parse `worker/src/lib.js:49-64` (`parseJsonLines` + `repairJson` for `\_` at `worker/src/lib.js:47`).
5. Apply verdicts: new → `worker/src/index.js:242-266` (`INSERT INTO opportunities … status="researching"`, `effectiveScore({…,"UNREVIEWED"})` capped ≤6000 at `:258`); supports → `worker/src/index.js:267-275` (`substr(notes || ?,-8000)`, newest kept); invalid action → `worker/src/index.js:232-235` and noise → `worker/src/index.js:276-278` (marked `processed=1`, dropped, uncounted); supports-without-id demoted to noise at `worker/src/index.js:239-241`.
6. Brief one–two bare opps (cron only): `worker/src/index.js:174` (`briefDeadline = trigger==="cron" ? 300000 : -1`, manual always skips); `worker/src/index.js:283-285` (`LEFT JOIN briefs … WHERE b.id IS NULL ORDER BY o.score DESC LIMIT 1`, skipped past deadline); `worker/src/index.js:291-303` (Llama brief JSON); `worker/src/index.js:310-318` (`INSERT INTO briefs`, skipped if `summary`/`first_steps` empty); extra oldest-unreviewed bare row when backlog >48h `worker/src/index.js:326-370` (`extraBare`, `retries:0`, `MAX_AI_CALLS` guard).
7. Finish run: `worker/src/index.js:161-167` (`UPDATE agent_runs SET finished_at,status,signals_seen,added,updated,briefs,ai_calls,error`).
8. Board reads: `public/app.js:644-663` (`refresh()` loads `/api/opportunities?limit=200`, `/api/experiments`, `/api/runs?limit=20`, `/api/health`, `/api/meta`, tracking `state.apiFailures`); list `functions/api/[[path]].js:39-66` (`unreviewed=1` filter, `oldest` sort, shared `effectiveScore` cap at `:61`, re-sort at `:62-64`); review chip + age `public/app.js:132-155`; review view `public/app.js:100-130`; failure banner `public/app.js:626-641` + `public/index.html:40`.
9. Human vets: `public/app.js:161-177` (Vet strips `UNREVIEWED`, lifts cap, keeps status; success toast with one-click Log experiment at `:172`) / `public/app.js:179-197` (Kill sets `killed` + prompted post-mortem) → `functions/api/[[path]].js:118-149` (`PATCH /api/opportunities/:id`, rescore via shared `effectiveScore` at `:137`).
10. Experiment loop: log/update `public/app.js:542-588` (`openExperimentModal`) → `functions/api/[[path]].js:223-257` (create: invalid-status 400 at `:228-230`, unknown-opp 404 at `:232-233`, won/lost closure rules at `:234-239`, stamps at `:243-244`) / `functions/api/[[path]].js:259-303` (update: same rules at `:270-281` plus outcome-ledger append at `:282-293`); list with age/orphan `functions/api/[[path]].js:201-221` (`days_in_status` at `:215-219`, `orphaned` via `LEFT JOIN` at `:208-217`); board stale-first + age chips + decisions header + stall nudge `public/app.js:233-280` (nudge at `:251-261`, `vetted_no_experiment` at `:240`/`:248`); detail drawer + admin re-optimize `public/app.js:333-471` with suggested rescore `public/app.js:388-398` / `:404-410`.
11. Health/verify: `functions/api/[[path]].js:318-350` (`/api/health`: `rev,db,opportunities,unreviewed,bare_without_brief,experiments_by_status,hours_since_last_ok_run,oldest_unreviewed_age_h,decisions_last_7d,vetted_last_7d,vetted_no_experiment,time`); `deploy/verify.sh:28-58` (7 markers + non-empty list + freshness ≤12h).

Where it most often breaks or goes silent: the human bottleneck after hop 5, unchanged across fetches. Live health proves it: `unreviewed:11/27` (41%, oldest 149.7h), `bare_without_brief:19/27` (70%), `experiments_by_status:{"planned":2}` with zero `running`/`won`/`lost`, `decisions_last_7d:0`, `vetted_last_7d:0` — and the counts are identical to the prior round's fetch at the same live rev `b86d2b0`, i.e. **zero queue movement across another fresh cycle** (`hours_since_last_ok_run:0`). Master `c9ffba9` now ships the stall nudge (`public/app.js:251-261`), vet handoff (`public/app.js:172`), and outcome ledger (`functions/api/[[path]].js:282-293`) that address exactly this stall — but live still serves `b86d2b0`, so none of it has touched real data yet. Secondary silent spots: invalid verdicts marked `processed=1` and dropped uncounted (`worker/src/index.js:232-235`); manual runs never brief (`briefDeadline=-1`, `worker/src/index.js:174`); triage takes newest-first `LIMIT 6` so older unprocessed signals starve (`worker/src/index.js:192-194`).

## 3. Health signals measured here

- Test suite (`npm test` = `node --test worker/src/lib.test.js worker/src/index.test.js functions/api/api.test.mjs public/tab-icon.test.mjs public/dashboard.test.mjs`): **PASS, 61/61, 24 suites, 0 fail**. Real tail: `# tests 61 / # suites 24 / # pass 61 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0 / # duration_ms 533.643189`. Prior round was 46/46; +15 from round-3 tasks 1–3 (create guards 5, vetted-without-experiment 1, outcomes 5, nudge/handoff 3, rescore 1). Warnings only: `UNDICI-EHPA` proxy-agent notice and `MODULE_TYPELESS_PACKAGE_JSON` reparsing hint for `[[path]].js`/`index.test.js`/`lib.test.js` (add `"type":"module"` to silence; no behavior change). One sandbox note: a terminal notice recorded a denied `localhost:80` request during the run — that is the health handler's best-effort `fetch(/release.json)` (`functions/api/[[path]].js:345-348`) failing gracefully inside the in-memory API tests (caught, `rev` stays `"unknown"`); all health tests still pass.
- Syntax checks (no writes): `node --check public/app.js` → exit 0; `node --check worker/src/index.js` → exit 0 (each with only the `UNDICI-EHPA` warning). `node --check` on `functions/api/[[path]].js` not run separately (bracketed path); the file is imported and exercised green by `npm test` instead.
- `python3 -m compileall -q`: NOT RUN — no Python sources in repo (glob `**/*.py` returns zero files) and `compileall` would write `__pycache__` into this read-only clone. The `node --check` line above is the real syntax signal.
- Dead code (inspected, not guessed): `worker/src/index.js:20` defines `const BRIEF_DEADLINE_MS = 18000` but the pass uses `worker/src/index.js:174` (`trigger==="cron" ? 300000 : -1`) — the constant is never referenced (search hits only line 20). `worker/src/index.js:139` imports `scoreOf` but the file never calls it (search hits only the import line). `worker/src/lib.js:36-39` exports `appendKeepNewest` yet production uses SQL `substr(notes || ?,-8000)` (`worker/src/index.js:271-275`, `functions/api/[[path]].js:290-292`); the helper is test-only (callers only in `worker/src/lib.test.js`). `worker/src/index.js:115` (`GITHUB_QUERIES.slice(0,2)`) silently drops the third query (`ai-side-project`, `worker/src/index.js:27`). All four survive from the prior round.
- Duplicated logic: `clamp10` exists twice — `functions/api/[[path]].js:12-16` vs `worker/src/lib.js:4-7` (same bounds/rounding; the API imports only `effectiveScore` at `functions/api/[[path]].js:10`, so the clamp stays in sync by inspection only). `scoreOf`/`effectiveScore` are correctly shared (cross-boundary test `functions/api/api.test.mjs:387-398` locks agreement). Auth compare is duplicated with divergent semantics: constant-time loop in API (`functions/api/[[path]].js:21-29`) vs `!==` in worker (`worker/src/index.js:393,408,439`, all `if (!want || got !== want) return json({ error: "unauthorized" }, 401);`, collapsing unset-token 503 into 401).
- TODO density: 0. Regex search for `TODO|FIXME|XXX|HACK|TBD` returns zero matches in shipped code. No `console.log`/`debugger`/`prefers-color-scheme`/`data-theme`/theme-toggle hooks in shipped `public/`+`functions/`+`worker/` (regex search clean).
- Live snapshot (read-only, from `_night/live/`): health JSON verbatim: `{"ok":true,"rev":"b86d2b0","db":"up","opportunities":27,"unreviewed":11,"bare_without_brief":19,"experiments_by_status":{"planned":2},"hours_since_last_ok_run":0,"oldest_unreviewed_age_h":149.7,"decisions_last_7d":0,"vetted_last_7d":0,"time":"2026-09-20T06:01:19.486Z"}` — rev is `b86d2b0` (one behind master `c9ffba9`), run fresh (0h), but 41% unreviewed (oldest 6.2d), 70% bare, zero running/won/lost, zero decisions/vetted in 7d. The live payload has **no `vetted_no_experiment` key** while master emits one (`functions/api/[[path]].js:330,349`) — independent proof live predates the round-3 port. Dashboard HTML content-matches repo `public/index.html` line-for-line (119 lines: title, favicon link + `theme-color #0d6b3f`, inline header brand SVG, triage-only button copy, `#api-errors` banner, review chip); `diff` reports only a CRLF-vs-LF line-ending difference (working tree CRLF, live snapshot LF), zero content delta.
- Surface rules (owner's 5): all PASS. Light palette only (`public/styles.css:4` → `color-scheme: light;`, zero `prefers-color-scheme`/`data-theme`/toggle hits in shipped HTML/CSS/JS); tab icon + theme-color on the served page (`public/index.html:9-10`); hand-written `public/favicon.svg:1` (32×32 viewBox, `#0d6b3f` rounded square, `$` glyph, 1 line, <2 KB, no external fonts); inline header brand mark reusing favicon geometry/accent/glyph (`public/index.html:16`, styled `public/styles.css:46`); guard test `public/tab-icon.test.mjs:48-113` (5/5 green: size, XML root, light-mode lock, brand-mark match, icon+theme-color links). No finding slots spent on surfaces.
- Tree state (blocker, pre-existing): `git status --short` shows 28 modified files (every tracked file, `git diff --stat` reports 5238+/5238−, i.e. whole-file rewrite) although this run wrote nothing before this audit file. The `diff` above confirms the cause: CRLF line terminators in the working tree vs HEAD — the M-flags are a pre-existing line-ending mismatch, not edits. Acceptance expects only this new file; that gate cannot pass without a line-ending normalization owned outside this read-only lane. `_night/` scratch exists on disk but shows no `??` entry (ignored outside `.gitignore`).

## 4. Ranked findings (max 8)

Ordered by movement on the lane metric (decisions/week, vetted→experiment conversion, backlog drained). Round-3 tasks 1–3 (create guards, stall nudge + vet handoff, outcome ledger) are done on master and verified; they do not reappear except where the port left gaps (F2).

### F1 — The money number is zero across another fresh cycle, and the fixes are not live yet

- Evidence: `_night/live/aimoney_pages_dev_api_health.html:1` → `"rev":"b86d2b0","experiments_by_status":{"planned":2},"decisions_last_7d":0,"vetted_last_7d":0,"hours_since_last_ok_run":0,"oldest_unreviewed_age_h":149.7` with no `vetted_no_experiment` key; master HEAD `c9ffba9` emits that key (`functions/api/[[path]].js:330,349`) and ships the nudge (`public/app.js:251-261`), vet handoff (`public/app.js:172`), and outcome ledger (`functions/api/[[path]].js:282-293`). Counts identical to the prior fetch; oldest aged 149.4h → 149.7h with zero movement.
- Why it costs money: the lane exists to move decisions/week, and the instrument proves another cycle produced nothing: two `planned` rows sit with no `running`/`won`/`lost`, 11 proposals wait 6.2d for review, and the shipped prompts that would surface the stall have never run against live data.
- Fix: operator deploys master and verifies — `./deploy/deploy.sh` then `./deploy/verify.sh` (no code change; credentials stay operator-owned). After deploy, confirm live `rev` reads `c9ffba9`, `vetted_no_experiment` appears in `/api/health`, and the Experiments header shows the stall nudge. Then the human work the nudge names: vet the 11, start one `planned` experiment. Touch no files; this is a deploy action, not an edit.
- Size: S. Risk: low (standard deploy path; `verify.sh` gates freshness ≤12h).

### F2 — The hand-port left four create/update asymmetries (range guard, ledger, brief parent, status)

- Evidence: `POST /opportunities` 400s when `est_monthly_low > est_monthly_high` (`functions/api/[[path]].js:104-106`) but `PATCH /api/opportunities/:id` clamps without checking (`functions/api/[[path]].js:133-135`, no comparison); `PATCH /api/experiments/:id` into `won`/`lost` appends the outcome-ledger line (`functions/api/[[path]].js:282-293`) but `POST /api/experiments` with `status:won|lost` inserts without appending (`functions/api/[[path]].js:223-257`, no notes write); `POST /api/experiments` 404s on unknown `opportunity_id` (`functions/api/[[path]].js:232-233`) but `POST /api/briefs` never checks the parent exists (`functions/api/[[path]].js:160-184`, straight to `INSERT`); experiment paths 400 on invalid status (`functions/api/[[path]].js:228-230`, `:270-272`) while opportunity update silently ignores it (`functions/api/[[path]].js:129` → `if (b.status !== undefined && OPP_STATUSES.has(b.status)) next.status = b.status;`).
- Why it costs money/users/trust: the board's core promises — "ranges are sane", "every close leaves a one-line outcome" (`README.md:73-74`) — hold on one path and leak on the other. A direct-created `won` pollutes `decisions_last_7d` with no parent-notes trace; a typo'd range or brief parent corrupts the data reviewers rank on.
- Fix: mirror each rule to its sibling path with the same error shapes (`field`/`fields` so toasts stay readable): range-400 in `updateOpportunity`, ledger-append in `createExperiment` for `won`/`lost`, parent-404 in `createBrief`, invalid-status 400 in `updateOpportunity`. Add one API test per 400 plus ledger-on-create happy path. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`.
- Size: S. Risk: low (write-path validation mirroring already-shipped shapes; keep `effectiveScore` shared).

### F3 — Brief backlog 70% bare; the main brief still chases top score while old rows wait 6.2d

- Evidence: `bare_without_brief:19` of 27 (`_night/live/aimoney_pages_dev_api_health.html:1`); main brief takes the top-scored bare row in `worker/src/index.js:283-285` (`` `… WHERE b.id IS NULL ORDER BY o.score DESC LIMIT 1` ``) while the extra-brief guard (`worker/src/index.js:326-370`) only fires when the oldest unreviewed exceeds 48h — live oldest is 149.7h, so both fire, yet coverage is still 8/27. Proposals append every run (`worker/src/index.js:242-266`); throughput is ≤2 briefs per cron tick (≤8/day) minus manual skips.
- Why it costs money: reviewers cannot vet what they cannot read — unbriefed proposals stall in `researching`, the 6000 cap holds, and good ideas wait 6+ days for their first evidence paragraph.
- Fix: when `oldest_unreviewed_age_h > 48`, flip the MAIN brief query to oldest-unreviewed-bare-first (`ORDER BY o.created_at ASC LIMIT 1`) instead of top-scored, keeping the extra-brief as the second-oldest; disclose the mode in the run's `error`/`phase` heartbeat so the Research log shows `brief:oldest-first`. No AI-budget change. Touch `worker/src/index.js`, `worker/src/index.test.js`.
- Size: S. Risk: low (query-order change on backlog ticks only; brief content path untouched).

### F4 — Triage drops are invisible: noise + invalid verdicts vanish without a count

- Evidence: invalid action → `worker/src/index.js:232-235` (`UPDATE signals SET processed=1`, continue); `supports`-without-id demoted to noise in `worker/src/index.js:239-241`; plain noise in `worker/src/index.js:276-278` — none counted. `agent_runs` has no noise/invalid column (`d1/schema.sql:85-98`: `signals_seen, added, updated, briefs, ai_calls` only); health exposes no triage breakdown (`functions/api/[[path]].js:318-350`).
- Why it costs money/trust: the prompt says `DEFAULT TO NOISE` (`worker/src/index.js:213`) — correct bias, but with zero visibility nobody can tell a strict-but-right triage from a broken model dropping winners. Debugging is guesswork.
- Fix: without any migration, add `noise_24h` to `/api/health` via `SELECT COUNT(*) FROM signals WHERE processed=1 AND opportunity_id IS NULL` (windowed by `created_at`), and show `+A/U · N noise` in the agent pill title and runs tab. Add an API test with seeded signals. Touch `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: S. Risk: low (read-only counts from existing columns).

### F5 — Triage starves older signals: newest-first `LIMIT 6` buries the excess behind future arrivals

- Evidence: `worker/src/index.js:192-194` (`SELECT * FROM signals WHERE processed = 0 ORDER BY id DESC LIMIT ?`, bound to `MAX_AI_SIGNALS = 6` at `worker/src/index.js:15`); same order in the debug probe (`worker/src/index.js:409-411`). Sources can insert up to ~72 rows per run (HN 4×8 + Reddit 3×8 + GitHub 2×8 at `worker/src/index.js:47-116`) while triage clears ≤6 per run (≤24/day over 4 cron ticks) — whenever a run sees more than 6 fresh signals, the excess waits behind every future arrival, indefinitely under steady inflow.
- Why it costs money: potential winners rot in `processed=0` while the model re-reads only the newest arrivals; the board's input stream is systematically biased toward whatever arrived last, not whatever is best.
- Fix: take the oldest unprocessed signals first (`ORDER BY id ASC LIMIT 6`) so every signal is triaged exactly once in arrival order; optionally add a staleness bound (e.g. skip signals older than 30d as noise with a count) so ancient rows cannot wedge the queue. Update the debug probe to match and add a worker test locking the order. Touch `worker/src/index.js`, `worker/src/index.test.js`.
- Size: S. Risk: low-medium (ordering change; the staleness bound keeps ancient backlog from dominating the first ticks after deploy).

### F6 — Orphaned experiments have no remediation path

- Evidence: list keeps dangling rows via `LEFT JOIN` with `orphaned:true` (`functions/api/[[path]].js:208-217`); board renders an `orphaned` pill (`public/app.js:269`) but the card click dead-ends in `public/app.js:274-279` (`if (c.dataset.orphan === "1") return toast("Orphaned experiment — its opportunity was deleted.");`); router has no `DELETE` for any resource (`functions/api/[[path]].js:312-375` lists only GET/POST/PATCH/PUT); schema FKs are advisory without `PRAGMA foreign_keys` (`d1/schema.sql:50`).
- Why it costs users/trust: orphan-safe display stopped the vanishing, but orphans now accumulate with no admin action — no delete, no relink, not even a drawer. The board slowly fills with unactionable cards.
- Fix: add admin-only `DELETE /api/experiments/:id` (same `authed` + 404 shape as update) and, on orphaned cards only, a `Delete` button behind the admin-token check with a confirm prompt; keep the toast for non-admins. Add API tests for 401/404/happy-path. Touch `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: M. Risk: medium (first DELETE endpoint; restrict to experiments, never cascade).

### F7 — Auth mismatch + dead worker code invite the next incident

- Evidence: API constant-time compare (`functions/api/[[path]].js:21-29`) vs worker `!==` in three places (`worker/src/index.js:393,408,439` → `if (!want || got !== want) return json({ error: "unauthorized" }, 401);`, collapsing unset-token 503 into 401); `const BRIEF_DEADLINE_MS = 18000` never referenced (`worker/src/index.js:20` vs live `worker/src/index.js:174`); unused `scoreOf` import (`worker/src/index.js:139`); test-only `appendKeepNewest` (`worker/src/lib.js:36-39`, zero production callers); `GITHUB_QUERIES.slice(0,2)` drops `ai-side-project` (`worker/src/index.js:115` vs `:27`) with no comment. All survive from the prior round.
- Why it costs trust/maintainability: timing side-channel on the worker token plus divergent 401/503 slows triage; the next editor tunes the wrong deadline constant or re-adds the dropped query as a bugfix. No direct money movement — ranked last.
- Fix: extract the constant-time compare into `worker/src/lib.js`, import it in both runtimes, align worker codes (503 unset / 401 mismatch); delete-or-wire `BRIEF_DEADLINE_MS`, drop `scoreOf` from the worker import, demote `appendKeepNewest` to a documented test oracle pointing at the SQL idiom, and comment the `slice(0,2)`. Touch `worker/src/lib.js`, `worker/src/index.js`, `functions/api/[[path]].js`, `worker/src/lib.test.js`.
- Size: S. Risk: low (auth tightens; keep tokens out of logs; `npm test` must stay green).

## 5. What NOT to change

- Scoring formula and honest seeds: `score = 100 * (value * confidence * fit) / (effort + 1)`, each input 1–10 (`README.md:56`; `worker/src/lib.js:15-16`). Do not retune weights without new evidence.
- Human-owns-status: "The agent never moves status." (`README.md:71`); "The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)." (`worker/src/index.js:4-5`); "`researching → testing` is a human decision: flip to `testing` when you start a real experiment … The agent never moves status." (`README.md:71`). No auto-transitions.
- Killed-with-learning: "Killed strategies stay on the board with their post-mortem — that is the point." (`README.md:64`); "Closing an experiment as `won`/`lost` requires `result` + `post_mortem`; `ended_at` stamps automatically." (`README.md:73`). Do not purge killed rows or relax closure rules.
- Capped humility: "Agent proposals enter as `researching` with an `UNREVIEWED` marker in notes and an effective score capped to ≤6000 until a human vets them" (`README.md:65`); "A human admin vets each row: "Vet" clears the `UNREVIEWED` marker (keeps status, lifts the 6000 cap) …" (`README.md:70`). Vet/kill stay human.
- Seed-vs-live accounting: "Seed runs once on an empty table; live count = 12 seeds + agent proposals + manual adds. Killed rows stay listed, so the count only grows." (`README.md:80`). Do not re-seed over live data; deploy's seed-if-empty (`deploy/deploy.sh:30-42`) stays.
- Auth split and secrets: "Reads are public. Writes require `Authorization: Bearer <ADMIN_TOKEN>`." (`functions/api/[[path]].js:1-2`); "`ADMIN_TOKEN` lives in two places: the Pages env var and the worker secret … No expiry; rotate manually when shared or leaked." (`README.md:84-85`). Anything needing `ADMIN_TOKEN`, `CLOUDFLARE_API_TOKEN`, D1 execute, or wrangler deploy is operator-owned and credential-gated — out of scope for code edits.
- Anything needing a login, payment, or human judgment: vetting `UNREVIEWED` rows, moving `researching → testing → scaling`, writing post-mortems, choosing niches/pricing/outreach, rotating tokens. This audit proposes surfacing and guardrails only — never auto-vetting, auto-scaling, or auto-spend.

## 6. Proposed next tasks (2–4)

### Task 1: Close the create/update guard gaps (range, ledger, brief parent, status)

- Owned files: `functions/api/[[path]].js`, `functions/api/api.test.mjs`.
- Acceptance: (a) `PATCH /api/opportunities/:id` 400s when `est_monthly_low > est_monthly_high` (same `field` shape as POST); (b) `POST /api/experiments` with `status:won|lost` appends the one-line outcome to parent notes like PATCH; (c) `POST /api/briefs` 404s on unknown `opportunity_id`; (d) `PATCH /api/opportunities/:id` 400s on invalid status instead of ignoring it; (e) one API test per new 400 plus ledger-on-create happy path; (f) `npm test` green.

### Task 2: Brief oldest-unreviewed first when backlog exceeds 48h

- Owned files: `worker/src/index.js`, `worker/src/index.test.js`.
- Acceptance: (a) main brief query flips to oldest-unreviewed-bare-first when the oldest unreviewed is older than 48h, top-scored otherwise; (b) run heartbeat discloses the mode (`brief:oldest-first` vs default) in the Research log; (c) extra-brief guard and `MAX_AI_CALLS` budget unchanged; (d) `npm test` green.

### Task 3: Expose triage noise count in health and agent pill

- Owned files: `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Acceptance: (a) `/api/health` reports `noise_24h` from existing `signals` columns (no migration); (b) agent pill title and runs tab show `+added/updated · N noise`; (c) API test with seeded signals locks the count; (d) `npm test` green; (e) no existing health key removed or renamed.

### Task 4: Admin-only delete for orphaned experiments

- Owned files: `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Acceptance: (a) `DELETE /api/experiments/:id` honors the same `authed` gate (401 without token, 404 unknown id) and deletes exactly one row, never cascading; (b) orphaned cards show a `Delete` button only when the admin token is set, with a confirm prompt; non-admins keep the current toast; (c) API tests cover 401/404/happy-path; (d) `npm test` green.
