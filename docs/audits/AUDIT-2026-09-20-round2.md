# Audit 2026-09-20 round 2 — aimoney (read-only, track B)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r2`. HEAD `f43c623` (night round 7); live rev `f43c623…` equals HEAD, so the round-7 deploy lag is gone. No network, no installs, no edits to existing files; only this file created at the root. Never read `.env`, secrets or tokens. One single-stage shell command per call, no inline code, no redirection. No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` at the root (authoritative root `ls`; `README.md` read first, then the live snapshots, then source). Note: `docs/audits/` already holds a round-2 audit from another track; this file is the track-B round-2 deliverable the delegation names.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests (`README.md:3-15`, `worker/src/index.js:4-5`).
- Entry points: `public/index.html` + `public/app.js` (1687 lines, dashboard), `functions/api/[[path]].js` (717 lines, D1 REST API), `worker/src/index.js` (730 lines, research agent) + `worker/src/lib.js` (79 lines, shared pure helpers).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `https://aimoney-research.<account>.workers.dev/`; live rev equals HEAD `f43c623` (see §3).
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + up to 2 briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:725`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema `:27` + shared migrations `:33-34` + worker `:38`) or manual `deploy/deploy.sh`, gated by `deploy/verify.sh` (public reads only, no credentials).

## 2. End-to-end walk-through

Signal to decision. The hop where it most often breaks or goes silent is hop 13.

1. Schedule: cron `0 */6 * * *` (`worker/wrangler.toml:9`) → `scheduled` (`worker/src/index.js:653`) → `runResearch` (`:313`); manual `POST /run` (`:715-726`, 202 `:725`, briefs skipped by deadline `:346`).
2. Collect: `hnSignals` (`:49`), `redditSignals` (`:81`), `githubSignals` (`:110`) via `timedJson` (`:37`, 6s timeout covering the body read). Per-query failures counted; all-queries-failed names the source in `src_fail`, partial failures report `src_partial` (`:76-77`, `:105-106`, `:134-135`); fully-rejected collectors caught by `Promise.allSettled` (`:348-354`).
3. Store: one batched `INSERT OR IGNORE INTO signals` (`:359-364`), then heartbeat (`:365-368`, `phase:collected`).
4. Sweep: unprocessed signals older than 30d become noise, counted (`:372-377`, `stale:N`).
5. Take: oldest-first `LIMIT 6` (`:379-381`, `MAX_AI_SIGNALS` `:15`).
6. Exact-URL pre-pass (no AI): pure `exactUrlTarget` (`:173`); bounded `IN` selects (`:395-406`); matches link as supports, notes newest-kept, no status move (`:408-415`). Failed links are skipped and counted (`prepass:N_failed`, `:412`), never thrown; skipped rows stay unprocessed.
7. Gates, read before classify: bare-without-brief > 10 caps inserts to 1/tick (`:431-434`); unreviewed > 10 pauses inflow to 0/tick (`:435-438`). Live unreviewed is 11, so every current tick is paused (`maxNewThisRun = 0`), overflow staying `processed = 0`.
8. Classify (one AI pass): skipped when no fresh signals or when paused (`:449-462`); otherwise `buildClassifyPrompt` (`:277`) + `aiComplete` (`:141`, budget `MAX_AI_CALLS = 4` `:19`), parsed by `parseJsonLines` (`worker/src/lib.js:64`, NDJSON + `repairJson` for `\_`). Paused ticks log `inflow:paused` on the finish line (`:639`) and still brief.
9. Verdict loop (`:474-542`): strict action enum (`:480`), supports-without-id becomes noise (`:487-489`), overflow stays unprocessed (`:491`). `new` INSERTs (`:502-512`) with an `UNREVIEWED` note (`:512`), capped `effectiveScore` (`worker/src/lib.js:30-33`, cap 6000 `:23`), and agent money estimates (`agentMoneyEstimates`, `worker/src/index.js:185-200`). Slug collisions link as supports (`:517-529`).
10. Verdict flush: `flushVerdictWrites` (`:296-312`); individual failures counted as `verdict:N_failed`.
11. Briefs: main pass (`:555-574`, top-scored live-only `:571-573` or oldest-unreviewed-first past 48h `:561-566`; live oldest is 186.8h so oldest-first fires) plus extra oldest-unreviewed pass (`:601-636`) past the same 48h gate (`:603`), via `buildBriefPrompt` (`:207`), `parseBriefJson` (`:218`), `insertBrief` (`:229`, never stores empty `:233`); poison rotation via `selectBareRow` (`:262-270`); failures as `brief:failed` / `brief:skipped`. F4 lives here: the main `aiComplete` (`:578-582`) sits outside the `try`, so a brief-model throw fails the whole tick, while the extra pass (`:617-633`) keeps its call inside and degrades to `brief:failed`.
12. Finish: single `finish("ok", …)` carrying mode/pause/stale/src markers (`:639`); errors finish `error` plus the `console.error("cron-failed", …)` tail marker (`:647`, also `:659`, `:723`).
13. Vet — the ONLY path from unreviewed to vetted, one sentence: a manual dashboard press — `public/app.js:562` `vetOpportunity` → `:538` `fetchDetailForWrite` (token-gated detail fetch, warms N+1 `:546`) → `:567` inner → `:485` `vettedNotes` (clears `UNREVIEWED` via `:478-480`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`:574-577`) with auto-advance (`:582` → `:729` → `:705`). Starter variant `:616`/`:621` (resume-safe via `:599` `findPlannedStarter`, flips to `testing` `:645-648`, advances `:653`). Kill (`:657`/`:662`, tag+post-mortem `:670`, PATCH killed `:673-676`, advances `:681`). `V`/`K` keys (`:734-749`): one human verdict per keypress, no bulk path. Drawer Vet/Kill for unreviewed rows (`:1078-1079`).
14. API writes: `updateOpportunity` (`functions/api/[[path]].js:232`, text bounds, status/range guards, tagless-clear 400 `:266-270` against `VERDICT_TAG` `:90`, `effectiveScore` `:272`); `createExperiment` (`:360`) / `updateExperiment` (`:423`, won/lost require `result` + `post_mortem`, stamps, `outcomeLedgerLine` `:98-103`, cents clamps, `revenue_source` retry `:402-412`/`:479-491` naming the loss in `dropped` `:411`/`:490`; any other missing column throws into the 503 naming only it, `:714`).
15. Reads: `listOpportunities` (`:138`, `needs_review` bit `:157`, brief excerpts `:159-160`, caps `:170-178`), `getOpportunity` (`:182`), `listExperiments` (`:338`, LEFT JOIN + `orphaned` `:346-356`, `days_in_status` `:323-337`), `listRuns` (`:495`), `meta` (`:688-694`).
16. Health (`:508-686`): 12 probes (`:524-541`) in one batch (`:542`) with per-probe isolation fallback (`:551-580`, column-only `health_probe_detail` via `:568`), lazy self-migration (`:589-605` via `ensureMoneyColumns` `:36-77`), `last_vetted`/`last_verdict` parsed in JS (`:639-652`) from 500-char tails; `ok:false` + nulls only on total outage (`:610`), `db` by whether the database answered (`:686`).
17. Dashboard paint: `refreshTargets` (`public/app.js:1640`, targeted per write, internal `refreshReview` `:1668`) → `renderLedger` (`:184`, review mode `:234` with decision `:215` + brief `:229` lines, no drawer round-trip) → `renderExperiments` (`:893`, health typeof-guards `:898-902`, list fallback `:910-911`, stall nudge `:927-936`) → `renderRuns` (`:973`, pill `:983-996` with probe tooltip `:987-989` and backlog bit `:341`) → banner (`:1503-1526`) → last-good cache persist (`:1676-1680`); boot paints cache synchronously, live refresh wins (`:1684-1687`).

Where it most often breaks or goes silent: hop 13, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 1.1) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (186.8h, +3.6h since the round-7 snapshot 3.5h earlier — the queue has not moved). Second: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F2); the same gap 503s Start/Win writes until the switch is set (F3). Third, code-silent: a main-brief AI throw fails an otherwise successful tick (F4).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:8`) → exit 0 this session (full TAP, 127,018 bytes; the tool display truncated the body, so counts were re-captured with one concise run: `node --test --test-reporter=dot` over the same 7 files → exit 0, 594 dots, no failure marks). Split from the runner baseline `_night/tests-baseline.log:287-294`: `# tests 451`, `# suites 143` (451 + 143 = 594, matching the dot count), `# pass 451`, `# fail 0`, `# duration_ms 90375.4898`. Both runs agree: green.
- Parse/compile: `python3 -m compileall -q public worker functions d1 deploy scripts` → exit 0, no output — there are no Python sources (all 11 directory listings show zero `*.py`; glob search was treated as unreliable per the round-7 note and not used for this claim). JS parse is covered by the green suite itself (every file imported) plus the shipped-JS parse gate suite and `deploy/verify.sh`'s `node --check` step.
- Dead code: one dead ternary — `worker/src/index.js:456` guards `!fresh.length ? [] : …` inside `if (fresh.length)` (`:449`), so the `[]` arm is unreachable (`fresh` is not mutated between). Harmless; not a finding. Every other export spot-checked has a shipped importer: `scoreOf` → `effectiveScore` (`worker/src/lib.js:31`), `daysSince` → `listExperiments` (`functions/api/[[path]].js:352`), `firstStepsFirstLine` (`public/app.js:162,216,627`), `startExperiment` (`:649,832,933`), `findStarterForResume` (`:636`), `openExperimentModal` (`:578,1107,1221,1390`). `appendKeepNewest` (`worker/src/lib.js:51-54`) is test-only by documented design (pinned JS spec of the SQL idiom), not dead. `console.*` hits only the three deliberate `cron-failed` tail markers (`worker/src/index.js:647,659,723`).
- Duplicated logic: remaining mirrors are documented-deliberate and verified intact — `centsDollars` ↔ dashboard `moneyCents` (`functions/api/[[path]].js:93-94` comment; byte-identical math), SQL `substr(notes || ?, -8000)` ↔ `appendKeepNewest`, `VERDICT_TAG` boolean gate (`:90`) vs the health `matchAll` date-capture shapes (`:645-649`, gate vs capture). Shared singles verified: `effectiveScore`/`tokensMatch`/`clamp10` (`worker/src/lib.js`, API import `functions/api/[[path]].js:10`), `vettedNotes`, `fetchDetailForWrite`, `buildBriefPrompt`/`parseBriefJson`/`insertBrief`, `buildClassifyPrompt`, `flushVerdictWrites`, `outcomeLedgerLine`, one migration script.
- TODO density: zero in shipped source. Regex `TODO|FIXME|XXX|HACK` over the 11 shipped paths matches nothing; the only repo hits are prose in `docs/audits/*.md`. Positive control: the same tool+mode matches `UNREVIEWED` 15 times across `worker/src/index.js` + `public/app.js`, so the zero is real.
- Matcher honesty note (the brief's check-that-matches-nothing rule, applied to this audit): every absence claim above carries its positive control in the same bullet (TODO→`UNREVIEWED`, dark-mode→`color-scheme: light` + favicon hits below, `.py`→11 `ls` listings instead of the known-unreliable glob search, dead-code→traced importers). The two test runs exist because the first run's TAP body was truncated in display — the rerun used a concise reporter, not a narrower test set.
- Surface rules: all pass, no finding. Light-only (`public/styles.css:4` `color-scheme: light`, ink-on-paper palette; regex `prefers-color-scheme|data-theme|theme-toggle|color-scheme: dark` over `public/` matches nothing, control hits `color-scheme: light` + icon tags); favicon linked with theme-color (`public/index.html:9-10`, byte-identical in the live snapshot); `public/favicon.svg` 284 bytes, 32×32 `$` on `#0d6b3f`; brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suite `public/tab-icon.test.mjs` green inside the exit-0 runs. First five seconds: the static shell (5595 bytes live) renders brand + `Every way to make money with AI, ranked.` + lede with zero JS; placeholders repaint from the last-good cache synchronously, then live refresh wins.
- Tree: HEAD `f43c623`; `git ls-files --eol` shows all 41 tracked files `i/lf w/lf` with `eol=lf` — the CRLF warning in the brief does not apply to this checkout. Sizes (`wc -l`): `public/app.js` 1687, `functions/api/[[path]].js` 717, `worker/src/index.js` 730, `worker/src/lib.js` 79; tests outweigh shipped code (`api.test.mjs` 2558, `dashboard.test.mjs` 1971, `index.test.js` 1560).
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (time `2026-09-21T19:06:56Z`, rev `f43c623…` = HEAD — the round-7 three-commit deploy lag is resolved, live is current): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:1.1, oldest_unreviewed_age_h:186.8, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{…:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. Prior snapshot (round 7, 15:36Z): same 11 unreviewed at 183.2h with `hours_since_last_ok_run` 3.6 — cron ticked twice since, the queue has not moved, age rises ~1d/d.
- Resolved since round 7 (verified in code, not re-proposed): starter-tap focus advance (`public/app.js:653`), dropped-source flag + toast (`functions/api/[[path]].js:411,490`, `public/app.js:872-874,886,1385`), `.env` ignore (`.gitignore:4-5` + pin test), partial-source markers (`worker/src/index.js:77,106,135`, `state.src_partial`), and the live==HEAD deploy catch-up. The dead-DB batch failure from the brief is fixed and live-proven: `db:up` with 2 named probe failures while the other 10 report.

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 186.8h / 7.8d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `last_verdict` null, `hours_since_last_ok_run` 1.1. Was: same 11 unreviewed at 183.2h — seven night rounds have shipped and the queue has not moved once; the number moves next the moment a human records the first verdict.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none recorded — no `won`/`lost` row exists (`experiments_by_status:{planned:2}`, `decisions_last_7d:0`), and the money legs read null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter (the seed `Spec-ad sprint: 10 brands, 10 free ads`, `d1/seed.sql:96-100`), run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.8 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:562` `vetOpportunity` → shared `vettedNotes` (`:485-488`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id` (`:574-577`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:512`: `— UNREVIEWED, scores capped until a human vets it`) and every other `UNREVIEWED` site in `index.js` is a create/count/select site — zero clearing paths (positive control: the `UNREVIEWED` matcher returns those sites, so the absence of a clearing path is real). So `vetted_last_7d` is 0 with `hours_since_last_ok_run` 1.1 by design, not by failure. Vet-round report as requested: path `public/app.js:562`; cause human-gated-by-design — there is no failing test because nothing is broken, and the passing real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs:2115`, green inside the exit-0 runs) is the positive proof; a single run of the path (one Vet press) produces exactly unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today. Health already dates the stall without being told: `last_vetted`/`last_verdict` (`functions/api/[[path]].js:639-652`), both null live.

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:435-438`), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change for the verdict itself — the two-minute path (`V`/`K` + auto-advance, `public/app.js:734-749`, one verdict per keypress, token-gated, N+1 prefetch, starter-tap advance `:653`) already ships with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines. Touch no repo files for this finding. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live: `revenue_last_7d/revenue_total/spent_total` null with `health_probe_failures:[revenue_week,revenue_lifetime]`, `health_probe_detail:{…:revenue_cents}`, `schema_missing_columns:[revenue_cents,spent_cents]`, `schema_migration:disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)`. The two failing probes are the only ones reading money (`functions/api/[[path]].js:535-536`, `SUM(revenue_cents)` / `SUM(revenue_cents/spent_cents)`). `d1/schema.sql:5` is `CREATE TABLE IF NOT EXISTS` (no-op on the existing table); the ADD COLUMNs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied after `schema.sql` by both CI (`.github/workflows/deploy-worker.yml:27` then `:33-34` `Apply D1 migrations in sorted order` via `./deploy/apply-d1-migrations.sh`) and `deploy/deploy.sh:36` then `:40` — one shared loop + tolerate-list (`deploy/apply-d1-migrations.sh:13-28`, duplicate-column no-op, anything else exits 1). The in-worker fallback is deliberately off: `functions/api/[[path]].js:58` (`if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` → report missing + disabled, change nothing). Migration-ask results, all green this session in the `self-migration switch` suite (`functions/api/api.test.mjs`, 9/9): old schema + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers on the same call; var unset leaves the table unchanged and names the missing columns; second run is a clean no-op (plus retry cases proving a transient PRAGMA failure no longer caches as decided). Exact variable name: `ALLOW_SCHEMA_MIGRATION`. Exact CI step (already in place, nothing to change): `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: earned-to-date is unknown rather than zero, so the money-first question cannot be answered from the project's own data — and the same gap gates writes, not just reads: Start/Win PATCHes always write the cents columns (`functions/api/[[path]].js:473-478`), so on the unmigrated live table they 503 naming the column (tested: `any other missing column still 503s naming only it`) until the switch is set. The decisions COUNT split (`:534`) already saved `decisions_last_7d` = 0 — the money legs and the money writes are what wait on the owner.

Fix: no code change; the path is already correct. The owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var and reloads — the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, and re-runs the failed money probes on the same call — or adds the two Cloudflare repo secrets and redeploys so CI applies the migrations. One variable, one decision; see §5 for the exact lines. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F3. Two planned experiments, zero running, zero decisions — the money path stalls at planned

Evidence. Live `experiments_by_status:{planned:2}`, `decisions_last_7d:0`; no `running`/`won`/`lost` keys. The seed starter is still planned by definition: `d1/seed.sql:96-100` (`Spec-ad sprint: 10 brands, 10 free ads`, `planned`, `metric: replies; pilots closed`, `target: >=3 replies; >=1 pilot at >=$500`). The one-tap moves already exist and are human-pressed: `startExperiment` (`public/app.js:825`, PATCH `running`, API stamps `started_at`), `loseExperiment`/`winExperiment` (`:842`/:`876`, human-entered $ → `revenue_cents` + optional spend/source), all token-gated with orphaned cards excluded. The API closure gate holds: won/lost require `result` + `post_mortem` (`functions/api/[[path]].js:450-455`), `ended_at` stamped, outcome ledger appended (`:460-466`, shared `outcomeLedgerLine` `:98-103`).

Why it costs: vetted → experiment → decision is the lane metric, and it reads 0/0/0. Signals seen and proposals added are attempts, not outcomes; without one running row there is no number within a week and no amount/date/source to make real. Note the ordering trap: Start 503s on the unmigrated table (F2), so the migration switch must land before the first Start press.

Fix: after F1 vets the parent row and F2's switch lands, the owner presses Start on the $0-capital starter, runs the 10-brand sprint outside the repo, then closes via Win/Lose with a human-entered $ amount and a one-line post-mortem. No code change; the dashboard, API gate, stamps, and ledger already enforce the shape. Touch no repo files. Size S (one human decision + the sprint). Risk none to the repo (token-gated PATCHes; no auto-transitions; never mark decided without evidence).

### F4. A main-brief AI throw fails the whole tick — the extra brief degrades gracefully, the main one does not

Evidence. `worker/src/index.js:574-594`: `const text = await aiComplete(…)` (`:578-582`) sits outside the `try`, which covers only parse+insert (`:583-593`, `briefFailed = true`). A brief-model failure (both attempts down, timeouts) therefore propagates to the outer catch (`:641-649`) and the tick finishes `error` — even though triage already flushed successfully (`flushVerdictWrites`, `:543`) and nothing was lost. The extra-brief pass handles the identical failure the honest way: its `aiComplete` (`:621-625`) is inside the `try` (`:617-633`), so the same throw records `brief:failed` and the tick still finishes `ok`. Classify AI throws correctly stay fatal (triage is the tick's core job; signals stay unprocessed for next tick) — only the main brief over-claims.

Why it costs: reliability + failure visibility on the overnight path. A transient brief-model blip (not a triage failure, not data loss) flips the pill red, ages `hours_since_last_ok_run`, and after 12h fails the `verify.sh` freshness gate — all for a best-effort pass that retries next tick by design. The run log reads `error` where the truth is `ok + brief:failed`.

Fix: move the main-brief `aiComplete` call inside the existing `try` (or extend the `try` upward to cover it), so a brief-model throw sets `briefFailed = true` and the tick finishes `ok` with the marker — mirroring the extra pass exactly. Pin with a worker test: stubbed AI throwing on the brief model asserts finish `ok`, `brief:failed` on the error line, and verdict writes still flushed. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk low (one pass's error path; triage, gates, budget, and the classify-stays-fatal rule untouched; the marker already exists in log parsers).

### F5. After the cents self-migrate, every Win still drops its `via` — the second track is undocumented

Evidence. The self-migration covers only probe-read cents: `MONEY_PROBE_COLUMNS = ["revenue_cents", "spent_cents"]` (`functions/api/[[path]].js:28`); `revenue_source` is deliberately excluded (write-only columns are never auto-migrated, `README.md:83`). The live table predates both 2026-09-20 migrate files (proven by the missing cents in F2), so it almost certainly lacks `revenue_source` too — same applier, same no-creds CI. After the owner sets the switch, cents appear but the source column does not: the next Win-with-source retries without the column and returns `dropped: ["revenue_source"]` (`:402-412`, `:479-491`), and the toast reads `…re-enter it after migration` (`public/app.js:874`). But re-entering never sticks until the CI/manual migration applies the revenue-source file, which needs the Cloudflare secrets + a redeploy — a path `docs/OWNER-ACTIONS.md` §2 never names (it covers only the var) and the toast never hints at.

Why it costs: an owner loop on the money-real moment. The first Win smiles, drops the `via`, and tells him to re-enter it; each re-entry drops again with the same line, and nothing he can see names the actual unblock (secrets + redeploy). Money provenance — amount + date + source — stays one-third missing indefinitely.

Fix: document the second track, don't widen the first (write-only columns stay out of the self-migration by design). Add one line to `docs/OWNER-ACTIONS.md` §2 naming the `revenue_source` path (CI secrets + redeploy, or manual `deploy/deploy.sh`), and reword the `droppedSourceSuffix` toast (`public/app.js:874`) to point at it instead of a bare `after migration`. Pin the toast wording in the dashboard suite's existing dropped-source cases. Touch `docs/OWNER-ACTIONS.md`, `public/app.js`, `public/dashboard.test.mjs` (pin only). Size S. Risk none (docs + toast string; write paths and retry behavior untouched).

### F6. The API lets a raw kill skip the post-mortem the board's point depends on

Evidence. `README.md:63-64`: `Killed strategies stay on the board with their post-mortem — that is the point.` The dashboard enforces it (inline post-mortem row, empty cancels: `public/app.js:686` for Kill, drawer admin prompt per `README.md:71`). The API does not: `updateOpportunity` (`functions/api/[[path]].js:232-284`) has no post-mortem check — its only notes guard is the tagless-clear 400 (`:266-270`), which a `[2026-09-21 killed]` tag with no text after it satisfies. A raw `PATCH {status:"killed", notes:"…[date killed]"}` (or a tagless kill of an already-reviewed row, which the clear-only guard never sees) lands a killed row with no `why`, permanently. Contrast experiment closure, where won/lost require `result` + `post_mortem` API-side (`:450-455`). Existing kill tests all send tag + text (`functions/api/api.test.mjs:2177,2264`: `[… killed] no demand`), so a narrow guard breaks no contract.

Why it costs: trust in the board's core promise. Killed rows are kept for their post-mortems; one raw-API (or future-client) kill without it writes a row the board exists to prevent, and the loss is silent — the 200 looks identical to a proper kill.

Fix: narrowly — when a PATCH both clears `UNREVIEWED` and carries a killed tag, require non-space text after the tag (the post-mortem line), else 400 naming it; leave reviewed-row kills and all other shapes exactly as today. Pin with API tests: empty-pm killed clear 400s with the row queued, tag+text kill passes (already covered), reviewed-row kill shapes unchanged. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Size S. Risk low (one transition's guard; dashboard already sends the required shape, so its bytes never change).

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, or auto-testing without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless, cents-only. `functions/api/[[path]].js:16-27` (switch docblock), `:58` (unset → report + change nothing), `:63-76` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate for decided outcomes. Do not default it on, do not add a second trigger, do not add a migration endpoint (`a URL that alters the schema is a liability even when it is authenticated`), and do not widen it to `revenue_source` (write-only columns stay out by design — F5 documents the second track instead).
- Never a data-losing migration here: ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- Classify-AI failure stays fatal (F4 boundary). Only the main-brief call moves inside the `try`; a classify throw still means no triage happened, signals stay unprocessed, and the tick correctly finishes `error`.
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change); unset behaviour is byte-identical to today. His one line: set `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var and reload `/api/health` once — the two money columns appear and the money legs fill on the same call (and Start/Win stop 503ing).
- The 11 verdicts are the owner's dashboard presses (needs `ADMIN_TOKEN` + per-row judgment), not agent writes. His one line: open Needs-review oldest-first and press V or K on each of the 11 rows.
- The $0 starter and its close are the owner's (Start the seed sprint, run it, Win/Lose with a human-entered $ amount + post-mortem). The 10 brand sends are his outward action, not the agent's.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action. (No deploy action is outstanding: live rev equals HEAD `f43c623`.)
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment from the decision + brief lines. Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure — the inline Win close takes a human-entered amount for exactly this reason.
- Never, whatever the ranking says: no spending, no account or identity creation, no outward sends, no accepting terms, no captcha solving, no fabricated reviews/receipts/testimonials, no scraping or messaging a platform's terms forbid, no financial advice or implied return.
- House rules honored by this audit: never read `.env`/secrets/tokens; no edits to existing files (this findings file only); LF endings verified (`git ls-files --eol`, all 41 tracked files `i/lf w/lf`).

## 6. Proposed next tasks (2-4)

Disposition of the focus-named work (it outranks anything found, so it is answered first): the vet-path round resolves to human-by-design (F1: path `public/app.js:562`, no failing test because nothing is broken, one Vet press yields exactly one verdict, stall already dated by `last_vetted`/`last_verdict`), and its smallest queue-clearing speedup already shipped (V/K + auto-advance + prefetch + starter advance, all verified in code). Every other focus-named code item is verified shipped with the suite green: probe isolation + failure naming + `db`-by-answer (`functions/api/[[path]].js:542-580,686`), deploy convergence via one shared script (`.github/workflows/deploy-worker.yml:33-34`, `deploy/apply-d1-migrations.sh:13-28`), the default-off self-migration with its three results (`functions/api/[[path]].js:36-77`, 9/9 suite), and live==HEAD. The unshipped remainder is human/owner work (11 verdicts, the migration switch, the $0 sprint) — not implementer-completable offline, so it lives in §5 as one line each. The tasks below are the remaining offline-completable code items, most valuable first.

### Task 1. Main-brief AI failure records brief:failed and finishes ok instead of erroring the tick

Owns: `worker/src/index.js`, `worker/src/index.test.js`. Context (F4): the main-brief `aiComplete` (`worker/src/index.js:578-582`) sits outside the `try`, so a brief-model throw fails a tick whose triage already flushed — while the extra pass (`:617-633`) degrades to `brief:failed` + `ok`.

Acceptance: the main-brief AI call is inside the existing `try` (or the `try` covers it); a stubbed brief-model throw finishes `ok` with `brief:failed` on the error line and verdict writes flushed; classify-throw-stays-fatal covered by a test; full `npm test` green.

### Task 2. Name the revenue_source migration path in owner docs and the dropped-source toast

Owns: `docs/OWNER-ACTIONS.md`, `public/app.js`, `public/dashboard.test.mjs` (pin only). Context (F5): the switch adds cents only, so post-switch Wins keep dropping `via` with a toast (`public/app.js:874`) pointing at an unnamed migration; `docs/OWNER-ACTIONS.md` §2 never names the secrets + redeploy path.

Acceptance: owner docs name the `revenue_source` second track (CI secrets + redeploy, or manual `deploy/deploy.sh`); the dropped-source toast points at it; the dashboard suite pins the wording; write paths and retry behavior untouched; full `npm test` green.

### Task 3. Require a post-mortem line on API kills that clear UNREVIEWED

Owns: `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Context (F6): the tagless-clear guard (`functions/api/[[path]].js:266-270`) accepts a killed tag with no text after it, so a raw kill can skip the post-mortem the board keeps killed rows for; all existing kill tests already send tag + text.

Acceptance: a killed clear of an `UNREVIEWED` row without post-mortem text 400s naming it with the row queued; tag+text kills pass; reviewed-row kill shapes byte-identical in behavior; full `npm test` green.
