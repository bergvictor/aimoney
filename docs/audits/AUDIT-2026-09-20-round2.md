# AUDIT-2026-09-20-round2 — aimoney (read-only)

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs, money-tracked experiments, and a cron research agent that proposes new rows; the owner vets, tests, kills/scales.
- Entry points: dashboard `public/index.html` + `public/app.js`; API `functions/api/[[path]].js` (D1); agent `worker/src/index.js` (`worker/src/lib.js` shared pure helpers).
- Live surfaces: `https://aimoney.pages.dev/` (dashboard), `/api/health`, `/api/opportunities`, `/api/runs`, `/release.json`; worker `https://aimoney-research.levitinvlad.workers.dev/` (see `deploy/public-surfaces.json`).
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:8-9`) — collect signals, AI-triage (6/run), brief 1+1 bare rows, log to `agent_runs`; manual `POST /run` is triage-only.
- How it deploys: `deploy/deploy.sh` (D1 schema + shared migrations + seed guard, stamp `release.json`, Pages + worker) or Git push (Pages auto-build via `scripts/stamp-release.sh`; worker via `.github/workflows/deploy-worker.yml`); `deploy/verify.sh` gates the rollout on the live revision.

## 2. End-to-end walk-through

Main flow: public signal → agent proposal → human vet → experiment → decided (won/lost with post-mortem and money).

1. Cron fires `scheduled` → `runResearch(env, "cron")` (`worker/src/index.js:576-584`, `:239`). Stuck `running` rows older than 30 min are reaped (`:243-248`); a new `agent_runs` row opens (`:249-258`).
2. Collect: `hnSignals` / `redditSignals` / `githubSignals` (`:48-131`) fetch in parallel via `timedJson` (`:36-46`, 6 s body-covering timeout). Per-query throws mark `state.src_fail` (`:73`, `:101`, `:129`); a fully rejected collector is named too (`:276-280`).
3. Signals insert in one `env.DB.batch` (`:286-290`); unprocessed signals older than 30 d are swept to noise with a count (`:299-303`); the oldest 6 unprocessed are taken (`:305-307`).
4. Exact-URL pre-pass (`:313-339`) links taken signals to an existing `source_url` via pure `exactUrlTarget` (`:167-177`) — no AI call, notes newest-kept, no status move.
5. Classify: top-60 list (`:351-353`) + fresh signals in one Workers AI call (`:354-370`, `aiComplete` `:135-152`); NDJSON parsed by `parseJsonLines` (`worker/src/lib.js:62-77`, `repairJson` `:60`). Quiet ticks skip fetch, prompt and call (`:350`, `:366`); manual runs share the skip.
6. Verdicts validate (first-wins index, strict enum, numeric-or-null id), inflow caps at `MAX_NEW_PER_RUN = 2`, dropping to 1 while bare-without-brief exceeds 10 (`:374-383`). `new` inserts a `researching` row with `UNREVIEWED` + score capped ≤6000 + reversible money estimates (`:418-433`, `agentMoneyEstimates` `:179-194`); slug collision links as supports (`:438-446`); verdict writes flush via `flushVerdictWrites` with per-write fallback (`:222-238`, called `:460`).
7. Briefs: one bare row (top-scored, or oldest-unreviewed-first past 48 h, `:471-486`) via shared `buildBriefPrompt`/`parseBriefJson` (`:201-215`), plus one extra oldest-unreviewed past 48 h on cron (`:519-559`); failures record `brief:failed` and retry next tick; the run log finishes once carrying the brief mode (`:562`).
8. Dashboard boots: `paintLastGood()` then `refresh()` → `refreshTargets()` (`public/app.js:1257`, `:1316`, `:1326`) fetching opportunities, experiments, runs, health in parallel; failures render as a named banner with Retry (`renderApiErrors` `:1180-1199`).
9. Review without the drawer: `deriveReviewList` filters `needs_review` oldest-first client-side (`:239`); `Vet` (`:421-426`), `Vet & log starter` (`:450`), `Kill` (`:487`) share `fetchDetailForWrite` (`:411`) then PATCH `/api/opportunities/:id` (`functions/api/[[path]].js:207-248`, text bounds `:216-221`, range guard `:232-234`).
10. Experiment loop: `openExperimentModal` (`public/app.js:1046`) POSTs `/api/experiments` (`functions/api/[[path]].js:324-373`, parent 404 `:333-334`, closure gate `:335-340`, stamps `:341-345`); `renderExperiments` (`public/app.js:648`) sorts stale-first with `Nd` chips from `days_in_status` (`functions/api/[[path]].js:287-301`); Start/Win/Lose PATCH `updateExperiment` (`:375-428`, gate `:398-403`, outcome-ledger append `:408-417`).
11. Health aggregates the lane: 11 batched probes (`functions/api/[[path]].js:456-471`) with isolation fallback (`:481-510`, failing probe named, missing column/table extracted `:497-499`), lazy self-migration on money-probe failure (`:519-535`), outage-honest payload (`:586`). The dashboard renders decisions/vetted/revenue with `typeof` guards plus `fallbackDecisions` (`public/app.js:562`, `:661`) and names failing probes in pill tooltip (`:726-727`) and banner (`:1193`).
12. Deploy converges the schema: `deploy.sh` runs `d1/schema.sql` (`:33`) then `./deploy/apply-d1-migrations.sh` (`:37`, sorted `d1/migrate-*.sql` loop with duplicate-column tolerate-list, `apply-d1-migrations.sh:13-28`); the seed guard aborts on an unreadable COUNT (`deploy.sh:38-55`); CI does schema (`.github/workflows/deploy-worker.yml:27`) then the same shared script (`:33-37`) after failing fast on empty secrets (`:20-26`).

Where it most often breaks or goes silent: (a) the live D1 predates the 2026-09-20 money columns, so the two money probes fail and four fields read null — isolated and named, not fatal — and experiment writes 503 on the uncovered `revenue_source`; (b) Workers AI timeouts / malformed brief JSON — retried next tick, marked `brief:failed`; (c) the human step: 11 unreviewed (oldest 173.1 h), 0 vetted/week, 2 planned / 0 running — the queue drains only when the owner taps Vet/Start/Win, and nothing in code blocks those taps.

## 3. Health signals measured here

- Test suite: `npm test` (`package.json:7`: `node --test` over `worker/src/lib.test.js worker/src/index.test.js functions/api/api.test.mjs d1/migrations.test.mjs deploy/deploy.test.mjs public/tab-icon.test.mjs public/dashboard.test.mjs`) → exit 0 (observed twice: full TAP run and `--test-reporter=dot` run, both exit 0, zero failures). Dot run printed 419 dots = 313 tests + 106 suites, matching the committed baseline (`_night/tests-baseline.log`: tests 313, suites 106, pass 313, fail 0, duration_ms 264.1969). Only noise: `UNDICI-EHPA` proxy and `MODULE_TYPELESS_PACKAGE_JSON` warnings — cosmetic. (The TAP tail with the `ℹ` summary lines was cut by tool-output truncation, so counts are evidenced by the dot run + baseline file, not by re-quoting the tail.)
- Self-migration switch, the three asked-for cases (suite `self-migration switch`, `functions/api/api.test.mjs`, 6/6 green in this run): (1) old-schema table + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers on the same call; (2) variable unset leaves the table unchanged and the payload names `schema_missing_columns` plus `schema_migration: "disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)"`; (3) a second run is a clean no-op (no re-PRAGMA, no re-ALTER). Exact variable name: `ALLOW_SCHEMA_MIGRATION`.
- CI step (already landed, verified present): `.github/workflows/deploy-worker.yml:33-37` — step `Apply D1 migrations in sorted order`, `run: ./deploy/apply-d1-migrations.sh`, after `wrangler d1 execute aimoney --file=d1/schema.sql --remote` (`:27`). Pinned by `d1/migrations.test.mjs:38-70` including the positive control "withholding any one migration file breaks convergence".
- No Python sources: a `**/*.py` filename search returned zero files while the same tool lists the 32-file JS/SQL/SH tree (positive control), so `compileall` does not apply. Runtime observed: `node --version` → `v22.22.3`.
- Dead code: one item. `appendKeepNewest` is exported (`worker/src/lib.js:49`) and unit-tested (`worker/src/lib.test.js:89-100`) but imported nowhere in shipped code — every writer inlines the SQL idiom `substr(notes || ?, -8000)` instead (e.g. `functions/api/[[path]].js:369`, `worker/src/index.js:335`). Tested spec of a live idiom, not rot (see F7).
- Duplicated logic: (1) `centsDollars` (`functions/api/[[path]].js:82`) vs `moneyCents` (`public/app.js:53`) — intentional documented mirror, keep; (2) `clamp10` in the API (`:74-78`) vs `worker/src/lib.js:4-7` — near-identical, the API imports only `effectiveScore`/`tokensMatch` (`:10`); (3) brief INSERT SQL still twice (main `worker/src/index.js:501-509` vs extra `:546-554`) though prompt+parse are now shared (`:201-215`, HEAD); (4) `inlinePostMortem` vs `inlineWinClose` (`public/app.js:294-332` vs `:338-400`) share structure, Win adds $ + source — documented mirrors, keep unless touched.
- TODO density: zero. Regex `TODO|FIXME|HACK|XXX` over `public functions worker d1 deploy scripts` → no hits; positive control: the same regex tool matches `UNREVIEWED` across 14 files, so the zero is a real zero. `console.*`/`debugger` in shipped source: only the two deliberate `console.error("cron-failed", …)` markers (`worker/src/index.js:570`, `:582`); dark-mode strings (`prefers-color-scheme`, `data-theme`, `theme-toggle`) appear only in guard-test assertions (`public/tab-icon.test.mjs:70-76`, `public/dashboard.test.mjs:164`), never in shipped code.
- Surface rules (all pass, zero findings): `color-scheme: light` (`public/styles.css:4`); `<link rel="icon" … href="/favicon.svg">` + `<meta name="theme-color" content="#0d6b3f">` (`public/index.html:9-10`); hand-written 32×32 `$`-on-`#0d6b3f` `public/favicon.svg` (1 line, <2 KB); inline brand mark with the same geometry/accent/glyph (`public/index.html:16`); guard suite `public/tab-icon.test.mjs` green. The saved live shell matches the served shell on every marker.
- Build identity: this surface publishes its revision via `/release.json` (stamped by `scripts/stamp-release.sh:5-8` on Pages builds and `deploy/deploy.sh:58` on manual deploys) and echoes it as `rev` in `/api/health` — one source of truth, already checked by `deploy/verify.sh:46-80` (JSON parse required, HTML/unparsable/`dev-undeployed` placeholder fail immediately, stale revisions retried bounded 6×10 s). No `/build.json` and none should be added. Live `/release.json` body was not fetched in this lane (only `/` and `/api/health` were saved; no network here), and the committed `public/release.json:1` is the `dev-undeployed` placeholder by design — do not judge the edge by the committed file.
- Live evidence (saved capture `_night/live/aimoney_pages_dev_api_health.html`, rev `8bb8446`, HEAD is `91598ff`): `{"ok":true,"rev":"8bb8446","db":"up","opportunities":27,"unreviewed":11,"bare_without_brief":19,"experiments_by_status":{"planned":2},"hours_since_last_ok_run":5.5,"oldest_unreviewed_age_h":173.1,"decisions_last_7d":0,"revenue_last_7d":null,"revenue_total":null,"spent_total":null,"vetted_last_7d":0,"vetted_no_experiment":0,"noise_24h":0,"time":"2026-09-21T05:27:26.344Z","health_probe_failures":["revenue_week","revenue_lifetime"],"health_probe_detail":{"revenue_week":"revenue_cents","revenue_lifetime":"revenue_cents"},"schema_missing_columns":["revenue_cents","spent_cents"],"schema_migration":"disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)"}`. The switch, the split decisions probe (`decisions_last_7d: 0`, no longer null), and the disabled line are all live.
- First five seconds: `#start-here` sits above `#ledger` (`public/index.html:42` vs `:57`), the strip renders the #1 actionable pick with $/mo, capital, next action and Vet/Kill-or-drawer actions (`public/app.js:103-133`), and the masthead compacts on ≤760 px (`public/styles.css:210-215`).
- Blocker (pre-existing, not mine): the working tree shows 36 modified files with equal insertions/deletions (`git diff --stat`: 10096/10096), and `file(1)` reports CRLF line terminators in the worktree (e.g. `public/app.js`, `public/favicon.svg`, `worker/src/index.js`); the repo carries no `.gitattributes`. Consistent with line-ending-only dirt. I changed nothing; the audit file below is the only new file I create. Acceptance's "only that new file" cannot hold until the CRLF dirt is normalised by whoever owns the checkout (see F6).

## 4. Ranked findings (max 8)

Money first (owner directive, numbers from this project's own data): this project is a tool the owner uses, not a product he sells — it tracks money, it earns none itself. Earned to date: none; last krone arrived: never (`revenue_total` is null = unknown on live, and `experiments_by_status` is `{"planned":2}` with zero `running`/`won`/`lost`, so there is not even a $0 close on record). The single step between the current state and the first tracked payment is a human closing one experiment as won with a real human-entered $ amount (one-click Win on a planned card); the measured conversion at that step is 0 decisions/week across five consecutive snapshots, with `vetted_last_7d` 0 and the vetted→experiment conversion likewise 0. The bottleneck is outside the code: 11 unreviewed (oldest 7.2 days), 19 of 27 rows without briefs, nothing running — no code change can vet or close a row.

F1. The pipeline has not moved in seven days — review the 11, brief the 19, start or kill the 2.
Evidence: live health (see §3): `unreviewed: 11`, `oldest_unreviewed_age_h: 173.1`, `bare_without_brief: 19`, `experiments_by_status: {"planned": 2}`, `vetted_last_7d: 0`, `decisions_last_7d: 0`. Four shipped rounds improved the machinery while the queue aged from six days to seven.
Why it costs: unreviewed backlog drained and experiments decided/week are this lane's number, and both sit at zero movement — every further code round without pipeline work is motion without progress.
Fix: do the pipeline work through the app's own paths, no new schema: Vet/Kill each of the 11 with reasons recorded (Vet appends `[YYYY-MM-DD vetted]`, Kill requires a one-line post-mortem — `public/app.js:421-521`); brief the bare rows (or record concretely why a row cannot have one); name the next concrete step + owner for each planned experiment; rank the remainder by expected money vs effort and name the single best candidate. This is proposed next task 1, not a code change. Size: human effort, no code. Risk: none to the codebase — verdicts are reversible at vet time and Kill keeps the post-mortem on the board.
Report: reviewed 0, rejected 0, briefs written 0, best candidate unnamed — this lane is read-only with no network and the row contents were never saved here, so no verdict could be recorded. The health endpoint's unreviewed count is not lower than 11 at the end of this round for exactly that reason: a read-only audit cannot move rows through the app's write paths.

F2. Four money fields stay null on live; the fix is one owner env var, no code.
Evidence: live `revenue_last_7d/revenue_total/spent_total: null` with `schema_missing_columns: ["revenue_cents","spent_cents"]` and the disabled line; the switch is live and default-off (`functions/api/[[path]].js:26-72`, `:519-535`).
Why it costs: every money figure on the dashboard reads unknown, so the owner cannot see the first krone even after closing an experiment — and the one line that tells him the fix exists lives only in raw JSON (see F3).
Fix: the owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages environment variable; the next `/api/health` call adds exactly the two `ADD COLUMN` statements from `d1/migrate-2026-09-20-experiment-cents.sql` and reports numbers on the same call. Note `revenue_source` stays unmigrated by design, so experiment writes keep answering 503 naming that column until the deploy path (F4) converges the schema. Files to touch: none. Size: 1 env var, no code. Risk: low — ADD-only with defaults, once per isolate, tested no-op on re-run.

F3. The dashboard never shows the migration one-liner, so the owner cannot find the switch.
Evidence: `public/app.js` renders `health_probe_failures` in the banner (`:1193`) and pill tooltip (`:726-727`) but references `health_probe_detail`, `schema_missing_columns` and `schema_migration` zero times (verified by search) — the owner sees "Health probes failing: revenue_week, revenue_lifetime" with no column and no action, while the payload already carries both.
Why it costs: F2's fix stays invisible where the owner actually looks; the brief's "that line is how he finds out the switch exists" fails at the last hop.
Fix: in `renderApiErrors` (`public/app.js:1180-1199`) append the detail map (`revenue_week → revenue_cents`) and, when present, the `schema_migration` line verbatim; mirror the column names into the pill tooltip (`:726-727`). Read-only, no new fetch, keep the existing `probe-failure disclosure` dashboard tests green and extend them. Files: `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk: low — render-only change to an already-failing state.

F4. CI has no Cloudflare secrets, so the worker and D1 never converge from CI.
Evidence: `.github/workflows/deploy-worker.yml:20-26` fails fast with named errors when `CF_API_TOKEN`/`CF_ACCOUNT_ID` are empty; the repository has no such secrets, so schema + shared migrations (`:27`, `:33-37`) and the worker deploy (`:38-41`) never run from CI. Pages (dashboard) auto-deploys via Git, which is why the edge moves while D1 and the worker config do not.
Why it costs: every migration depends on a manual `deploy.sh` run with a local token; the `revenue_source` column (F2) and any future schema change cannot reach live D1 through the checked-in path.
Fix: the owner adds `CF_API_TOKEN` and `CF_ACCOUNT_ID` in this repository's Settings → Secrets → Actions — one line, his action, no code. After that the next push touching `worker/**` or `d1/**` converges schema + migrations + worker by itself. Files to touch: none. Size: 2 secrets, no code. Risk: low — the workflow already fails loudly rather than silently, and migrations are duplicate-column no-ops on re-run.

F5. HEAD is one commit ahead of the edge — deploy it, then verify.
Evidence: live `rev` is `8bb8446` while HEAD is `91598ff` (`git log --oneline -12`); HEAD adds D1-dead cron surfacing (`worker/src/index.js:570`, `:582`) and the `fetchDetailForWrite` (`public/app.js:411`) and `buildBriefPrompt`/`parseBriefJson` (`worker/src/index.js:201-215`) dedupes.
Why it costs: the cron-failure tail marker from the latest round is not live, so an overnight D1 outage still reads as a quiet night on the deployed worker.
Fix: deploy HEAD to Pages (push or `deploy/deploy.sh` — this lane must not touch production), then run `deploy/verify.sh` and require the `live-revision` gate to print the new short SHA. Files to touch: none. Size: deploy only, no code. Risk: low — covered by the existing revision gate and the green suite.

F6. CRLF dirt rewrites all 36 files and will corrupt the next fragment edit.
Evidence: `git status --short` lists 36 modified files; `git diff --stat` shows 10096 insertions / 10096 deletions; `file(1)` reports CRLF terminators in the worktree; no `.gitattributes` exists (glob search, zero hits). The brief documents tonight's `export async function` → `port async function` corruption from exactly this cause.
Why it costs: acceptance ("only that new file") is uncheckable, every diff is noise, and any search-and-replace written with `\n` risks a one-character-off corruption like the one that already ended a round unfinished.
Fix: add `.gitattributes` pinning text endings (`* text=auto`, scripts/sh `eol=lf`), convert the worktree once, and re-run `npm test` to confirm byte-identical behaviour; whoever owns the checkout does the conversion so no agent's in-flight edits are clobbered. Files: `.gitattributes` (new), worktree conversion. Size S. Risk: low-medium — mechanical, but touch nothing else in the same pass and re-read one converted file before committing.

F7. `appendKeepNewest` is tested but imported nowhere — delete it or use it.
Evidence: exported at `worker/src/lib.js:49`, covered by `worker/src/lib.test.js:89-100`, zero imports in shipped code (search over `public functions worker d1 deploy scripts`); every writer inlines `substr(notes || ?, -8000)` instead.
Why it costs: almost nothing — a reader must check every call site to learn the helper is not the implementation. Kept this long only because its test pins the live idiom.
Fix: either delete the helper and its suite (the SQL idiom is already pinned by behaviour tests) or import it in the one JS-side notes append if any appears. Do not expand its API. Files: `worker/src/lib.js`, `worker/src/lib.test.js`. Size S. Risk: none — no behaviour change either way.

## 5. What NOT to change

- Pipeline verdicts are the owner's (or a human-delegated reviewer's): Vet/Kill decisions, brief content, experiment starts/closes, and the single-best-candidate ranking. This audit lane is read-only; a future worker does them through the app's own write paths (`public/app.js:421-521` review handlers, `/api/briefs`, `/api/experiments`), never by touching production D1 directly.
- Credentials and deploys to production: never create, enter, print or store `CLOUDFLARE_API_TOKEN` / `CF_API_TOKEN` / `CF_ACCOUNT_ID` / `ADMIN_TOKEN`; never run `wrangler d1 execute --remote` or any migration against live D1. Owner's one-liners: add `CF_API_TOKEN` + `CF_ACCOUNT_ID` in repo Settings → Secrets → Actions (F4); set `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var (F2).
- Never spend money, enter payment details, create an account, sign up for anything, or accept terms — every one of those is the owner's. Never invent a revenue figure, never mark an experiment decided without evidence, and never present a projection as a result.
- The self-migration stays exactly as specified: default-off, ADD-only (`revenue_cents`, `spent_cents`), once per isolate, no second trigger, no endpoint that runs migrations (`functions/api/[[path]].js:26-72`). Do not extend it to `revenue_source` — the 503 that names the column is the intended behaviour until the deploy path converges.
- No second version source: `/release.json` is the one the deploy stamps and verifies (`deploy/verify.sh:46-80`); do not add `/build.json`. The committed `dev-undeployed` placeholder is by design — judge the edge only by a fetched, JSON-parsed body.
- Deliberate behaviours to keep, quoted: "The agent never moves status" (`worker/src/index.js:4-5`); "scores capped until a human vets it"; manual `POST /run` "202 carries `briefs_skipped:true`" (`:655`); "Killed strategies stay on the board with their post-mortem" (`README.md:64`); outcome-ledger rescore "never auto-applied" (`README.md:74`); `console.error("cron-failed", …)` is "deliberate: worker tail is the only surface that survives D1 death" (`worker/src/index.js:569`).

## 6. Proposed next tasks (2–4)

- [ ] Task 1: Vet or kill all 11 unreviewed rows via the dashboard (51 chars)
  Human pipeline work through the app's own paths; no new schema, no new file. For each of the 11 unreviewed rows record a verdict with its reason in the store the app already uses (`Vet` clears `UNREVIEWED` and appends `[YYYY-MM-DD vetted]`; `Kill` sets `killed` with a one-line post-mortem — `public/app.js:421-521`, API `functions/api/[[path]].js:207-248`). Write briefs for the rows that have none, or record concretely why a row cannot have one yet. For each of the 2 planned experiments state the next concrete step and who takes it (worker or owner; owner items as one actionable line each). Rank the remainder by expected money vs effort and name the single best candidate with its case. Never spend money, create accounts, start costly experiments, or invent opportunities — rejecting honestly with reasons is a good outcome.
  Owned files: none (live app paths only). Acceptance: `/api/health` `unreviewed` is lower than 11; `vetted_last_7d` > 0 or kills carry post-mortems; brief count is up or every bare row has a recorded why-not; the report names ONE best candidate with its case plus the exact owner-only list.
- [ ] Task 2: Surface the schema-migration one-liner in the dashboard banner (59 chars)
  Render `health_probe_detail` and the `schema_missing_columns`/`schema_migration` line in `renderApiErrors` (`public/app.js:1180-1199`) and the pill tooltip (`:726-727`), read-only with no new fetch.
  Owned files: `public/app.js`, `public/dashboard.test.mjs`. Acceptance: with the live-shaped payload the banner names the column (`revenue_cents`) and prints the `ALLOW_SCHEMA_MIGRATION=1` line; pill tooltip names columns; existing probe-disclosure tests green and extended; full `npm test` exit 0.
- [ ] Task 3: Deploy HEAD to Pages and gate on verify.sh live-revision (53 chars)
  Ship `91598ff` (one commit ahead of the live `8bb8446`) via push or `deploy/deploy.sh`, then require `./deploy/verify.sh` to print `ok live-revision` for the new short SHA.
  Owned files: none (deploy + verify scripts, unchanged). Acceptance: fetched `/release.json` parses as JSON with `revision` equal to the deployed short SHA; `verify.sh` exits 0; `/api/health` `rev` matches.
- [ ] Task 4: Pin text endings via .gitattributes and normalize CRLF dirt (56 chars)
  Add `.gitattributes` (`* text=auto`, shell/SQL/JS `eol=lf`), convert the 36-file CRLF worktree once, re-run the suite to confirm byte-identical behaviour.
  Owned files: `.gitattributes` (new), worktree conversion. Acceptance: `git status --short` shows only intended files; `git diff --stat` no longer rewrites all 36 files; `npm test` exit 0; one converted file re-read to confirm content intact.
