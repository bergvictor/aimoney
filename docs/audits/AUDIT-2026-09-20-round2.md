# AUDIT-2026-09-20-round2 — aimoney (round)

Clone: `/root/coding-projects/_workspace/day1-aimoney-r2` at `0fb4fb2` (read-only; this file is the only write).
Live snapshots read: `_night/live/aimoney_pages_dev.html` (HTTP 200, 5390 bytes, 119 lines), `_night/live/aimoney_pages_dev_api_health.html` (HTTP 200, 310 bytes, 1 line).
No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` in clone; `README.md` used as governing doc. `.env`/secrets never read. No git writes, no network, no installs. One single-stage shell command per tool call; no inline code.
HEAD `0fb4fb2` ("Night round 1 (aimoney): Implemented audit Tasks 1-3: starter strip, honest drawer scores, oldest-first triage") verified present with tests; parked branch `night/day1-aimoney-round1` is absent (`git branch -a` shows only `master`, `remotes/origin/master`) and fetch is forbidden, so the branch delta stays unverified — what master carries at `0fb4fb2` is what was audited.

Lane metric (aimoney row): experiments reaching a decision (`won`/`lost` with post-mortem) per week, plus vetted proposals becoming experiments, with the unreviewed backlog drained. Current value from the repo's own live data: **0 decisions and 0 vetted in 7d** (`decisions_last_7d:0`, `vetted_last_7d:0`), **11 unreviewed of 27 (oldest 155.4h ≈ 6.5d)**, **19/27 bare without brief**, `experiments_by_status:{"planned":2}` with zero `running`/`won`/`lost`. Live rev is `d925ae6` — one deploy behind master `0fb4fb2` — and the counts are identical to the four prior snapshots (`27/11/19/planned:2`, oldest aging 149.4h → 155.4h over days): the queue has not moved one row while the run stays fresh (`hours_since_last_ok_run:5.7`).

## 1. What this project is and how it runs

- Purpose: Cloudflare-native AIMoney Lab — ranked AI-money opportunities with scores, research briefs, experiments, and a cron research agent.
- Entry points: dashboard `public/index.html` + `public/app.js`; API `functions/api/[[path]].js`; agent `worker/src/index.js` + `worker/src/lib.js`; DB `d1/schema.sql`.
- Live surfaces: `https://aimoney.pages.dev`, `https://aimoney.pages.dev/api/health`, worker `https://aimoney-research.<account>.workers.dev`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`) — collect signals, AI triage oldest-first with 30d sweep, 1–2 briefs per cron tick, log to `agent_runs`.
- How it deploys: `./deploy/deploy.sh` (D1 schema + seed-if-empty, stamp `public/release.json`, Pages deploy, worker deploy); Pages Git-connected rebuild + `.github/workflows/deploy-worker.yml` on `worker/**` push; `./deploy/verify.sh` checks markers + 12h freshness.

## 2. End-to-end walk-through

Main loop: signal found → agent proposal → human vet → experiment → close with learning → rescore.

1. Cron/manual triggers pass: `worker/src/index.js:404-406` (`scheduled` → `runResearch(env,"cron")`) and `worker/src/index.js:460-469` (`POST /run` → `waitUntil(runResearch(env,"manual"))`, 202 `{"status":"accepted","briefs_skipped":true,"reason":"manual skips brief pass"}`).
2. Collect signals in parallel: `worker/src/index.js:176` (`Promise.allSettled([hnSignals(), redditSignals(), githubSignals()])`); sources `worker/src/index.js:47-69` (HN ×4), `worker/src/index.js:71-93` (Reddit ×3), `worker/src/index.js:95-116` (GitHub, `slice(0,2)` of 3 queries at `:115`); fetch helper `worker/src/index.js:35-45` (`timedJson`, 6s abort covering body read).
3. Batch-insert signals + heartbeat + sweep: `worker/src/index.js:181-186` (`INSERT OR IGNORE INTO signals`, one `DB.batch`); `worker/src/index.js:187-190` (`phase:collected/classify-ai/...` heartbeat); reap stuck runs `worker/src/index.js:145-150` (`running` older than 30 min → `error`); 30d stale sweep `worker/src/index.js:194-199` (unprocessed older than 30d → noise with `state.stale` count).
4. Classify oldest-first vs live list: `worker/src/index.js:201-203` (`SELECT * FROM signals WHERE processed = 0 ORDER BY id ASC LIMIT ?`, bound to `MAX_AI_SIGNALS = 6` at `:15`); `worker/src/index.js:210-212` (top-60 opps); `worker/src/index.js:226-230` (`aiComplete` Mistral NDJSON); parse `worker/src/lib.js:49-64` (`parseJsonLines` + `repairJson` for `\_` at `worker/src/lib.js:47`).
5. Apply verdicts: new → `worker/src/index.js:251-275` (`INSERT INTO opportunities … status="researching"`, `effectiveScore({…,"UNREVIEWED"})` capped ≤6000 at `:267`); supports → `worker/src/index.js:276-284` (`substr(notes || ?,-8000)`, newest kept); invalid action → `worker/src/index.js:241-243` and noise → `worker/src/index.js:285-287` (marked `processed=1`, dropped); supports-without-id demoted to noise at `worker/src/index.js:248-250`.
6. Brief one–two bare opps (cron only): `worker/src/index.js:174` (`briefDeadline = trigger==="cron" ? 300000 : -1`, manual always skips); `worker/src/index.js:293-302` (mode flag: `top-scored` default, `oldest-first` when oldest unreviewed exceeds 48h); main brief query `:303-308`; Llama brief JSON `:314-326`; `INSERT INTO briefs` skipped if `summary`/`first_steps` empty (`:332`); extra oldest-unreviewed bare row when backlog >48h `worker/src/index.js:349-393` (`retries:0`, `MAX_AI_CALLS` guard); double `finish("ok")` at `:394-395` (first write immediately overwritten by the mode-disclosing second).
7. Board reads: `public/app.js:732-751` (`refresh()` loads `/api/opportunities?limit=200`, `/api/experiments`, `/api/runs?limit=20`, `/api/health`, `/api/meta`, tracking `state.apiFailures`); list `functions/api/[[path]].js:39-66` (`unreviewed=1` filter, `oldest` sort, shared `effectiveScore` cap at `:61`, re-sort at `:62-64`, `brief_count`/`experiment_count` at `:54-56`); starter strip `public/app.js:89-117` + `public/index.html:55` with `Zero spend` chip (`public/index.html:50`, `public/app.js:76-77`); review chip + age `public/app.js:179-214`; review view with Vet/Kill `public/app.js:147-177`; failure banner `public/app.js:714-729` + `public/index.html:40`.
8. Human vets: `public/app.js:220-236` (Vet strips `UNREVIEWED`, lifts cap, keeps status, appends `[YYYY-MM-DD vetted]`; success toast with one-click Log experiment at `:231`) / `public/app.js:238-256` (Kill sets `killed` + prompted post-mortem) → `functions/api/[[path]].js:118-155` (`PATCH /api/opportunities/:id`, range 400 at `:139-141`, status 400 at `:129-131`, rescore via shared `effectiveScore` at `:143`).
9. Experiment loop: log/update `public/app.js:623-668` (`openExperimentModal`) → `functions/api/[[path]].js:231-273` (create: invalid-status 400 at `:236-238`, unknown-opp 404 at `:240-241`, won/lost closure rules at `:242-247`, stamps at `:251-252`, ledger append at `:264-271`) / `functions/api/[[path]].js:275-319` (update: same rules plus outcome-ledger append at `:302-309`); list with age/orphan `functions/api/[[path]].js:209-229` (`days_in_status` at `:223-227`, `orphaned` via `LEFT JOIN` at `:217-226`); board stale-first + age chips + decisions header + stall nudge `public/app.js:300-347` (nudge at `:321-328`, `vetted_no_experiment` at `:307`); detail drawer + admin re-optimize `public/app.js:414-552` (capped-score marker at `:421`, suggested rescore `:469-479` / `:486-491`, never auto-applied).
10. Health/verify: `functions/api/[[path]].js:334-368` (`/api/health`: `rev,db,opportunities,unreviewed,bare_without_brief,experiments_by_status,hours_since_last_ok_run,oldest_unreviewed_age_h,decisions_last_7d,vetted_last_7d,vetted_no_experiment,noise_24h,time`); `deploy/verify.sh:28-58` (7 markers + non-empty list + freshness ≤12h).

Where it most often breaks or goes silent: the human bottleneck after hop 5, unchanged across five snapshots. Live health proves it: `unreviewed:11/27` (41%, oldest 155.4h), `bare_without_brief:19/27` (70%), `experiments_by_status:{"planned":2}` with zero `running`/`won`/`lost`, `decisions_last_7d:0`, `vetted_last_7d:0` — identical counts while the oldest aged days. Master `0fb4fb2` now ships exactly the tooling for this stall (starter strip + zero-spend chip, drawer cap marker + no-brief pill, oldest-first triage + 30d sweep — all green in `npm test`) but live still serves `d925ae6`, so none of it has touched real data yet. Secondary silent spots: the drawer — where vetting decisions are made — has no Vet/Kill and its status→killed path skips the post-mortem the review Kill requires (F4); the Experiments header mixes a 7d vetted count with an all-time experiment total in one "conversion" readout (F7).

## 3. Health signals measured here

- Test suite (`npm test` = `node --test worker/src/lib.test.js worker/src/index.test.js functions/api/api.test.mjs public/tab-icon.test.mjs public/dashboard.test.mjs`): **PASS, 81/81, 32 suites, 0 fail**. Real tail: `# tests 81 / # suites 32 / # pass 81 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0 / # duration_ms 1022.400267`. Prior round was 69/69; +12 from round-1 Tasks 1–3 (starter strip 4, drawer/brief-pill 3, detail-cap agreement 3, oldest-first triage 2). Windows baseline from the brief was `duration_ms 242.3521` — the slower 1022ms here is sandbox overhead. Warnings only: `UNDICI-EHPA` proxy-agent notice and `MODULE_TYPELESS_PACKAGE_JSON` reparsing hint for `[[path]].js`/`index.test.js`/`lib.test.js` (add `"type":"module"` to silence; no behavior change). One sandbox note: the runner recorded a denied `localhost:80` request during the run — that is the health handler's best-effort `fetch(/release.json)` (`functions/api/[[path]].js:363-366`) failing gracefully inside the in-memory API tests (caught, `rev` stays `"unknown"`); all health tests still pass.
- Syntax checks (no writes): `node --check public/app.js` → exit 0; `node --check worker/src/index.js` → exit 0; `node --check worker/src/lib.js` → exit 0 (each with only the `UNDICI-EHPA` warning). `node --check` on `functions/api/[[path]].js` not run separately (bracketed path); the file is imported and exercised green by `npm test` instead.
- `python3 -m compileall -q`: NOT RUN — no Python sources in repo (glob `**/*.py` returns zero files) and `compileall` would write `__pycache__` into this read-only clone. The `node --check` line above is the real syntax signal.
- Dead code (inspected, not guessed): `worker/src/index.js:20` defines `const BRIEF_DEADLINE_MS = 18000` but the pass uses `worker/src/index.js:174` (`trigger==="cron" ? 300000 : -1`) — the constant is never referenced (search hits only line 20). `worker/src/index.js:139` imports `scoreOf` but the file never calls it (search hits only the import line). `worker/src/lib.js:36-39` exports `appendKeepNewest` yet production uses SQL `substr(notes || ?,-8000)` (`worker/src/index.js:281`, `functions/api/[[path]].js:269`, `:307`); the helper is test-only (callers only in `worker/src/lib.test.js`). `worker/src/index.js:115` (`GITHUB_QUERIES.slice(0,2)`) silently drops the third query (`ai-side-project`, `worker/src/index.js:27`). `worker/src/index.js:394-395` calls `await finish("ok")` twice in a row — the first write is immediately overwritten by the mode-disclosing second. All survive from prior rounds.
- Duplicated logic: `clamp10` exists twice — `functions/api/[[path]].js:12-16` vs `worker/src/lib.js:4-7` (same bounds/rounding; the API imports only `effectiveScore` at `functions/api/[[path]].js:10`, so the clamp stays in sync by inspection only). Auth compare is duplicated with divergent semantics: constant-time loop in API (`functions/api/[[path]].js:21-29`, unset token → 503 `writes disabled`) vs `!==` in worker (`worker/src/index.js:417,432,463`, all `if (!want || got !== want) return json({ error: "unauthorized" }, 401);`, collapsing unset-token 503 into 401). The Llama brief prompt is near-duplicated (`worker/src/index.js:318-325` main vs `:368-375` extra). `scoreOf`/`effectiveScore` are correctly shared (cross-boundary detail-cap tests lock agreement).
- TODO density: 0. Regex search for `TODO|FIXME|XXX|HACK|TBD` returns zero matches in shipped `public/`+`functions/`+`worker/`+`d1/`+`deploy/`+`scripts/`. No `console.log`/`debugger`/`prefers-color-scheme`/`data-theme`/theme-toggle hooks in shipped `public/`+`functions/`+`worker/` (regex search clean).
- Live snapshot (read-only, from `_night/live/`): health JSON verbatim: `{"ok":true,"rev":"d925ae6","db":"up","opportunities":27,"unreviewed":11,"bare_without_brief":19,"experiments_by_status":{"planned":2},"hours_since_last_ok_run":5.7,"oldest_unreviewed_age_h":155.4,"decisions_last_7d":0,"vetted_last_7d":0,"vetted_no_experiment":0,"noise_24h":0,"time":"2026-09-20T11:42:25.444Z"}` — rev is `d925ae6` (one behind master `0fb4fb2`), run fresh (5.7h), but 41% unreviewed (oldest 6.5d), 70% bare, zero running/won/lost, zero decisions/vetted in 7d. Dashboard HTML is 119 lines vs repo `public/index.html` 121 lines (`wc -l` both files): the live page content-matches except it lacks the `Zero spend` chip (`public/index.html:50`) and the `#start-here` strip container (`public/index.html:55`) — independent proof live predates `0fb4fb2`.
- Surface rules (owner's 5): all PASS. Light palette only (`public/styles.css:4` → `color-scheme: light;`, zero `prefers-color-scheme`/`data-theme`/toggle hits in shipped HTML/CSS/JS); tab icon + theme-color on the served page (`public/index.html:9-10`, live snapshot lines 9–10 identical); hand-written `public/favicon.svg:1` (32×32 viewBox, `#0d6b3f` rounded square, `$` glyph, 285 bytes, no external fonts); inline header brand mark reusing favicon geometry/accent/glyph (`public/index.html:16`, styled `public/styles.css:46`, live snapshot line 16 identical); guard test `public/tab-icon.test.mjs` (5/5 green inside `npm test`). No finding slots spent on surfaces.
- Tree state (blocker, pre-existing): `git status --short` shows 29 modified files (every tracked file, whole-file rewrite) although this run wrote nothing before this audit file. `file public/index.html` reports `with CRLF line terminators` in the working tree — the M-flags are a pre-existing CRLF-vs-HEAD line-ending mismatch, not edits. Acceptance expects only this new file; that gate cannot pass without a line-ending normalization owned outside this read-only lane. `_night/` scratch exists on disk but shows no `??` entry (ignored outside `.gitignore`).

## 4. Ranked findings (max 8)

Ordered by the owner's money ranking: (1) zero-spend starters flagged, (2) outcomes measurable in money, (3) review queue draining, (4) honest scoring. Round-1 Tasks 1–3 (starter strip, honest drawer + no-brief pill, oldest-first triage with sweep) are done on master and verified; they do not reappear except where deployment (F1) and adjacent gaps (F3, F4, F7) remain.

### F1 — Live is one deploy behind master: the strip, chip, drawer honesty, and oldest-first triage have never touched real data

- Evidence: `git log --oneline -5` HEAD is `0fb4fb2` ("Implemented audit Tasks 1-3") but `_night/live/aimoney_pages_dev_api_health.html:1` reads `"rev":"d925ae6"`; the live dashboard is 119 lines vs repo `public/index.html` 121 lines, with zero hits for `start-here|data-zero|Zero spend` in the live file while the repo owns the `Zero spend` chip (`public/index.html:50` → `<button class="chip" data-zero="1" type="button">Zero spend</button>`) and the strip container (`public/index.html:55` → `<div id="start-here" class="hidden"></div>`). Health counts are identical to the four prior snapshots while the oldest aged to `155.4h`.
- Why it costs money: every fix aimed at the measured bottleneck — the "do this today" starter (owner rule 1), the drawer cap marker and no-brief pill (rule 4), oldest-first triage plus the 30d sweep (rule 3) — sits on master while the real board still runs the old code. The fifth straight snapshot (0 decisions, 0 vetted) was produced by the old deployment, not by the new tooling failing.
- Fix: operator deploys master and verifies — run `./deploy/deploy.sh` then `./deploy/verify.sh` (no code change; credentials stay operator-owned). After deploy, confirm live `rev` reads `0fb4fb2`, the dashboard shows the strip + chip, and `/api/health` freshness stays ≤12h. Then the human work the strip names: vet the 11 oldest-first, start one `planned` experiment. Touch no files; this is a deploy action, not an edit.
- Size: S. Risk: low (standard deploy path; `verify.sh` gates markers + freshness).

### F2 — Experiment outcomes are still unmeasurable in money: no revenue field, spend is free text

- Evidence: `d1/schema.sql:54-58` declares `budget_cap TEXT`, `spent TEXT`, `metric TEXT`, `target TEXT`, `result TEXT` — all free text, and no `revenue`/`revenue_earned` column exists anywhere (search for `revenue` in `public/`+`functions/`+`d1/` returns only prose: a table tooltip, triage-prompt wording, and seed copy). The API never parses money (`functions/api/[[path]].js:231-319` slices strings but validates no amounts). Health counts decisions, not dollars (`functions/api/[[path]].js:344` counts `won`/`lost` rows; nothing sums revenue). The seed target mixes activity and money in one unparseable string (`d1/seed.sql:71` → `'>=3 replies; >=1 pilot at >=$500'`). A `won` experiment can therefore close with `result: "went great"` and the board records a decision with no number.
- Why it costs money: the owner ranks "outcome measurable in money rather than activity" second only to zero-spend starts. Today a won experiment proves nothing about dollars — replies and pilots are activity, and the $500 pilot clause is prose no query can aggregate. The lane metric (decisions/week) can climb while revenue stays unknown.
- Fix: migrate `experiments` with `revenue_cents INTEGER DEFAULT 0` + `spent_cents INTEGER DEFAULT 0` (new `d1/migrate-*.sql` applied by `deploy.sh`, since `schema.sql` is `CREATE IF NOT EXISTS` and never alters), accept both in create/update, show them on board cards, and add `revenue_last_7d` to `/api/health` summed from closed rows. Keep `result`/`post_mortem` prose required. Add API tests for the new columns and the health sum. Touch `d1/schema.sql`, `d1/migrate-*.sql` (new), `deploy/deploy.sh`, `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: M. Risk: medium (first real migration; keep it additive with defaults so existing rows and tests stay green).

### F3 — The Add-opportunity modal never asks for money fields, so manual adds are invisible to the zero-spend filter and EV ranking

- Evidence: the Add modal renders only `Title`, `One-liner`, `Category`, `Status`, `Value/Effort/Confidence/Fit` (`public/app.js:584-600`) and POSTs only those plus `slug`/`source` (`public/app.js:606-615`) — no `est_monthly_low/high`, no `capital_needed`, no `time_to_first_dollar` — while the API accepts all three (`functions/api/[[path]].js:94-97`). Every manual add therefore lands with `est 0/0` (ledger shows `money(0,0)` → `"—"`, `public/app.js:43-47`) and `capital_needed ""` (drawer shows `"—"`, `public/app.js:428`), and `isZeroSpend` (`public/app.js:76-77` → `String(...).trim().startsWith("$0")`) returns false. Fixing it needs a second edit round-trip through the drawer admin zone (`public/app.js:503` `$/mo low–high`).
- Why it costs money: the zero-spend rule and the EV ranking only work on rows that carry money facts. Manual adds — the rows a human cared enough to write by hand — enter as the least measurable rows on the board, unfilterable by the `Zero spend` chip and rankable by score alone.
- Fix: add `$/mo low–high`, `capital_needed`, and `time_to_first_dollar` inputs to the Add modal with the same parsing the drawer uses (`split(",")`, `Number(x.trim()) || 0`), include them in the create POST, and keep server-side range validation as the backstop. Add a dashboard static test asserting the inputs exist and a `$0` manual add matches `isZeroSpend`. Touch `public/app.js`, `public/dashboard.test.mjs`.
- Size: S. Risk: low (form fields + payload keys the API already accepts and validates).

### F4 — The drawer has no Vet/Kill where the vetting decision is made, and its status→killed path skips the required post-mortem

- Evidence: the review list offers per-row `Vet`/`Kill` (`public/app.js:155-158`), but opening any row (`public/app.js:168-170`) replaces that context with a drawer whose admin zone offers only a raw status select, score inputs, money, append-note, save, and log-experiment (`public/app.js:492-510`) — no Vet (strip `UNREVIEWED` + vetted tag, `public/app.js:220-236`) and no Kill-with-prompt (`public/app.js:238-256`, prompt at `:240` → `"One-line post-mortem (required to kill):"`). The drawer save sends `status` straight through (`public/app.js:521` → `status: $("#az-status").value`), and the API enforces no killed-postmortem rule (`functions/api/[[path]].js:118-155`), so drawer status→`killed` silently bypasses the post-mortem the review Kill requires (`README.md:71` documents Kill as requiring "a one-line post-mortem in notes").
- Why it costs money/users/trust: vetting happens where the evidence is — the drawer with its brief, facts, and notes. Forcing the reviewer to close the drawer, re-find the row in a 200-row list, and click Vet there taxes exactly the bottleneck action (11 waiting, oldest 6.5d). Worse, the drawer kill path breaks the board's core promise that killed ideas carry their learning.
- Fix: when the drawer row carries `UNREVIEWED`, render `Vet`/`Kill` buttons reusing `vetOpportunity`/`killOpportunity` (same vetted-tag write, same post-mortem prompt), and gate the admin status select so choosing `killed` routes through the same prompt instead of saving directly. Add dashboard static tests for the drawer buttons and the prompt parity. Touch `public/app.js`, `public/dashboard.test.mjs`.
- Size: S. Risk: low (reuses shipped handlers; no API or scoring change).

### F5 — Orphaned experiments have no remediation path

- Evidence: list keeps dangling rows via `LEFT JOIN` with `orphaned:true` (`functions/api/[[path]].js:217-226`); board renders an `orphaned` pill (`public/app.js:336`) but the card click dead-ends in `public/app.js:341-346` (`if (c.dataset.orphan === "1") return toast("Orphaned experiment — its opportunity was deleted.");`); the router has no `DELETE` for any resource (`functions/api/[[path]].js:376-393` lists only GET/POST/PATCH/PUT; search for `DELETE` in prod code hits zero lines); schema FKs are advisory without `PRAGMA foreign_keys` (`d1/schema.sql:50`). Carried from the prior round; still present at `0fb4fb2`.
- Why it costs users/trust: orphan-safe display stopped the vanishing, but orphans now accumulate with no admin action — no delete, no relink, not even a drawer. The board slowly fills with unactionable cards, and each one pollutes the `experiments` count the header reports.
- Fix: add admin-only `DELETE /api/experiments/:id` (same `authed` + 404 shape as update) and, on orphaned cards only, a `Delete` button behind the admin-token check with a confirm prompt; keep the toast for non-admins. Add API tests for 401/404/happy-path. Touch `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: M. Risk: medium (first DELETE endpoint; restrict to experiments, never cascade).

### F6 — Update paths accept unbounded strings that create paths truncate

- Evidence: `createOpportunity` slices every string (`functions/api/[[path]].js:87-101`: `title` 200, `one_liner` 500, `category` 40, `time_to_first_dollar`/`capital_needed` 120, `source` 40, `source_url` 500, `notes` 8000) but `updateOpportunity` stores raw (`functions/api/[[path]].js:124-128` → `if (b[f] !== undefined) next[f] = String(b[f]);`, no slice on any of the eight fields). Same split on experiments: create slices (`:258-263`: `name` 200, `hypothesis`/`result`/`post_mortem` 8000, caps/metrics 120–300) while update stores raw (`:282-285`). A single `PATCH` with a megabyte `title` passes every guard the `POST` path enforces. Carried from the prior round; still present at `0fb4fb2`.
- Why it costs users/trust: oversized values break ledger rendering (10k-char titles in a table row), bloat the newest-kept-8000 notes idiom downstream, and make the create/update contract a lie — reviewers cannot trust that "the API validates" when only half of it does. No money movement directly; ranked below the queue and scoring findings.
- Fix: mirror the create-path `slice()` bounds into both update handlers with the same limits, and add two API tests (oversized title truncated on opportunity PATCH; oversized name truncated on experiment PATCH). Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`.
- Size: S. Risk: low (write-path truncation matching already-shipped create limits).

### F7 — The Experiments header mixes a 7-day vetted count with an all-time experiment total in one "conversion" readout

- Evidence: `public/app.js:306` reads `vetted` from `state.health.vetted_last_7d` (7-day window) but `public/app.js:314` renders `` `${vetted} vetted → ${exps.length} experiments` `` where `exps.length` is every experiment row ever (`public/app.js:301` → `const exps = state.experiments;`, unfiltered list from `functions/api/[[path]].js:209-229`). Live today it reads `0 vetted → 2 experiments`; after a vetting burst it could read `5 vetted → 2 experiments` — above-100% "conversion" against rows logged months before the window.
- Why it costs trust (and then money): this header is the lane-metric readout the owner watches (decisions/week, vetted→experiment conversion). A conversion fraction whose numerator and denominator cover different windows misleads exactly the prioritization call it exists to inform — it can look like follow-through is failing (or succeeding) when only the windows disagree.
- Fix: label the windows honestly in the same string — e.g. `` `${vetted} vetted this week → ${exps.length} total experiments` `` — or count experiments created in 7d for a true windowed conversion (read-only, from existing `created_at`). Either is one line plus a dashboard static test locking the copy. Touch `public/app.js`, `public/dashboard.test.mjs`.
- Size: S. Risk: low (header copy only; health payload and scoring untouched).

### F8 — Auth mismatch + dead worker code invite the next incident (polish, ranked last)

- Evidence: API constant-time compare (`functions/api/[[path]].js:21-29`) vs worker `!==` in three places (`worker/src/index.js:417,432,463` → `if (!want || got !== want) return json({ error: "unauthorized" }, 401);`, collapsing unset-token 503 into 401); `const BRIEF_DEADLINE_MS = 18000` never referenced (`worker/src/index.js:20` vs live `:174`); unused `scoreOf` import (`worker/src/index.js:139`); test-only `appendKeepNewest` (`worker/src/lib.js:36-39`, zero production callers); `GITHUB_QUERIES.slice(0,2)` drops `ai-side-project` (`worker/src/index.js:115` vs `:27`) with no comment; redundant double `await finish("ok")` (`worker/src/index.js:394-395`). All survive from prior rounds.
- Why it costs trust/maintainability: timing side-channel on the worker token plus divergent 401/503 slows triage; the next editor tunes the wrong deadline constant or re-adds the dropped query as a bugfix. No direct money movement — ranked last per the owner's polish rule.
- Fix: extract the constant-time compare into `worker/src/lib.js`, import it in both runtimes, align worker codes (503 unset / 401 mismatch); delete the redundant first `finish("ok")`, delete-or-wire `BRIEF_DEADLINE_MS`, drop `scoreOf` from the worker import, demote `appendKeepNewest` to a documented test oracle pointing at the SQL idiom, and comment the `slice(0,2)`. Touch `worker/src/lib.js`, `worker/src/index.js`, `functions/api/[[path]].js`, `worker/src/lib.test.js`.
- Size: S. Risk: low (auth tightens; keep tokens out of logs; `npm test` must stay green).

## 5. What NOT to change

- Scoring formula and honest seeds: `score = 100 * (value * confidence * fit) / (effort + 1)`, each input 1–10 (`README.md:56`; `worker/src/lib.js:15-16`). Do not retune weights without new evidence. F7 relabels the header; it does not touch scoring.
- Human-owns-status: "The agent never moves status." (`README.md:72`); "The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)." (`worker/src/index.js:4-5`); "`researching → testing` is a human decision: flip to `testing` when you start a real experiment … The agent never moves status." (`README.md:72`). No auto-transitions, no auto-vetting, no auto-scaling.
- Manual-triage-only + briefs-on-cron: "Manual `Run triage now (briefs on cron)` collects and triages only (`POST /run` 202 carries `briefs_skipped:true`); briefs land on cron ticks" (`README.md:77`; `worker/src/index.js:174`). The 30s `waitUntil` cap that forces this is documented in `worker/src/index.js:7-11`. Do not "fix" manual runs to brief.
- Killed-with-learning: "Killed strategies stay on the board with their post-mortem — that is the point." (`README.md:64`); "Closing an experiment as `won`/`lost` requires `result` + `post_mortem`; `ended_at` stamps automatically." (`README.md:74`). Do not purge killed rows or relax closure rules. F4 extends the post-mortem rule to the drawer kill path; it does not relax it.
- Capped humility + honest display: "Agent proposals enter as `researching` with an `UNREVIEWED` marker in notes and an effective score capped to ≤6000 until a human vets them" (`README.md:65`); "the drawer shows the same capped score as the ledger with a "(capped — unreviewed)" marker until a human vets the row, and rows without a brief carry a "no brief" pill" (`README.md:78`). Vet/kill stay human.
- Never-auto-applied rescore: "the drawer admin zone then offers a suggested ±1 value/confidence rescore the human applies with one click — never auto-applied." (`README.md:74`; `public/app.js:466-479`). Do not wire outcomes to scores automatically.
- Read-only warnings: "`testing · 0 experiments` and `running exp · opp still researching` badges flag status mismatches read-only — the human still owns every status move." (`README.md:79`). Do not convert badges into transitions.
- Seed-vs-live accounting: "Seed runs once on an empty table; live count = 12 seeds + agent proposals + manual adds. Killed rows stay listed, so the count only grows." (`README.md:83`). Do not re-seed over live data; deploy's seed-if-empty (`deploy/deploy.sh:30-42`) stays.
- Auth split and secrets: "Reads are public. Writes require `Authorization: Bearer <ADMIN_TOKEN>`." (`functions/api/[[path]].js:1-2`); "`ADMIN_TOKEN` lives in two places: the Pages env var and the worker secret … No expiry; rotate manually when shared or leaked." (`README.md:87-88`). Anything needing `ADMIN_TOKEN`, `CLOUDFLARE_API_TOKEN`, D1 execute, or wrangler deploy is operator-owned and credential-gated — F1 names the deploy; it does not perform it.
- Anything needing a login, payment, or human judgment: vetting the 11 `UNREVIEWED` rows, moving `researching → testing → scaling`, starting the 2 `planned` experiments, writing post-mortems, choosing niches/pricing/outreach, rotating tokens. This audit proposes surfacing and guardrails only — never auto-spend. The single most valuable move on the lane metric right now is not code at all: deploy (F1), then a human vetting the 11 and starting one `planned` experiment.
- Live surfaces already compliant: light palette, favicon + theme-color, inline brand mark, and guard test all pass (§3) — do not restyle, re-palette, or re-icon the dashboard.

## 6. Proposed next tasks (2–4)

### Task 1: Deploy master 0fb4fb2 to live and verify

- Owned files: none (operator runbook — run `./deploy/deploy.sh` then `./deploy/verify.sh`; no edits).
- Acceptance: (a) live `/release.json` rev reads `0fb4fb2`; (b) dashboard shows the "Start here today" strip and `Zero spend` chip; (c) `verify.sh` passes including freshness ≤12h; (d) no code, data, or secret files touched.

### Task 2: Ask for $/mo, capital, and time-to-first-$ in the Add opportunity modal

- Owned files: `public/app.js`, `public/dashboard.test.mjs`.
- Acceptance: (a) Add modal has $/mo low–high, `capital_needed`, and `time_to_first_dollar` inputs; (b) create POST sends them and the API range guard still 400s on bad ranges; (c) a manual `$0` add appears under the `Zero spend` chip; (d) dashboard static tests lock the inputs; (e) `npm test` green; (f) no scoring, status, or API change.

### Task 3: Add Vet/Kill with post-mortem parity to the opportunity drawer

- Owned files: `public/app.js`, `public/dashboard.test.mjs`.
- Acceptance: (a) drawer rows carrying `UNREVIEWED` show `Vet` (strips marker, appends vetted tag, lifts cap) and `Kill` (prompted post-mortem) reusing the review handlers; (b) drawer status→`killed` routes through the same post-mortem prompt instead of saving directly; (c) dashboard static tests cover both; (d) `npm test` green; (e) no API or scoring change.

### Task 4: Record experiment revenue and spend as cents with a 7d revenue health sum

- Owned files: `d1/schema.sql`, `d1/migrate-*.sql` (new), `deploy/deploy.sh`, `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Acceptance: (a) additive migration adds `revenue_cents`/`spent_cents` defaulting 0 (existing rows and tests unaffected); (b) experiment create/update accept both; (c) board cards show the $ figures; (d) `/api/health` reports `revenue_last_7d` summed from closed rows; (e) API tests cover columns + sum; (f) `npm test` green.
