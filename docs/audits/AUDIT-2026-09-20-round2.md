# Audit 2026-09-20 round 2 — aimoney (read-only)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r2`. No existing file edited, no network, no installs, never read `.env`/secrets/tokens. Only this file created. One single-stage shell command per check; no inline code, no redirection.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests.
- Entry points: `public/index.html` + `public/app.js` (dashboard), `functions/api/[[path]].js` (D1-backed REST API), `worker/src/index.js` (research agent).
- Live surfaces: `https://aimoney.pages.dev/` (dashboard), `/api/health`, `/api/opportunities`, `/api/runs`, worker `https://aimoney-research.levitinvlad.workers.dev/`.
- Scheduled jobs: worker cron `0 */6 * * *` (every 6h: collect + triage + briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema + shared migrations + worker) or manual `deploy/deploy.sh`, gated by `deploy/verify.sh`.

## 2. End-to-end walk-through

Signal to decision. The hop where it most often goes silent is hop 12.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:78` `redditSignals`, `:106` `githubSignals` via `timedJson` (`:37`, 6s timeout covering the body read). A source whose queries all fail lands in `src_fail`.
2. Store: `:354-359` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:360-363` (`phase:collected`).
3. Sweep: `:368-372` unprocessed signals older than 30d become noise (`stale:N`).
4. Take: `:374-376` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`).
5. Exact-URL pre-pass (no AI): `:382-408` via pure `exactUrlTarget` (`:168`); exact-URL matches link as supports, notes newest-kept, no status move.
6. Inflow gates: `:421-428` bare-without-brief > 10 caps inserts to 1/tick; unreviewed > 10 pauses inflow to 0/tick (`maxNewThisRun = 0`). Overflow stays `processed = 0`.
7. Classify (one AI pass, skipped when paused): `:439-451` `buildClassifyPrompt` (`:272`) + `aiComplete` (`:136`), parsed by `parseJsonLines` (`worker/src/lib.js:62`, NDJSON + `repairJson`). Paused ticks spend 0 classify calls and log `inflow:paused`.
8. Verdict writes: `:464-532` accumulated then `flushVerdictWrites` (`:291`, batch with per-write fallback, failures as `verdict:N_failed`). New rows `INSERT` (`:492-502`) with `UNREVIEWED` and capped `effectiveScore`; slug collision links as supports (`:511-515`).
9. Briefs: main `:545-581` (top-scored, or oldest-unreviewed-first past 48h) plus extra `:588-623` when the oldest unreviewed exceeds 48h, via `buildBriefPrompt` (`:202`), `parseBriefJson` (`:213`), `insertBrief` (`:224`, never stores empty), poison rotation `:248-265` (`brief:skipped:N`).
10. Finish: `:626` single `finish("ok", …)` carrying `brief:mode`, `inflow:paused`, `stale:N`, `src_fail:…`, `brief:failed`, `verdict:N_failed`; errors `:628-636` finish `error` plus the `console.error("cron-failed", …)` tail marker.
11. Dashboard review: `public/app.js:242` `refreshReview` (client-side `deriveReviewList` `:239`, API fallback only at `>= 200` rows), `:189` `renderReview` with decision line (`:167` capital + next + source) and brief line (`:181` summary or `No brief yet`) plus Vet / Vet-&-starter / Kill (`:200-202`); `V`/`K` triage keys with auto-advance (`:585-600`); Start-here strip (`:103`) with Vet/Kill for unreviewed top picks.
12. Vet (the ONLY path that moves unreviewed to vetted): `public/app.js:443` `vetOpportunity` → `:433` `fetchDetailForWrite` (token-gated detail fetch) → `:448` inner → `:423` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id`. Starter variant `:473` adds a planned experiment and flips the row to `testing`.
13. API writes: `functions/api/[[path]].js:209` `updateOpportunity` (text bounds, status/range guards, recomputed `effectiveScore`), `:326` `createExperiment` / `:390` `updateExperiment` (won/lost require `result` + `post_mortem`, `started_at`/`ended_at` stamps, outcome ledger `:427-436`, cents clamps, `revenue_source` retry tolerance).
14. Health: `:474-638` 12-probe batch (`:494-507`) with per-probe isolation fallback (`:517-546`, failures named in `health_probe_failures` + column-only `health_probe_detail`), lazy self-migration (`:555-571`), `last_vetted` JS parse over a 500-char tail (`:599-608`), payload `:638` (`ok:false` + nulls only on total outage).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet (see F1): the worker creates `UNREVIEWED` rows and never clears them, cron ticks succeed (`hours_since_last_ok_run` 4) while `vetted_last_7d` stays 0 and the oldest unreviewed ages (177.7h and rising 1d/d). Second: the money probes (hop 14) return null because `revenue_cents`/`spent_cents` never reached live D1 and the self-migration is disabled (see F2).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over `worker/src/lib.test.js`, `worker/src/index.test.js`, `functions/api/api.test.mjs`, `d1/migrations.test.mjs`, `deploy/deploy.test.mjs`, `public/tab-icon.test.mjs`, `public/dashboard.test.mjs`) → `tests 364, suites 121, pass 364, fail 0, cancelled 0, skipped 0`, `duration_ms 90685.03`, exit 0, `node v22.22.3`. Real run in this session, whole suite, no narrowing. (Stderr only: sandbox `UNDICI-EHPA` proxy warning and the known `MODULE_TYPELESS_PACKAGE_JSON` hint.)
- Parse: `node --check public/app.js`, `node --check worker/src/index.js`, `node --check worker/src/lib.js` → exit 0 each. The in-suite shipped-JS parse gate (green inside the 364) covers every shipped JS file including `functions/api/[[path]].js`.
- Python: `python3 -m compileall` not applicable — no `.py` files in the repo (repo file tree plus a `**/*.py` filename search agree; control: the same search mode lists the 4 shipped `.js` files).
- Dead code: none material. `console.*` in shipped source is only the three deliberate `console.error("cron-failed", …)` markers (`worker/src/index.js:634,646,710`); no `debugger`. One micro-dead spot: the second replace in `cleanUnreviewed` (`public/app.js:417`) can never match after the first (see F6).
- Duplicated logic: one live case — `deriveReviewList` (`public/app.js:239-240`) inlines the `isNeedsReview` bit-vs-notes fallback (`:234-237`) instead of reusing it (see F6). Everything else is shared: `effectiveScore`/`tokensMatch` (`worker/src/lib.js`), `vettedNotes`, `fetchDetailForWrite`, brief/classify/insert helpers, one migration script.
- TODO density: zero in shipped source. Regex `TODO|FIXME|XXX|HACK` over `public/ functions/ worker/ d1/ deploy/ scripts/` → no hits; positive control: the same tool and mode matches `UNREVIEWED|revenue_cents` dozens of times across the same paths, so the zero is real. (An earlier literal-mode search with `|` pipes was discarded as meaningless — it could only match the pipe string itself.)
- Surface rules: all pass, zero findings. `color-scheme: light` (`public/styles.css:4`); regex `prefers-color-scheme|data-theme|theme-toggle|themeToggle` over shipped `index.html`/`app.js`/`styles.css` → no hits, with `color-scheme:\s*light` matching as control; `<link rel="icon" … href="/favicon.svg">` + `<meta name="theme-color" content="#0d6b3f">` (`public/index.html:9-10`); hand-written 32×32 `$`-on-`#0d6b3f` `public/favicon.svg` (284 bytes, under 2 KB); inline brand mark with the same geometry/accent/glyph (`public/index.html:16`); guard suites green inside the 364.
- Line endings: LF throughout. Repo-wide regex `\r` filename search → zero files; `file` on `public/app.js`, `public/index.html`, `worker/src/index.js`, `d1/schema.sql`, `deploy/deploy.sh` reports no CRLF; `.gitattributes:4` pins `* text=auto eol=lf`.
- Working tree: `git status --short` → empty before this file. `HEAD` is `cdef1fb` ("Night round 1: Bound last_vetted probe to 500-char tail and stamped full-SHA plus release"); live serves `170d772`, the parent commit ("Night round 4: Review triage keys, paused-tick classify skip, pill backlog mirror") — the edge is exactly one commit behind this tree.
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (rev `170d772`, `2026-09-21T10:02:57Z`): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:4, oldest_unreviewed_age_h:177.7, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{…revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. Dashboard shell `_night/live/aimoney_pages_dev.html` (121 lines) matches `public/index.html`: light, favicon, brand, agent pill, tabs, review chip, neutral `…` placeholders.
- Release marker: this repo publishes `release.json`, not `build.json` — the one checked source of truth (stamped full-SHA + package `release` by `scripts/stamp-release.sh` / `deploy/deploy.sh:61`, consumed by `/api/health` rev, gated by `deploy/verify.sh:66-106` with bounded stale retries and immediate fail on HTML/unparsable/placeholder bodies). Per the brief's scope rule no second source should be added. Live `/release.json` body was not fetched for this audit and there is no network here; the live health body above carries `"rev":"170d772"`.

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Current: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 177.7h / 7.4d, rising 1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `hours_since_last_ok_run` 4.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells; rank by decisions/week and owner time, measured the same way. Earned to date: none recorded — `revenue_total` null (unknown, not zero: the live D1 lacks the money columns so the probes cannot run), and structurally there is nothing to close (exactly 2 experiment rows exist, both `planned`, zero `won`/`lost`). Last krone arrived: never recorded (`last_vetted` null, zero closes). Single step between now and the next payment: vet one review row and Start its zero-spend starter (`V`, then Vet-&-starter toast → Start). Measured conversion at that step: 0/11 vetted in 7d (0%), 0/2 planned started (0%), 0 decisions/week; vetted→experiment conversion unmeasured (zero attempts). Honesty: no projection presented as result, no rate × volume figure, no attempt counted as an outcome (signals seen ≠ proposals ≠ vetted; planned ≠ running). What it was / is / moves it: unreviewed 11 → 11, oldest 175.5h → 177.7h, decisions 0 → 0 across consecutive snapshots — nothing moved; the single owner action is Task 2, one line: open Needs review and press V or K eleven times.

### F1. Nothing automated was ever supposed to vet — the 11-row queue waits on a human press

Evidence. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` The worker creates the marker (`:502`: `… — UNREVIEWED, scores capped until a human vets it …`) and every other worker `UNREVIEWED` hit is create/count/select (`:426-427,548,556,597,601`) — no worker path clears it (positive control: the same search returns those creation/count sites, so the absence of a clearing path is real). `README.md:71`: `A human admin vets each row: "Vet" clears the UNREVIEWED marker … and appends a [YYYY-MM-DD vetted] tag`; `README.md:72`: `The agent never moves status.` The moving path is a manual dashboard button: `public/app.js:443` `vetOpportunity` (and `:473` `vetAndLogStarter`) via shared `vettedNotes` (`:423-425`) → `PATCH /api/opportunities/:id`, with `V`/`K` + auto-advance at `:585-600`. Live: `vetted_last_7d` 0, `last_vetted` null, unreviewed 11, oldest 177.7h, while `hours_since_last_ok_run` is 4 — something runs (cron triage + briefs) and yields no verdicts by design, not by failure. A single cron run now produces: 0 inserts, 0 verdicts (inflow paused at unreviewed 11 > 10, `:426-428`; classify skipped, `:439-451`), plus 1–2 briefs.

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick, and 19/27 rows lack the brief that makes a sub-minute vet possible. Six shipped rounds improved the machinery; the queue never moved because the move is human-gated and the human has not pressed.

Fix: no code change moves this number — the smallest queue-clearing aid (inline decision + brief lines, Vet-&-starter, V/K auto-advance) already shipped. The fix is Task 2, the human pass, with Task 1 certifying the path by a real-path test first. Touch no repo files for the number itself. Size S (the pass). Risk none (reversible token-gated PATCHes, one verdict per press, no bulk/auto path; never add auto-vet without an owner-written rule).

### F2. Live money legs are null — the columns never reached D1 and the migration is disabled

Evidence. Live `_night/live/aimoney_pages_dev_api_health.html:1`: `"revenue_last_7d":null,"revenue_total":null,"spent_total":null,"health_probe_failures":["revenue_week","revenue_lifetime"],"health_probe_detail":{"revenue_week":"revenue_cents","revenue_lifetime":"revenue_cents"},"schema_missing_columns":["revenue_cents","spent_cents"],"schema_migration":"disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)"`. The two failing probes are the only money readers (`functions/api/[[path]].js:501-502`). `d1/schema.sql:5` is `CREATE TABLE IF NOT EXISTS` (no-op on the existing table); the ADD COLUMNs exist only in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied by CI `.github/workflows/deploy-worker.yml:27,33-37` and `deploy/deploy.sh:36,40` via `deploy/apply-d1-migrations.sh:13-28` (one sorted loop, duplicate-column tolerated, anything else fails loudly) — which never ran against live D1 (no `CF_API_TOKEN`/`CF_ACCOUNT_ID` repo secrets; fail-fast `:20-26`). The in-worker fallback is deliberately off: `[[path]].js:53` `if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` → report missing + disabled, change nothing. The three migration outcomes are already pinned green in this session's 364 (`functions/api/api.test.mjs` self-migration switch suite: old schema + `ALLOW_SCHEMA_MIGRATION=1` ends migrated with numbers; unset leaves the table unchanged and names the columns; second run is a clean no-op).

Why it costs: earned-to-date is unknown rather than zero, so the money-first question cannot be answered from the project's own data; lifetime and weekly money stay dark while the rest of the payload reports. The decisions COUNT split (`:500`, no money column) already saved `decisions_last_7d` (= 0, not null) — the money legs are the only remaining nulls.

Fix: no code change; the path is already correct (isolation fallback, column-only detail, ADD-only self-migration, sorted shared migrations, convergence tests with a positive control — all in the 364 green). The owner either sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var, after which the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, and re-runs the failed money probes on the same call (second run a clean no-op); or adds the two Cloudflare repo secrets and redeploys so CI applies the migrations. One variable, one decision — see §5 for the exact lines. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F3. `experiments_by_status` reports `{}` when its probe fails — reads as "no experiments" instead of unknown

Evidence. `functions/api/[[path]].js:628-629` builds `experiments_by_status` from possibly-empty results with no `probeFailed(3)` guard (`healthProbeNames[3]` is `"experiments_by_status"`, `:490-493`), and `:638` always emits the key — while every other count in the same payload goes null on its probe's failure. On a probe failure or total outage the key reads `{}` ("no experiments") rather than null ("unknown") — precisely the 0-vs-unknown confusion the money directive bans. `functions/api/api.test.mjs:1326` currently pins the `{}` shape on the failure path. The dashboard does not mask this for external readers: `experiments_by_status` has zero hits in `public/app.js` (repo-wide search hits only the API, its tests, and prior audits), and `#exp-count` renders from the `/api/experiments` list (`:736`) — so orchestrator/owner reads of health are the consumers at risk.

Why it costs: a failed probe would report the experiment pipeline as empty instead of unmeasured, and any monitor alerting on `{}` vs missing would draw the wrong conclusion during exactly the outage it should flag.

Fix: emit `experiments_by_status: null` when `healthDown || probeFailed(3)`, mirroring the neighboring counts; update the pinning assertion in `functions/api/api.test.mjs:1326` to expect null plus a case where one failed status probe keeps the other keys byte-identical. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Size S. Risk low (dashboard never reads the key; key order preserved).

### F4. The top-scored brief pick briefs killed/paused rows — AI budget spent on decided ideas

Evidence. Main brief pick `worker/src/index.js:558-560`: `SELECT o.* … WHERE b.id IS NULL ORDER BY o.score DESC LIMIT 1` — no status filter, so `killed`/`paused` bare rows are eligible. The two unreviewed-scoped picks (`:556`, `:597-602`) exclude decided rows implicitly, but killing cleans the marker: `killOpportunityInner` (`public/app.js:522`) writes `cleanUnreviewed(notes) + [killed]`, and `updateOpportunity` (`functions/api/[[path]].js:238`) then stores the UNCAPPED `effectiveScore` — so killing a high-value bare proposal both lifts its cap and leaves it brief-eligible at the top of the score order. Bare-by-design seeds (`d1/seed.sql:1`: 12 opportunities, briefs for the top 4 only) widen the pool. Live incidence is unmeasured from health (no per-status split), but the mechanism is deterministic in code, and each wasted brief costs 1 of 4 AI calls plus the slot a live row would have taken.

Why it costs: with 19/27 rows bare and brief capacity at 1–2 per 6h tick, every brief written for a decided row delays evidence for a live row by a tick — and the vet-vs-kill call the lane metric waits on gets slower, not faster.

Fix: add `AND o.status NOT IN ('killed','paused')` to the main top-scored pick (leave the unreviewed-scoped picks byte-identical), and consider the same exclusion in the bare counts (`worker/src/index.js:422`, `functions/api/[[path]].js:497`) so decided rows stop inflating the evidence gap. Pin with an `index.test.js` case driving the real `runResearch` against a stubbed D1: a high-score bare killed row is skipped for the top live bare row. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk low (read-only selection change; no status moves).

### F5. Paused ticks pile up unprocessed signals — the resume triages weeks-old leads first

Evidence. While unreviewed > 10 (live: 11), collect still inserts every 6h (`worker/src/index.js:343-359`, up to 9 queries × 8 signals per tick) but classify is skipped and `fresh` stays unprocessed (`:439-451`); the only drain is the 30d stale sweep (`:367-372`). The take is oldest-first (`:374-376`). So a pause of days-to-weeks banks hundreds of unprocessed signals, and the first tick after the owner clears the queue spends its 6-signal take and AI budget on the stalest leads first while fresh signals wait behind them.

Why it costs: Workers AI calls and run time burned on near-worthless old leads at exactly the moment the queue starts moving; fresh-signal triage — the input the lane metric actually needs — is delayed by the stale backlog.

Fix: while `maxNewThisRun == 0`, tighten the stale sweep for unprocessed signals (e.g. 7d instead of 30d) reusing the same `stale:N` run-log accounting, so the paused backlog cannot age past one week; document the tradeoff (old leads noised sooner — the existing 30d sweep already concedes the principle). Pin with an `index.test.js` real-`runResearch` case: paused tick with 10d-old unprocessed signals noises them and logs `stale:N`; unpaused tick keeps them. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk low (reversible, no status moves, logged).

### F6. (Minor) Review-bit check duplicated inline; one dead string replace

Evidence. `isNeedsReview` (`public/app.js:234-237`) owns the needs_review-bit-vs-notes fallback, but `deriveReviewList` (`:239-240`) re-implements the same ternary inline — the two must stay in sync by hand across every future change to the bit contract. Separately, `cleanUnreviewed` (`:417`) chains `.replace(/UNREVIEWED,?\s*/g, "")` with `.replace(/UNREVIEWED/g, "")`, where the second pass can never match after the first removed every occurrence.

Why it costs: divergence risk, not money — a future bit-semantics change applied to one site silently desyncs the review queue from the row check.

Fix: call `isNeedsReview` inside the `deriveReviewList` filter; drop the dead second replace. Existing `dashboard.test.mjs` review-derivation cases already pin the behavior. Touch `public/app.js` only. Size S. Risk none.

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, bulk verdicts, or auto-testing without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless. `functions/api/[[path]].js:16-25` (switch docblock), `:53` (unset → report + change nothing), `:59-67` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate (`:35,69-71`). Do not default it on, do not add a second trigger, and do not add a migration endpoint: `a URL that alters the schema is a liability even when it is authenticated.`
- Never a data-losing migration here: ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- No second version source: `release.json` is the one checked marker (stamped, consumed by health rev, gated by verify). Do not add `/build.json`.
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change); unset behaviour is byte-identical to today. His one line: set `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var and reload `/api/health` once — the two money columns appear and the money legs fill on the same call.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action. CI step reference (unchanged this round, read-only lane): `.github/workflows/deploy-worker.yml:33-34` ("Apply D1 migrations in sorted order" via the shared script).
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment from the decision + brief lines. Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure — the inline Win close takes a human-entered amount for exactly this reason.
- Never, whatever the ranking says: no spending, no account or identity creation, no outward sends, no accepting terms, no captcha solving, no fabricated reviews/receipts/testimonials, no scraping or messaging a platform's terms forbid, no financial advice or implied return.
- House rules honored by this audit: never read `.env`/secrets/tokens; no edits to existing files (this findings file only); CRLF care verified (LF throughout, whole-line evidence).

## 6. Proposed next tasks (2–4)

### Task 1. Vetting-path round: prove with a real-path test where unreviewed→vetted stalls

Owns: `public/app.js`, `public/dashboard.test.mjs`, `functions/api/api.test.mjs` (no behavior change expected). Context (F1): the only path from unreviewed to vetted is the manual Vet button (`public/app.js:443` via `vettedNotes` `:423` → `PATCH /api/opportunities/:id`); the worker never vets by design (`worker/src/index.js:4-5`). Drive the real path against a stubbed D1: an `UNREVIEWED` row → Vet PATCH → `/api/health` `vetted_last_7d` +1 and `last_vetted` = today; and a real `runResearch` tick at unreviewed 11 → 0 verdicts with `inflow:paused` (already true; pin it). If the tests certify human-only, say so in the report and stop — do NOT add auto-vet; the queue-clearing aid (V/K, decision + brief lines, Vet-&-starter) already shipped.

Acceptance: new tests drive Vet→PATCH→health and the paused tick end to end against stubbed D1 (not helper unit tests); they fail if the tag contract (`[YYYY-MM-DD vetted]`) or the pause gate drifts; report states the file:line of the path, the cause, and what a single run produces; `npm test` green (364+).

### Task 2. Owner clears all 11 unreviewed via V/K triage; queue empty, inflow resumes

Owns: live dashboard + D1 via the API (no repo files changed). Non-code queue clearing (F1): a human opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines — `V` vets and auto-advances, `K` opens the inline post-mortem and Confirm kills and advances. A couple of minutes; no drawer round-trip needed.

Acceptance: `/api/health` reads `unreviewed` 0, `oldest_unreviewed_age_h` null, `vetted_last_7d` + kills in the window = 11, `last_vetted` = today; every Vet carries a `[YYYY-MM-DD vetted]` tag, every Kill a one-line post-mortem in notes; next cron resumes inflow (unreviewed ≤ 10 → cap 2/tick). Requires `ADMIN_TOKEN` and human judgment per row; never mark decided without evidence.

### Task 3. Exclude killed/paused rows from the top-scored brief pick

Owns: `worker/src/index.js`, `worker/src/index.test.js`. Waste cut (F4): the main brief pick (`worker/src/index.js:558-560`) has no status filter, so decided bare rows can consume AI briefs; add `AND o.status NOT IN ('killed','paused')` and leave the unreviewed-scoped picks byte-identical.

Acceptance: a real-`runResearch` test against stubbed D1 with a high-score bare killed row briefs the top live bare row instead; bare counts and unreviewed gates unchanged; `npm test` green.

### Task 4. Null experiments_by_status when its health probe fails instead of {}

Owns: `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Failure-visibility fix (F3): emit `experiments_by_status: null` when `healthDown || probeFailed(3)`, mirroring the neighboring counts, so a failed probe reads unknown rather than "no experiments".

Acceptance: failed status probe → key null with `health_probe_failures` naming `experiments_by_status` and all other keys byte-identical; total outage → key null alongside the other nulls; `api.test.mjs:1326` updated to the null contract; `npm test` green.
