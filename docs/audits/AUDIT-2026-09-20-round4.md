# Audit 2026-09-20 round 4 — aimoney (read-only, track B)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r4`. HEAD `2511e5d` (night round 3); live rev `b8ae5b5` (night round 2). No git writes, no network, no installs, no edits to existing files. Only this file created at the root. Never read `.env`, secrets or tokens. One single-stage shell command per call, no inline code, no redirection. No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` at the root; `README.md` read first, then the live snapshots in `_night/live/`, then source.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests (`README.md:3-15`, `worker/src/index.js:4-5`).
- Entry points: `public/index.html` + `public/app.js` (1702 lines, dashboard), `functions/api/[[path]].js` (735 lines, D1 REST API), `worker/src/index.js` (733 lines, research agent) + `worker/src/lib.js` (79 lines, shared pure helpers).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `/` + `POST /run`; live snapshot `_night/live/aimoney_pages_dev_api_health.html:1` at `2026-09-21T20:14:09Z`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + up to 2 briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:718-728`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema `:27` + shared migrations `:33-34` + worker `:38`) or manual `deploy/deploy.sh`, gated by `deploy/verify.sh` (public reads only).

## 2. End-to-end walk-through

Signal to decision. The hop where it most often goes silent is hop 12 (human Vet), with hop 14 (money probes) null alongside it.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:81` `redditSignals`, `:110` `githubSignals` via `timedJson` (`:37-47`, 6s timeout covering the body read). Per-query throws counted; all-failed names the source in `src_fail` (`:76`, `:105`, `:134`), partial in `src_partial` (`:77`, `:106`, `:135`).
2. Store: `:359-364` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:365-368` (`phase:collected`).
3. Sweep: `:373-377` unprocessed signals older than 30d become noise (`stale:N`); a failed sweep is silent (F5).
4. Take: `:379-381` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:391-418` via pure `exactUrlTarget` (`:173-183`); bounded `IN` selects (`:400-405`, failures silent as `[]`, F5); a match links as supports (notes newest-kept, no status move); failed links skipped and counted (`prepass:N_failed`, `:411`).
6. Gates, read before classify: `:431-434` bare-without-brief > 10 caps inserts to 1/tick; `:435-438` unreviewed > 10 pauses inflow to 0/tick (`maxNewThisRun = 0`). Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:449-461` `buildClassifyPrompt(opps, fresh)` (`:277`) + `aiComplete` (`:141`, budget `MAX_AI_CALLS = 4`, `:19`), parsed by `parseJsonLines` (`worker/src/lib.js:64`, NDJSON + `repairJson` for `\_`). Inflow-paused ticks skip classify entirely and still brief (finish line carries `inflow:paused`, `:642`).
8. Verdict loop `:474-542`: strict action enum (`:480`), supports-without-id becomes noise (`:487-489`), overflow stays unprocessed (`:491`). `new` INSERTs (`:502-512`) with an `UNREVIEWED` note (`:512`) and capped `effectiveScore` (`worker/src/lib.js:30`, cap 6000 `:23`).
9. Verdict flush: `:543` `flushVerdictWrites` (`:296-312`); failures as `verdict:N_failed`.
10. Briefs: main pass (`:555-597`, top-scored live-only or oldest-unreviewed-first past 48h, `:557-563`) plus extra pass (`:604-638`) when the oldest unreviewed exceeds 48h (live oldest 187.9h, so both fire), via `buildBriefPrompt` (`:207`), `parseBriefJson` (`:218`), `insertBrief` (`:229`, never stores empty); poison rotation via `briefSkippedIds` (`:253-270`); failures `brief:failed`. Manual runs skip briefs by deadline (`:346`, `briefDeadline = -1`).
11. Finish: `:642` single `finish("ok", …)` carrying mode/pause/stale/src markers; errors finish `error` plus `console.error("cron-failed", …)` (`:650`, also `:661-663`, `:725-727`). `scheduled` `:656-664`, `POST /run` `:718-729`.
12. Vet — the ONLY path from unreviewed to vetted, one sentence: a manual dashboard press — `public/app.js:562` `vetOpportunity` → `:538` `fetchDetailForWrite` (token-gated detail fetch, warms N+1) → `:567` inner → `:485` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`:574-577`) with auto-advance (`:582`). Starter variant `:586+` (vet + planned starter + flip to `testing`, resume-safe) auto-focuses like Vet/Kill; Kill writes `[date killed]` + post-mortem. `V`/`K` keys with auto-advance: one human verdict per keypress, no bulk path.
13. API writes: `updateOpportunity` (`functions/api/[[path]].js:237`, text bounds, status/range guards, tagless-clear 400 `:271-274` against `VERDICT_TAG` `:90`, killed-clear post-mortem guard `:282-288`, `effectiveScore` `:290`); `createExperiment` (`:378`) / `updateExperiment` (`:441`, won/lost require `result` + `post_mortem`, stamps, `outcomeLedgerLine` `:103-108`, cents clamps, `revenue_source` retry `:420-430`/`:497-509` with `dropped` flag).
14. Health: `functions/api/[[path]].js:526-704` — 12 probes (`:546-559`) in one batch (`:560`) with per-probe isolation fallback (`:569-598`, failures named in `health_probe_failures` + column-only `health_probe_detail`), lazy self-migration (`:607-623` via `ensureMoneyColumns` `:36-77`), `last_vetted`/`last_verdict` parsed in JS (`:657-671`) from 500-char tails (`:558`, `LIMIT 2000`); payload `:704` (`ok:false` + nulls only on total outage; `db` is `up` when any probe answers).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 2.2) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (187.9h). Second: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F2) — and until it runs, money writes 503 too (F6). Third, code-silent: three worker failure shapes with no marker (F5) and a paused triage that reads as clean in health (F4).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:8`) → exit 0 this session, run once. TAP summary: `# tests 467`, `# suites 147`, `# pass 467`, `# fail 0`, `# duration_ms 90632.084408` (brief baseline on Windows: 90228.1742 — same shape, suite grew). Node `v22.22.3`.
- Parse/compile: no Python sources — glob `**/*.py` matches nothing. Positive control: the same tool+mode matches `UNREVIEWED` dozens of times and lists 22 shipped JS/SQL/SH/JSON files, so the zero is real. JS parse is covered by the green suite (every file imported) plus the `shipped-JS parse gate` suite (`node --check` every shipped JS + a broken-syntax positive control, green) and the deploy `dashboard-appjs parse gate` suite (green).
- Dead code: none material in shipped source. `appendKeepNewest` (`worker/src/lib.js:51-54`) is test-only by documented design (pinned JS spec of the SQL `substr(notes || ?, -8000)` idiom, `:49-50`), not dead; `scoreOf`/`isUnreviewed`/`UNREVIEWED_*` are pinned via `effectiveScore` the same way. `console.*` in shipped source is only the three deliberate `cron-failed` tail markers (`worker/src/index.js:650,662,726`) plus the design comment (`:648`). Control: the same search matches those lines, so no hidden logging.
- Duplicated logic: shared singles verified — `effectiveScore`/`tokensMatch`/`clamp10` (`worker/src/lib.js`, API import `functions/api/[[path]].js:10`), `vettedNotes`/`fetchDetailForWrite`, `buildBriefPrompt`/`parseBriefJson`/`insertBrief`, `buildClassifyPrompt`, `flushVerdictWrites`, `outcomeLedgerLine` (`:103-108`), one migration script (`deploy/apply-d1-migrations.sh`, called by `deploy/deploy.sh:40` and CI `:33-34`). Remaining mirrors are documented-deliberate: `centsDollars` ↔ dashboard `moneyCents` (`:97-99` comment), SQL `substr(notes || ?, -8000)` ↔ `appendKeepNewest`, `VERDICT_TAG` boolean gate (`:90`) vs health `matchAll` date-capture (`:663-668`, gate vs capture).
- TODO density: zero in shipped source. Regex `TODO|FIXME|HACK|XXX` over the repo hits only prose in `docs/audits/*.md`. Control: the same tool+mode matches `UNREVIEWED` across `worker/src/index.js` + `public/app.js` + `worker/src/lib.js`, so the zero is real.
- Matcher honesty (the brief's check-that-matches-nothing rule): every absence above carries its positive control in the same bullet (py→JS/`UNREVIEWED` hits, TODO→`UNREVIEWED`, dead-code→traced importers, console→`cron-failed` hits). The suite reports its denominator (`# tests 467`, `# pass 467`) — a zero-test pass would read as `tests 0`, not success.
- Surface rules: all pass, no finding. Light-only (`public/styles.css:4` `color-scheme: light`, ink-on-paper palette; only media queries are `prefers-reduced-motion` and `max-width`, `:210-218`); favicon linked with theme-color (`public/index.html:9-10`, byte-identical in `_night/live/aimoney_pages_dev.html:9-10`); `public/favicon.svg` 284 bytes, 32×32 `$` on `#0d6b3f`; brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suite `public/tab-icon.test.mjs:62-113` green inside the exit-0 run. First five seconds: the static shell (5595 bytes live) renders brand + `Every way to make money with AI, ranked.` + lede with zero JS; counts fill from `…` placeholders, then the last-good snapshot, then live refresh.
- Tree: HEAD `2511e5d`; `git ls-files --eol` shows all 40 tracked files `i/lf w/lf` with `eol=lf` (`* text=auto eol=lf`, `.gitattributes:4`) — the CRLF warning in the brief does not apply to this checkout. Sizes (`wc -l`): `public/app.js` 1702, `functions/api/[[path]].js` 735, `worker/src/index.js` 733, `worker/src/lib.js` 79.
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (time `2026-09-21T20:14:09Z`, rev `b8ae5b5…`, one commit behind HEAD `2511e5d`, F3): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:2.2, oldest_unreviewed_age_h:187.9, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. The brief's 08:45 measurement read the same 11 unreviewed at 174.4h — the queue has not moved all day; age rises ~1d/d while cron stays healthy.

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 187.9h / 7.8d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `last_verdict` null, `hours_since_last_ok_run` 2.2. Was at 08:45: same 11 unreviewed at 174.4h — the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none — no `won`/`lost` row exists (`experiments_by_status:{planned:2}`, `decisions_last_7d:0`), and the money legs read null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter, run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment a human records the first verdict. The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.8 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:562` `vetOpportunity` → shared `vettedNotes` (`:485-488`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id` (`:574-577`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:512`: `— UNREVIEWED, scores capped until a human vets it`) and every other `UNREVIEWED` site in `index.js` is a create/count/select site (`:18,436-437,509,558,566,613,617`) — zero clearing paths and zero `vetted` strings in the worker (positive control: the case-insensitive `vetted|UNREVIEWED` matcher returns those `UNREVIEWED` sites, so the absence of a vet path is real). So `vetted_last_7d` 0 with `hours_since_last_ok_run` 2.2 is human-gated-by-design, not a broken automation. Vet-round report as requested: path `public/app.js:562`; cause human-gated-by-design — there is no failing test because nothing is broken, and the passing real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (green inside the exit-0 run) is the positive proof; a single run of the path (one Vet press) produces exactly unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today. Health already dates the stall without being told: `last_vetted`/`last_verdict` (`functions/api/[[path]].js:657-671`, `:704`), both null live.

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:435-438`, classify skipped `:449-461`), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change for the verdict itself — the two-minute path (`V`/`K` + auto-advance, one verdict per keypress, token-gated, N+1 prefetch `public/app.js:524-530`, detail-before-write `:538-560`) already ships with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines (`docs/OWNER-ACTIONS.md:7-20`). Touch no repo files for this finding. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live: `revenue_last_7d/revenue_total/spent_total` null with `health_probe_failures:[revenue_week,revenue_lifetime]`, `health_probe_detail:{…:revenue_cents}`, `schema_missing_columns:[revenue_cents,spent_cents]`, `schema_migration:disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)`. The two failing probes are the only ones reading money (`functions/api/[[path]].js:553-554`, `SUM(revenue_cents)` / `SUM(revenue_cents/spent_cents)`). `d1/schema.sql:5` is `CREATE TABLE IF NOT EXISTS` (no-op on the existing table); the ADD COLUMNs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied after `schema.sql` by both CI (`.github/workflows/deploy-worker.yml:27` then `:33-34` `Apply D1 migrations in sorted order` via `./deploy/apply-d1-migrations.sh`) and `deploy/deploy.sh:36` then `:40` — one shared loop + tolerate-list (`deploy/apply-d1-migrations.sh:13-28`, duplicate-column no-op, anything else exits 1). The in-worker fallback is deliberately off: `functions/api/[[path]].js:58` (`if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` → report missing + disabled, change nothing). Migration-ask results, all green this session in the `self-migration switch` suite (9/9): old schema + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers on the same call (`:63-76`, one ADD at a time, duplicate-column tolerated); var unset leaves the table unchanged and names the missing columns plus the disabled line; second run is a clean no-op (decided outcomes cached `:33-34`, transient PRAGMA/empty answers never cached `:44-55`). Exact variable name: `ALLOW_SCHEMA_MIGRATION`. Exact CI step (already in place, no change needed): `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: earned-to-date is unknown rather than zero, so the money-first question cannot be answered from the project's own data — and until the columns land, money writes 503 too (F6). (The decisions COUNT split, `functions/api/[[path]].js:552`, already saved `decisions_last_7d` = 0 — the money legs are the only remaining nulls.)

Fix: no code change; the path is already correct. The owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var — the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, and re-runs the failed money probes on the same call — or adds the two Cloudflare repo secrets and redeploys so CI applies the migrations. One variable, one decision; see §5 for the exact lines. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F3. Live still serves the round-2 revision — one shipped round is committed but not deployed

Evidence. Live `/api/health` reports `rev: b8ae5b5…`, which equals the night-round-2 commit `b8ae5b5` (`git log`: `Night round 2 (aimoney)`), while the clone HEAD is `2511e5d` (`Night round 3 (aimoney): timed triage POST plus money-column hints`). One commit sits between them, `READY TO SHIP` per the run brief.

Why it costs: the round-3 changes — timed manual-triage POST and money-column write hints — are merged but unserved, so the live surface (and the owner's phone) does not reflect the repository. Trust cost, not a code defect: the code is right, it is just not running.

Fix: no code change. The owner merges/deploys the branch (or opens the Pages dashboard and checks the latest deployment status) — one line in §5. The next audit should confirm live rev == HEAD. Touch no repo files. Size S. Risk none (deploy-only; `deploy/verify.sh` gates the rollout on public reads).

### F4. A paused triage reads as a clean triage in health — `inflow:paused` never leaves the run log

Evidence. While unreviewed > 10 the worker pauses inflow (`worker/src/index.js:435-438`, `maxNewThisRun = 0`), skips the classify AI call entirely (`:449-461`), keeps `fresh` unprocessed, and still finishes `ok` with `inflow:paused` on the run-log line (`:642`). Live this is the steady state: unreviewed 11, `hours_since_last_ok_run` 2.2, `noise_24h` 0. Health has no paused key — literal `inflow` over `functions/api/[[path]].js` matches nothing (control: the same matcher hits `worker/src/index.js` repeatedly, so the absence is real). A reader seeing `ok:true` + fresh `hours_since_last_ok_run` + `noise_24h` 0 concludes triage is healthy; in fact no signal has been classified for days and the unprocessed backlog grows toward the 30d sweep.

Why it costs: failure visibility on the machine's input — the exact shape the brief bans (a check that reads as success when it did nothing). Debugging "why so few proposals last week" starts from a green board.

Fix: derive the paused bit health already has the inputs for — `unreviewed > 10` from probe 1 — and expose it (e.g. `inflow_paused: true/false`) alongside the existing keys, byte-identical otherwise; have the dashboard pill/tooltip mirror it next to the backlog bit. Pin with real-path health tests against a stubbed D1: 11 unreviewed → `inflow_paused:true`, ≤10 → `false`, outage → null like its neighbors. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`, `public/app.js`, `public/dashboard.test.mjs` (pin only). Size S. Risk low (additive key; no probe, gate, or AI behavior changes).

### F5. Three worker failure shapes finish `ok` with no marker anywhere

Evidence. (1) Stale sweep: `worker/src/index.js:373-377` — `UPDATE signals SET processed = 1 … -30 days` wrapped in `try { … } catch { /* stale sweep failed; triage proceeds anyway */ }`. No count, no marker, no log. (2) Pre-pass bounded selects: `:400-405` — `oppUrls = … .catch(() => [])` and `linkedUrls = … .catch(() => [])`. On failure the pre-pass silently finds no matches and burns AI budget on rows it should have linked for free. (3) Bare-row select: `selectBareRow` (`:262-270`) — both branches `.catch(() => null)`; a failing bare query reads as "no bare rows" (brief pass skipped, no `brief:failed`, which only covers AI/insert throws at `:596`/`:636`). All three finish `ok` with a finish line (`:642`) indistinguishable from a healthy tick.

Why it costs: the overnight-reliability shape — a D1 hiccup on any of these three degrades the pass for days (wedged old backlog, wasted AI calls, stalled briefs) while every surface reports green and the 30d sweep quietly buries the evidence.

Fix: count each and carry a compact conditional marker on the existing finish line next to `src_fail`/`src_partial`/`brief:failed` (e.g. `stale:failed`, `prepass_select:failed`, `bare_select:failed`), keeping every existing marker byte-identical so log parsers and tests keep matching. Each shape gets a test with a stubbed D1 that throws exactly there: run still finishes `ok`, marker present, verdict/brief behavior otherwise unchanged. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk low (log-string-only; no signal, verdict, brief, or AI-budget changes).

### F6. Two planned experiments, zero running, zero decisions — the money path stalls at planned, then 503s

Evidence. Live `experiments_by_status:{planned:2}`, `decisions_last_7d:0`; no `running`/`won`/`lost` keys. The one-tap moves exist and are human-pressed: `startExperiment` (`public/app.js`, PATCH `running`, API stamps `started_at` at `functions/api/[[path]].js:467`), Win/Lose closes with human-entered cents + optional source/spend; the API closure gate holds (won/lost require `result` + `post_mortem`, `:468-473`, `ended_at` stamped, outcome ledger appended `:478-484` via shared `outcomeLedgerLine` `:103-108`). But every experiment write carries the money columns (`:460-466` defaults, `:486-488` args, `:491-496` SQL), the retry covers only `revenue_source` (`:497-509`), and any other missing column 503s naming only it (`:732`, `missingColumnOf` `:122-126`). The live table lacks the cents columns (F2), so Start and Win/Lose on live 503 today with the switch one-liner — the outage blocks the lane metric, not just the ledger.

Why it costs: vetted → experiment → decision is the lane metric, and it reads 0/0/0 with the first step blocked. Signals seen and proposals added are attempts, not outcomes; without one running row there is no number within a week and no amount/date/source to make real.

Fix: first the owner enables F2 (one variable — Start works on the very next call after the columns land). Then, after F1 vets the parent row, the owner presses Start on the $0-capital starter, runs the smallest real-world test inside a week, then closes via Win/Lose with a human-entered $ amount and a one-line post-mortem (`docs/OWNER-ACTIONS.md:43-54`). No code change; the dashboard, API gate, stamps, ledger, and 503 hint already enforce the shape. Touch no repo files. Size S (one human decision + the sprint). Risk none to the repo (token-gated PATCHes; no auto-transitions; never mark decided without evidence).

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, or auto-testing without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless. `functions/api/[[path]].js:16-27` (switch docblock), `:58` (unset → report + change nothing), `:63-76` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate for decided outcomes. Do not default it on, do not add a second trigger, and do not add a migration endpoint: `a URL that alters the schema is a liability even when it is authenticated.`
- Never a data-losing migration here: ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- `revenue_source` arrives only via CI/manual migration, never via the self-migration (probe-read cents only, `README.md:83`). Writes against a table missing it retry once without it and name the drop (`dropped: ["revenue_source"]`, `functions/api/[[path]].js:429,508`); any other missing column 503s naming only it (`:732`). Do not widen the retry to cents — a dropped $ figure is worse than a loud 503.
- Manual `POST /run` skips briefs by design (30s `waitUntil` cap, `worker/src/index.js:7-11,346`); the 202 discloses it (`:728`, `briefs_skipped:true`, `reason: manual skips brief pass`) and the dashboard button is labeled `Run triage now (briefs on cron)` (`public/index.html:26`). Do not move briefs onto the manual path.
- Owner actions (human-only, one line each): (1) clear Needs-review oldest-first with V/K, 11 rows, ~1 min/row (`docs/OWNER-ACTIONS.md:7-20`); (2) set `ALLOW_SCHEMA_MIGRATION=1` as a Pages production env var and retry the latest deployment (`docs/OWNER-ACTIONS.md:22-33`); (3) add `CF_API_TOKEN` + `CF_ACCOUNT_ID` repo secrets and redeploy so CI applies `revenue_source` (`docs/OWNER-ACTIONS.md:35-41`; workflow fails fast without them, `.github/workflows/deploy-worker.yml:20-26`); (4) deploy HEAD so live stops trailing at `b8ae5b5` (F3); (5) Start one $0 starter and drive it to won/lost with a human-entered $ + post-mortem (`docs/OWNER-ACTIONS.md:43-54`). Never spend, create accounts, enter payment/personal details, send anything outward, accept terms, or submit forms — draft, queue, and leave the decision to him.

## 6. Proposed next tasks (2–4)

- Task 1: Prove the unreviewed-to-vetted path and why vetted_last_7d is 0
  - Owned files: `public/app.js`, `functions/api/[[path]].js`, `worker/src/index.js`, `functions/api/api.test.mjs` (existing real-path proof, no new file unless the path is broken).
  - Acceptance: report the path in one sentence with file:line (expected: manual `vetOpportunity` `public/app.js:562` → `vettedNotes` `:485` → `PATCH /api/opportunities/:id`); cite the green real-path test (`Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today`) as the passing proof a single run yields unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today; confirm `last_vetted`/`last_verdict` (`functions/api/[[path]].js:657-671`) already date stalls (both null live = 7.8d stall, no code change); if instead an automated path exists and is broken, fix it with a failing-then-passing real-path test against stubbed D1 (not a helper unit test). `npm test` green.
- Task 2: Expose inflow-paused in /api/health so a paused triage never reads as clean
  - Owned files: `functions/api/[[path]].js`, `functions/api/api.test.mjs`, `public/app.js`, `public/dashboard.test.mjs` (pin only).
  - Acceptance: health carries `inflow_paused` derived from the existing unreviewed probe (`> 10` → true); stubbed D1 with 11 unreviewed → true, ≤10 → false, total outage → null like its neighbors; existing keys byte-identical for `deploy/verify.sh`; dashboard pill/tooltip mirrors the bit without a new fetch; `npm test` green.
- Task 3: Name silent worker failures: stale sweep, pre-pass selects, bare select
  - Owned files: `worker/src/index.js`, `worker/src/index.test.js`.
  - Acceptance: each of the three shapes (`:373-377` stale catch, `:400-405` pre-pass `catch(()=>[])`, `:262-270` `selectBareRow` `catch(()=>null)`) appends a compact conditional marker to the finish line beside `src_fail`/`src_partial`/`brief:failed`; all-failed, partial, and quiet-tick shapes byte-identical; three stubbed-D1 tests (one per shape) finish `ok` with the marker present; `npm test` green.
