# Audit 2026-09-20 round 4 — aimoney (read-only)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r4`. HEAD `49a75f8`, tree clean before this file; no network, no installs, no edits to existing files. Only this file created.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests.
- Entry points: `public/index.html` + `public/app.js` (dashboard), `functions/api/[[path]].js` (D1-backed REST API), `worker/src/index.js` (research agent).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `https://aimoney-research.levitinvlad.workers.dev/`; live rev `0b51cd7`, one commit behind HEAD `49a75f8`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:715`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema + shared migrations + worker) or manual `deploy/deploy.sh`, gated by `deploy/verify.sh`.

## 2. End-to-end walk-through

Signal to decision; the hop where it most often goes silent is hop 13.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:78` `redditSignals`, `:106` `githubSignals` via `timedJson` (`:37`, 6s timeout covering the body read). A query that throws marks its source; all-failed names it in `src_fail`.
2. Store: `:354-359` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:360-363` (`phase:collected`).
3. Sweep: `:367-372` unprocessed signals older than 30d become noise (`stale:N`).
4. Take: `:374-376` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:382-408` via pure `exactUrlTarget` (`:168`); bounded `IN` selects (`:390-397`) match fresh URLs against opportunity `source_url`s and linked signal URLs; a match links as supports (notes newest-kept, no status move). Note `:403` is unguarded (F5).
6. Gates, read before classify: `:421-424` bare-without-brief > 10 caps inserts to 1/tick; `:426-428` unreviewed > 10 pauses inflow to 0/tick. Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:439-452` runs only with fresh signals AND `maxNewThisRun > 0`; `buildClassifyPrompt` (`:272`) + `aiComplete` (`:136`, 60s cron / 12s manual, retries 1/0), parsed by `parseJsonLines` (`worker/src/lib.js:62`, NDJSON + `repairJson` for `\_`). Inflow-paused ticks skip classify (finish line carries `inflow:paused`, `:629`) and still brief.
8. Verdict loop `:464-532`: first-wins validation, strict action enum, numeric-or-null id. `new` INSERTs (`:492-502`) with an `UNREVIEWED` note (`:502`) and capped `effectiveScore` (`worker/src/lib.js:29`, cap 6000 `:22`); overflow stays unprocessed (`:481`); slug collision links as supports (`:507-519`).
9. Verdict flush: `:533` `flushVerdictWrites` (`:291`, one batch with per-write fallback; failures as `verdict:N_failed`).
10. Briefs: main `:545-584` (top-scored live-only `:561-563`, or oldest-unreviewed-first past 48h `:551-556`) plus extra `:591-626` when the oldest unreviewed exceeds 48h, via `buildBriefPrompt` (`:202`), `parseBriefJson` (`:213`), `insertBrief` (`:224`, never stores empty); poison rotation `:248-265` (`brief:skipped:N`); failures `brief:failed`.
11. Finish: `:629` single `finish("ok", …)` carrying mode/pause/stale/src_fail markers; errors `:631-639` finish `error` plus the `console.error("cron-failed", …)` tail marker (`:637`, also `:649`, `:713`). `scheduled` `:643-651`, `POST /run` `:705-716`.
12. Dashboard review: `refreshReview` (`public/app.js:280`) derives via `deriveReviewList` (`:277`, `needs_review` bit with notes fallback); `renderReview` (`:227`) shows decision + brief lines with Vet / Vet-&-starter / Kill; `renderStartHere` (`:141`) carries Vet/Kill for unreviewed top picks; the pill mirrors the backlog via `reviewBacklogBit` (`:332`) with a review-filter jump.
13. Vet — the path that moves unreviewed to vetted: `public/app.js:481` `vetOpportunity` → `:471` `fetchDetailForWrite` (token-gated detail fetch) → `:486` inner → `:461` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id`. Starter variant `:511` adds a planned experiment and flips the row to `testing`. Kill (`:547`, drawer `:1037`) writes `[date killed]` + post-mortem. `V`/`K` keys (`:623`) with auto-advance (`:618`, `:595`): one human verdict per keypress, no bulk path.
14. API writes: `updateOpportunity` (`functions/api/[[path]].js:218`, text bounds, status/range guards, `effectiveScore` `:247`); `createExperiment` (`:335`) / `updateExperiment` (`:396`, won/lost require `result` + `post_mortem`, stamps, `outcomeLedgerLine` `:86`, cents clamps, `revenue_source` retry `:376-385`/`:451-460`).
15. Health: `functions/api/[[path]].js:477-652` — 12 probes (`:493-510`) in one batch (`:511`) with per-probe isolation fallback (`:520-549`, failures named in `health_probe_failures` + column-only `health_probe_detail`), lazy self-migration (`:558-574`), `last_vetted`/`last_verdict` parsed in JS (`:605-619`) from 500-char tails (`:509`); payload `:652` (`ok:false` + nulls only on total outage).

Where it most often breaks or goes silent: hop 13, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 5.1) while `vetted_last_7d` stays 0, `last_vetted` stays null, and the oldest unreviewed ages (178.7h and rising). Second: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F3).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:7`) → exit 0 this session. Per-file TAP totals, each run separately with no narrowing: `worker/src/lib.test.js` 16/16; `d1/migrations.test.mjs` 5/5; `deploy/deploy.test.mjs` 20/20; `public/tab-icon.test.mjs` 5/5; `public/dashboard.test.mjs` 132/132; `worker/src/index.test.js` 95/95 (`duration_ms 90832.74`); `functions/api/api.test.mjs` 116/116. Total 389 pass, 0 fail — identical to the Windows baseline `_night/tests-baseline.log:282-289` (389/389, `duration_ms 90278.5984`).
- Parse: `node --check public/app.js`, `node --check worker/src/index.js`, `node --check worker/src/lib.js` → exit 0 each (only the sandbox `UNDICI-EHPA` proxy warning on stderr). `node --check` on `functions/api/[[path]].js` was denied twice by the approval gate (bracket path); it is covered instead by `api.test.mjs` (116 pass, imports that file) and the shipped-JS parse-gate suite (includes a broken-syntax positive control). `python3 -m compileall -q .` → exit 0, no output: no Python sources exist (glob `**/*.py` matches nothing; positive control: same tool and mode on `**/*.js` matches the 4 shipped JS files).
- Dead code: none in shipped source. Regex `console\.|debugger` over `functions`, `worker/src/index.js`, `worker/src/lib.js`, `public/app.js` hits only the three deliberate `console.error("cron-failed", …)` markers (`worker/src/index.js:637,649,713`) plus one comment mentioning `console.*` (`:635`); no `debugger`. Positive control: the same regex matches 8 harness captures in `worker/src/index.test.js`, so the three is real.
- Duplicated logic: none material. `effectiveScore` is defined once (`worker/src/lib.js:29`) and imported by the API (`functions/api/[[path]].js:10`, 4 call sites); `tokensMatch` is shared (3 worker checks + API `authed`); brief/classify/insert share `buildBriefPrompt` (`:202`), `parseBriefJson` (`:213`), `buildClassifyPrompt` (`:272`), `insertBrief` (`:224`); verdict writes share `flushVerdictWrites` (`:291`); both close paths share `outcomeLedgerLine` (`functions/api/[[path]].js:86`).
- TODO density: zero in shipped source. Regex `TODO|FIXME|HACK|XXX` over the repo hits only 2 lines of `docs/audits/*.md` prose. Positive control: the same tool matches `UNREVIEWED` 30+ times across `worker/src` + `public/app.js`, so the zero is real.
- Surface rules: light-only (`public/styles.css:4` `color-scheme: light`; regex `prefers-color-scheme|data-theme|theme-toggle|setTheme` over `public/` hits only the negative assertions in `tab-icon.test.mjs` + `dashboard.test.mjs`, zero shipped hits; control: `color-scheme` matches `styles.css:4`). Favicon linked (`public/index.html:9-10`); `public/favicon.svg` is 284 bytes (`wc -c`), 32×32 `$` on `#0d6b3f`; brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suite `tab-icon` 5/5 green. Live dashboard HTML is byte-identical to the repo (`cmp _night/live/aimoney_pages_dev.html public/index.html`, exit 0).
- Tree: `git status --short` empty before this file; HEAD `49a75f8` (`git log` top: `49a75f8 Night round 3 … kill verdict date, dashboard timeouts, and shared ledger builder`); `.gitattributes:4` pins `eol=lf`.
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (rev `0b51cd7`, one commit behind HEAD): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:5.1, oldest_unreviewed_age_h:178.7, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. No `last_verdict` key — live predates HEAD. Prior snapshot (`docs/audits/AUDIT-2026-09-20-round4.md:43`): same 11 unreviewed at 175.5h — the queue has not moved between snapshots.
- Release marker (build-identity scope): single source of truth is `/release.json` (no `/build.json`, correctly per scope). Repo `public/release.json:1` carries the `dev-undeployed` placeholder by design (stamped at build: `scripts/stamp-release.sh:9` full SHA + release; `deploy/deploy.sh:61` same); `deploy/verify.sh:66-106` requires JSON with `revision` == deployed commit (HTML/unparsable/placeholder fail immediately, stale retried 6×10s). Live `/release.json` was not fetched for this round (no network; only dashboard + health snapshots provided), so its exact live body is unmeasured here — indirect evidence the live stamp works: `/api/health` `rev` is the full 40-char `0b51cd73a2db9c0c54a51b3892b73695579e5eff`, which the handler reads from `release.json` (`functions/api/[[path]].js:644-651`).

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 178.7h / 7.4d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `hours_since_last_ok_run` 5.1. Was: same 11 unreviewed at 175.5h — five rounds shipped tonight and the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none measured — `revenue_total`, `revenue_last_7d`, `spent_total` all null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter (the seed `Spec-ad sprint: 10 brands, 10 free ads`, `d1/seed.sql:96-100`), run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.4 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:481` `vetOpportunity` → shared `vettedNotes` (`:461`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id`. The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:502`: `` `… — UNREVIEWED, scores capped until a human vets it …` ``) and the other 9 `UNREVIEWED` hits in `index.js` are create/count/select sites (`:18,:416,:426,:427,:499,:548,:556,:600,:604`) — zero clearing paths (positive control: the same search returns those 10 sites, so the absence of a clearing path is real). So `vetted_last_7d` is 0 with `hours_since_last_ok_run` 5.1 by design, not by failure: cron triage + briefs run, verdicts are human-gated. A single run of the path is certified by the real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs`, in the 116 green): one Vet press yields unreviewed −1, `vetted_last_7d` +1, `last_vetted` = today.

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:426-428`), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change — the 2-minute keys (`V`/`K` + auto-advance, `public/app.js:623,618,595`, one verdict per keypress, token-gated, buttons/drawer untouched) already shipped in HEAD with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines. Touch no repo files. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Live serves the previous commit — V/K keys, last_verdict, and timeouts are shipped but not serving

Evidence. Live `/api/health` `rev` is `0b51cd73a2db9c0c54a51b3892b73695579e5eff`; HEAD is `49a75f8` (`git log`: `49a75f8 Night round 3 (aimoney): Add kill verdict date, dashboard timeouts, and shared ledger builder`, parent `0b51cd7 Night round 2 …`). The live payload carries `last_vetted` but no `last_verdict` key, while the HEAD handler emits both (`functions/api/[[path]].js:652`, parsed `:605-619`); the V/K triage keys (`public/app.js:623-631`) and 15s/25s API timeouts are likewise in HEAD tests (dashboard 132 green) but not on the live surface.

Why it costs: the owner clears 11 rows without the keys that make it a 2-minute job; the verdict-date stall signal (`last_verdict`, including kill-heavy clears) is invisible live; hung taps hang instead of failing visibly into the existing toast/banner paths.

Fix: deploy HEAD — Pages auto-deploys on push to `master` (Git-connected, `scripts/stamp-release.sh` stamps the full SHA + release), then run `deploy/verify.sh` and require PASSED: the live-revision gate (`deploy/verify.sh:66-106`) fetches `/release.json` cache-busted, demands JSON (HTML/placeholder fail immediately), accepts full-or-short prefix, and retries stale 6×10s. The worker leg of the deploy needs the two repo secrets (§5); without them ship Pages-only and say so. Touch no repo files. Size S. Risk low (389 green + the verify gate fails closed).

### F3. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live: `revenue_last_7d/revenue_total/spent_total` null with `health_probe_failures:[revenue_week,revenue_lifetime]`, `health_probe_detail:{…:revenue_cents}`, `schema_missing_columns:[revenue_cents,spent_cents]`, `schema_migration:disabled…`. The two failing probes are the only ones reading money (`functions/api/[[path]].js:504-505`, `SUM(revenue_cents)` / `SUM(revenue_cents/spent_cents)`). `d1/schema.sql:5` is `CREATE TABLE IF NOT EXISTS` (no-op on the existing table); the ADD COLUMNs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied after `schema.sql` by both CI (`.github/workflows/deploy-worker.yml:27` then `:33-34` `Apply D1 migrations in sorted order` via `./deploy/apply-d1-migrations.sh`) and `deploy/deploy.sh:36` then `:40` — one shared loop + tolerate-list (`deploy/apply-d1-migrations.sh:13-28`, duplicate-column no-op, anything else exits 1), pinned by the convergence tests (`d1/migrations.test.mjs` 5/5, incl. the withhold-one-file positive control). The in-worker fallback is deliberately off: `functions/api/[[path]].js:53` (`if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` → report missing + disabled, change nothing). Migration-ask results, all measured green this session: old schema + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers; var unset leaves the table unchanged and names the missing columns; second run is a clean no-op (`self-migration switch` suite, 6/6 inside the 116). Exact variable name: `ALLOW_SCHEMA_MIGRATION`.

Why it costs: earned-to-date is unknown rather than zero, so the money-first question cannot be answered from the project's own data. (The decisions COUNT split, `:503`, already saved `decisions_last_7d` = 0 — the money legs are the only remaining nulls.)

Fix: no code change; the path is already correct. The owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var — the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, and re-runs the failed money probes on the same call — or adds the two Cloudflare repo secrets and redeploys so CI applies the migrations. One variable, one decision; see §5 for the exact lines. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F4. A tagless notes PATCH can silently exit the review queue without recording a verdict

Evidence. `updateOpportunity` (`functions/api/[[path]].js:218-259`) applies text bounds plus status/range guards but has no `UNREVIEWED`/verdict-tag logic: a token holder PATCHing `notes` without the marker and without a tag is accepted, and the row leaves the unreviewed probe (`:499`, `notes LIKE '%UNREVIEWED%'`) while the vetted probe (`:506` tag-date OR + `:611` regex) counts nothing — unreviewed drops, `vetted_last_7d` never rises, no verdict date recorded. The dashboard never does this: all three `cleanUnreviewed` call sites append a verdict tag (`public/app.js:463` vetted, `:560` killed, `:1037` drawer killed — verified by search), so the hole is raw-API-only today.

Why it costs: silent lane-metric corruption — rows can leave the queue uncounted through a typo or a naive integration, and the stall-age math (`oldest_unreviewed_age_h`, `last_vetted`/`last_verdict`) quietly loses rows it should date.

Fix: in `updateOpportunity`, when `cur.notes` includes `UNREVIEWED` and `next.notes` includes neither `UNREVIEWED` nor `/\[\d{4}-\d{2}-\d{2} (vetted|killed)\]/`, return 400 naming the missing verdict tag; row untouched. Pin in `functions/api/api.test.mjs`: tagless clear 400s, tagged vet/kill pass, invalid-tag-date still 400s. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Size S. Risk low (no dashboard path sends a tagless clear, proven above; only raw-API misuse changes shape).

### F5. Unguarded signal UPDATE in the exact-URL pre-pass can abort the whole cron tick

Evidence. `worker/src/index.js:403`: `await env.DB.prepare("UPDATE signals SET processed=1, opportunity_id=? WHERE id=?").bind(target, sig.id).run();` — no `.catch`, while the sibling notes append on `:404` ends `.catch(() => null)`. A transient D1 throw on `:403` propagates to the `runResearch` catch (`:631-639`) → `finish("error")`: the tick's triage, verdict writes, and both brief passes are lost for 6h, and `hours_since_last_ok_run` ages toward the verify.sh 12h freshness fail (`deploy/verify.sh:117-124`). (`state.updated++` on `:402` runs before the writes, so the error row also overstates updates.)

Why it costs: one flaky write out of ~12 per tick can cost the brief half of the pass while 19 rows wait bare — the reliability failure mode (a hang/crash instead of an honest degrade) on the project's only scheduled job.

Fix: give `:403` the same best-effort treatment as `:404` — either `.catch(() => null)` with a counter on the finish line beside `verdict:N_failed`, or push both pre-pass writes into the `verdictWrites` batch for the shared `flushVerdictWrites` (`:291`) isolation fallback — and move `state.updated++` after success. Pin in `worker/src/index.test.js` driving the real `runResearch` against a stubbed D1 that throws once on that UPDATE: the tick still finishes `ok` with briefs. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk low (strictly fewer aborted ticks; unprocessed signals still retry next tick).

### F6. `package.json` lacks `"type": "module"` — every test run pays reparse warnings + overhead

Evidence. Every `node --test` run this session prints, per `.js`-entry suite: `[MODULE_TYPELESS_PACKAGE_JSON] Warning: Module type of file://…/worker/src/lib.test.js is not specified and it doesn't parse as CommonJS. Reparsing as ES module because module syntax was detected. This incurs a performance overhead. To eliminate this warning, add "type": "module" to …/package.json.` All shipped sources are already ESM (`import`/`export` throughout `worker/src`, `functions/api`, `public/app.js`).

Why it costs: slower test boot on every run and warning noise that buries real warnings (e.g. the `cron-failed` honesty markers the suites assert on). No behaviour is at stake — the files already execute as ESM.

Fix: add `"type": "module"` to `package.json` so `.js` parses as ESM natively (the `.mjs` suites are unaffected; wrangler already serves ESM), then run the full `npm test` green to confirm. Touch `package.json`. Size S. Risk low (parse mode matches current effective semantics; the 389-test suite pins behaviour).

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, or auto-testing without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless. `functions/api/[[path]].js:16-25` (switch docblock), `:53` (unset → report + change nothing), `:59-67` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate (`:35,:69-71`). Do not default it on, do not add a second trigger, and do not add a migration endpoint: `a URL that alters the schema is a liability even when it is authenticated.`
- Never a data-losing migration here: ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change); unset behaviour is byte-identical to today. His one line: set `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var and reload `/api/health` once — the two money columns appear and the money legs fill on the same call.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action.
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment from the decision + brief lines. Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure — the inline Win close takes a human-entered amount for exactly this reason.
- Never, whatever the ranking says: no spending, no account or identity creation, no outward sends, no accepting terms, no captcha solving, no fabricated reviews/receipts/testimonials, no scraping or messaging a platform's terms forbid, no financial advice or implied return.
- House rules honored by this audit: never read `.env`/secrets/tokens; no edits to existing files (this findings file only); CRLF care (whole-line matches — unneeded here since nothing was edited; `.gitattributes:4` pins `eol=lf`).

## 6. Proposed next tasks (2–4)

### Task 1. Vet-or-kill all 11 unreviewed rows via Needs-review + V/K; empty the queue

Owns: live dashboard + D1 via the API (no repo files changed). Context (F1 — the focus-named vet ROUND, code half already shipped): the only path from unreviewed to vetted is the manual Vet button (`public/app.js:481` via `vettedNotes` `:461` → `PATCH /api/opportunities/:id`); the worker never vets by design, so no code change can move `vetted_last_7d`. A human opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines — one keypress per verdict (`V` vets and auto-advances, `K` opens the inline post-mortem, Confirm kills and advances), a couple of minutes once Task 2 lands the keys live (live buttons work the same today, slower).

Acceptance: `/api/health` reads `unreviewed` 0, `oldest_unreviewed_age_h` null, `vetted_last_7d` + dated kills in the window = 11, `last_vetted` (and post-Task-2 `last_verdict`) = today; every Vet carries a `[YYYY-MM-DD vetted]` tag, every Kill a one-line post-mortem in notes; next cron resumes inflow (unreviewed ≤ 10 → cap 2/tick). Requires `ADMIN_TOKEN` and human judgment per row; never mark decided without evidence.

### Task 2. Deploy HEAD 49a75f8 to live; verify full-SHA revision + last_verdict key

Owns: git push + `deploy/verify.sh` run (no repo file changes). Context (F2): HEAD adds the kill-verdict date, dashboard timeouts, and the shared ledger builder on top of the V/K + pill-backlog + classify-skip already in history; live still serves `0b51cd7` without the `last_verdict` key.

Acceptance: live `/api/health` `rev` equals the full 40-char HEAD SHA, `last_verdict` key present (null until the first verdict), `deploy/verify.sh` PASSED including the live-revision gate (JSON body, full-or-short prefix match) and agent-freshness; if the worker leg cannot deploy for missing secrets (§5), ship Pages-only and note it explicitly.

### Task 3. Reject tagless UNREVIEWED clears in updateOpportunity (400 + test)

Owns: `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Context (F4): a notes PATCH that drops `UNREVIEWED` without appending a `[YYYY-MM-DD vetted|killed]` tag silently exits the review queue uncounted. When `cur.notes` includes `UNREVIEWED` and `next.notes` includes neither the marker nor the tag regex, return 400 naming the missing verdict tag; row untouched.

Acceptance: tagless clear 400s with the tag named and the row untouched; tagged vet/kill PATCHes pass unchanged; `npm test` green (389+).

### Task 4. Best-effort the pre-pass signal UPDATE so one throw can't abort the tick

Owns: `worker/src/index.js`, `worker/src/index.test.js`. Context (F5): the exact-URL pre-pass signal UPDATE (`worker/src/index.js:403`) is the only unguarded await in the pass — one transient D1 throw aborts triage + both brief passes for 6h. Give it the sibling's best-effort treatment (or route both pre-pass writes through `flushVerdictWrites`) and count `updated` on success.

Acceptance: a test driving the real `runResearch` against a stubbed D1 that throws once on that UPDATE still finishes `ok` with briefs; `npm test` green.

