# AUDIT 2026-09-20 round 4 — aimoney (read-only)

Lane metric: experiments reaching a decision (won/lost with post-mortem) per week, and vetted proposals that become experiments; unreviewed backlog drained.
Live snapshot used: `_night/live/aimoney_pages_dev_api_health.html` (rev 91598ff, 2026-09-21T05:57:04Z). HEAD here is 1ef42e3.

## 1. What this project is and how it runs

- Purpose: ranked list of AI-money opportunities with research briefs, experiments, and a cron research agent that proposes new rows; the human vets, tests, kills/scales (`README.md:3-16`).
- Entry points: `public/index.html` + `public/app.js` (dashboard); `functions/api/[[path]].js` (REST API on D1); `worker/src/index.js` (research agent: cron + `POST /run`).
- Live surfaces: `https://aimoney.pages.dev` (dashboard), `/api/health`, `/api/opportunities`, `/api/runs`, worker `GET /` status (`deploy/public-surfaces.json:32-45`).
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`); each tick collects signals, triages (≤6), briefs (1+1).
- Deploy: Git-connected Pages build stamps `public/release.json` via `scripts/stamp-release.sh:5-8`; `deploy/deploy.sh` applies `d1/schema.sql` + shared `deploy/apply-d1-migrations.sh` + Pages + worker; CI `.github/workflows/deploy-worker.yml` runs schema (`:27`) + the same shared migration script (`:33-34`), then worker deploy; `deploy/verify.sh:46-80` gates the rollout on live revision.

## 2. End-to-end walk-through

Signal → triage → proposal → review → brief → experiment → decision → money:

1. Cron fires `scheduled` (`worker/src/index.js:585`), `runResearch` (`:258`) reaps runs stuck past 30 min (`:262-267`) and opens a run row (`:268-277`).
2. Collect: `hnSignals` (`:48`), `redditSignals` (`:77`), `githubSignals` (`:105`) in parallel (`:293`), one batched `INSERT OR IGNORE` (`:304-309`), 30d stale sweep (`:317-322`), heartbeat `phase:` mark (`:310-313`).
3. Take oldest 6 unprocessed (`:324-326`); exact-URL pre-pass links duplicates as supports with no AI call (`exactUrlTarget` `:167`, pre-pass `:332-358`).
4. Classify: `buildClassifyPrompt` (`:222`, called `:373`) → `aiComplete` (`:135`, budgeted, racing a timeout) → `parseJsonLines` (`worker/src/lib.js:62`); verdict loop (`:400`) INSERTs `new` rows as `researching` + `UNREVIEWED` with score capped ≤6000 (`:427-438`, `effectiveScore` `worker/src/lib.js:29-32`), or `supports` (`:456-464`) / `noise` (`:466`); inflow capped at 2/run, 1 while bare-without-brief > 10 (`:384-392`); writes flush via `flushVerdictWrites` with per-write fallback (`:241-257`, called `:469`).
5. Brief: top-scored bare row, or oldest-unreviewed-first past 48h (`:480-495`); one extra oldest-unreviewed bare row past 48h on cron (`:528-567`); shared `buildBriefPrompt`/`parseBriefJson` (`:201-215`); run log carries mode + `src_fail`/`brief:failed`/`verdict:N_failed` (`:571`); D1-dead cron ticks log `cron-failed` to the tail (`:579`, `:590-592`).
6. Health: 11 probes, one `env.DB.batch` (`functions/api/[[path]].js:460-476`), per-probe isolation fallback naming `health_probe_failures` + column detail (`:485-514`), self-migration retry of money probes (`:522-539`), payload (`:590`).
7. Review: dashboard chip → `listOpportunities` (`[[path]].js:113`, unreviewed filter `:123`, oldest sort `:125`); Vet (`public/app.js:421-442`) clears `UNREVIEWED` and appends `[YYYY-MM-DD vetted]` (`:431`) which the `vetted_7d` probe counts (`[[path]].js:472`); Kill requires a post-mortem (`public/app.js:487-514`); Vet-&-starter (`:450-485`) composes Vet + planned experiment + flip to `testing`.
8. Experiment: `POST /experiments` (`[[path]].js:324`); Start stamps `started_at` (`:401`); Win/Lose require `result` + `post_mortem` (`:402-407`), stamp `ended_at`, append the `$X rev / $Y spent` outcome-ledger line to the parent (`:412-421`); `decisions_last_7d` counts won/lost closed in 7d (`:469`), revenue probes SUM cents (`:470-471`); the Experiments header renders the week line from these keys (`README.md:75`).
9. Where it most often breaks or goes silent: the human step. Cron is alive (`hours_since_last_ok_run` 5.9) and the machinery reports honestly, but 11 rows sit unreviewed (oldest 173.6 h), 19/27 have no brief, 2 experiments sit `planned` with zero running, and `decisions_last_7d`/`vetted_last_7d` are 0 — nothing has moved through steps 7–8 in 7+ days. Second suspect: brief throughput is static while the 48h extra-brief gate should fire every tick (finding 5). Third: the money leg reads null until the owner flips one switch (finding 2).

## 3. Health signals measured here

- Test suite: `npm test` → `node --test` over 7 files, exit code 0. Visible TAP window shows top-level suites ok 1–46+ (all `ok`, zero `not ok` in the visible 40,000 bytes; full output 90,901 bytes). Failing-path suites are visibly green in that window (health probe isolation incl. "failed batch with nine answering probes reports their numbers, db up", outage honesty, experiment-write 503, self-migration switch, migration convergence) — the suite executes real tests, not zero. (The saved-output tail lives outside the workspace and its read was denied, so totals are exit-code 0 + visible window, stated honestly.)
- Compile: N/A `compileall` (JS repo). All source modules load under the suite, including ESM `functions/api/[[path]].js` (one `MODULE_TYPELESS_PACKAGE_JSON` warning, non-failing, seen in the run output).
- TODO density: zero in shipped source. Literal `TODO` search hits only `docs/audits/*.md`; positive control: the same tool returns `effectiveScore` hits in `functions/api/[[path]].js`, `worker/src/*`, `public/app.js` — so the zero is real. `console.*` in shipped source is only the two deliberate `console.error("cron-failed", …)` markers (`worker/src/index.js:579`, `:591`); no `debugger`.
- Dead code: one item, unchanged. `appendKeepNewest` is exported (`worker/src/lib.js:49-52`) and unit-tested (`worker/src/lib.test.js:89-100`) but imported nowhere in shipped code — every writer inlines the SQL idiom `substr(notes || ?, -8000)` instead. Tested spec of a live idiom, not rot; leave or delete, no behaviour change either way.
- Duplicated logic: (1) `centsDollars` (`functions/api/[[path]].js:82`) vs `moneyCents` (`public/app.js:53`) — intentional documented mirror (fixed-2dp), keep; (2) `clamp10` in the API (`:74-78`) vs `worker/src/lib.js:4-7` — near-identical, the API imports only `effectiveScore`/`tokensMatch` (`:10`); (3) brief INSERT SQL twice (main `:510-517` vs extra `:555-562`) — finding 7; (4) Vet notes-tag built twice (`public/app.js:431` vs `:460`) — finding 8. Fixed this HEAD, no longer findings: experiment PATCH bounds (`[[path]].js:384-386`) and one shared classify-prompt builder (`worker/src/index.js:222`, used `:373` and `:635`).
- Line endings (implementers read this): `git ls-files --eol` shows a mixed worktree — `functions/api/[[path]].js`, `public/app.js`, `worker/src/index.js` are LF, most other files are CRLF in the worktree (index all LF, `.gitattributes:4` pins `eol=lf`). Match the file's actual endings, replace whole lines, re-read after every edit.
- Surface rules (all pass, zero findings): `color-scheme: light` (`public/styles.css:4`), no `prefers-color-scheme`/`data-theme`/theme-toggle string in shipped `public/` (those strings appear only in guard-test assertions — `public/tab-icon.test.mjs:70-76`, `public/dashboard.test.mjs:164` — the matcher is proven live); `<link rel="icon" … href="/favicon.svg">` + `<meta name="theme-color" content="#0d6b3f">` (`public/index.html:9-10`); hand-written 32×32 `$`-on-`#0d6b3f` `public/favicon.svg` (1 line, <2 KB); inline brand mark reusing the same geometry/accent/glyph (`public/index.html:16`); guard suites green inside `npm test`. Saved live HTML matches the served shell on all markers (`_night/live/aimoney_pages_dev.html:9-10`, `:16`).
- Build identity: one source of truth — `/release.json` stamped by `scripts/stamp-release.sh:5-8` on Pages builds and `deploy/deploy.sh:58` on manual deploys, echoed as `rev` in `/api/health`, checked by `deploy/verify.sh:46-80` (JSON parse required; HTML/unparsable/`dev-undeployed` placeholder fail immediately; stale revisions retried bounded 6×10s). The committed `public/release.json:1` is the `dev-undeployed` placeholder by design — do not judge the edge by the committed file. No second source should be added.

Money-first answers (owner directive), in numbers from this project's own data: earned to date **none** — `revenue_total` is null (unknown: migration disabled) and no experiment has ever closed (`experiments_by_status {"planned": 2}`, zero won/lost), so no revenue row can exist; last krone arrived **never**. This lane is a tool the owner uses, not a product he sells — it ranks by decisions/week and vetted→experiment conversion, both currently 0. Single step to the next payment: a human vets one opportunity and starts its zero-spend starter (Vet & log starter → Start). Measured conversion at that step: unmeasured (zero attempts).

## 4. Ranked findings (max 8)

1. **The queue has not moved in 7+ days; that is the whole deficit.**
   Evidence: live health `{"unreviewed":11,"bare_without_brief":19,"vetted_last_7d":0,"decisions_last_7d":0,"experiments_by_status":{"planned":2},"oldest_unreviewed_age_h":173.6}` (`_night/live/aimoney_pages_dev_api_health.html:1`).
   Why it costs: the lane metric is decisions/week and vetted→experiments — both 0 across every snapshot this week while four rounds improved machinery; no code change moves it.
   Fix: do the pipeline work as task 1 — review the 11 with verdicts + reasons in the rows' own notes/tags (no new schema), brief or concretely block the 19 bare, next-step the 2 planned with an owner for each step, and name one best candidate with its case. Rejecting nine of eleven with reasons is a good outcome. No code files. Size M (human time). Risk: none — verdicts are reversible text.
2. **Live revenue fields are null because the self-migration switch is off — owner action.**
   Evidence: live `{"health_probe_failures":["revenue_week","revenue_lifetime"],"health_probe_detail":{"revenue_week":"revenue_cents","revenue_lifetime":"revenue_cents"},"schema_missing_columns":["revenue_cents","spent_cents"],"schema_migration":"disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)"}`; gate `if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` at `functions/api/[[path]].js:53`.
   Why it costs: the money leg of the lane metric is invisible — four fields read unknown instead of honest numbers.
   Fix: owner sets Pages env var `ALLOW_SCHEMA_MIGRATION=1`; the next health call ADDs exactly the two columns from `d1/migrate-2026-09-20-experiment-cents.sql` (`[[path]].js:27-30`) and reports numbers on the same call (`:522-539`). No code change. Size S. Risk: low — ADD COLUMN with DEFAULT, once per isolate, no endpoint.
3. **CI still cannot converge the schema: the repository has no Cloudflare secrets.**
   Evidence: `.github/workflows/deploy-worker.yml:20-26` fail-fast on empty `CF_API_TOKEN`/`CF_ACCOUNT_ID`; the migration step `:33-34` never runs without them, so `revenue_source` can never reach live D1 via CI and experiment writes keep 503ing (`functions/api/[[path]].js:614-618`).
   Why it costs: the deploy path is correct but unfireable; every schema fix waits on a human with credentials.
   Fix: owner adds `CF_API_TOKEN` and `CF_ACCOUNT_ID` to repo Settings → Secrets → Actions, then re-runs the Deploy research worker workflow. Nothing else changes. Size S (owner). Risk: none to code.
4. **The live edge is two rounds stale: it serves round-1 rev while HEAD is round-3.**
   Evidence: saved health `"rev":"91598ff"` vs `git rev-parse --short HEAD` → `1ef42e3`; `git log --oneline` shows `1ef42e3` (round 3) on top of `7420a40` (round 2) on top of `91598ff` (round 1). Rounds 2–3 shipped code the owner cannot see yet.
   Why it costs: every round is judged by the live surface; a stale edge reads as failed work, and `deploy/verify.sh:46-80` would fail its live-revision gate against HEAD right now.
   Fix: check the Pages deployment for `7420a40`/`1ef42e3` (build log for the Git-connected project), let the queued build finish or re-run the deploy, then run `deploy/verify.sh 1ef42e3`. Do not add a second version source. Not this lane (needs deploy credentials). Size S. Risk: low.
5. **Bare-without-brief is static at 19 while cron is alive — the brief pass is the suspected silent break.**
   Evidence: `bare_without_brief:19` in every snapshot this week + `hours_since_last_ok_run:5.9` (cron ticking) + oldest unreviewed 173.6 h, which is past the 48h extra-brief gate (`worker/src/index.js:528-530`) that should brief up to 2 rows × 4 ticks/day; the 19 should have drained days ago.
   Why it costs: unbriefed rows cannot be decided in under a minute from a phone; the review queue stays unactionable and finding 1 never clears.
   Fix: measure first (task 3) — briefs/week and `brief:failed`/`src_fail` rate from `/api/runs` — before touching code; if failures cluster, fix that cause, not capacity. Read-only. Size S. Risk: none.
6. **Manual `POST /run` swallows failures silently; cron logs `cron-failed`.**
   Evidence: `worker/src/index.js:654` — `ctx.waitUntil(runResearch(env, "manual").catch(() => null));` — vs the cron path `:590-592`, which logs `console.error("cron-failed", …)` on the same rejection.
   Why it costs: a D1-dead manual run returns 202 "accepted", leaves no run row and no tail line — it reads as success on exactly the path the owner taps himself (`public/app.js:1128-1148` shows "Triage started" and refreshes 45s later onto unchanged data).
   Fix: log the same one-line `cron-failed` marker in the manual catch (one line, same shape as `:591`), and add a test with a D1 stub that rejects everything asserting the marker fires for `trigger === "manual"`. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk: low — observability-only on an already-failed path.
7. **Brief INSERT is duplicated between the main and extra passes.**
   Evidence: `worker/src/index.js:510-517` vs `:555-562` — only prompt/parse were shared (`buildBriefPrompt`, `parseBriefJson`); the non-empty guard (`String(b.summary …) && String(b.first_steps …)`) plus the 9-column INSERT repeat.
   Why it costs: the "never store an empty brief" guard can diverge by path so one path writes blank briefs; every brief-schema change needs two identical edits.
   Fix: extract a shared `insertBrief(env, opportunityId, parsed, sigs)` returning bool (guard inside), call it from both passes, keep existing brief tests green. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk: low.
8. **Vet notes-tag built twice; the tag is the health contract.**
   Evidence: `public/app.js:431` (`vetOpportunityInner`) vs `:460` (`vetAndLogStarterInner`) — byte-identical `` `[${day} vetted] Human vetted; cap lifted.` `` strings today; the `vetted_7d` probe counts that tag (`functions/api/[[path]].js:472`).
   Why it costs: one divergent edit silently breaks `vetted_last_7d`, a lane-metric input, with no test failing at the divergence site.
   Fix: one `vettedNotes(notes)` helper used by both inners; extend the dashboard suite to pin both call sites to it. Touch `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk: negligible.

## 5. What NOT to change

- `ALLOW_SCHEMA_MIGRATION=1` is the owner's switch: "Default-off: unset changes nothing" (`functions/api/[[path]].js:23`); ADD-only, once per isolate, no endpoint (`:25-32`, `:58-72`). One line for him: Pages → Settings → Environment variables → add `ALLOW_SCHEMA_MIGRATION=1`, redeploy.
- `CF_API_TOKEN` / `CF_ACCOUNT_ID` repo secrets are his to add (finding 3). One line: repo Settings → Secrets → Actions → add both, re-run Deploy research worker.
- Promoting rounds 2–3 to the live edge (finding 4) needs deploy credentials and is not this lane's; never enter or store a credential here.
- Never spend money, create accounts, enter payment details, sign up for anything, or accept terms — all his, per the brief's hard boundaries. Never start an experiment that incurs cost or makes an outward-facing commitment; planning is the work, running is his decision.
- Never touch the production database directly (`wrangler d1 execute --remote`); write through the app's own paths. Applying migrations to live D1 is his action.
- `revenue_source` is deliberately NOT auto-migrated: "write-only columns are never auto-migrated" (`README.md:83`); experiment writes 503 naming only that column by design (`functions/api/[[path]].js:86-96`).
- The agent never moves status: "it never moves status to testing/scaling/killed" (`worker/src/index.js:5`); "`researching → testing` is a human decision" (`README.md:72`). Do not automate it, and do not invent opportunities to make the queue look worked.
- Never invent a revenue figure or mark an experiment decided without evidence; never present projections as results; never multiply an unverified rate by a volume.
- The committed `dev-undeployed` placeholder in `public/release.json:1` is by design (the stamp step replaces it at build time); `verify.sh` fails a live placeholder. Do not hardcode a revision.

## 6. Proposed next tasks

Pipeline report for this round: reviewed 0, rejected 0 (no commonest reason), briefs written 0, no best candidate named from live data — all pipeline work is task 1 below.
Unreviewed is still 11 because this lane was read-only (findings file only, no network, no writes), so no verdict could be recorded.

1. **Review 11 unreviewed, brief/block 19 bare, next-step 2 planned, name 1 best** — owned files: none (live data via dashboard + API paths only; no new schema, no new file). Acceptance: `/api/health` unreviewed < 11 with each verdict + reason in the row's own notes/tags; every bare row has a brief or a recorded concrete blocker; each planned experiment has a next concrete step + who takes it (owner steps as one actionable line: account to create, field to set, number to decide); report names the single best candidate with its case, counts reviewed/rejected + commonest reason, briefs written, and the exact owner-only next actions. (Seed-only hint, to be confirmed or overturned against the live 11 at execution time: `ai-ugc-ads-service`, score 16000, $2–8k/mo, $0–200 capital, briefed week-1 spec-ad sprint, `d1/seed.sql:9`.)
2. **Log manual /run failures to worker tail like cron does** — owned files: `worker/src/index.js`, `worker/src/index.test.js`. Acceptance: D1-rejecting test asserts the `cron-failed` marker fires for manual runs; full `npm test` green; whole-line edits verified by re-read (file is LF in this worktree).
3. **Measure brief throughput vs bare backlog from /api/runs** — owned files: none (or a note in the next audit). Acceptance: report briefs/week, `brief:failed`/`src_fail` rate, and a verdict: brief pass failing (name cause) vs working (name drain date for the 19).
4. **Share one brief-insert helper between main and extra passes** — owned files: `worker/src/index.js`, `worker/src/index.test.js`. Acceptance: the non-empty guard + INSERT live in one helper used by both passes; empty-brief test covers both paths; full `npm test` green.
