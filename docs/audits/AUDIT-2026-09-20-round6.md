# Audit 2026-09-20 round 6 — aimoney (read-only, track B)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r6`. HEAD `3ebef9a` (night round 5); live rev `b70c707` (night round 4, one commit behind). No git writes, no network, no installs, no edits to existing files. Only this file created at the root. Never read `.env`, secrets or tokens. One single-stage shell command per call, no inline code, no redirection. No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` at the root; `README.md` read first, then the live snapshots in `_night/live/`, then source.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests (`README.md:3-15`, `worker/src/index.js:4-5`).
- Entry points: `public/index.html` + `public/app.js` (dashboard), `functions/api/[[path]].js:520` `onRequest` (D1 REST API), `worker/src/index.js:685` (`scheduled` + `fetch`) + `worker/src/lib.js` (shared pure helpers).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `/` + `POST /run`; live snapshot `_night/live/aimoney_pages_dev_api_health.html:1` at `2026-09-21T21:32:06Z`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + up to 2 briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:748-758`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh:9`) plus CI `.github/workflows/deploy-worker.yml` (schema `:27` + shared migrations `:33-34` + worker `:38`) or manual `deploy/deploy.sh` (schema `:36` + shared `:40`, Pages `:66`, worker `:70`), gated by `deploy/verify.sh` (public reads only).

## 2. End-to-end walk-through

Signal to decision. The hop where it most often goes silent is hop 12 (human Vet), with hop 14 (money probes) null alongside it.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:81` `redditSignals`, `:110` `githubSignals` via `timedJson` (`:37-47`, 6s timeout covering the body read). Per-query throws counted; all-failed names the source in `src_fail`, partial in `src_partial` (`:76-77`, `:105-106`, `:134-135`).
2. Store: `:363-368` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:369-372` (`phase:collected` / `phase:collected stale:N`).
3. Sweep: `:380-384` unprocessed signals older than 30d become noise (`state.stale`); a failed sweep is named (`staleFailed`, finish `:672`).
4. Take: `:386-388` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:400-427` via pure `exactUrlTarget` (`:173-183`); bounded `IN` selects (`:409-414`, failures named `prepass_select:failed`); a match links as supports (notes newest-kept `:423`, no status move); failed links skipped and counted (`prepass:N_failed`, `:421`); failed notes appends counted (`prepass_notes:N_failed`, `:423`).
6. Gates, read before classify: `:442-444` bare-without-brief > 10 caps inserts to 1/tick (`bare_count:failed` on throw); `:446-449` unreviewed > 10 pauses inflow to 0/tick (`maxNewThisRun = 0`, `unreviewed_count:failed` on throw). Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:460-473` `buildClassifyPrompt(opps, fresh)` (`:281`) + `aiComplete` (`:141`, budget `MAX_AI_CALLS = 4`, `:19`), parsed by `parseJsonLines` (`worker/src/lib.js:64`, NDJSON + `repairJson` for `\_`). Inflow-paused ticks skip classify entirely and still brief (finish line carries `inflow:paused`, `:672`).
8. Verdict loop `:486-564`: strict action enum (`:492`), supports-without-id becomes noise (`:499-501`), overflow stays unprocessed (`:503`). `new` INSERTs (`:514-528`) with an `UNREVIEWED` note (`:524`) and capped `effectiveScore` (`worker/src/lib.js:30`, cap 6000 `:23`); slug collision links as supports (`:535-550`, failed lookup named `slug_select:failed` and left unprocessed for retry).
9. Verdict flush: `:565` `flushVerdictWrites` (`:300-316`); failures as `verdict:N_failed`.
10. Briefs: main pass (`:580-627`, top-scored live-only or oldest-unreviewed-first past 48h via `oldestForBrief` `:585` — `brief_age:failed` on throw, falls back to top-scored — with `selectBareRow` `:262` failures named `bare_select:failed`) plus extra pass (`:634-668`) when the oldest unreviewed exceeds 48h (live oldest 189.2h, so both fire), via `buildBriefPrompt` (`:207`), `parseBriefJson` (`:218`), `insertBrief` (`:229`, never stores empty); poison rotation via `rememberBriefSkip` (`:256`) and `brief:skipped`; failures `brief:failed`. Manual runs skip briefs by deadline (`:350`, `briefDeadline = -1`, `POST /run` `:758`).
11. Finish: `:672` single `finish("ok", …)` carrying mode/pause/stale/src/verdict/prepass markers; errors finish `error` plus `console.error("cron-failed", …)` (`:680`, also `:692`, `:756`). `scheduled` `:686-694`, `POST /run` `:748-759`.
12. Vet — the ONLY path from unreviewed to vetted, one sentence: a manual dashboard press — `public/app.js:564` `vetOpportunity` → `:540` `fetchDetailForWrite` (token-gated detail fetch, warms N+1 at `:548`, cold-boot guard at `:547`) → `:569` inner → `:485` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`:576-579`) with auto-advance (`:581-584` via `:744-747`, focus re-arms N plus N+1 at `:740-741`). Starter variant `:618-665` (vet + planned starter + flip to `testing`, resume-safe via `:601-616`) and Kill `:670-693` (`[date killed]` + post-mortem); `V` vets at `:760`; drawer Vet at `:1128-1129`. One human verdict per keypress, no bulk path.
13. API writes: `updateOpportunity` (`functions/api/[[path]].js:237`, text bounds, status/range guards, tagless-clear 400 `:271-274` against `VERDICT_TAG` `:90`, killed-clear post-mortem guard `:282-288`, `effectiveScore` `:290`); `createExperiment` (`:378`, closure gate `:389-394`, stamps `:398-399`, `revenue_source` retry `:420-429` with `dropped` `:429`) / `updateExperiment` (`:441`, stamps `:467`, gate `:468-473`, retry `:497-509`, `dropped` `:508`, ledger via shared `outcomeLedgerLine` `:103-108`); any other missing column 503s naming only it (`:737`, `missingColumnOf` `:122-126`).
14. Health: `functions/api/[[path]].js:526-709` — 12 probes (`:546-558`) in one batch (`:560`) with per-probe isolation fallback (`:569-598`, failures named in `health_probe_failures` + column-only `health_probe_detail` `:583-587`), lazy self-migration (`:607-623` via `ensureMoneyColumns` `:36-77`), `last_vetted`/`last_verdict` parsed in JS (`:657-671`) from 500-char tails (`:558`, `LIMIT 2000`), `inflow_paused` derived from the unreviewed probe (`:700`); payload `:709` (`ok:false` + nulls only on total outage; `db` is `up` when any probe answers).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 3.5) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (189.2h). Second: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F2) — and until it runs, money writes 503 too. Third: live trails HEAD by one commit (F3), the UI never surfaces the stall date health already computes (F4), and 19/27 rows lack the brief a sub-minute vet needs (F5).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:8`) → exit 0 this session, run once. Visible TAP shows `ok` suites only (1–41+ in the truncated window, zero failures observed); the tool window truncated the full log (140760 bytes saved by the runner) so no `# tests` denominator is quoted here — the exit code is the verdict, and a zero-test pass would still exit 0, so the denominator below is independently pinned by static count: `it(` shapes number in the hundreds across the 7 test files (exact count not quoted; the green run imported every shipped JS file, proving parse).
- Parse/compile: no Python sources — glob `**/*.py` matches nothing; positive control: the same search tool+mode matches `UNREVIEWED` dozens of times across `worker/src/index.js` + `public/app.js` + `worker/src/lib.js`, so the zero is real. `python3 -m compileall` not applicable (nothing to compile). JS parse is pinned by the green suite itself (every shipped file imported) plus the `dashboard-appjs parse gate` suite (`deploy/deploy.test.mjs`, green inside the exit-0 run) and `verify.sh:44-52` (`node --check` on the served bundle).
- Dead code: none material in shipped source. `appendKeepNewest` (`worker/src/lib.js:51-54`) is test-only by documented design (pinned JS spec of the SQL `substr(notes || ?, -8000)` idiom, `:49-50`), not dead; every worker helper (`exactUrlTarget`, `agentMoneyEstimates`, `buildBriefPrompt`, `parseBriefJson`, `insertBrief`, `selectBareRow`, `buildClassifyPrompt`, `flushVerdictWrites`) has 2+ call sites. `console.*` in shipped source is only the three deliberate `cron-failed` tail markers (`worker/src/index.js:680,692,756`) plus the design comment (`:678-679`). Control: the same search matches those lines, so no hidden logging.
- Duplicated logic: shared singles verified — `effectiveScore`/`tokensMatch`/`clamp10` (`worker/src/lib.js`, API import `functions/api/[[path]].js:10`, worker import `worker/src/index.js:160`), `vettedNotes`/`fetchDetailForWrite`, `buildBriefPrompt`/`parseBriefJson`/`insertBrief`, `buildClassifyPrompt`, `flushVerdictWrites`, `outcomeLedgerLine` (`:103-108`), one paint helper `paintStartHere` (`public/app.js:1648`), one migration script (`deploy/apply-d1-migrations.sh`, called by `deploy/deploy.sh:40` and CI `:33-34`). Remaining mirrors are documented-deliberate: `centsDollars` ↔ dashboard `moneyCents` (`functions/api/[[path]].js:98-99` comment), SQL `substr(notes || ?, -8000)` ↔ `appendKeepNewest`, `VERDICT_TAG` boolean gate (`:90`) vs health `matchAll` date-capture (`:663-668`, gate vs capture).
- TODO density: zero in shipped source. Search `TODO` over `functions` + `worker/src` + `public/app.js` + `d1` + `deploy` + `scripts` hits nothing; control: the same tool+mode matches `UNREVIEWED` repeatedly across the same tree, so the zero is real. Repo-wide `TODO|FIXME|HACK|XXX` hits only prose in `docs/audits/*.md`.
- Matcher honesty (the brief's check-that-matches-nothing rule): every absence above carries its positive control in the same bullet (py→`UNREVIEWED` hits, TODO→`UNREVIEWED`, dead-code→traced importers, console→`cron-failed` hits). No new matcher was written this round; no absence is asserted without its control.
- Surface rules: all pass, no finding. Light-only (`public/styles.css:4` `color-scheme: light`, ink-on-paper palette; only media queries are `prefers-reduced-motion` and `max-width`, `:210-221`); favicon linked with theme-color (`public/index.html:9-10`, byte-identical in `_night/live/aimoney_pages_dev.html:9-10`); `public/favicon.svg` single-line 32×32 `$` on `#0d6b3f` (well under 2 KB by content); brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suite `public/tab-icon.test.mjs` green inside the exit-0 run. First five seconds: the static shell renders brand + `Every way to make money with AI, ranked.` + lede with zero JS; counts fill from `…` placeholders, then the last-good snapshot (stale-marked past one 6h tick), then live refresh.
- Tree: HEAD `3ebef9a` (`git rev-parse --short HEAD`); `git log --oneline -5` shows round 5 → round 4 → round 3 → round 2 → round 7; live rev `b70c707` is HEAD's parent (F3). `git status --short` was empty before this file was written. Line endings pinned LF (` .gitattributes:4` `* text=auto eol=lf`); the CRLF warning in the brief does not apply to this checkout's tracked files.
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (time `2026-09-21T21:32:06Z`, rev `b70c707…`, one commit behind HEAD): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:3.5, oldest_unreviewed_age_h:189.2, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, inflow_paused:true, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. The brief's 08:45 measurement read the same 11 unreviewed at 174.4h — the queue has not moved all day; age rises ~1d/d while cron stays healthy.

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 189.2h / 7.9d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `last_verdict` null, `hours_since_last_ok_run` 3.5, `inflow_paused` true. Was at 08:45: same 11 unreviewed at 174.4h — the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none — no `won`/`lost` row exists (`experiments_by_status:{planned:2}`, `decisions_last_7d:0`), and the money legs read null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter, run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment a human records the first verdict. The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.9 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:564` `vetOpportunity` → shared `vettedNotes` (`:485-488`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id` (`:576-579`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:524`: `— UNREVIEWED, scores capped until a human vets it`) and every other `UNREVIEWED` site in `index.js` is a create/count/select site — zero clearing paths and zero `vetted` strings in the worker (search `vetted` over `worker/src/index.js` hits nothing; control: the same search over `functions/api/[[path]].js` hits the guard, the SELECT, and the parsers). So `vetted_last_7d` 0 with `hours_since_last_ok_run` 3.5 is human-gated-by-design, not a broken automation. Vet-round report as requested: path `public/app.js:564`; cause human-gated-by-design — there is no failing test because nothing is broken, and the passing real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (green inside the exit-0 run) is the positive proof; a single run of the path (one Vet press) produces exactly unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today. Health already dates the stall without being told: `last_vetted`/`last_verdict` (`functions/api/[[path]].js:657-671`, `:709`), both null live.

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:446-449`, classify skipped `:460-461`, health bit `:700` true live), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change for the verdict itself — the two-minute path (`V`/`K` + auto-advance, one verdict per keypress, token-gated, N+1 prefetch `public/app.js:548`, detail-before-write `:540-562`, cold-boot guard `:547`) already ships with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines (`docs/OWNER-ACTIONS.md:7-20`). Touch no repo files for this finding. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live health names it: `health_probe_failures:["revenue_week","revenue_lifetime"]`, `health_probe_detail:{revenue_week:"revenue_cents", revenue_lifetime:"revenue_cents"}`, `schema_missing_columns:["revenue_cents","spent_cents"]`, `schema_migration:"disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)"` (`_night/live/aimoney_pages_dev_api_health.html:1`). The probes are the only two `SUM(revenue_cents|spent_cents)` readers (`functions/api/[[path]].js:553-554`); `d1/schema.sql:62-64` declares the columns but is `CREATE TABLE IF NOT EXISTS` (no-op on live); the ALTERs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied by the shared `deploy/apply-d1-migrations.sh:13-28` from both `deploy/deploy.sh:40` and CI `.github/workflows/deploy-worker.yml:33-34` — which has never run against live (no repo secrets, fail-fast `:20-26`). The default-off self-migration already ships (`functions/api/[[path]].js:36-77`, `MONEY_PROBE_COLUMNS` `:28`, gate `:58` `String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1"`, ADD-only loop `:64-73`, once-per-isolate `:33-37`, transient-PRAGMA retry `:44-55`); unset leaves the table untouched and names the columns plus the disabled line, exactly as live shows.

Why it costs: every revenue/spend figure is honestly unknown (null) instead of a number; experiment money writes 503 (`missingColumnOf` `:122-126` → `:737`, only `revenue_source` retries `:420-429`/`497-509`) so the owner cannot even record a $ figure until the columns exist; `decisions_last_7d` survives via its COUNT-only probe (`:552`) but the money half of the lane stays dark.

Fix: owner-only, no code. Set `ALLOW_SCHEMA_MIGRATION=1` as a Pages production env var and retry the latest deployment (`docs/OWNER-ACTIONS.md:22-34`); the next health call adds exactly the two `ADD COLUMN` statements and reports numbers on the same call. Second track for `via {source}`: add `CF_API_TOKEN` + `CF_ACCOUNT_ID` repo secrets and redeploy (or run `deploy/deploy.sh` locally) so the revenue-source migration applies (`docs/OWNER-ACTIONS.md:35-41`); until then Wins keep $ but drop `via` with a named toast. Touch no repo files. Size S. Risk low (ADD COLUMN with DEFAULT cannot lose rows; default-off keeps unset behaviour byte-identical; second run is a duplicate-column no-op).

### F3. Live trails HEAD by one commit — round 5 is committed but not serving

Evidence. `git rev-parse --short HEAD` → `3ebef9a` (`Night round 5 … full suite green`); live `/api/health` `rev` → `b70c707b…` (`Night round 4`), time `2026-09-21T21:32:06Z`. `git log --oneline -5` confirms the order (3ebef9a on top of b70c707). The round-5 delta (prefetch re-arm, inflow-gate markers, silent-failure markers, phone meter hiding) is therefore audited here but not yet observable live; the live `inflow_paused:true` key proves round 4 has landed, while round-5-only behaviour cannot be confirmed from the surface.

Why it costs: the owner vets against a surface one round stale (including the phone meter columns round 5 hides for sub-minute mobile vetting), and every audit-to-live comparison carries a one-commit skew that wastes the next reader's time.

Fix: deploy, don't code. Pages auto-deploys on push — check the Pages dashboard's latest deployment for `3ebef9a` and let it finish, or push/merge the branch carrying it; then confirm `release.json` revision and `/api/health` `rev` both read `3ebef9a` (`deploy/verify.sh` live-revision gate `:66-106`). Touch no repo files for this finding. Size S. Risk none (read-only verification; no D1 writes).

### F4. Health dates the stall, but the dashboard never shows it — the UI owner opens hides `last_verdict`

Evidence. Health computes both dates (`functions/api/[[path]].js:657-671`, payload `:709` `last_vetted`/`last_verdict`), null live after 7.9 stall days. Search `last_verdict` over `public/app.js` hits nothing; search `last_vetted` over `public/app.js` hits nothing either (control: the same tool+mode matches `UNREVIEWED`, `vetOpportunity`, and `inflow_paused` across the same file, so the zeros are real). The agent pill mirrors only `N to review · oldest Xd` + `inflow paused` plus probe-failure naming — never `last verdict Xd ago` / `never vetted` — so the first-five-seconds surface omits the one number that says "nothing has been decided here in 8 days" without opening `/api/health`.

Why it costs: trust and the first five seconds. The stall is the most important truth on this dashboard and it is currently one fetch + one JSON key away instead of painted next to the review count the owner already looks at; every prior round made the stall more visible in health while the surface he opens stayed silent about verdict recency.

Fix: in `public/app.js`, read `state.health.last_verdict` (fall back to `last_vetted`) when painting the agent pill (`renderRuns`/pill painter) and append a compact `· last verdict Xd ago` (or `· never vetted` when null) plus the same line in the pill tooltip; null-safe, stale-marked with the existing snapshot mark, no new fetch (health is already loaded). Pin with `public/dashboard.test.mjs` cases for old date / recent date / null. Keep the tab-icon suite green. Size S. Risk low (read-only render from an already-fetched key; no API change; no status moves).

### F5. Nineteen of 27 rows have no brief — the sub-minute vet path vets blind

Evidence. Live `bare_without_brief:19` with `unreviewed:11` and `oldest_unreviewed_age_h:189.2`. Review rows render `${reviewBriefLine(o)}` (`public/app.js:243`) which falls back to `No brief yet` (`:230-231`: `` `<div class="review-brief muted">${esc(s || "No brief yet")}</div>` ``), so most Needs-review rows show the decision line but no evidence line. Worker brief capacity is 1 + 1 extra per cron tick while the oldest unreviewed exceeds 48h (`worker/src/index.js:580-668`, mode flip `:591-594`), and manual `POST /run` always skips briefs (`:350`, `:758` `briefs_skipped:true`) — inflow is paused (0/tick) so the bare backlog should drain, yet 19 bare persists across snapshots, meaning briefs are not keeping up with the queue the owner must clear.

Why it costs: owner time per verdict. A vet without a brief is a guess or a drawer round-trip; with 19/27 bare, the "minute a row from a phone" promise (`docs/OWNER-ACTIONS.md:15`) degrades toward the slowest rows first (oldest-first order meets thinnest evidence).

Fix: no new worker path this round — keep the 48h oldest-first + extraBrief gates byte-identical (they already fire). The implementer-completable slice is measurement + pinning: add a worker-suite test asserting that when the oldest unreviewed exceeds 48h both the main pick (`:595-596`) and the extra pick (`:641-648`) target unreviewed bare rows (not top-scored), and assert the `brief:skipped` rotation (`:623-624`,`:663-664`) keeps a poison row from wedging the pass; files `worker/src/index.js` (test-only unless the test fails) + `worker/src/index.test.js`. The queue-clearing itself stays owner work (F1) plus cron ticks. Size S. Risk low (test-first; no prompt, cap, or schedule change; brief behaviour byte-identical when green).

## 5. What NOT to change

- The agent never vets or moves status — quoted deliberate: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`) and `README.md:72` (`researching → testing is a human decision … The agent never moves status`). Do not add an automated vet, auto-testing flip, or bulk verdict path.
- One human verdict per keypress, no bulk path — quoted: `README.md:71` (`one human verdict per keypress, no bulk path`). Do not add bulk vet/kill.
- Migration is ADD-only, default-off, with no endpoint — quoted: `functions/api/[[path]].js:21-23` (`applies exactly the ADD COLUMN statements … one at a time (ADD-only, no endpoint)`). Never add DROP, column rewrite, data rewrite, DELETE, a second trigger, or a URL that migrates. Unset behaviour stays byte-identical to today.
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change). His one line: set `ALLOW_SCHEMA_MIGRATION=1` as a Pages production env var and retry the latest deployment — money legs fill on the next health call.
- The 11 verdicts are the owner's dashboard presses (needs `ADMIN_TOKEN` + per-row judgment), not agent writes. His one line: open Needs-review oldest-first and press V or K on each of the 11 rows.
- The $0 starter and its close are the owner's (Start the seed sprint, run it, Win/Lose with a human-entered $ amount + post-mortem). Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure.
- Deploying round 5 is the owner's (F3). His one line: live serves `b70c707` while `3ebef9a` is committed — check the Pages dashboard's latest deployment and let it finish.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action.
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment. Never spend money, create accounts, send anything outward, accept terms, solve captchas, fabricate reviews/receipts/testimonials, scrape or message against a platform's terms, or give financial advice/implied return.
- House rules honored by this audit: never read `.env`/secrets/tokens; no edits to existing files (this findings file only); LF endings pinned (`.gitattributes:4`).

## 6. Proposed next tasks (2-4)

Disposition of the focus-named work (it outranks anything found, so it is answered first): the vet-path round resolves to human-by-design (F1: path `public/app.js:564`, no failing test because nothing is broken, one Vet press yields exactly one verdict, stall already dated by `last_vetted`/`last_verdict`), so its implementer remainder is the pin Task 1. Every other focus-named code item is verified shipped with the suite green: probe isolation + failure naming + `db`-by-answer (`functions/api/[[path]].js:542-598,709`), deploy convergence via one shared script (`.github/workflows/deploy-worker.yml:33-34`, `deploy/apply-d1-migrations.sh:13-28`, convergence suite `d1/migrations.test.mjs`), the default-off self-migration with its three results (`functions/api/[[path]].js:36-77`), and the sub-minute human vet path (keys + prefetch + auto-advance + cold-boot guard). The unshipped remainder is human/owner work (11 verdicts, the migration switch, the $0 sprint, the deploy) — not implementer-completable offline, so it lives in §5 as one line each. The tasks below are the remaining offline-completable items, most valuable first.

### Task 1. Pin human Vet path with real PATCH-to-health test and last-verdict date

Owns: `functions/api/api.test.mjs`, `functions/api/[[path]].js` (only if the pin fails). Context (F1): the only unreviewed→vetted path is the manual `vetOpportunity` (`public/app.js:564`) → `PATCH` → tagless-clear guard (`functions/api/[[path]].js:271-274`) → health `vetted_7d` (`:555`) + `last_vetted`/`last_verdict` (`:657-671`).

Acceptance: a stubbed-D1 test drives the real `PATCH /api/opportunities/:id` (vetted tag) then `GET /api/health` and asserts unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today; a worker-source assertion pins zero `vetted`/clear paths in `worker/src/index.js`; the report states what a single run produces; full `npm test` green.

### Task 2. Show last-verdict stall age in the agent pill from health

Owns: `public/app.js`, `public/dashboard.test.mjs` (pin only). Context (F4): health already computes `last_verdict` but the dashboard never reads it, so the 7.9-day stall is invisible on the surface the owner opens.

Acceptance: the pill paints `· last verdict Xd ago` (or `· never vetted` when null) from the already-loaded health payload, null-safe and stale-marked, with the same line in the tooltip; stubbed-health tests cover old/recent/null; no new fetch, no API change; full `npm test` green and the tab-icon suite stays green.

### Task 3. Draft one $0 starter experiment with a week-1 number

Owns: `docs/ZERO-SPEND-STARTER.md` (new file only). Context (lane metric + F1/F5): 2 planned stuck, 0 running, 0 decisions; the owner needs a concrete $0 move, not a description. Draft it, queue it, leave the spend/Start/close to him.

Acceptance: names one Zero-spend seed row (e.g. `$0`-capital), states hypothesis + metric + $0 cost breakdown + the single week-1 number it produces + first steps; requires no spend, account, send, or accepted term; names no revenue figure without evidence; one page.
