# Audit 2026-09-20 round 6 — aimoney (read-only, track B)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r6`. HEAD `84442f0` (night round 5); tree clean before this file (`git status --short` empty this session); no network, no installs, no edits to existing files. Only this file created at the root. Never read `.env`, secrets or tokens. Note: `docs/audits/AUDIT-2026-09-20-round6.md` is a different, earlier file (same basename, older HEAD `29ee528`, older snapshot); this root file is tonight's track-B round-6 audit at HEAD `84442f0` with the newer live snapshot below.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests (`README.md:3-15`, `worker/src/index.js:4-5`).
- Entry points: `public/index.html` + `public/app.js` (1685 lines, dashboard), `functions/api/[[path]].js` (706 lines, D1 REST API), `worker/src/index.js` (725 lines, research agent) + `worker/src/lib.js` (shared pure helpers).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `https://aimoney-research.levitinvlad.workers.dev/`; live snapshot rev `d4262cd` trails HEAD `84442f0` by two night-round commits (see §3).
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:720`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema `:27` + shared migrations `:33-34` + worker `:38`) or manual `deploy/deploy.sh`, gated by `deploy/verify.sh` (public reads only, no credentials).

## 2. End-to-end walk-through

Signal to decision. The hop where it most often goes silent is hop 12.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:78` `redditSignals`, `:106` `githubSignals` via `timedJson` (`:37`, 6s timeout covering the body read). A throwing query marks its source; all-failed names it in `src_fail`.
2. Store: `:354-359` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:360-363` (`phase:collected`).
3. Sweep: `:367-372` unprocessed signals older than 30d become noise (`stale:N`).
4. Take: `:374-376` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:385-413` via pure `exactUrlTarget` (`:168`); bounded `IN` selects (`:394-400`) match fresh URLs against opportunity `source_url`s and linked signal URLs; a match links as supports (notes newest-kept, no status move). Failed links are skipped and counted (`prepass:N_failed`, `:385,:407`), never thrown.
6. Gates, read before classify: `:427-429` bare-without-brief > 10 caps inserts to 1/tick; `:431-433` unreviewed > 10 pauses inflow to 0/tick (`maxNewThisRun = 0`). Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:444-457` runs only with fresh signals AND `maxNewThisRun > 0`; `buildClassifyPrompt` (`:272`) + `aiComplete` (`:136`, 60s cron / 12s manual, retries 1/0), parsed by `parseJsonLines` (`worker/src/lib.js:64`, NDJSON + `repairJson` for `\_`). Inflow-paused ticks skip classify and still brief (finish line carries `inflow:paused`, `:634`).
8. Verdict loop `:469-537`: first-wins validation, strict action enum, numeric-or-null id. `new` INSERTs (`:496-511`) with an `UNREVIEWED` note (`:507`) and capped `effectiveScore` (`worker/src/lib.js:30`, cap 6000 `:23`); overflow stays unprocessed (`:486`); slug collision links as supports (`:512-524`).
9. Verdict flush: `:538` `flushVerdictWrites` (`:291`, one batch with per-write fallback; failures as `verdict:N_failed`).
10. Briefs: main `:550-589` (top-scored live-only `:566-568`, or oldest-unreviewed-first past 48h `:553-561`) plus extra `:596-631` when the oldest unreviewed exceeds 48h (live oldest 182.8h, so both fire), via `buildBriefPrompt` (`:202`), `parseBriefJson` (`:213`), `insertBrief` (`:224`, never stores empty); poison rotation `:248-265` (`brief:skipped:N`); failures `brief:failed`.
11. Finish: `:634` single `finish("ok", …)` carrying mode/pause/stale/src_fail markers; errors `:636-644` finish `error` plus the `console.error("cron-failed", …)` tail marker (`:642`, also `:654`, `:718`). `scheduled` `:648-656`, `POST /run` `:710-721`.
12. Vet — the ONLY path from unreviewed to vetted, one sentence: a manual dashboard press — `public/app.js:552` `vetOpportunity` → `:528` `fetchDetailForWrite` (token-gated detail fetch, warms N+1 via `:536`) → `:557` inner → `:475` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`:564`). Starter variant `:606` `vetAndLogStarter` (resume-safe via `:589` `findPlannedStarter`) adds a planned experiment and flips the row to `testing`. Kill (`:645`, drawer `:1166`) writes `[date killed]` + post-mortem. `V`/`K` keys (`:722-736`, bound once `:737`) with auto-advance (`:717`): one human verdict per keypress, no bulk path.
13. API writes: `updateOpportunity` (`functions/api/[[path]].js:227`, text bounds, status/range guards, tagless-clear 400 `:261-265` against `VERDICT_TAG` `:87`, `effectiveScore` `:267`); `createExperiment` (`:355`) / `updateExperiment` (`:416`, won/lost require `result` + `post_mortem`, stamps, `outcomeLedgerLine` `:95`, cents clamps, `revenue_source` retry `:396-405`/`:471-480`).
14. Health: `functions/api/[[path]].js:497-675` — 12 probes (`:513-530`) in one batch (`:531`) with per-probe isolation fallback (`:540-569`, failures named in `health_probe_failures` + column-only `health_probe_detail`), lazy self-migration (`:570-594` via `ensureMoneyColumns` `:34-72`), `last_vetted`/`last_verdict` parsed in JS (`:628-642`) from 500-char tails; payload `:675` (`ok:false` + nulls only on total outage).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 3.1) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (182.8h, +2.9h since the prior audit's snapshot). Second: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F2).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:8`) → exit 0 this session, run once. Static count of `it(` blocks across the 7 files is 400+ (matcher display cap hit at 400; prior exact count 395 at older HEAD — the suite grew with rounds 4–5; every suite in the visible window shows `ok`, zero `not ok`). The `MODULE_TYPELESS_PACKAGE_JSON` reparse warnings from the prior audit are gone from the run output (`package.json:5` now declares `"type": "module"`).
- Parse/compile: no `python3 -m compileall` sources exist — glob `**/*.py` matches nothing (positive control: the shipped `.js`/`.mjs` files were read directly this session, so the glob tool reaches the tree). JS parse is covered by `npm test`: the `every shipped JS file parses (node --check)` suite plus a broken-syntax positive control (`public/dashboard.test.mjs:1078,1093`), both green inside the exit-0 run. No shell `node --check` re-run (one green gate already; no repeats).
- Dead code: none in shipped source. `console.(log|error|warn)` over shipped paths hits only the three deliberate `console.error("cron-failed", …)` markers (`worker/src/index.js:642,654,718`); `debugger` over shipped non-test paths hits nothing (control: the same tool+mode matches the console markers and the test-harness captures in `worker/src/index.test.js:626-627,796-797`, so both result sets are real). `appendKeepNewest` (`worker/src/lib.js:51-54`) is test-only by documented design, not dead.
- Duplicated logic: two live cases — the Start-here strip painter exists twice (F5) and `clamp10` exists twice (F6). The prior audit's two dedup items both landed and are verified fixed: `deriveReviewList` now reuses `isNeedsReview` (`public/app.js:276-277`, pinned by `public/dashboard.test.mjs:687`), and `cleanUnreviewed` is a single replace (`public/app.js:468-470`). Everything else stays shared: `effectiveScore`/`tokensMatch` (`worker/src/lib.js:30,39`, API import `functions/api/[[path]].js:10`), `vettedNotes`, `fetchDetailForWrite`, brief/classify/insert helpers, `flushVerdictWrites`, `outcomeLedgerLine`, one migration script.
- TODO density: zero in shipped source. Regex `TODO|FIXME|HACK|XXX` over the repo hits only prose lines in `docs/audits/*.md`, zero in shipped source. Control: the same tool+mode matches `UNREVIEWED` repeatedly across `worker/src/index.js` + `public/app.js`, so the zero is real.
- Surface rules: light-only (`public/styles.css:4` `color-scheme: light`, ink-on-paper palette `:5-15`); `prefers-color-scheme` over shipped `public/` hits nothing and `data-theme`/`theme-toggle`/`color-scheme` over `public/` hits only `styles.css:4` plus the negative assertions in the guard tests (control: `color-scheme` matches `styles.css:4`, so the zeroes are real). Favicon linked with theme-color (`public/index.html:9-10`, matches the live snapshot); `public/favicon.svg` is 284 bytes, 32×32 `$` on `#0d6b3f`; brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suites (`tab-icon` 5 its, dashboard surface cases) green inside the exit-0 run. No surface findings this round. First-five-seconds: the static shell renders brand + `Every way to make money with AI, ranked.` + lede with zero JS; counts fill from `…` placeholders (`public/dashboard.test.mjs:924-937` pins both halves).
- Tree: HEAD `84442f0`; `git status --short` empty before this file; `git ls-files --eol` shows all 40 tracked files `i/lf w/lf` with `eol=lf` — the CRLF warning in the brief does not apply to this checkout. Sizes (`wc`): `public/app.js` 1685 lines, `functions/api/[[path]].js` 706, `worker/src/index.js` 725, `worker/src/lib.js` 79; tests outweigh shipped code (`api.test.mjs` 2446, `dashboard.test.mjs` 1927, `index.test.js` 1464 lines).
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (time `2026-09-21T15:09:04Z`, rev `d4262cd…`, which equals the night-round-3 commit — live trails HEAD `84442f0` by two commits, so the round-4/5 dashboard honesty fixes were merged but not yet served at snapshot time; cause unmeasured from here, no deploy access): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:3.1, oldest_unreviewed_age_h:182.8, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. Prior snapshot: same 11 unreviewed at 179.9h — the queue has not moved between snapshots; age rises ~1d/d while cron stays healthy (0.2 → 3.1h since last ok run is one normal 6h-tick interval).

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 182.8h / 7.6d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `last_verdict` null, `hours_since_last_ok_run` 3.1. Was: same 11 unreviewed at 179.9h — seven rounds have shipped against this repository and the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none measured — `revenue_total`, `revenue_last_7d`, `spent_total` all null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter (the seed `Spec-ad sprint: 10 brands, 10 free ads`, `d1/seed.sql:98-100`), run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment a human records the first verdict. The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.6 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:552` `vetOpportunity` → shared `vettedNotes` (`:475-478`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id` (`:564`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:507`: `— UNREVIEWED, scores capped until a human vets it`) and every other `UNREVIEWED` site in a full read of `index.js` (725 lines) is a create/count/select site (`:18,421,431-432,504,553,561,605,609`) — zero clearing paths (positive control: the `UNREVIEWED` matcher returns those sites, so the absence of a clearing path is real). So `vetted_last_7d` is 0 with `hours_since_last_ok_run` 3.1 by design, not by failure: cron triage + briefs run, verdicts are human-gated. A single run of the path is certified by the real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs:2003`, green inside the exit-0 run): one Vet press yields unreviewed −1, `vetted_last_7d` +1, `last_vetted` = today. Health already dates the stall without being told: `last_vetted`/`last_verdict` (`functions/api/[[path]].js:628-642,675`), both null live. (Vet-round report as requested: path `public/app.js:552`; cause human-gated-by-design with the passing real-path test above instead of a failing one — nothing is broken; one run produces exactly one verdict.)

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:431-433`), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change — the 2-minute keys (`V`/`K` + auto-advance, `public/app.js:722-737,717`, one verdict per keypress, token-gated, typing contexts ignored) already ship with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines. Touch no repo files. (Hygiene note, not a task: `docs/OWNER-ACTIONS.md:7` freezes the age as `oldest 7.5 days`; live is already 7.6d — that doc should point at the chip/health instead of a frozen number.) Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live: `revenue_last_7d/revenue_total/spent_total` null with `health_probe_failures:[revenue_week,revenue_lifetime]`, `health_probe_detail:{…:revenue_cents}`, `schema_missing_columns:[revenue_cents,spent_cents]`, `schema_migration:disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)`. The two failing probes are the only ones reading money (`functions/api/[[path]].js:524-525`, `SUM(revenue_cents)` / `SUM(revenue_cents/spent_cents)`). `d1/schema.sql:5` is `CREATE TABLE IF NOT EXISTS` (no-op on the existing table); the ADD COLUMNs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied after `schema.sql` by both CI (`.github/workflows/deploy-worker.yml:27` then `:33-34` `Apply D1 migrations in sorted order` via `./deploy/apply-d1-migrations.sh`) and `deploy/deploy.sh:36` then `:40` — one shared loop + tolerate-list (`deploy/apply-d1-migrations.sh:13-28`, duplicate-column no-op, anything else exits 1), pinned by the convergence tests (`d1/migrations.test.mjs:95,117`, incl. the withhold-one-file positive control). The in-worker fallback is deliberately off: `functions/api/[[path]].js:53` (`if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` → report missing + disabled, change nothing). Migration-ask results, all measured green this session: old schema + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers; var unset leaves the table unchanged and names the missing columns; second run is a clean no-op (`self-migration switch` suite, 6/6 at `functions/api/api.test.mjs:1670-1754`). Exact variable name: `ALLOW_SCHEMA_MIGRATION`. Exact CI step (already in place): `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: earned-to-date is unknown rather than zero, so the money-first question cannot be answered from the project's own data. (The decisions COUNT split, `:523`, already saved `decisions_last_7d` = 0 — the money legs are the only remaining nulls.)

Fix: no code change; the path is already correct. The owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var — the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, and re-runs the failed money probes on the same call — or adds the two Cloudflare repo secrets and redeploys so CI applies the migrations. One variable, one decision; see §5 for the exact lines. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F3. Inconclusive self-migration probes are cached as decided — one transient blip hides the switch line for the isolate's lifetime

Evidence. `functions/api/[[path]].js:34-47` `ensureMoneyColumns`: the `!env.DB` path (`:37`), the PRAGMA-throw path (`:42-47`), and the empty-`table_info` path (`:50`) all store the `none` outcome in `schemaMigrationState`, and `:35` returns the cached outcome on every later call. The first is conclusive per isolate (bindings are fixed), but the other two are `cannot tell` states: a transient D1 error on the PRAGMA, with the var set and columns missing, means this isolate never migrates again — and worse, later calls return `schemaNote.disabled === false`, so the payload also drops the `schema_missing_columns` + `disabled (set ALLOW_SCHEMA_MIGRATION=1 …)` line that the brief calls `how he finds out the switch exists`. The reader still sees the probe names (isolation fallback), but loses the one actionable line.

Why it costs: reliability on the money path — the migration hint degrades from `present` to `silently absent` after exactly the kind of transient failure an overnight tick hits, and the owner never learns the switch exists from the endpoint that was built to tell him.

Fix: return `none` WITHOUT caching it on the PRAGMA-throw and empty-`table_info` paths; keep caching the three decided outcomes (no-missing, disabled-with-missing, migrated) so the `unset checks PRAGMA at most once per isolate` test (`functions/api/api.test.mjs:1732`) stays green. Add a test with a PRAGMA stub that throws once then answers: var set → the later call migrates and reports numbers; var unset → the later call names the missing columns plus the disabled line. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Size S. Risk low (health-only; ADD-only, default-off, and once-per-isolate semantics for decided outcomes are unchanged).

### F4. Two planned experiments, zero running, zero decisions — the money path stalls at planned

Evidence. Live `experiments_by_status:{planned:2}`, `decisions_last_7d:0`; no `running`/`won`/`lost` keys. The seed starter is still planned by name: `d1/seed.sql:98-100` (`Spec-ad sprint: 10 brands, 10 free ads`, `planned`, `metric: replies; pilots closed`, `target: >=3 replies; >=1 pilot at >=$500`). The one-tap moves already exist and are human-pressed: `startExperiment` (`public/app.js:813`, PATCH `running`, API stamps `started_at`), `loseExperiment` (`:830`) and `winExperiment` (`:857`, human-entered $ → `revenue_cents` + optional spend/source), all token-gated with orphaned cards excluded. The API closure gate holds: won/lost require `result` + `post_mortem` (`functions/api/[[path]].js:366-371,443-448`), `ended_at` stamped, outcome ledger appended (`:406-412`, shared `outcomeLedgerLine` `:95`).

Why it costs: vetted → experiment → decision is the lane metric, and it reads 0/0/0. Signals seen and proposals added are attempts, not outcomes; without one running row there is no number within a week and no amount/date/source to make real.

Fix: after F1 vets the parent row, the owner presses Start on the $0-capital starter (first candidate named at `public/app.js:811`), runs the 10-brand sprint outside the repo, then closes via Win/Lose with a human-entered $ amount and a one-line post-mortem. No code change; the dashboard, API gate, stamps, and ledger already enforce the shape. Touch no repo files. Size S (one human decision + the sprint). Risk none to the repo (token-gated PATCHes; no auto-transitions; never mark decided without evidence).

### F5. Nineteen of 27 rows have no brief — the evidence gap that makes each vet slower

Evidence. Live `bare_without_brief:19` with `opportunities:27`. Brief capacity is 1 + 1 per cron tick: main `:550-589` (top-scored live-only `:566-568`, or oldest-unreviewed-first past 48h `:553-561`) plus extra `:596-631` when the oldest unreviewed exceeds 48h (live oldest 182.8h, so both fire). Each costs 1 of 4 AI calls (`MAX_AI_CALLS`, `:19`); poison rows rotate via `briefSkippedIds` (`:248-265`) and failures land as `brief:failed`. Inflow is already paused at 0/tick while unreviewed > 10 (`:431-433`), so the bare queue drains 2/tick with no new bare rows arriving.

Why it costs: a vet without a brief is a guess, not a sub-minute decision — the reviewer opens the drawer, reads raw notes, and slows the 11-row clear that unpauses inflow. The brief backlog is the second queue behind the review queue.

Fix: no code change; the 48h oldest-first flip + extra brief already prioritize the review queue, and the pause already stops new bare inflow. The owner vets from the inline decision lines ($/mo range, capital, next action, summary excerpt) while cron drains 2 bare rows per 6h tick; the next audit should confirm `bare_without_brief` falling. Touch no repo files. Size S. Risk none (read-only for the agent; cron already paces within its AI budget).

### F6. Start-here strip is painted by two near-identical blocks — live and cache will diverge

Evidence. `renderStartHere` (`public/app.js:141-171`) and the last-good-cache painter (`:1597-1622`) build the same wrapper div, the same three paragraphs, the same Open/Vet/Kill buttons with the same ids and the same listener wiring — ~25 lines each, differing only in data source (`top` vs `cachedTop`) plus the stale mark (`:1602,1609`). They already compute the Vet/Kill gate differently: live uses `isNeedsReview(top)` (`:154`, bit with notes fallback) while cached uses `Number(cachedTop.needs_review) === 1` (`:1605`).

Why it costs: maintainability on the surface the owner opens first — the next strip change (copy, button, gate) must land in two places or the boot paint and the live paint disagree, and the gate already has two spellings of one rule.

Fix: extract one `paintStartHere(el, row, { staleMark })` helper over normalized fields (`title`, `$` range, capital, `brief_first_steps`, boolean `needsReview`) and call it from both sites; the cached site keeps passing its stale mark and snapshot bit. Pin with the existing strip suites (live + cache cases); run `npm test` green. Touch `public/app.js`, `public/dashboard.test.mjs` only if a new pin is needed. Size S. Risk low (pure client-side render; PATCH shapes, tags, and refresh behavior untouched).

### F7. `clamp10` lives in two homes — the API kept a local copy of the shared helper

Evidence. `functions/api/[[path]].js:74-78` defines a local `clamp10` (`Number`, finite-check, round, clamp 1–10 with default) while `worker/src/lib.js:4-7` exports the shared `clamp10` (`Math.round(Number(v))`, non-finite → default, clamp 1–10) — behaviorally equivalent on every input class (finite, NaN, ±Infinity, numeric strings), and the API already imports its siblings `effectiveScore`/`tokensMatch` from that same module (`functions/api/[[path]].js:10`).

Why it costs: maintainability — two homes for `clamp an input 1–10 with a default`, so a future bounds change (or a subtle NaN/Infinity fix) must land twice or create and update paths diverge.

Fix: delete the API-local `clamp10` and import the shared one from `../../worker/src/lib.js` beside the existing import; the 400+test suite (create/update bounds cases included) pins behavior — run `npm test` green. Touch `functions/api/[[path]].js`. Size S. Risk low (equivalent semantics; no call-site signature change).

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, or auto-testing without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless. `functions/api/[[path]].js:16-25` (switch docblock), `:53` (unset → report + change nothing), `:59-67` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate for decided outcomes. Do not default it on, do not add a second trigger, and do not add a migration endpoint: `a URL that alters the schema is a liability even when it is authenticated.`
- Never a data-losing migration here: ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change); unset behaviour is byte-identical to today. His one line: set `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var and reload `/api/health` once — the two money columns appear and the money legs fill on the same call.
- The 11 verdicts are the owner's dashboard presses (needs `ADMIN_TOKEN` + per-row judgment), not agent writes. His one line: open Needs-review oldest-first and press V or K on each of the 11 rows.
- The $0 starter and its close are the owner's (Start the seed sprint, run it, Win/Lose with a human-entered $ amount + post-mortem). The 10 brand sends are his outward action, not the agent's.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action.
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment from the decision + brief lines. Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure — the inline Win close takes a human-entered amount for exactly this reason.
- Live rev trailing HEAD (`d4262cd` vs `84442f0` at snapshot time) is a watch item, not a code defect — Pages auto-deploys on push. If the next snapshot still trails after a push, his one line: open the Pages dashboard and check the latest deployment status.
- Never, whatever the ranking says: no spending, no account or identity creation, no outward sends, no accepting terms, no captcha solving, no fabricated reviews/receipts/testimonials, no scraping or messaging a platform's terms forbid, no financial advice or implied return.
- House rules honored by this audit: never read `.env`/secrets/tokens; no edits to existing files (this findings file only); LF endings verified (`git ls-files --eol`, all `i/lf w/lf`).

## 6. Proposed next tasks (2-4)

Disposition of the focus-named work (it outranks anything found, so it is answered first): every code item the focus names is verified shipped with the suite green — probe isolation + failure naming + `db`-by-answer (`functions/api/[[path]].js:540-569,675`), deploy convergence via one shared script (`.github/workflows/deploy-worker.yml:33-34`, `deploy/apply-d1-migrations.sh:13-28`, convergence test `d1/migrations.test.mjs:95-117`), the default-off self-migration with its three results (`functions/api/[[path]].js:34-72`, 6/6 suite), the `last_vetted`/`last_verdict` stall dates (`:628-642`), and the sub-minute human vet path (`public/app.js:722-737` keys, `:480-520` prefetch). The unshipped remainder is human/owner work (11 verdicts, the migration switch, the $0 sprint) — not implementer-completable offline, so it lives in §5 as one line each. The tasks below are the remaining offline-completable code items, most valuable first.

### Task 1. Retry inconclusive self-migration probes instead of caching them as decided

Owns: `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Context (F3): `ensureMoneyColumns` (`functions/api/[[path]].js:34-50`) caches the `none` outcome even when the PRAGMA threw or `table_info` came back empty — `cannot tell` states that one transient D1 blip turns into a never-migrate, never-hint isolate. Return `none` without storing it on those two paths; keep caching the decided outcomes (no-missing, disabled-with-missing, migrated).

Acceptance: a test with a PRAGMA stub that throws once then answers healthy — var set → a later health call migrates and reports money numbers; var unset → a later call names the missing columns plus the disabled line; the `PRAGMA at most once per isolate` suite still passes; full `npm test` green.

### Task 2. Paint the Start-here strip from one shared helper for live and cached renders

Owns: `public/app.js`, `public/dashboard.test.mjs` (only if a new pin is needed). Context (F6): `renderStartHere` (`public/app.js:141-171`) and the cache painter (`:1597-1622`) duplicate ~25 lines of strip HTML + wiring with two spellings of the Vet/Kill gate. Extract one painter over normalized fields; the cached site keeps passing its stale mark.

Acceptance: one paint function serves both sites; live and cached strips render identically modulo the stale mark; the Vet/Kill gate is computed once from the same predicate; full `npm test` green.

### Task 3. Share clamp10 from worker lib in the API; drop the local duplicate

Owns: `functions/api/[[path]].js`. Context (F7): the API-local `clamp10` (`functions/api/[[path]].js:74-78`) duplicates the shared `worker/src/lib.js:4-7` with equivalent semantics, beside the existing shared import (`functions/api/[[path]].js:10`). Delete the local and import the shared one.

Acceptance: no local `clamp10` in the API file; create/update bounds suites unchanged and green; full `npm test` green.
