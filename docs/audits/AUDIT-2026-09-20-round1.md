# AUDIT-2026-09-20-round1 — aimoney (round)

Clone: `/root/coding-projects/_workspace/night3-aimoney-r1` at `b86d2b0` (read-only; no writes except this file).
Live snapshots read: `_night/live/aimoney_pages_dev.html` (HTTP 200, 119 lines), `_night/live/aimoney_pages_dev_api_health.html` (HTTP 200, 1 line).
No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` in clone; `README.md` used as governing doc. `.env`/secrets never read.
Prior rounds tonight SHIPPED to master: stale-age surfacing, header brand mark, orphan-safe list (e5a12a1); decisions/week, triage-only runs, API failure warnings (b86d2b0) — all verified present below.
Parked round-3 branch `origin/night/aimoney-round2` (d5198af) is ABSENT in this clone (`git branch -a` shows only `master`, `remotes/origin/master`); no fetch allowed, so its diff is unverified here.

Lane metric (aimoney row): experiments reaching a decision (`won`/`lost` with post-mortem) per week, plus vetted proposals becoming experiments, with the unreviewed backlog drained. Current value from the repo's own live data: **0 decisions, 0 vetted in 7d** (`decisions_last_7d:0`, `vetted_last_7d:0`), **11 unreviewed (oldest 149.4h ≈ 6.2d)**, **19/27 bare without brief**, `experiments_by_status:{"planned":2}` with zero `running`/`won`/`lost`. Third straight snapshot at 27/11/19/planned:2 (b92e2eb → e5a12a1 → b86d2b0): zero queue movement while runs stay fresh (`hours_since_last_ok_run:5.8`).

## 1. What this project is and how it runs

- Purpose: Cloudflare-native AIMoney Lab — ranked AI-money opportunities with scores, research briefs, experiments, and a cron research agent.
- Entry points: dashboard `public/index.html` + `public/app.js`; API `functions/api/[[path]].js`; agent `worker/src/index.js` + `worker/src/lib.js`; DB `d1/schema.sql`.
- Live surfaces: `https://aimoney.pages.dev`, `https://aimoney.pages.dev/api/health`, worker `https://aimoney-research.<account>.workers.dev`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`) — collect signals, AI triage, 1–2 briefs per cron tick, log to `agent_runs`.
- How it deploys: `./deploy/deploy.sh` (D1 schema + seed-if-empty, stamp `public/release.json`, Pages deploy, worker deploy); Pages Git-connected rebuild + `.github/workflows/deploy-worker.yml` on `worker/**` push; `./deploy/verify.sh` checks markers + 12h freshness.

## 2. End-to-end walk-through

Main loop: signal found → agent proposal → human vet → experiment → close with learning → rescore.

1. Cron/manual triggers pass: `worker/src/index.js:380-382` (`scheduled` → `runResearch(env,"cron")`) and `worker/src/index.js:436-445` (`POST /run` → `waitUntil(runResearch(env,"manual"))`, 202 `{"status":"accepted","briefs_skipped":true}`).
2. Collect signals in parallel: `worker/src/index.js:176` (`Promise.allSettled([hnSignals(), redditSignals(), githubSignals()])`); sources `worker/src/index.js:47-69` (HN ×4), `worker/src/index.js:71-93` (Reddit ×3), `worker/src/index.js:95-116` (GitHub, `slice(0,2)` of 3 queries); fetch helper `worker/src/index.js:35-45` (`timedJson`, 6s abort covering body read).
3. Batch-insert signals + heartbeat: `worker/src/index.js:181-186` (`INSERT OR IGNORE INTO signals`, one `DB.batch`); `worker/src/index.js:187-191` (`phase:collected/classify-ai/...` heartbeat); reap stuck runs `worker/src/index.js:145-150` (`running` older than 30 min → `error`).
4. Classify fresh vs live list: `worker/src/index.js:192-194` (`SELECT * FROM signals WHERE processed=0 LIMIT 6`); `worker/src/index.js:201-203` (top-60 opps); `worker/src/index.js:217-221` (`aiComplete` Mistral NDJSON); parse `worker/src/lib.js:49-64` (`parseJsonLines` + `repairJson` for `\_`).
5. Apply verdicts: new → `worker/src/index.js:242-266` (`INSERT INTO opportunities … status="researching"`, `effectiveScore({…,"UNREVIEWED"})` capped ≤6000); supports → `worker/src/index.js:267-275` (`substr(notes || ?,-8000)`, newest kept); noise/invalid → `worker/src/index.js:276-278` / `worker/src/index.js:232-235` (marked `processed=1`, dropped, uncounted).
6. Brief one–two bare opps (cron only): `worker/src/index.js:283-285` (`LEFT JOIN briefs … WHERE b.id IS NULL ORDER BY o.score DESC LIMIT 1`, skipped when `Date.now()-t0 >= briefDeadline`); `worker/src/index.js:291-303` (Llama brief JSON); `worker/src/index.js:310-318` (`INSERT INTO briefs`, skipped if `summary`/`first_steps` empty); extra oldest-unreviewed bare row when backlog >48h `worker/src/index.js:326-370` (`extraBare`, `retries:0`, `MAX_AI_CALLS` guard).
7. Finish run: `worker/src/index.js:161-167` (`UPDATE agent_runs SET finished_at,status,signals_seen,added,updated,briefs,ai_calls,error`).
8. Board reads: `public/app.js:572-591` (`refresh()` loads `/api/opportunities?limit=200`, `/api/experiments`, `/api/runs?limit=20`, `/api/health`, `/api/meta`, tracking `state.apiFailures`); list `functions/api/[[path]].js:39-66` (`unreviewed=1` filter, `oldest` sort, shared `effectiveScore` cap, re-sort); review chip + age `public/app.js:123-146`; review view `public/app.js:91-121`; failure banner `public/app.js:554-569` + `public/index.html:40`.
9. Human vets: `public/app.js:152-168` (Vet strips `UNREVIEWED`, lifts cap, keeps status) / `public/app.js:170-188` (Kill sets `killed` + prompted post-mortem) → `functions/api/[[path]].js:115-146` (`PATCH /api/opportunities/:id`, rescore via shared `effectiveScore`).
10. Experiment loop: log/update `public/app.js:470-515` (`openExperimentModal`) → `functions/api/[[path]].js:220-237` (create, no guards) / `functions/api/[[path]].js:239-271` (update: `running` stamps `started_at`, `won`/`lost` require `result`+`post_mortem` and stamp `ended_at`, invalid status 400); list with age/orphan `functions/api/[[path]].js:198-218` (`days_in_status`, `orphaned` via `LEFT JOIN`); board stale-first + age chips + decisions header `public/app.js:214-249`; detail drawer + admin re-optimize `public/app.js:302-399`.
11. Health/verify: `functions/api/[[path]].js:286-317` (`/api/health`: `rev,db,opportunities,unreviewed,bare_without_brief,experiments_by_status,hours_since_last_ok_run,oldest_unreviewed_age_h,decisions_last_7d,vetted_last_7d,time`); `deploy/verify.sh:28-58` (7 markers + non-empty list + freshness ≤12h).

Where it most often breaks or goes silent: the human bottleneck after hop 5, unchanged across three snapshots. Live health proves it: `unreviewed:11/27` (41%, oldest 149.4h), `bare_without_brief:19/27` (70%), `experiments_by_status:{"planned":2}` with zero `running`/`won`/`lost`, `decisions_last_7d:0`, `vetted_last_7d:0` — and the counts at b92e2eb and e5a12a1 were the identical `27/11/19/planned:2`, i.e. **zero queue movement between rounds** despite fresh runs (`hours_since_last_ok_run:5.8`). Vetting keeps status `researching` with no next step (`public/app.js:152-168`), status moves are manual-only (`worker/src/index.js:4-5`, `README.md:71`), and nothing links a vetted row to its later experiment. Secondary silent spots: invalid verdicts marked `processed=1` and dropped uncounted (`worker/src/index.js:232-235`); manual runs never brief (`briefDeadline=-1`, `worker/src/index.js:174`); `POST /experiments` bypasses the closure guards that `PATCH` enforces (`functions/api/[[path]].js:220-237` vs `:239-271`).

## 3. Health signals measured here

- Test suite (`npm test` = `node --test worker/src/lib.test.js worker/src/index.test.js functions/api/api.test.mjs public/tab-icon.test.mjs public/dashboard.test.mjs`): **PASS, 46/46, 19 suites, 0 fail**. Real tail: `# tests 46 / # suites 19 / # pass 46 / # fail 0 / # cancelled 0 / # duration_ms 538.147867`. Prior round was 32/32; +14 from round-2 tasks (decisions/vetted, triage disclosure, banner/badges). Warnings only: `UNDICI-EHPA` proxy-agent notice and `MODULE_TYPELESS_PACKAGE_JSON` reparsing hint for `[[path]].js`/`index.test.js`/`lib.test.js` (add `"type":"module"` to silence; no behavior change).
- Syntax checks (no writes): `node --check public/app.js` → exit 0; `node --check worker/src/index.js` → exit 0 (each with only the `UNDICI-EHPA` warning). `node --check` on `functions/api/[[path]].js` not run separately (bracketed path); the file is imported and exercised green by `npm test` instead.
- `python3 -m compileall -q`: NOT RUN — no Python sources in repo (glob `**/*.py` returns zero files) and `compileall` would write `__pycache__` into this read-only clone. The `node --check` line above is the real syntax signal.
- Dead code (inspected, not guessed): `worker/src/index.js:20` defines `const BRIEF_DEADLINE_MS = 18000` but the pass uses `worker/src/index.js:174` (`trigger==="cron" ? 300000 : -1`) — the constant is never referenced (search hits only line 20). `worker/src/index.js:139` imports `scoreOf` but the file never calls it (search hits only the import line). `worker/src/lib.js:36-39` exports `appendKeepNewest` yet production uses SQL `substr(notes || ?,-8000)` (`worker/src/index.js:271-275`); the helper is test-only (callers only in `worker/src/lib.test.js`). `worker/src/index.js:115` (`GITHUB_QUERIES.slice(0,2)`) silently drops the third query (`ai-side-project`, `worker/src/index.js:27`). All four survive from the prior round.
- Duplicated logic: `clamp10` exists twice — `functions/api/[[path]].js:12-16` vs `worker/src/lib.js:4-7` (same bounds/rounding; kept in sync by inspection only, no shared import). `scoreOf`/`effectiveScore` are correctly shared (`functions/api/[[path]].js:10` imports from `worker/src/lib.js`; cross-boundary test `functions/api/api.test.mjs:212-223` locks agreement). Auth compare is duplicated with divergent semantics: constant-time loop in API (`functions/api/[[path]].js:21-29`) vs `!==` in worker (`worker/src/index.js:393,408,439`, all `if (!want || got !== want) return json({ error: "unauthorized" }, 401);`, collapsing unset-token 503 into 401).
- TODO density: 0. Regex search for `TODO|FIXME|XXX|HACK|TBD` returns zero matches in shipped code (only the prior audit's own mention in `docs/audits/`). No `console.log`/`debugger`/`prefers-color-scheme`/`data-theme`/theme-toggle hooks in shipped `public/`+`functions/`+`worker/` (regex search clean).
- Live snapshot (read-only, from `_night/live/`): dashboard HTML content-matches repo `public/index.html` (title `AIMoney Lab — ranked ways to make money with AI`, favicon link + `theme-color #0d6b3f`, inline header brand SVG, triage-only button copy, `#api-errors` banner, review chip — `_night/live/aimoney_pages_dev.html:6-50` identical to repo lines). Health JSON verbatim: `{"ok":true,"rev":"b86d2b0","db":"up","opportunities":27,"unreviewed":11,"bare_without_brief":19,"experiments_by_status":{"planned":2},"hours_since_last_ok_run":5.8,"oldest_unreviewed_age_h":149.4,"decisions_last_7d":0,"vetted_last_7d":0,"time":"2026-09-20T05:47:41.404Z"}` — rev matches HEAD `b86d2b0`, run fresh (5.8h), but 41% unreviewed (oldest 6.2d), 70% bare, zero running/won/lost, zero decisions/vetted in 7d.
- Surface rules (owner's 5): all PASS. Light palette only (`public/styles.css:4` → `color-scheme: light;`, zero `prefers-color-scheme`/`data-theme`/toggle hits in shipped HTML/CSS/JS); tab icon + theme-color on the served page (`public/index.html:9-10`); hand-written `public/favicon.svg:1` (32×32 viewBox, `#0d6b3f` rounded square, `$` glyph, 1 line, <2 KB, no external fonts); inline header brand mark reusing favicon geometry/accent/glyph (`public/index.html:16`, styled `public/styles.css:46`); guard test `public/tab-icon.test.mjs:48-113` (5/5 green: size, XML root, light-mode lock, brand-mark match, icon+theme-color links). No finding slots spent on surfaces.
- Tree state (blocker, pre-existing): `git status --short` shows 28 modified files (every tracked file, `git diff --stat` reports 4924+/4924−, i.e. whole-file rewrite) although this run wrote nothing before this audit file. `file public/index.html` reports `with CRLF line terminators` in the working tree — the M-flags are a pre-existing CRLF-vs-HEAD line-ending mismatch, not edits. Acceptance expects only this new file; that gate cannot pass without a line-ending normalization owned outside this read-only lane. `_night/` scratch exists on disk but shows no `??` entry (ignored outside `.gitignore`).

## 4. Ranked findings (max 8)

Ordered by movement on the lane metric (decisions/week, vetted→experiment conversion, backlog drained). Round-1 F1–F3 (stale age, brand mark, orphans) and round-2 T1–T3 (decisions/vetted, triage disclosure, banner/badges) are fixed and verified; they do not reappear.

### F1 — The money number is zero for the third straight snapshot

- Evidence: `_night/live/aimoney_pages_dev_api_health.html:1` → `"experiments_by_status":{"planned":2},"decisions_last_7d":0,"vetted_last_7d":0`; seed experiment still `'planned'` in `d1/seed.sql:67-71`; identical `27/11/19/planned:2` at b92e2eb (`docs/AUDIT-2026-09-20.md:4-6`), e5a12a1 (`docs/audits/AUDIT-2026-09-20-round1.md:8`), and b86d2b0 (live) — zero movement while `hours_since_last_ok_run:5.8` proves the agent is fresh. Board header now renders the zero honestly (`public/app.js:226-229` → `` `${decisions} decisions this week · ${vetted} vetted → ${exps.length} experiments` ``) but nothing prompts the next move.
- Why it costs money: the lane exists to move decisions/week, and the instrument now proves the week is empty. Two `planned` rows sit with no `running`/`won`/`lost`; every idle week delays the first real evidence and the rescore it should drive.
- Fix: keep human-owns-status, but make the stall actionable: on the Experiments header, when `decisions_last_7d==0` and stale `planned`/`running` cards exist (reuse `days_in_status`), render a one-line nudge naming the stalest card and its age with a link that opens its drawer. Add a dashboard static test for the nudge copy. Touch `public/app.js`, `public/dashboard.test.mjs`.
- Size: S. Risk: low (presentation only; no status change).

### F2 — Vetted rows stay `researching` with no next step; vetted→experiment is unlinked

- Evidence: `public/app.js:152-168` (`vetOpportunity` patches only `{ notes }` — clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`, keeps status, lifts the 6000 cap) with success toast `Vetted — cap lifted` and no follow-up; `functions/api/[[path]].js:115-146` (`updateOpportunity` accepts any status, never checks experiments); health counts `vetted_last_7d` from the tag (`functions/api/[[path]].js:297`) but no query lists vetted-without-experiment. Live: `vetted_last_7d:0`, `unreviewed:11`.
- Why it costs money: vetting is the lane's first conversion step, but a vetted row lands back in `researching` indistinguishable from unvetted work except by notes text. Reviewers clear the queue without ever starting the experiment the lane counts.
- Fix: after a successful vet, change the toast to `Vetted — log the experiment or move to testing` and offer a one-click `Log experiment` opener for that opportunity (reuse `openExperimentModal(null, id)`); add a `Vetted, no experiment` count to `/api/health` from existing columns (`notes LIKE '%vetted]%'` + `experiment_count==0`) and show it on the Experiments header. Touch `public/app.js`, `functions/api/[[path]].js`, `functions/api/api.test.mjs`.
- Size: M. Risk: low (read-only count + UI prompt; no auto-transition).

### F3 — Create paths bypass every guard the update paths enforce

- Evidence: `functions/api/[[path]].js:220-237` (`createExperiment` inserts any `EXP_STATUSES` value with zero checks — `won`/`lost` need no `result`/`post_mortem`, `running` stamps no `started_at`, and `opportunity_id` is never verified to exist) vs `functions/api/[[path]].js:239-271` (`updateExperiment` 400s on `won`/`lost` without `result`+`post_mortem`, stamps `started_at`/`ended_at`, 400s on invalid status). `functions/api/[[path]].js:81-113` (`createOpportunity` clamps 1–10 and slices strings but never checks `est_monthly_low <= est_monthly_high`, duplicate title/slug beyond the UNIQUE error text, or `skills_needed` shape). No `POST` tests exist in `functions/api/api.test.mjs` (only PATCH/GET/health).
- Why it costs money/users/trust: the board's core promise — "closing requires result + post-mortem" (`README.md:73`) — is a PATCH-only convention. One `POST` with `status:won` and empty strings creates a decision-less decision that pollutes `decisions_last_7d`, and a typo'd `opportunity_id` creates an instant orphan. This is the parked round-3 "create guards" gap.
- Fix: mirror the update rules on create: reject `won`/`lost` without `result`+`post_mortem` (same `fields` shape), stamp `started_at`/`ended_at` as on update, 400 on invalid status, 404 when `opportunity_id` has no row; on opportunity create, 400 when `est_monthly_low > est_monthly_high`. Add API tests for each 400 plus the happy paths. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`. (If the parked branch returns, port its guards instead of inventing new ones.)
- Size: M. Risk: low-medium (write-path validation; keep error shapes consistent so `public/app.js` toasts stay readable).

### F4 — Scores never learn: closing an experiment rescores nothing

- Evidence: `updateExperiment` (`functions/api/[[path]].js:239-271`) validates closure (`result`+`post_mortem` required, `ended_at` stamped) but never touches its opportunity; `updateOpportunity` (`functions/api/[[path]].js:115-146`) rescores only on manual field edits; no code path maps `won`/`lost` to `value`/`confidence`. Still true from the prior round; live `decisions_last_7d:0` means the gap has not yet bitten, but the first real close will land on a board that ignores it.
- Why it costs money: after the first real evidence lands, the board keeps ranking on pre-experiment guesses — winners stay under-ranked, losers stay over-ranked, and the next experiment picks wrong.
- Fix: on `won`/`lost` close, append a one-line outcome summary to the parent opportunity's notes (newest-kept, same `substr(…,-8000)` idiom as `worker/src/index.js:271-275`) and surface a suggested (never auto-applied) confidence/value delta in the drawer admin zone for one-click human rescore. Keep human-owns-status. Touch `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: M. Risk: medium (touches the write path; keep `effectiveScore` shared and add API tests).

### F5 — Brief backlog 70% bare; the main brief still chases top score while old rows wait

- Evidence: `bare_without_brief:19` of 27 (`_night/live/aimoney_pages_dev_api_health.html:1`); main brief takes the top-scored bare row in `worker/src/index.js:283-285` (`` `… WHERE b.id IS NULL ORDER BY o.score DESC LIMIT 1` ``) while the extra-brief guard (`worker/src/index.js:326-370`) only fires when the oldest unreviewed exceeds 48h — live oldest is 149.4h, so both fire, yet coverage is still 8/27 after weeks of runs. Proposals append every run (`worker/src/index.js:242-266`); throughput is ≤2 briefs per cron tick (≤8/day) minus manual skips.
- Why it costs money: reviewers cannot vet what they cannot read — unbriefed proposals stall in `researching`, the 6000 cap holds, and good ideas wait 6+ days for their first evidence paragraph.
- Fix: when `oldest_unreviewed_age_h > 48`, flip the MAIN brief query to oldest-unreviewed-bare-first (`ORDER BY o.created_at ASC LIMIT 1`) instead of top-scored, keeping the extra-brief as the second-oldest; disclose the mode in the run's `error`/`phase` heartbeat so the Research log shows `brief:oldest-first`. No AI-budget change. Touch `worker/src/index.js`, `worker/src/index.test.js`.
- Size: S. Risk: low (query-order change on backlog ticks only; brief content path untouched).

### F6 — Triage drops are invisible: noise + invalid verdicts vanish without a count

- Evidence: invalid action → `worker/src/index.js:232-235` (`UPDATE signals SET processed=1`, continue); `supports`-without-id demoted to noise in `worker/src/index.js:239-241`; plain noise in `worker/src/index.js:276-278` — none counted. `agent_runs` has no noise/invalid column (`d1/schema.sql:85-98`: `signals_seen, added, updated, briefs, ai_calls` only); health exposes no triage breakdown (`functions/api/[[path]].js:316`). Still true from the prior round.
- Why it costs money/trust: the prompt says `DEFAULT TO NOISE` (`worker/src/index.js:213`) — correct bias, but with zero visibility nobody can tell a strict-but-right triage from a broken model dropping winners. Debugging is guesswork.
- Fix: without any migration, add `noise_24h` to `/api/health` via `SELECT COUNT(*) FROM signals WHERE processed=1 AND opportunity_id IS NULL` (windowed by `created_at`), and show `+A/U · N noise` in the agent pill title and runs tab. Add an API test with seeded signals. Touch `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: S. Risk: low (read-only counts from existing columns).

### F7 — Orphaned experiments have no remediation path

- Evidence: list keeps dangling rows via `LEFT JOIN` with `orphaned:true` (`functions/api/[[path]].js:205-217`); board renders an `orphaned` pill (`public/app.js:238`) but the card click dead-ends in `public/app.js:244-246` (`if (c.dataset.orphan === "1") return toast("Orphaned experiment — its opportunity was deleted.");`); router has no `DELETE` for any resource (`functions/api/[[path]].js:325-342` lists only GET/POST/PATCH/PUT); schema FKs are advisory without `PRAGMA foreign_keys` (`d1/schema.sql:50`).
- Why it costs users/trust: orphan-safe display (round 1) stopped the vanishing, but orphans now accumulate with no admin action — no delete, no relink, not even a drawer. The board slowly fills with unactionable cards.
- Fix: add admin-only `DELETE /api/experiments/:id` (same `authed` + 404 shape as update) and, on orphaned cards only, a `Delete` button behind the admin-token check with a confirm prompt; keep the toast for non-admins. Add API tests for 401/404/happy-path. Touch `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: M. Risk: medium (first DELETE endpoint; restrict to experiments, never cascade).

### F8 — Auth mismatch + dead worker code invite the next incident

- Evidence: API constant-time compare (`functions/api/[[path]].js:21-29`) vs worker `!==` in three places (`worker/src/index.js:393,408,439` → `if (!want || got !== want) return json({ error: "unauthorized" }, 401);`, collapsing unset-token 503 into 401); `const BRIEF_DEADLINE_MS = 18000` never referenced (`worker/src/index.js:20` vs live `worker/src/index.js:174`); unused `scoreOf` import (`worker/src/index.js:139`); test-only `appendKeepNewest` (`worker/src/lib.js:36-39`, zero production callers); `GITHUB_QUERIES.slice(0,2)` drops `ai-side-project` (`worker/src/index.js:115` vs `:27`) with no comment. All survive from the prior round.
- Why it costs trust/maintainability: timing side-channel on the worker token plus divergent 401/503 slows triage; the next editor tunes the wrong deadline constant or re-adds the dropped query as a bugfix. No direct money movement — ranked last.
- Fix: extract the constant-time compare into `worker/src/lib.js`, import it in both runtimes, align worker codes (503 unset / 401 mismatch); delete-or-wire `BRIEF_DEADLINE_MS`, drop `scoreOf` from the worker import, demote `appendKeepNewest` to a documented test oracle pointing at the SQL idiom, and comment the `slice(0,2)`. Touch `worker/src/lib.js`, `worker/src/index.js`, `functions/api/[[path]].js`, `worker/src/lib.test.js`.
- Size: S. Risk: low (auth tightens; keep tokens out of logs; `npm test` must stay green).

## 5. What NOT to change

- Scoring formula and honest seeds: `score = 100 * (value * confidence * fit) / (effort + 1)`, each input 1–10 (`README.md:56`; `worker/src/lib.js:15-16`). Do not retune weights without new evidence.
- Human-owns-status: "The agent never moves status." (`README.md:71`); "The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)." (`worker/src/index.js:4-5`); "`researching → testing` is a human decision: flip to `testing` when you start a real experiment … The agent never moves status." (`README.md:71`). No auto-transitions.
- Killed-with-learning: "Killed strategies stay on the board with their post-mortem — that is the point." (`README.md:64`); "Closing an experiment as `won`/`lost` requires `result` + `post_mortem`; `ended_at` stamps automatically." (`README.md:73`). Do not purge killed rows or relax closure rules.
- Capped humility: "Agent proposals enter as `researching` with an `UNREVIEWED` marker in notes and an effective score capped to ≤6000 until a human vets them" (`README.md:65`); "A human admin vets each row: "Vet" clears the `UNREVIEWED` marker (keeps status, lifts the 6000 cap) …" (`README.md:70`). Vet/kill stay human.
- Seed-vs-live accounting: "Seed runs once on an empty table; live count = 12 seeds + agent proposals + manual adds. Killed rows stay listed, so the count only grows." (`README.md:80`). Do not re-seed over live data; deploy's seed-if-empty (`deploy/deploy.sh:30-42`) stays.
- Auth split and secrets: "Reads are public. Writes require `Authorization: Bearer <ADMIN_TOKEN>`." (`functions/api/[[path]].js:1-2`); "`ADMIN_TOKEN` lives in two places: the Pages env var and the worker secret … No expiry; rotate manually when shared or leaked." (`README.md:84-85`). Anything needing `ADMIN_TOKEN`, `CLOUDFLARE_API_TOKEN`, D1 execute, or wrangler deploy is operator-owned and credential-gated — out of scope for code edits.
- Anything needing a login, payment, or human judgment: vetting `UNREVIEWED` rows, moving `researching → testing → scaling`, writing post-mortems, choosing niches/pricing/outreach, rotating tokens. This audit proposes surfacing and guardrails only — never auto-vetting, auto-scaling, or auto-spend.

## 6. Proposed next tasks (2–4)

### Task 1: Port parked round-3 create guards onto master by hand, tests green

- Owned files: `functions/api/[[path]].js`, `functions/api/api.test.mjs`, `public/app.js`, `public/index.html`.
- Acceptance: (a) `POST /experiments` mirrors PATCH closure rules (`won`/`lost` need `result`+`post_mortem`, invalid status 400, `started_at`/`ended_at` stamped, 404 on unknown `opportunity_id`) with new API tests; (b) any remaining parked chip/banner delta ported without `git merge` (branch `origin/night/aimoney-round2` was absent here — fetcher must diff d5198af first); (c) `npm test` green; (d) no change to scoring, review flow, or existing health keys.

### Task 2: Nudge stale planned rows and link vetted rows to their next experiment

- Owned files: `public/app.js`, `public/dashboard.test.mjs`, `functions/api/[[path]].js`, `functions/api/api.test.mjs`.
- Acceptance: (a) Experiments header shows a stale-nudge naming the stalest `planned`/`running` card when `decisions_last_7d==0`; (b) vet success toast offers one-click `Log experiment` for that opportunity; (c) `/api/health` reports `vetted_no_experiment` from existing columns with an API test; (d) `npm test` green; (e) no auto-transitions added.

### Task 3: Append closed-experiment outcomes to parent notes with suggested rescore

- Owned files: `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Acceptance: (a) `PATCH …/won|lost` appends a one-line outcome to the parent opportunity notes (newest-kept 8000) without changing its score; (b) drawer admin zone shows a suggested confidence/value delta for one-click human rescore, never auto-applied; (c) API tests cover both; (d) `npm test` green.
