# AUDIT-2026-09-20-round1 — aimoney (round)

Clone: `/root/coding-projects/_workspace/day1-aimoney-r1` at `d925ae6` (read-only; this file is the only write).
Live snapshots read: `_night/live/aimoney_pages_dev.html` (HTTP 200, 119 lines), `_night/live/aimoney_pages_dev_api_health.html` (HTTP 200, 1 line).
No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` in clone; `README.md` used as governing doc. `.env`/secrets never read. No git writes, no network, no installs. One single-stage shell command per tool call; no inline code.
Prior rounds SHIPPED to master through `d925ae6` (API guard gaps closed, oldest-first briefs, triage noise count) and `c9ffba9` (create guards, stall nudge with vet handoff, outcome ledger) — all verified present below. Parked branches `night/aimoney-round1-2` and `night/round3a-aimoney-round1-2` are absent (`git branch -a` shows only `master`, `remotes/origin/master`); no fetch allowed, nothing to port — master already carries the work.

Lane metric (aimoney row): experiments reaching a decision (`won`/`lost` with post-mortem) per week, plus vetted proposals becoming experiments, with the unreviewed backlog drained. Current value from the repo's own live data: **0 decisions and 0 vetted in 7d** (`decisions_last_7d:0`, `vetted_last_7d:0`), **11 unreviewed of 27 (oldest 155.1h ≈ 6.5d)**, **19/27 bare without brief**, `experiments_by_status:{"planned":2}` with zero `running`/`won`/`lost`. Live rev is `d925ae6` = HEAD (deploy gap from the prior round is closed), and the counts are identical to the three prior snapshots (`27/11/19/planned:2` at b92e2eb → e5a12a1 → b86d2b0 → d925ae6): a fourth fresh cycle (`hours_since_last_ok_run:5.4`) with zero queue movement.

## 1. What this project is and how it runs

- Purpose: Cloudflare-native AIMoney Lab — ranked AI-money opportunities with scores, research briefs, experiments, and a cron research agent.
- Entry points: dashboard `public/index.html` + `public/app.js`; API `functions/api/[[path]].js`; agent `worker/src/index.js` + `worker/src/lib.js`; DB `d1/schema.sql`.
- Live surfaces: `https://aimoney.pages.dev`, `https://aimoney.pages.dev/api/health`, worker `https://aimoney-research.<account>.workers.dev`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`) — collect signals, AI triage, 1–2 briefs per cron tick, log to `agent_runs`.
- How it deploys: `./deploy/deploy.sh` (D1 schema + seed-if-empty, stamp `public/release.json`, Pages deploy, worker deploy); Pages Git-connected rebuild + `.github/workflows/deploy-worker.yml` on `worker/**` push; `./deploy/verify.sh` checks markers + 12h freshness.

## 2. End-to-end walk-through

Main loop: signal found → agent proposal → human vet → experiment → close with learning → rescore.

1. Cron/manual triggers pass: `worker/src/index.js:395-397` (`scheduled` → `runResearch(env,"cron")`) and `worker/src/index.js:451-460` (`POST /run` → `waitUntil(runResearch(env,"manual"))`, 202 `{"status":"accepted","briefs_skipped":true,"reason":"manual skips brief pass"}`).
2. Collect signals in parallel: `worker/src/index.js:176` (`Promise.allSettled([hnSignals(), redditSignals(), githubSignals()])`); sources `worker/src/index.js:47-69` (HN ×4), `worker/src/index.js:71-93` (Reddit ×3), `worker/src/index.js:95-116` (GitHub, `slice(0,2)` of 3 queries at `:115`); fetch helper `worker/src/index.js:35-45` (`timedJson`, 6s abort covering body read).
3. Batch-insert signals + heartbeat: `worker/src/index.js:181-186` (`INSERT OR IGNORE INTO signals`, one `DB.batch`); `worker/src/index.js:187-191` (`phase:collected/classify-ai/...` heartbeat); reap stuck runs `worker/src/index.js:145-150` (`running` older than 30 min → `error`).
4. Classify fresh vs live list: `worker/src/index.js:192-194` (`SELECT * FROM signals WHERE processed=0 ORDER BY id DESC LIMIT ?`, bound to `MAX_AI_SIGNALS = 6` at `:15`); `worker/src/index.js:201-203` (top-60 opps); `worker/src/index.js:217-221` (`aiComplete` Mistral NDJSON); parse `worker/src/lib.js:49-64` (`parseJsonLines` + `repairJson` for `\_` at `worker/src/lib.js:47`).
5. Apply verdicts: new → `worker/src/index.js:242-266` (`INSERT INTO opportunities … status="researching"`, `effectiveScore({…,"UNREVIEWED"})` capped ≤6000 at `:258`); supports → `worker/src/index.js:267-275` (`substr(notes || ?,-8000)`, newest kept); invalid action → `worker/src/index.js:232-235` and noise → `worker/src/index.js:276-278` (marked `processed=1`, dropped); supports-without-id demoted to noise at `worker/src/index.js:239-241`.
6. Brief one–two bare opps (cron only): `worker/src/index.js:174` (`briefDeadline = trigger==="cron" ? 300000 : -1`, manual always skips); `worker/src/index.js:285-299` (mode flag `briefMode`: `top-scored` default, `oldest-first` when oldest unreviewed exceeds 48h, `:288-293`); main brief query `:294-299`; Llama brief JSON `:305-317`; `INSERT INTO briefs` skipped if `summary`/`first_steps` empty (`:323`); extra oldest-unreviewed bare row when backlog >48h `worker/src/index.js:340-384` (`extraBare`, `retries:0`, `MAX_AI_CALLS` guard); double `finish("ok")` at `:385-386` (first write immediately overwritten by the mode-disclosing second).
7. Board reads: `public/app.js:658-677` (`refresh()` loads `/api/opportunities?limit=200`, `/api/experiments`, `/api/runs?limit=20`, `/api/health`, `/api/meta`, tracking `state.apiFailures`); list `functions/api/[[path]].js:39-66` (`unreviewed=1` filter, `oldest` sort, shared `effectiveScore` cap at `:61`, re-sort at `:62-64`, `brief_count`/`experiment_count` at `:55-56`); review chip + age `public/app.js:132-155`; review view `public/app.js:100-130`; failure banner `public/app.js:640-655` + `public/index.html:40`.
8. Human vets: `public/app.js:161-177` (Vet strips `UNREVIEWED`, lifts cap, keeps status; success toast with one-click Log experiment at `:172`) / `public/app.js:179-197` (Kill sets `killed` + prompted post-mortem) → `functions/api/[[path]].js:118-155` (`PATCH /api/opportunities/:id`, range 400 at `:139-141`, status 400 at `:129-131`, rescore via shared `effectiveScore` at `:143`).
9. Experiment loop: log/update `public/app.js:556-601` (`openExperimentModal`) → `functions/api/[[path]].js:231-273` (create: invalid-status 400 at `:236-238`, unknown-opp 404 at `:240-241`, won/lost closure rules at `:242-247`, stamps at `:251-252`, ledger append at `:264-271`) / `functions/api/[[path]].js:275-319` (update: same rules plus outcome-ledger append at `:302-309`); list with age/orphan `functions/api/[[path]].js:209-229` (`days_in_status` at `:223-227`, `orphaned` via `LEFT JOIN` at `:217-226`); board stale-first + age chips + decisions header + stall nudge `public/app.js:233-280` (nudge at `:254-261`, `vetted_no_experiment` at `:240`/`:248`); detail drawer + admin re-optimize `public/app.js:347-485` with suggested rescore `public/app.js:402-412` / `:419-424`.
10. Health/verify: `functions/api/[[path]].js:334-368` (`/api/health`: `rev,db,opportunities,unreviewed,bare_without_brief,experiments_by_status,hours_since_last_ok_run,oldest_unreviewed_age_h,decisions_last_7d,vetted_last_7d,vetted_no_experiment,noise_24h,time`); `deploy/verify.sh:28-58` (7 markers + non-empty list + freshness ≤12h).

Where it most often breaks or goes silent: the human bottleneck after hop 5, unchanged across four snapshots. Live health proves it: `unreviewed:11/27` (41%, oldest 155.1h), `bare_without_brief:19/27` (70%), `experiments_by_status:{"planned":2}` with zero `running`/`won`/`lost`, `decisions_last_7d:0`, `vetted_last_7d:0` — and the counts are identical to the three prior fetches, i.e. **zero queue movement across another fresh cycle** (`hours_since_last_ok_run:5.4`) even though live now serves `d925ae6` with the stall nudge (`public/app.js:254-261`), vet handoff (`public/app.js:172`), outcome ledger (`functions/api/[[path]].js:264-271`, `:302-309`), oldest-first briefs (`worker/src/index.js:288-299`), and noise count (`functions/api/[[path]].js:347-348`) all deployed. The board now names the stall honestly; nobody has acted on it. Secondary silent spots: triage takes newest-first `LIMIT 6` so older unprocessed signals starve (`worker/src/index.js:192-194`); manual runs never brief (`briefDeadline=-1`, `worker/src/index.js:174`); the drawer shows the UNCAPPED score for `UNREVIEWED` rows while the ledger shows the capped one (F3).

## 3. Health signals measured here

- Test suite (`npm test` = `node --test worker/src/lib.test.js worker/src/index.test.js functions/api/api.test.mjs public/tab-icon.test.mjs public/dashboard.test.mjs`): **PASS, 69/69, 28 suites, 0 fail**. Real tail: `# tests 69 / # suites 28 / # pass 69 / # fail 0 / # cancelled 0 / # skipped 0 / # todo 0 / # duration_ms 1344.086377`. Matches the Windows baseline exactly (69/69; baseline `duration_ms 207.3504` — the slower 1344ms here is sandbox overhead). Warnings only: `UNDICI-EHPA` proxy-agent notice and `MODULE_TYPELESS_PACKAGE_JSON` reparsing hint for `[[path]].js`/`index.test.js`/`lib.test.js` (add `"type":"module"` to silence; no behavior change). One sandbox note: the runner recorded a denied `localhost:80` request during the run — that is the health handler's best-effort `fetch(/release.json)` (`functions/api/[[path]].js:363-366`) failing gracefully inside the in-memory API tests (caught, `rev` stays `"unknown"`); all health tests still pass.
- Syntax checks (no writes): `node --check public/app.js` → exit 0; `node --check worker/src/index.js` → exit 0; `node --check worker/src/lib.js` → exit 0 (each with only the `UNDICI-EHPA` warning). `node --check` on `functions/api/[[path]].js` not run separately (bracketed path); the file is imported and exercised green by `npm test` instead.
- `python3 -m compileall -q`: NOT RUN — no Python sources in repo (glob `**/*.py` returns zero files) and `compileall` would write `__pycache__` into this read-only clone. The `node --check` line above is the real syntax signal.
- Dead code (inspected, not guessed): `worker/src/index.js:20` defines `const BRIEF_DEADLINE_MS = 18000` but the pass uses `worker/src/index.js:174` (`trigger==="cron" ? 300000 : -1`) — the constant is never referenced (search hits only line 20). `worker/src/index.js:139` imports `scoreOf` but the file never calls it (search hits only the import line). `worker/src/lib.js:36-39` exports `appendKeepNewest` yet production uses SQL `substr(notes || ?,-8000)` (`worker/src/index.js:271-275`, `functions/api/[[path]].js:268-270`, `:306-308`); the helper is test-only (callers only in `worker/src/lib.test.js`). `worker/src/index.js:115` (`GITHUB_QUERIES.slice(0,2)`) silently drops the third query (`ai-side-project`, `worker/src/index.js:27`). NEW this round: `worker/src/index.js:385-386` calls `await finish("ok")` twice in a row — the first write is immediately overwritten by the mode-disclosing second (`brief:oldest-first`/`top-scored`/empty). All prior-round items survive.
- Duplicated logic: `clamp10` exists twice — `functions/api/[[path]].js:12-16` vs `worker/src/lib.js:4-7` (same bounds/rounding; the API imports only `effectiveScore` at `functions/api/[[path]].js:10`, so the clamp stays in sync by inspection only). `scoreOf`/`effectiveScore` are correctly shared (cross-boundary test `functions/api/api.test.mjs:400-412` locks agreement). Auth compare is duplicated with divergent semantics: constant-time loop in API (`functions/api/[[path]].js:21-29`, unset token → 503 `writes disabled`) vs `!==` in worker (`worker/src/index.js:408,423,454`, all `if (!want || got !== want) return json({ error: "unauthorized" }, 401);`, collapsing unset-token 503 into 401).
- TODO density: 0. Regex search for `TODO|FIXME|XXX|HACK|TBD` returns zero matches in shipped `public/`+`functions/`+`worker/` (only the two prior audits' own mentions in `docs/audits/`). No `console.log`/`debugger`/`prefers-color-scheme`/`data-theme`/theme-toggle hooks in shipped `public/`+`functions/`+`worker/` (regex search clean).
- Live snapshot (read-only, from `_night/live/`): health JSON verbatim: `{"ok":true,"rev":"d925ae6","db":"up","opportunities":27,"unreviewed":11,"bare_without_brief":19,"experiments_by_status":{"planned":2},"hours_since_last_ok_run":5.4,"oldest_unreviewed_age_h":155.1,"decisions_last_7d":0,"vetted_last_7d":0,"vetted_no_experiment":0,"noise_24h":0,"time":"2026-09-20T11:24:51.363Z"}` — rev matches HEAD `d925ae6`, run fresh (5.4h), new keys `vetted_no_experiment`/`noise_24h` present (both 0), but 41% unreviewed (oldest 6.5d), 70% bare, zero running/won/lost, zero decisions/vetted in 7d. Dashboard HTML content-matches repo `public/index.html` line-for-line (119 lines: title, favicon link + `theme-color #0d6b3f`, inline header brand SVG, triage-only button copy, `#api-errors` banner, review chip).
- Surface rules (owner's 5): all PASS. Light palette only (`public/styles.css:4` → `color-scheme: light;`, zero `prefers-color-scheme`/`data-theme`/toggle hits in shipped HTML/CSS/JS); tab icon + theme-color on the served page (`public/index.html:9-10`); hand-written `public/favicon.svg:1` (32×32 viewBox, `#0d6b3f` rounded square, `$` glyph, 285 bytes, no external fonts); inline header brand mark reusing favicon geometry/accent/glyph (`public/index.html:16`, styled `public/styles.css:46`); guard test `public/tab-icon.test.mjs:48-113` (5/5 green: size, XML root, light-mode lock, brand-mark match, icon+theme-color links). No finding slots spent on surfaces.
- Tree state (blocker, pre-existing): `git status --short` shows 29 modified files (every tracked file, `git diff --stat` reports 5509+/5509−, i.e. whole-file rewrite) although this run wrote nothing before this audit file. `file public/index.html` reports `with CRLF line terminators` in the working tree — the M-flags are a pre-existing CRLF-vs-HEAD line-ending mismatch, not edits. Acceptance expects only this new file; that gate cannot pass without a line-ending normalization owned outside this read-only lane. `_night/` scratch exists on disk but shows no `??` entry (ignored outside `.gitignore`).

## 4. Ranked findings (max 8)

Ordered by the owner's money ranking: (1) zero-spend starters flagged, (2) outcomes measurable in money, (3) review queue draining, (4) honest scoring. Round-3 tasks 1–3 (create guards, stall nudge + vet handoff, outcome ledger) and round-2 tasks (guard-gap closure, oldest-first briefs, noise count) are done on master and verified; they do not reappear except where the port left gaps (F3, F4, F7).

### F1 — No zero-spend starter is surfaced; the "do this today" action does not exist

- Evidence: `public/index.html` (119 lines) has no top-pick hero, no "start here", no "next action" copy (regex search for `top pick|start here|next action|expected revenue|zero-spend` in `public/` returns zero hits). `capital_needed` is rendered only inside the drawer (`public/app.js:361` → `<dt>Capital needed</dt><dd>${esc(o.capital_needed || "—")}</dd>`) — never in the ledger, never filterable. The seed holds real $0 rows (`d1/seed.sql:7` `'ai-freelance-outcomes' … '$0'`; `d1/seed.sql:14` `'digital-products-prompts' … '$0'`) that the board cannot flag. The owner directive demands the single highest-EV opportunity surfaced first with its expected revenue, its cost to start, and the one concrete next action for today — none of the three exists as a unit; the ledger sorts by score, and score ≠ expected revenue (top-scored `ai-ugc-ads-service` shows `$2k–8k` while lower-scored `micro-saas-ai-tool` carries a `$1k–20k` ceiling, `d1/seed.sql:6,10`).
- Why it costs money: the lane's bottleneck is human action (0 decisions, 0 vetted, fourth straight snapshot). A visitor who opens the daily page sees 27 rows and no instruction; the cheapest experiment on the board ($0, 1–3 weeks to first dollar) is indistinguishable from a $400/mo-telephony build until every drawer is opened by hand. Rank without a next action does not convert.
- Fix: add a "Start here today" strip above the ledger showing the #1 by score with its `$/mo` range, `capital_needed`, and brief `first_steps` line 1 plus a deep link into its drawer; add a `Zero spend` chip filtering `capital_needed` values starting with `$0`. Reuse `money()`, `briefHtml`, and `openDrawer` — no API change. Add dashboard static tests for the strip copy and the chip. Touch `public/app.js`, `public/index.html`, `public/dashboard.test.mjs`.
- Size: M. Risk: low (presentation only; no status change, no scoring change).

### F2 — Experiment outcomes are still unmeasurable in money: no revenue field, spend is free text

- Evidence: `d1/schema.sql:54-58` declares `budget_cap TEXT`, `spent TEXT`, `metric TEXT`, `target TEXT`, `result TEXT` — all free text, and no `revenue`/`revenue_earned` column exists anywhere (search for `revenue` in `public/`+`functions/`+`d1/` returns zero production hits). The API never parses money (`functions/api/[[path]].js:231-319` slices strings but validates no amounts). Health counts decisions, not dollars (`functions/api/[[path]].js:344` counts `won`/`lost` rows; nothing sums revenue). The seed target mixes activity and money in one unparseable string (`d1/seed.sql:71` → `'>=3 replies; >=1 pilot at >=$500'`). A `won` experiment can therefore close with `result: "went great"` and the board records a decision with no number.
- Why it costs money: the owner ranks "outcome measurable in money rather than activity" second only to zero-spend starts. Today a won experiment proves nothing about dollars — replies and pilots are activity, and the $500 pilot clause is prose no query can aggregate. The lane metric (decisions/week) can climb while revenue stays unknown.
- Fix: migrate `experiments` with `revenue_cents INTEGER DEFAULT 0` + `spent_cents INTEGER DEFAULT 0` (new `d1/migrate-*.sql` applied by `deploy.sh`, since `schema.sql` is `CREATE IF NOT EXISTS` and never alters), accept both in create/update, show them on board cards, and add `revenue_last_7d` to `/api/health` summed from closed rows. Keep `result`/`post_mortem` prose required. Add API tests for the new columns and the health sum. Touch `d1/schema.sql`, `d1/migrate-*.sql` (new), `deploy/deploy.sh`, `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: M. Risk: medium (first real migration; keep it additive with defaults so existing rows and tests stay green).

### F3 — The drawer shows the UNCAPPED score for UNREVIEWED rows; the ledger shows the capped one

- Evidence: `getOpportunity` returns the raw stored row with no cap (`functions/api/[[path]].js:68-79` — `SELECT * FROM opportunities WHERE id = ?` then `return json({ opportunity: row, … })`, zero calls to `effectiveScore`), while `listOpportunities` caps (`functions/api/[[path]].js:61` → `score: effectiveScore(r)`). The drawer renders the raw value (`public/app.js:354` → `<b class="mono">${esc(o.score)}</b>`). For an `UNREVIEWED` row stored at 17500, the ledger honestly shows 6000 and the drawer shows 17500 — the same opportunity with two scores on one screen. The cap test covers only the list (`functions/api/api.test.mjs:400-412`).
- Why it costs trust (and then money): the honest-scoring rule exists so capped proposals never outrank vetted work. The drawer silently breaks it exactly where vetting decisions are made — a reviewer opening an unreviewed row sees an inflated number with no marker, and the admin rescore zone below it (`public/app.js:414-485`) reasons from raw `value`/`confidence` against that inflated display.
- Fix: apply the shared `effectiveScore` in `getOpportunity` before returning (one line, same import already at `functions/api/[[path]].js:10`), and add `(capped — unreviewed)` next to the drawer score when `notes` carries `UNREVIEWED`. Add an API test asserting detail-view cap agreement with the list. Touch `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: S. Risk: low (read-path display; stored scores untouched).

### F4 — 19 of 27 rows have no brief, yet the ledger shows their full scores with no no-evidence badge

- Evidence: `bare_without_brief:19` of 27 (live health). The API already returns the signal (`functions/api/[[path]].js:54-56` → `(SELECT COUNT(*) FROM briefs …) AS brief_count`), but `public/app.js` never reads `brief_count` (search hits: zero; only `experiment_count` is consumed, at `public/app.js:628` for the testing badge). Eight of the twelve human seeds have no brief either (`d1/seed.sql:20` briefs only the top 4) and display full scores indistinguishable from briefed rows. The only disclosure is buried in the drawer (`public/app.js:332` → `No brief yet — the agent writes one…`), visible one row at a time after a click.
- Why it costs money: a score with no evidence behind it must say so (owner rule 4). Reviewers cannot triage what they cannot see — unbriefed rows stall in `researching` behind confident-looking numbers, the 6000 cap holds on agent rows, and the queue ages (oldest now 155.1h) while looking healthy.
- Fix: render a `no brief` pill in the ledger's score cell when `brief_count == 0` (same `.warn-badge` style as the existing mismatch badges, `public/styles.css:200-204`), and append `· N without briefs` to the review chip title from the already-fetched `bare_without_brief` health key. Add a dashboard static test for the pill. Touch `public/app.js`, `public/dashboard.test.mjs`.
- Size: S. Risk: low (presentation only; reuses returned columns).

### F5 — Triage starves older signals: newest-first `LIMIT 6` buries the excess behind future arrivals

- Evidence: `worker/src/index.js:192-194` (`SELECT * FROM signals WHERE processed = 0 ORDER BY id DESC LIMIT ?`, bound to `MAX_AI_SIGNALS = 6` at `worker/src/index.js:15`); same order in the debug probe (`worker/src/index.js:425`). Sources can insert up to ~72 rows per run (HN 4×8 + Reddit 3×8 + GitHub 2×8 at `worker/src/index.js:47-116`) while triage clears ≤6 per run (≤24/day over 4 cron ticks) — whenever a run sees more than 6 fresh signals, the excess waits behind every future arrival, indefinitely under steady inflow. Carried from the prior round; still present at `d925ae6`.
- Why it costs money: potential winners rot in `processed=0` while the model re-reads only the newest arrivals; the board's input stream is systematically biased toward whatever arrived last, not whatever is best. With `noise_24h:0` live, the current triage looks clean — but only over the 6 newest rows it ever sees.
- Fix: take the oldest unprocessed signals first (`ORDER BY id ASC LIMIT 6`) so every signal is triaged exactly once in arrival order; add a staleness bound (signals older than 30d marked noise with a count) so ancient rows cannot wedge the queue after deploy. Update the debug probe to match and add a worker test locking the order. Touch `worker/src/index.js`, `worker/src/index.test.js`.
- Size: S. Risk: low-medium (ordering change; the staleness bound keeps ancient backlog from dominating the first ticks after deploy).

### F6 — Orphaned experiments have no remediation path

- Evidence: list keeps dangling rows via `LEFT JOIN` with `orphaned:true` (`functions/api/[[path]].js:217-226`); board renders an `orphaned` pill (`public/app.js:269`) but the card click dead-ends in `public/app.js:276` (`if (c.dataset.orphan === "1") return toast("Orphaned experiment — its opportunity was deleted.");`); router has no `DELETE` for any resource (`functions/api/[[path]].js:376-393` lists only GET/POST/PATCH/PUT; search for `DELETE` hits only a code comment); schema FKs are advisory without `PRAGMA foreign_keys` (`d1/schema.sql:50`). Carried from the prior round (its proposed Task 4); still present at `d925ae6`.
- Why it costs users/trust: orphan-safe display stopped the vanishing, but orphans now accumulate with no admin action — no delete, no relink, not even a drawer. The board slowly fills with unactionable cards, and each one pollutes the `experiments` count the header reports.
- Fix: add admin-only `DELETE /api/experiments/:id` (same `authed` + 404 shape as update) and, on orphaned cards only, a `Delete` button behind the admin-token check with a confirm prompt; keep the toast for non-admins. Add API tests for 401/404/happy-path. Touch `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Size: M. Risk: medium (first DELETE endpoint; restrict to experiments, never cascade).

### F7 — Update paths accept unbounded strings that create paths truncate

- Evidence: `createOpportunity` slices every string (`functions/api/[[path]].js:87-101`: `title` 200, `one_liner` 500, `category` 40, `time_to_first_dollar`/`capital_needed` 120, `source` 40, `source_url` 500, `notes` 8000) but `updateOpportunity` stores raw (`functions/api/[[path]].js:124-128` → `if (b[f] !== undefined) next[f] = String(b[f]);`, no slice on any of the eight fields). Same split on experiments: create slices (`:258-263`: `name` 200, `hypothesis`/`result`/`post_mortem` 8000, caps/metrics 120–300) while update stores raw (`:282-285`). A single `PATCH` with a megabyte `title` passes every guard the `POST` path enforces.
- Why it costs users/trust: oversized values break ledger rendering (10k-char titles in a table row), bloat the newest-kept-8000 notes idiom downstream, and make the create/update contract a lie — reviewers cannot trust that "the API validates" when only half of it does. No money movement directly; ranked below the queue and scoring findings.
- Fix: mirror the create-path `slice()` bounds into both update handlers with the same limits, and add two API tests (oversized title truncated on opportunity PATCH; oversized name truncated on experiment PATCH). Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`.
- Size: S. Risk: low (write-path truncation matching already-shipped create limits).

### F8 — Auth mismatch + dead worker code invite the next incident (polish, ranked last)

- Evidence: API constant-time compare (`functions/api/[[path]].js:21-29`) vs worker `!==` in three places (`worker/src/index.js:408,423,454` → `if (!want || got !== want) return json({ error: "unauthorized" }, 401);`, collapsing unset-token 503 into 401); `const BRIEF_DEADLINE_MS = 18000` never referenced (`worker/src/index.js:20` vs live `:174`); unused `scoreOf` import (`worker/src/index.js:139`); test-only `appendKeepNewest` (`worker/src/lib.js:36-39`, zero production callers); `GITHUB_QUERIES.slice(0,2)` drops `ai-side-project` (`worker/src/index.js:115` vs `:27`) with no comment; redundant double `await finish("ok")` (`worker/src/index.js:385-386`). All but the last survive from prior rounds.
- Why it costs trust/maintainability: timing side-channel on the worker token plus divergent 401/503 slows triage; the next editor tunes the wrong deadline constant or re-adds the dropped query as a bugfix. No direct money movement — ranked last per the owner's polish rule.
- Fix: extract the constant-time compare into `worker/src/lib.js`, import it in both runtimes, align worker codes (503 unset / 401 mismatch); delete the redundant first `finish("ok")`, delete-or-wire `BRIEF_DEADLINE_MS`, drop `scoreOf` from the worker import, demote `appendKeepNewest` to a documented test oracle pointing at the SQL idiom, and comment the `slice(0,2)`. Touch `worker/src/lib.js`, `worker/src/index.js`, `functions/api/[[path]].js`, `worker/src/lib.test.js`.
- Size: S. Risk: low (auth tightens; keep tokens out of logs; `npm test` must stay green).

## 5. What NOT to change

- Scoring formula and honest seeds: `score = 100 * (value * confidence * fit) / (effort + 1)`, each input 1–10 (`README.md:56`; `worker/src/lib.js:15-16`). Do not retune weights without new evidence.
- Human-owns-status: "The agent never moves status." (`README.md:71`); "The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)." (`worker/src/index.js:4-5`); "`researching → testing` is a human decision: flip to `testing` when you start a real experiment … The agent never moves status." (`README.md:71`). No auto-transitions, no auto-vetting, no auto-scaling.
- Killed-with-learning: "Killed strategies stay on the board with their post-mortem — that is the point." (`README.md:64`); "Closing an experiment as `won`/`lost` requires `result` + `post_mortem`; `ended_at` stamps automatically." (`README.md:73`). Do not purge killed rows or relax closure rules.
- Capped humility: "Agent proposals enter as `researching` with an `UNREVIEWED` marker in notes and an effective score capped to ≤6000 until a human vets them" (`README.md:65`); "A human admin vets each row: "Vet" clears the `UNREVIEWED` marker (keeps status, lifts the 6000 cap) …" (`README.md:70`). Vet/kill stay human (F3 extends the cap to the drawer; it does not lift it).
- Seed-vs-live accounting: "Seed runs once on an empty table; live count = 12 seeds + agent proposals + manual adds. Killed rows stay listed, so the count only grows." (`README.md:80`). Do not re-seed over live data; deploy's seed-if-empty (`deploy/deploy.sh:30-42`) stays.
- Auth split and secrets: "Reads are public. Writes require `Authorization: Bearer <ADMIN_TOKEN>`." (`functions/api/[[path]].js:1-2`); "`ADMIN_TOKEN` lives in two places: the Pages env var and the worker secret … No expiry; rotate manually when shared or leaked." (`README.md:84-85`). Anything needing `ADMIN_TOKEN`, `CLOUDFLARE_API_TOKEN`, D1 execute, or wrangler deploy is operator-owned and credential-gated — out of scope for code edits.
- Anything needing a login, payment, or human judgment: vetting the 11 `UNREVIEWED` rows, moving `researching → testing → scaling`, starting the 2 `planned` experiments, writing post-mortems, choosing niches/pricing/outreach, rotating tokens. This audit proposes surfacing and guardrails only — never auto-spend. The single most valuable move on the lane metric right now is not code at all: a human vetting the 11 and starting one `planned` experiment.
- Live surfaces already compliant: light palette, favicon + theme-color, inline brand mark, and guard test all pass (§3) — do not restyle, re-palette, or re-icon the dashboard.

## 6. Proposed next tasks (2–4)

### Task 1: Surface the zero-spend starter with today's next action above the ledger

- Owned files: `public/app.js`, `public/index.html`, `public/dashboard.test.mjs`.
- Acceptance: (a) a "Start here today" strip above the ledger shows the #1-by-score opportunity with its `$/mo` range, `capital_needed`, brief `first_steps` line 1, and a drawer deep link; (b) a `Zero spend` chip filters `capital_needed` `$0` rows; (c) dashboard static tests lock the strip copy and chip; (d) `npm test` green; (e) no scoring, status, or API change.

### Task 2: Make scores honest in the drawer and flag rows without briefs

- Owned files: `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`, `public/dashboard.test.mjs`.
- Acceptance: (a) `GET /api/opportunities/:id` returns the shared-`effectiveScore` capped score (detail agrees with list); (b) drawer shows `(capped — unreviewed)` next to capped scores; (c) ledger renders a `no brief` pill when `brief_count == 0`; (d) API + dashboard tests cover all three; (e) `npm test` green; (f) stored scores untouched.

### Task 3: Triage oldest signals first with a 30-day staleness bound

- Owned files: `worker/src/index.js`, `worker/src/index.test.js`.
- Acceptance: (a) fresh-signal take flips to `ORDER BY id ASC LIMIT 6` in both the pass and the debug probe; (b) signals older than 30d are marked noise with a count instead of wedging the queue; (c) a worker test locks oldest-first order; (d) `npm test` green; (e) `MAX_AI_CALLS` budget and brief behavior unchanged.

### Task 4: Admin-only delete for orphaned experiments

- Owned files: `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`.
- Acceptance: (a) `DELETE /api/experiments/:id` honors the same `authed` gate (401 without token, 404 unknown id) and deletes exactly one row, never cascading; (b) orphaned cards show a `Delete` button only when the admin token is set, with a confirm prompt; non-admins keep the current toast; (c) API tests cover 401/404/happy-path; (d) `npm test` green.
