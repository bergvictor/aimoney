# Audit 2026-09-20 round 8 — aimoney (read-only, track B)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r8`. HEAD `a738d37` (Night round 7); live rev `efcd038` (Night round 6, one commit behind). No git writes, no network, no installs, no edits to existing files — only this file created at the root. Never read `.env`, secrets or tokens. One single-stage shell command per call, no inline code, no redirection. No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` at the root (authoritative `git ls-files` inventory, 42 files, zero `*.py`); `README.md` read first, then the live snapshots in `_night/live/`, then source.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests (`README.md:3-15`, `worker/src/index.js:4-5`).
- Entry points: `public/index.html` + `public/app.js` (1749 lines, dashboard), `functions/api/[[path]].js:520` `onRequest` (740 lines, D1 REST API), `worker/src/index.js:686` (`scheduled` `:687` + `fetch` `:696`, 767 lines) + `worker/src/lib.js` (79 lines, shared pure helpers).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `/` (now with `rev`, `:704`) + `POST /run`; live snapshot `_night/live/aimoney_pages_dev_api_health.html:1` at `2026-09-21T22:35:57Z`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + up to 2 briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:752-762`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema `:27` + shared migrations `:33-34` + shared rev stamp `:40-41` + worker `:42`) or manual `deploy/deploy.sh` (schema `:36` + shared `:40`, Pages `:66`, stamp `:73` + worker `:75`), gated by `deploy/verify.sh` (public reads only, no credentials).

## 2. End-to-end walk-through

Signal to decision. The hop where it most often goes silent is hop 12 (human Vet, frozen 7.9 days); hop 10 (brief output) produced zero net briefs across ~14h and ≥2 cron ticks — see F4.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:81` `redditSignals`, `:110` `githubSignals` via `timedJson` (`:37-47`, 6s timeout covering the body read). Per-query throws counted; all-failed names the source in `src_fail`, partial in `src_partial` (`:76-77`, `:105-106`, `:134-135`).
2. Store: `:365-368` one batched `INSERT OR IGNORE INTO signals` (no isolation fallback — unlike the health and verdict batches), then heartbeat `:370-373` (`phase:collected` / `phase:collected stale:N`).
3. Sweep: `:381-384` unprocessed signals older than 30d become noise (`state.stale`); a failed sweep is named (`stale:failed`, finish `:673`).
4. Take: `:387-389` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:401-428` via pure `exactUrlTarget` (`:174-184`); bounded `IN` selects (`:410-415`, failures named `prepass_select:failed`); a match links as supports (notes newest-kept `:424`, no status move); failed links/notes appends skipped and counted (`prepass:N_failed` `:421`, `prepass_notes:N_failed` `:424`).
6. Gates, read before classify: `:443-445` bare-without-brief > 10 caps inserts to 1/tick (`bare_count:failed` on throw); `:447-449` unreviewed > 10 pauses inflow to 0/tick (`maxNewThisRun = 0`, `unreviewed_count:failed` on throw). Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:460-473` `buildClassifyPrompt(opps, fresh)` (`:282`) + `aiComplete` (`:141`, budget `MAX_AI_CALLS = 4`, `:19`), parsed by `parseJsonLines` (`worker/src/lib.js:64`, NDJSON + `repairJson` for `\_`). Inflow-paused ticks skip classify entirely and still brief (finish line carries `inflow:paused`, `:673`). An AI throw finishes `error`; AI garbage that parses to zero verdicts finishes `ok` with no marker — see F6.
8. Verdict loop `:487-565`: strict action enum (`:493`), supports-without-id becomes noise (`:500-502`), overflow stays unprocessed (`:504`). `new` INSERTs (`:515-529`) with an `UNREVIEWED` note (`:525`) and capped `effectiveScore` (`worker/src/lib.js:30`, cap 6000 `:23`); slug collision links as supports (`:536-551`, failed lookup named `slug_select:failed`, signal left unprocessed for retry).
9. Verdict flush: `:566` `flushVerdictWrites` (`:301-317`); failures as `verdict:N_failed`.
10. Briefs: main pass (`:581-628`, top-scored live-only or oldest-unreviewed-first past 48h via `oldestForBrief` `:584-586` — `brief_age:failed` on throw, falls back to top-scored — with `selectBareRow` `:263` failures named `bare_select:failed`) plus extra pass (`:635-670`) when the oldest unreviewed exceeds 48h (live oldest 190.2h, so both fire), via `buildBriefPrompt` (`:208`), `parseBriefJson` (`:219`), `insertBrief` (`:230`, never stores empty); poison rotation via `rememberBriefSkip` (`:257`, in-memory per isolate — see F4) and `brief:skipped`; failures `brief:failed`. Manual runs skip briefs by deadline (`:351`, `POST /run` `:762`).
11. Finish: `:673` single `finish("ok", …)` carrying mode/pause/stale/src/verdict/prepass markers; errors finish `error` plus `console.error("cron-failed", …)` (`:681`, also `:693`, `:760`). `scheduled` `:687-695`, `POST /run` `:752-762`.
12. Vet — the ONLY path from unreviewed to vetted, one sentence: a manual dashboard press — `public/app.js:587` `vetOpportunity` → `:563` `fetchDetailForWrite` (token-gated detail fetch, warms N+1 `:571`, cold-boot guard `:570`) → `:592` inner → `:508` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`:599-602`) with auto-advance (`:607` via `:767`). Starter variant `:611-692` (vet + planned starter + flip to `testing`, resume-safe via `:624` `findPlannedStarter`) and Kill `:694-723` (`[date killed]` + post-mortem); `V` vets at `:783`; drawer Vet at `:1155`. One human verdict per keypress, no bulk path.
13. API writes: `updateOpportunity` (`functions/api/[[path]].js:237`, text bounds, status/range guards, tagless-clear 400 `:271-274` against `VERDICT_TAG` `:90`, killed-clear post-mortem guard `:282-288` via `KILLED_TAG_TAIL` `:95`, `effectiveScore` `:290`); `createExperiment` (`:378`, closure gate `:389-394`, stamps `:398-399`, `revenue_source` retry `:420-429` with `dropped` `:429`) / `updateExperiment` (`:441`, stamps `:467`, gate `:468-473`, retry `:497-509`, `dropped` `:508`, ledger via shared `outcomeLedgerLine` `:103-108`); any other missing column 503s naming only it (`:737`, `missingColumnOf` `:122-126`) — on the live old schema that is every experiment write, see F2.
14. Health: `functions/api/[[path]].js:526-709` — 12 probes (`:546-558`) in one batch (`:560`) with per-probe isolation fallback (`:569-598`, failures named in `health_probe_failures` + column-only `health_probe_detail` `:583-587`), lazy self-migration (`:607-623` via `ensureMoneyColumns` `:36-77` — partial-failure gap, see F7), `last_vetted`/`last_verdict` parsed in JS (`:657-671`) from 500-char tails (`:558`, `LIMIT 2000`), `inflow_paused` derived from the unreviewed probe (`:700`); payload `:709` (`ok:false` + nulls only on total outage; `db` is `up` when any probe answers). Dashboard paints pill (`public/app.js:1011-1040`, backlog `:341` + verdict `:359` bits, probe names `:1027-1028`), review queue (`:234-272`), experiments header with `· $V lifetime` (`:958`), and refreshes health on every action (`refreshTargets`, `:1702-1706`).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 4.6) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (190.2h, +15.8h since the brief's 08:45 reading at 174.4h). Second: hop 10 produced zero net briefs across ~14h and ≥2 cron ticks while 19 rows stand bare (F4). Third: every experiment write 503s on the live old schema and every money leg reads null because the self-migration is disabled (F2).

## 3. Health signals measured here

- Test suite: `npm test` (the 7 files in `package.json:8`) → exit 0 this session, run twice (once directly, once via a sandbox script for the summary lines). TAP: `# tests 514`, `# suites 158`, `# pass 514`, `# fail 0`, `# cancelled 0`, `# skipped 0`, `# todo 0`, `# duration_ms 90547.4`. The denominator is asserted, not assumed: 514 ran, 0 failed — a zero-test pass would have shown `# tests 0`. Windows baseline in `_night/tests-baseline.log:284-291` reads 514/158/514/0 at 90288.5ms — same shape, same count (+10 tests / +3 suites vs round 7's 504/155: the round-7 pins).
- Parse/compile: no Python sources — `git ls-files` (42 files, read in full) lists zero `*.py`, so `python3 -m compileall` has nothing to compile. JS parse is pinned by the green suite itself (every shipped file imported) plus the `dashboard-appjs parse gate` suite (`deploy/deploy.test.mjs:137-162`, green) and `verify.sh:36-52` (`node --check` on the served bundle). Positive control for the inventory: the same `git ls-files` output lists all shipped/test JS files plus dotfiles, so the zero-`.py` is real.
- Dead code: none material in shipped source. `appendKeepNewest` (`worker/src/lib.js:51-54`) is test-only by documented design (pinned JS spec of the SQL `substr(notes || ?, -8000)` idiom, `:49-50`), not dead; every worker helper has 2+ call sites. `console.*` in shipped source is only the three deliberate `cron-failed` tail markers (`worker/src/index.js:681,693,760`) plus the design comment (`:677-680`). Control: the same regex search matches those lines, so no hidden logging.
- Duplicated logic: shared singles verified — `effectiveScore`/`tokensMatch`/`clamp10` (`worker/src/lib.js`, API import `functions/api/[[path]].js:10`, worker import `worker/src/index.js:160`), `vettedNotes`/`fetchDetailForWrite`, `buildBriefPrompt`/`parseBriefJson`/`insertBrief`, `buildClassifyPrompt`, `flushVerdictWrites`, `outcomeLedgerLine` (`:103-108`), one migration script (`deploy/apply-d1-migrations.sh`, called by `deploy/deploy.sh:40` and CI `:33-34`), one rev-stamp helper (`deploy/stamp-worker-rev.sh`, called by `deploy/deploy.sh:73` and CI `:40-41`). Remaining mirrors are documented-deliberate: `centsDollars` ↔ dashboard `moneyCents` (`functions/api/[[path]].js:98-99` comment), SQL `substr(notes || ?, -8000)` ↔ `appendKeepNewest`, `VERDICT_TAG` boolean gate (`:90`) vs health `matchAll` date-capture (`:663-668`, gate vs capture).
- TODO density: zero in shipped source. Regex search `TODO|FIXME|HACK|XXX` over `worker/src` + `functions/api` + `public/app.js` + `d1` + `deploy` + `scripts` hits nothing (literal-mode search would also hit nothing — the regex mode was used deliberately after catching that); control: the same tool+mode matches `UNREVIEWED` 5+ times in `worker/src/index.js` alone, so the zero is real. Repo-wide hits are prose in `docs/audits/*.md` only.
- Matcher honesty (the brief's check-that-matches-nothing rule): every absence above carries its positive control in the same bullet. The vet-path absence (F1) is held to the same bar: case-insensitive `vetted` over `worker/src/index.js` hits nothing while the identical search over `functions/api/api.test.mjs` hits 100+ lines, so the worker really never vets.
- Surface rules: all pass, no finding. Light-only (`public/styles.css:4` `color-scheme: light`; the only `color-scheme` match in `public/`; no `prefers-color-scheme`, `data-theme`, or toggle in any shipped `public/` file — the strings appear only as guard assertions in `public/tab-icon.test.mjs` and `public/dashboard.test.mjs`); favicon linked with theme-color (`public/index.html:9-10`, byte-identical in `_night/live/aimoney_pages_dev.html:9-10`); `public/favicon.svg` is one line, 32×32 `$` on `#0d6b3f` (~300 bytes, well under 2 KB); brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suite `public/tab-icon.test.mjs` (113 lines) green inside the exit-0 run. First five seconds: the static shell renders brand + `Every way to make money with AI, ranked.` + lede with zero JS; counts fill from `…` placeholders, then the last-good snapshot (stale-marked past one 6h tick), then live refresh. Live HTML now carries round 5's `meter-col` phone hiding (`_night/live/aimoney_pages_dev.html:64-67`), matching repo `public/index.html:64-67`.
- Tree: HEAD `a738d37` (`git rev-parse --short HEAD`); `git log --oneline -8` shows round 7 → round 6 (`efcd038`, the live rev) → round 5 → round 4 → round 3 → round 2 → f43c623 → a2a48d3. `git status --short` was empty before this file was written. Line endings pinned LF: `git ls-files --eol` shows all 42 tracked files `i/lf w/lf` under `.gitattributes:4` (`* text=auto eol=lf`); the CRLF warning in the brief does not apply to this checkout.
- Sizes (`wc -l`): shipped `public/app.js` 1749, `functions/api/[[path]].js` 740, `worker/src/index.js` 767 (+4 vs round 7: the rev stamp), `worker/src/lib.js` 79; tests outweigh shipped code (`api.test.mjs` 2689, `dashboard.test.mjs` 2363, `index.test.js` 2287).
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (time `2026-09-21T22:35:57Z`, rev `efcd038…`, one commit behind HEAD): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:4.6, oldest_unreviewed_age_h:190.2, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, inflow_paused:true, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. The brief's 08:45 measurement read the same 27/11/19 at 174.4h — nothing moved in ~14h except the age (+15.8h) while cron stayed healthy.
- Round-7 task verification (all three landed, suite green): received-only revenue label `public/app.js:1401` + README one-liner (`README.md:74`); worker-never-vets static guard `worker/src/index.test.js:2280-2284` (with `runResearch` positive control); worker rev stamp (`worker/src/rev.js`, `deploy/stamp-worker-rev.sh`, served at `worker/src/index.js:704-713`, gated in `deploy/verify.sh:108-136`).

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 190.2h / 7.9d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `last_verdict` null, `hours_since_last_ok_run` 4.6, `inflow_paused` true. Was at 08:45: same 11 unreviewed at 174.4h — seven night rounds have shipped against this repository and the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none — no `won`/`lost` row exists (`experiments_by_status:{planned:2}`, `decisions_last_7d:0`), and the money legs read null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter, run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment a human records the first verdict. The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.9 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:587` `vetOpportunity` → `:592` inner → shared `vettedNotes` (`:508-511`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id` (`:599-602`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:525`: `— UNREVIEWED, scores capped until a human vets it`) and a case-insensitive `vetted` search over `worker/src/index.js` + `worker/src/lib.js` hits nothing — with the positive control that the identical search over `functions/api/api.test.mjs` hits 100+ lines, so the absence is real, not a broken matcher. Vet-round report as requested: path `public/app.js:587`; cause human-gated-by-design — there is no failing test because nothing is broken, and the passing real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs:2187`, green inside the exit-0 run) drives the real `PATCH` → `GET /api/health` against a stubbed D1 and is the positive proof; a single run of the path (one Vet press) produces exactly unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today. Health already dates the stall without being told: `last_vetted`/`last_verdict` (`functions/api/[[path]].js:657-671`), both null live — and the dashboard paints it (`verdictRecencyBit`, `public/app.js:359-372`, wired into the pill `:1024-1038`; the worker half is now pinned too, `worker/src/index.test.js:2280`).

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:447-449`, classify skipped `:461-462`, health bit `:700` true live), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change for the verdict itself — the two-minute path (`V`/`K` + auto-advance, one verdict per keypress, token-gated, N+1 prefetch `public/app.js:571`, detail-before-write `:563-585`, cold-boot guard `:570`) already ships with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines (`docs/OWNER-ACTIONS.md:7-20`). Touch no repo files for this finding. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null and every experiment write 503s — the columns never reached live D1 and the migration is disabled

Evidence. Live health names it: `health_probe_failures:["revenue_week","revenue_lifetime"]`, `health_probe_detail:{revenue_week:"revenue_cents", revenue_lifetime:"revenue_cents"}`, `schema_missing_columns:["revenue_cents","spent_cents"]`, `schema_migration:"disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)"` (`_night/live/aimoney_pages_dev_api_health.html:1`). The probes are the only two `SUM(revenue_cents|spent_cents)` readers (`functions/api/[[path]].js:553-554`); `d1/schema.sql:62-64` declares the columns but is `CREATE TABLE IF NOT EXISTS` (no-op on live); the ALTERs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied by the shared `deploy/apply-d1-migrations.sh:13-28` from both `deploy/deploy.sh:40` and CI `.github/workflows/deploy-worker.yml:33-34` ("Apply D1 migrations in sorted order") — which has never run against live (no repo secrets; fail-fast `:20-26`). The default-off self-migration already ships (`functions/api/[[path]].js:36-77`, `MONEY_PROBE_COLUMNS` `:28`, gate `:58` `String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1"`, ADD-only loop `:64-73`, once-per-isolate `:33-37`, transient-PRAGMA retry `:44-55`); unset leaves the table untouched and names the columns plus the disabled line, exactly as live shows. Stronger than previously stated: it is not just money display — every experiment INSERT/UPDATE carries the money columns (`:414-419`, `:491-496`), the retry covers only `revenue_source` (`:420-429`/`497-509`), and any other missing column 503s (`:737`), so on the live old schema the owner cannot log or Start any experiment at all, including the $0 starter. Migration-ask report: exact variable name `ALLOW_SCHEMA_MIGRATION`; the three results are pinned green in `functions/api/api.test.mjs:1737-1812` (`old schema + ALLOW_SCHEMA_MIGRATION=1 ends with every probed money column and reports numbers` `:1742`; var unset leaves the table unchanged and names the missing columns `:1769`; second run is a clean no-op `:1788`). Exact CI step already in place: `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: every revenue/spend figure is honestly unknown (null) instead of a number; no experiment can be logged, started, or closed until the columns exist, so the lane metric cannot move by any path; `decisions_last_7d` survives via its COUNT-only probe (`:552`) but reads a true 0.

Fix: owner-only, no code. Set `ALLOW_SCHEMA_MIGRATION=1` as a Pages production env var and retry the latest deployment (`docs/OWNER-ACTIONS.md:22-34`); the next health call adds exactly the two `ADD COLUMN` statements and reports numbers on the same call. Second track for `via {source}`: add `CF_API_TOKEN` + `CF_ACCOUNT_ID` repo secrets and redeploy (or run `deploy/deploy.sh` locally) so the revenue-source migration applies (`docs/OWNER-ACTIONS.md:35-41`); until then Wins keep $ but drop `via` with a named toast. Touch no repo files. Size S. Risk low (ADD COLUMN with DEFAULT cannot lose rows; default-off keeps unset behaviour byte-identical; second run is a duplicate-column no-op).

### F3. Live trails HEAD by one commit — round 7 is committed but not serving

Evidence. `git rev-parse --short HEAD` → `a738d37` (Night round 7); live `/api/health` `rev` → `efcd038…` (Night round 6), time `2026-09-21T22:35:57Z`. `git log --oneline -8` confirms the order. The gap narrowed by one since round 7 (live was `b70c707` then): round 5's phone meter-hiding is now observable live (`_night/live/aimoney_pages_dev.html:64-67` carries `class="meter-col"`, matching repo `public/index.html:64-67`). Still unserved: round 7's received-only revenue label, the worker-never-vets pin (test-only), and the worker `rev` key — so the live worker's vintage is still unknowable from any surface (F4's missing input).

Why it costs: the owner vets against a surface one round stale — including the `Revenue received ($)` guard against projection-as-result — and the F4 diagnosis still cannot read which worker code is ticking.

Fix: deploy, don't code. Pages auto-deploys on push — check the Pages dashboard's latest deployment for `a738d37` and let it finish; then confirm `release.json` revision and `/api/health` `rev` both read `a738d37` (`deploy/verify.sh` live-revision gate `:66-106`). Touch no repo files for this finding. Size S. Risk none (read-only verification; no D1 writes).

### F4. Brief output gap: 19 bare rows, zero net briefs across ~14h and ≥2 cron ticks

Evidence. `bare_without_brief` reads 19 in the brief's 08:45 measurement (174.4h) and 19 again in the 22:35 snapshot (190.2h) — while cron ticks every 6h (`worker/wrangler.toml:9`), the 12:00 and 18:00 UTC ticks fall inside the window, and `hours_since_last_ok_run` 4.6 proves a tick completed ~18:00Z. With the backlog far past 48h, both the main oldest-first pick (`worker/src/index.js:596-597`) and the extra pick (`:641-649`) fire per tick — up to 2 briefs per tick, and inflow is paused (0 new rows, opportunities steady at 27, and no code path deletes briefs), so the count can only fall. Two ticks × up to 2 briefs should have moved 19 → ≤15. It did not move at all. Ranked hypotheses, honest about what is unknowable offline: (1) the brief AI calls are failing (`brief:failed` in the run log); (2) the poison-row rotation cannot survive fresh isolates — `briefSkippedIds` is a bounded in-memory array (`:255-262`, "each isolate learns within one tick"), so every tick re-selects the same deterministic oldest-first poison row, and the extra pass re-selects the same deterministic second-oldest; two poison rows wedge both passes forever. Cause (1) vs (2) is readable in one place this audit cannot reach offline: the `error` column of the last runs (`/api/runs`, or the dashboard Research log) — and once F3's deploy lands, the worker `rev` key will say which code ticked.

Why it costs: owner time per verdict. A vet without a brief is a guess or a drawer round-trip (`No brief yet`, `public/app.js:230-231`); with 19/27 bare, the "minute a row from a phone" promise (`docs/OWNER-ACTIONS.md:15`) degrades toward the slowest rows first (oldest-first order meets thinnest evidence) — and if the brief pass is wedged rather than slow, the gap never closes no matter how long he waits.

Fix: two tracks. Diagnose (no code): the orchestrator's next snapshot round should fetch `/api/runs?limit=5` alongside `/api/health` (reads are public); the owner can read the same in the Research log's Error column (`brief:failed` → AI/outage; clean `ok` with `briefs:0` and `brief:skipped:<same ids>` repeating → wedge). Durable fix (implementer, offline-safe): make the poison skip survive fresh isolates — read recent `brief:skipped:<ids>` from the last few `agent_runs` error lines (a bounded public read, no schema change) and union them with the in-memory set in `selectBareRow` (`worker/src/index.js:263-275`), keeping the unfiltered fallback when everything is skipped; pin in `worker/src/index.test.js` in the existing stubbed-D1 style (a row that yields an empty brief twice is skipped on the next tick under a fresh isolate while a new bare row still briefs; run log still names skips). Size M. Risk low-medium (one extra bounded D1 read per tick; selection semantics otherwise unchanged; the wedge it fixes is proven structurally even if the live cause turns out to be AI failure, in which case the change is harmless).

### F5. The experiment modal has two spend fields and only one is measured — spend typed into `Spent` never reaches `spent_total`

Evidence. `public/app.js:1400` renders legacy free-text `Spent` (`m-spent` → `spent`, `:1418`) directly above `:1402` `Precise spend ($)` (`m-spend` → `spent_cents`, `:1421`), with no hint which one counts. Only `spent_cents` feeds `spent_total` (health probe `functions/api/[[path]].js:554`: `SELECT … SUM(spent_cents) … FROM experiments`) and the board (`public/app.js:982`: `` `${moneyCents(e.spent_cents)} spent` ``). So `$45` typed into the first, familiar field records zero measured spend — the money-path silent drop: the figure enters the modal and never reaches the number that proves it. This is the spend-side twin of round 7's F5 (revenue), which shipped the `Revenue received ($)` label (`:1401`); the spend side was left ambiguous. Today no wrong figure exists (the columns do not even exist live — writes 503, F2), but the shape allows it with one keystroke the moment the switch flips.

Why it costs: trust in the spend half of the one money number the lane will ever show, and wasted owner keystrokes that measure nothing. The first `$V lifetime`/`spent` pair he sees must both be real.

Fix: keep the probe and the API (request shapes and payload keys byte-identical), but disambiguate the modal in the existing builder (`public/app.js:1400-1402`): hint on `Precise spend ($)` that it counts toward lifetime spend on save (mirroring `:1401`'s revenue hint), and relabel or hint the legacy `Spent` as free text that is not counted; pin in `public/dashboard.test.mjs` in the suite's source-shape style (labels + hint present, payload keys unchanged) plus a README one-liner beside the money-column docs (`README.md:74`). Check the drawer spend display for the same ambiguity while there (read-only check; change only the label if it mirrors the modal). Size S. Risk low (label/copy only; probes, guards, and request shapes byte-identical).

### F6. Classify returning garbage reads as a clean tick — zero usable verdicts carry no marker

Evidence. When `fresh.length > 0` and inflow is open, `worker/src/index.js:468-472` runs the classify AI call and `parseJsonLines` (`worker/src/lib.js:64-79`) drops every unparseable line. If all lines drop, `verdicts` is `[]`, the validation loop (`:487-565`, `seen` at `:477`) does nothing, `flushVerdictWrites([])` (`:301-317`) returns 0, and the tick finishes `ok` at `:673` — whose marker line carries `src_fail`, `verdict:N_failed`, `prepass…`, `brief:failed`, but no `classify:` marker of any kind (verified: `classify` appears in `worker/src/index.js` only in comments, the prompt builder name, and the `/debug-classify` route). An AI throw finishes `error` (named), and paused/quiet ticks skip classify by design (logged `inflow:paused` / `no fresh signals`, `:673-674`) — the silent shape is specifically AI-success-with-garbage: signals stay `processed = 0` for retry (correct) but the run log gives no hint that the model, not the queue, is the problem. Partial drops (`seen.size < fresh.length`) are equally silent. This is the brief's check-that-matches-nothing shape in shipped code: a parser that matched nothing reports success.

Why it costs: every future triage stall starts with "is the model answering garbage?" asked into the void; today that question cannot even be asked from the log, and the F4-class of diagnosis (which pass is failing) stays blind on the classify leg.

Fix: after the validation loop, when classify actually ran (fresh non-empty, `maxNewThisRun > 0`, no throw) and `seen.size === 0`, append `classify:empty` to the finish line (`:673`); keep paused ticks (`inflow:paused`), quiet ticks, and error ticks byte-identical. Pin in `worker/src/index.test.js` in the existing stubbed-AI style (AI stub returns garbage → status `ok`, signals unprocessed, error contains `classify:empty`; paused tick and empty-fresh tick carry no new marker — the positive controls). Size S. Risk low (additive log token; no D1, schedule, or verdict-path change).

### F7. A set-but-failed migration never retries and names nothing — the ALTER error is swallowed into a cached shrug

Evidence. In `ensureMoneyColumns` (`functions/api/[[path]].js:36-77`), each missing column's `ALTER TABLE` runs inside a `try` whose `catch` (`:67-72`) keeps only the duplicate-column no-op and drops every other error message. When `ALLOW_SCHEMA_MIGRATION=1` is set but an ALTER fails (transient D1 blip, lock, permission), `stillMissing` is non-empty and the outcome is cached as decided (`:74-76`) — the next call returns the cached failure and never retries, unlike the transient-PRAGMA path which deliberately does not cache (`:44-55`). Worse, the payload spread (`:709`) attaches `schema_missing_columns` + the migration line only when `schemaNote.disabled` is true — a set-but-failed outcome (`{missing:[…], migrated:false, disabled:false}`) attaches neither, so the reader sees only the probe failures + column detail with no hint a migration was attempted and failed. The failure-visibility gap is narrow (the ALTER path is exercised only on old schemas with the var set) but it sits exactly on the F2 critical path: the one call that is supposed to heal the money legs can fail silently and then never try again for the isolate's lifetime.

Why it costs: a transient D1 hiccup on the heal call converts a self-healing migration into a permanently dark money panel with no next action — the owner did the right thing (set the switch) and the endpoint shows him a probe name instead of "migration failed, it retries next call".

Fix: in `functions/api/[[path]].js:63-76`, do not cache when `stillMissing` is non-empty (return uncached so the next call re-PRAGMAs and retries — the same rule the PRAGMA path already follows); and when the var is set but columns remain missing, attach `schema_missing_columns` plus a `schema_migration:"failed (…)"`-style line that says the attempt failed and will retry, distinct from the `disabled …` line. Pin in `functions/api/api.test.mjs` in the existing stubbed-D1 style (ALTER fails once then succeeds → second call migrates and reports numbers; ALTER always fails → payload names the columns plus the failed line; var-unset and clean-migrate cases byte-identical). Size S. Risk low (default-off path untouched; set-path only gains a retry and a line).

## 5. What NOT to change

- The agent never vets or moves status — quoted deliberate: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`) and `README.md:72` (`researching → testing is a human decision … The agent never moves status`). Do not add an automated vet, auto-testing flip, or bulk verdict path — not even for the F1 queue. The brief's rank-(2) asks what the worker could decide without him; the answer here is nothing, until he writes the rule himself.
- One human verdict per keypress, no bulk path — quoted: `README.md:71` (`one human verdict per keypress, no bulk path`). Pinned both sides now (`worker/src/index.test.js:2280`, `functions/api/api.test.mjs:2291`); do not implement either half away.
- Migration stays default-off, ADD-only, triggerless — quoted: `functions/api/[[path]].js:21-23` (`applies exactly the ADD COLUMN statements … one at a time (ADD-only, no endpoint)`). Never add DROP, column rewrite, data rewrite, DELETE, a second trigger, or a URL that migrates. Unset behaviour stays byte-identical to today. F7 adds only a retry + a line on the already-opted-in path.
- Lifetime revenue sums all rows — quoted deliberate: `README.md:75` (`cents from all rows regardless of status (open rows count once recorded)`). F5 hardens the spend labels only; do not "fix" it by bounding the probe to won/lost.
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change). His one line: set `ALLOW_SCHEMA_MIGRATION=1` as a Pages production env var and retry the latest deployment — money legs fill and experiment writes start working on the next health call.
- The 11 verdicts are the owner's dashboard presses (need `ADMIN_TOKEN` + per-row judgment), not agent writes. His one line: open Needs-review oldest-first and press V or K on each of the 11 rows.
- The $0 starter and its close are the owner's (Start the seed sprint or the drafted census `docs/ZERO-SPEND-STARTER.md:9-42`, run it, Win/Lose with a human-entered $ amount + post-mortem). Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure.
- Deploying round 7 is the owner's (F3). His one line: live serves `efcd038` while `a738d37` is committed — check the Pages dashboard's latest deployment and let it finish.
- The F4 cause needs live runs data this round cannot reach (no network by design). Orchestrator one line: fetch `/api/runs?limit=5` alongside `/api/health` in the next snapshot round (public read); owner equivalent: open the Research log and read the Error column on the last two ticks.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action.
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment. Never spend money, create accounts, send anything outward, accept terms, solve captchas, fabricate reviews/receipts/testimonials, scrape or message against a platform's terms, or give financial advice/implied return.
- House rules honored by this audit: never read `.env`/secrets/tokens (`.gitignore:4-5` covers `.env`/`.env.*`, pinned by `deploy/deploy.test.mjs`); no edits to existing files (this findings file only); LF endings pinned (`.gitattributes:4`).

## 6. Proposed next tasks (2–4)

Disposition of the focus-named work (it outranks anything found, so it is answered first): the vet-path round resolves to human-by-design (F1: path `public/app.js:587`, no failing test because nothing is broken — the cause is the absence of a human press, proven by the worker's zero `vetted` strings with a positive control and pinned by `worker/src/index.test.js:2280` — one Vet press yields exactly unreviewed −1 / `vetted_last_7d` +1 / `last_verdict` = today per `functions/api/api.test.mjs:2187`, and health already dates the stall via `last_vetted`/`last_verdict`, painted in the pill). Every other focus-named code item is verified shipped with the suite green: probe isolation + failure naming + `db`-by-answer (`functions/api/[[path]].js:542-598,709`), deploy convergence via one shared script (`.github/workflows/deploy-worker.yml:33-34`, `deploy/apply-d1-migrations.sh:13-28`, convergence suite `d1/migrations.test.mjs:38-70`), the default-off self-migration with its three results (`functions/api/[[path]].js:36-77`, suite `functions/api/api.test.mjs:1737-1812`, variable `ALLOW_SCHEMA_MIGRATION`), the sub-minute human vet path (keys + prefetch + auto-advance + cold-boot guard), the received-only revenue label, the dropped-source flag, partial-source markers, the worker `rev` key + gate, and the $0 starter draft. The unshipped remainder is human/owner work (11 verdicts, the migration switch, the $0 sprint, the deploy, the F4 runs read) — not implementer-completable offline, so it lives in §5 as one line each. The tasks below are the remaining offline-completable items, most valuable first.

### Task 1. Remember poison brief rows across isolates so one bad row cannot wedge the oldest-first pass

Owns: `worker/src/index.js`, `worker/src/index.test.js` (pin only). Context (F4): bare-without-brief sits at 19 across ~14h and ≥2 cron ticks while both brief passes fire per tick; the poison rotation is a per-isolate memory array (`worker/src/index.js:255-262`) that a fresh isolate forgets, so a poison oldest row is re-selected every tick forever.

Acceptance: recent `brief:skipped:<ids>` from the last few `agent_runs` error lines (bounded read, no schema change) union with the in-memory set in `selectBareRow`, keeping the unfiltered fallback when everything is skipped; a row that yields an empty brief twice is skipped on the next tick under a fresh isolate while a new bare row still briefs; run log still names skips; paused/quiet/error ticks unchanged; full `npm test` green.

### Task 2. Label which experiment spend field is measured so typed spend stops vanishing from lifetime totals

Owns: `public/app.js`, `public/dashboard.test.mjs` (pin only), `README.md` (one line). Context (F5): the modal renders legacy free-text `Spent` (`public/app.js:1400`) above `Precise spend ($)` (`:1402`) with no hint which counts, while only `spent_cents` feeds `spent_total` (`functions/api/[[path]].js:554`) — the spend-side twin of the shipped revenue fix.

Acceptance: `Precise spend ($)` carries a hint that it counts toward lifetime spend on save and the legacy field is marked as uncounted free text; request shapes and payload keys byte-identical; dashboard-suite source-shape cases pin the labels + hint; a README one-liner beside the money docs states the rule; the drawer spend display checked for the same ambiguity; full `npm test` green and the tab-icon suite stays green.

### Task 3. Name empty classify output on the run log so model garbage stops reading as a clean tick

Owns: `worker/src/index.js`, `worker/src/index.test.js` (pin only). Context (F6): when the classify call succeeds but every line drops in `parseJsonLines`, the tick finishes `ok` with no marker — the check-that-matches-nothing shape; `classify` appears in the worker only in comments, the prompt builder, and the debug route.

Acceptance: when classify ran on non-empty fresh with inflow open and zero usable verdicts result, the finish line carries a `classify:empty` token; paused ticks (`inflow:paused`), quiet ticks, and error ticks byte-identical; stubbed-AI tests pin the marker plus the paused/quiet negative controls; full `npm test` green.

### Task 4. Retry a failed self-migration ALTER next call and say it failed instead of caching the shrug

Owns: `functions/api/[[path]].js`, `functions/api/api.test.mjs` (pin only). Context (F7): with the switch set, a failed ALTER is swallowed (`:67-72`) and cached as decided (`:74-76`), and the payload names nothing (`:709` spread requires `disabled`); the PRAGMA path already follows the don't-cache-inconclusive rule (`:44-55`).

Acceptance: a partially/fully failed ALTER set is not cached — the next call re-PRAGMAs and retries; when the var is set but columns remain missing, the payload carries `schema_missing_columns` plus a failed-and-retrying line distinct from the `disabled …` line; tests pin fail-once-then-heal and always-fail shapes with the unset and clean-migrate cases byte-identical; full `npm test` green.
