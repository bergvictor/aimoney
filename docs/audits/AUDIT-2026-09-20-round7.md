# Audit 2026-09-20 round 7 — aimoney (read-only, track B)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r7` (`/mnt/c/coding-projects/_workspace/trackb-aimoney-r7` on the orchestrator side). HEAD `efcd038` (Night round 6); live rev `b70c707` (Night round 4, two commits behind). No git writes, no network, no installs, no edits to existing files — only this file created at the root. Never read `.env`, secrets or tokens. One single-stage shell command per call, no inline code, no redirection (the first call of this session batched two stages and was denied; retried as single commands per the rule). No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` at the root (authoritative `git ls-files` inventory, 42 files); `README.md` read first, then the live snapshots in `_night/live/`, then source.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests (`README.md:3-15`, `worker/src/index.js:4-5`).
- Entry points: `public/index.html` + `public/app.js` (1749 lines, dashboard), `functions/api/[[path]].js:520` `onRequest` (740 lines, D1 REST API), `worker/src/index.js:685` (`scheduled` + `fetch`, 763 lines) + `worker/src/lib.js` (79 lines, shared pure helpers).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `/` + `POST /run`; live snapshot `_night/live/aimoney_pages_dev_api_health.html:1` at `2026-09-21T21:56:23Z`.
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + up to 2 briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:748-759`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema `:27` + shared migrations `:33-34` + worker `:38`) or manual `deploy/deploy.sh` (schema `:36` + shared `:40`, Pages `:66`, worker `:70`), gated by `deploy/verify.sh` (public reads only, no credentials).

## 2. End-to-end walk-through

Signal to decision. The hop where it most often goes silent is hop 12 (human Vet, frozen 7.9 days); hop 10 (brief output) is newly suspect — see F4.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:81` `redditSignals`, `:110` `githubSignals` via `timedJson` (`:37-47`, 6s timeout covering the body read). Per-query throws counted; all-failed names the source in `src_fail`, partial in `src_partial` (`:76-77`, `:105-106`, `:134-135`).
2. Store: `:363-368` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:369-372` (`phase:collected` / `phase:collected stale:N`).
3. Sweep: `:380-384` unprocessed signals older than 30d become noise (`state.stale`); a failed sweep is named (`stale:failed`, finish `:672`).
4. Take: `:386-388` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:400-427` via pure `exactUrlTarget` (`:173-183`); bounded `IN` selects (`:409-414`, failures named `prepass_select:failed`); a match links as supports (notes newest-kept `:423`, no status move); failed links/notes appends skipped and counted (`prepass:N_failed` `:421`, `prepass_notes:N_failed` `:423`).
6. Gates, read before classify: `:442-444` bare-without-brief > 10 caps inserts to 1/tick (`bare_count:failed` on throw); `:446-449` unreviewed > 10 pauses inflow to 0/tick (`maxNewThisRun = 0`, `unreviewed_count:failed` on throw). Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:460-473` `buildClassifyPrompt(opps, fresh)` (`:281`) + `aiComplete` (`:141`, budget `MAX_AI_CALLS = 4`, `:19`), parsed by `parseJsonLines` (`worker/src/lib.js:64`, NDJSON + `repairJson` for `\_`). Inflow-paused ticks skip classify entirely and still brief (finish line carries `inflow:paused`, `:672`).
8. Verdict loop `:486-564`: strict action enum (`:492`), supports-without-id becomes noise (`:499-501`), overflow stays unprocessed (`:503`). `new` INSERTs (`:514-528`) with an `UNREVIEWED` note (`:524`) and capped `effectiveScore` (`worker/src/lib.js:30`, cap 6000 `:23`); slug collision links as supports (`:535-550`, failed lookup named `slug_select:failed`, signal left unprocessed for retry).
9. Verdict flush: `:565` `flushVerdictWrites` (`:300-316`); failures as `verdict:N_failed`.
10. Briefs: main pass (`:580-627`, top-scored live-only or oldest-unreviewed-first past 48h via `oldestForBrief` `:585` — `brief_age:failed` on throw, falls back to top-scored — with `selectBareRow` `:262` failures named `bare_select:failed`) plus extra pass (`:634-668`) when the oldest unreviewed exceeds 48h (live oldest 189.6h, so both fire), via `buildBriefPrompt` (`:207`), `parseBriefJson` (`:218`), `insertBrief` (`:229`, never stores empty); poison rotation via `rememberBriefSkip` (`:256`, in-memory per isolate) and `brief:skipped`; failures `brief:failed`. Manual runs skip briefs by deadline (`:350`, `POST /run` `:758`).
11. Finish: `:672` single `finish("ok", …)` carrying mode/pause/stale/src/verdict/prepass markers; errors finish `error` plus `console.error("cron-failed", …)` (`:680`, also `:692`, `:756`). `scheduled` `:686-694`, `POST /run` `:748-759`.
12. Vet — the ONLY path from unreviewed to vetted, one sentence: a manual dashboard press — `public/app.js:587` `vetOpportunity` → `:563` `fetchDetailForWrite` (token-gated detail fetch, warms N+1 `:571`, cold-boot guard `:570`) → `:592` inner → `:508` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`:599-602`) with auto-advance (`:607` via `:767`). Starter variant `:653-692` (vet + planned starter + flip to `testing`, resume-safe via `:624` `findPlannedStarter`, advances focus `:690`) and Kill `:694-723` (`[date killed]` + post-mortem); `V` vets at `:783`; drawer Vet at `:1155`. One human verdict per keypress, no bulk path.
13. API writes: `updateOpportunity` (`functions/api/[[path]].js:237`, text bounds, status/range guards, tagless-clear 400 `:271-274` against `VERDICT_TAG` `:90`, killed-clear post-mortem guard `:282-288` via `KILLED_TAG_TAIL` `:95`, `effectiveScore` `:290`); `createExperiment` (`:378`, closure gate `:389-394`, stamps `:398-399`, `revenue_source` retry `:420-429` with `dropped` `:429`) / `updateExperiment` (`:441`, stamps `:467`, gate `:468-473`, retry `:497-509`, `dropped` `:508`, ledger via shared `outcomeLedgerLine` `:103-108`); any other missing column 503s naming only it (`:737`, `missingColumnOf` `:122-126`).
14. Health: `functions/api/[[path]].js:526-709` — 12 probes (`:546-558`) in one batch (`:560`) with per-probe isolation fallback (`:569-598`, failures named in `health_probe_failures` + column-only `health_probe_detail` `:583-587`), lazy self-migration (`:607-623` via `ensureMoneyColumns` `:36-77`), `last_vetted`/`last_verdict` parsed in JS (`:657-671`) from 500-char tails (`:558`, `LIMIT 2000`), `inflow_paused` derived from the unreviewed probe (`:700`); payload `:709` (`ok:false` + nulls only on total outage; `db` is `up` when any probe answers). Dashboard paints pill (`public/app.js:1011-1040`, backlog `:341` + verdict `:359` bits, probe names `:1027-1028`), review queue (`:234-272`), experiments header with `· $V lifetime` (`:958`), and refreshes health on every action (`refreshTargets`, `:1702-1706`).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 3.9) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (189.6h, +15.2h since the brief's 08:45 reading). Second, new this round: hop 10 produced zero net briefs across ~13h and ≥2 cron ticks while 19 rows stand bare (F4). Third: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F2) — and until it runs, money writes 503 too.

## 3. Health signals measured here

- Test suite: `npm test` (the 7 files in `package.json:8`) → exit 0 this session, run once. Full TAP: `# tests 504`, `# suites 155`, `# pass 504`, `# fail 0`, `# cancelled 0`, `# skipped 0`, `# duration_ms 90680.2`. The denominator is asserted, not assumed: 504 ran, 0 failed — a zero-test pass would have shown `# tests 0`. Windows baseline in `_night/tests-baseline.log:284-291` reads 504/155/504/0 at 90288.7ms — same shape, same count.
- Parse/compile: no Python sources — `git ls-files` (42 files, read in full) lists zero `*.py`, so `python3 -m compileall` has nothing to compile. JS parse is pinned by the green suite itself (every shipped file imported) plus the `dashboard-appjs parse gate` suite (`deploy/deploy.test.mjs:137-162`, green) and `verify.sh:36-52` (`node --check` on the served bundle). Positive control for the inventory: the same `git ls-files` output lists all 6 shipped/test JS files plus dotfiles, so the zero-`.py` is real.
- Dead code: none material in shipped source. `appendKeepNewest` (`worker/src/lib.js:51-54`) is test-only by documented design (pinned JS spec of the SQL `substr(notes || ?, -8000)` idiom, `:49-50`), not dead; every worker helper (`exactUrlTarget`, `agentMoneyEstimates`, `buildBriefPrompt`, `parseBriefJson`, `insertBrief`, `selectBareRow`, `buildClassifyPrompt`, `flushVerdictWrites`) has 2+ call sites. `console.*` in shipped source is only the three deliberate `cron-failed` tail markers (`worker/src/index.js:680,692,756`) plus the design comment (`:676-679`). Control: the same search matches those lines, so no hidden logging.
- Duplicated logic: shared singles verified — `effectiveScore`/`tokensMatch`/`clamp10` (`worker/src/lib.js`, API import `functions/api/[[path]].js:10`, worker import `worker/src/index.js:160`), `vettedNotes`/`fetchDetailForWrite`, `buildBriefPrompt`/`parseBriefJson`/`insertBrief`, `buildClassifyPrompt`, `flushVerdictWrites`, `outcomeLedgerLine` (`:103-108`), one paint helper `paintStartHere` (`public/app.js:161`), one migration script (`deploy/apply-d1-migrations.sh`, called by `deploy/deploy.sh:40` and CI `:33-34`). Remaining mirrors are documented-deliberate: `centsDollars` ↔ dashboard `moneyCents` (`functions/api/[[path]].js:98-99` comment), SQL `substr(notes || ?, -8000)` ↔ `appendKeepNewest`, `VERDICT_TAG` boolean gate (`:90`) vs health `matchAll` date-capture (`:663-668`, gate vs capture).
- TODO density: zero in shipped source. Search `TODO|FIXME|HACK|XXX` over `worker/src` + `functions/api` + `public/app.js` + `d1` + `deploy` hits nothing; control: the same tool+mode matches `UNREVIEWED` 30+ times across the same files, so the zero is real. Repo-wide hits are prose in `docs/audits/*.md` only.
- Matcher honesty (the brief's check-that-matches-nothing rule): every absence above carries its positive control in the same bullet. The vet-path absence (F1) is held to the same bar: the `vetted`-over-worker search hits nothing while the identical search over `functions/api/api.test.mjs` hits 20+ lines, so the worker really never vets.
- Surface rules: all pass, no finding. Light-only (`public/styles.css:4` `color-scheme: light`; the only `color-scheme` match in `public/`; no `prefers-color-scheme`, `data-theme`, or toggle anywhere); favicon linked with theme-color (`public/index.html:9-10`, byte-identical in `_night/live/aimoney_pages_dev.html:9-10`); `public/favicon.svg` is one line, 32×32 `$` on `#0d6b3f` (~300 bytes, well under 2 KB); brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suite `public/tab-icon.test.mjs` (113 lines) green inside the exit-0 run. First five seconds: the static shell renders brand + `Every way to make money with AI, ranked.` + lede with zero JS; counts fill from `…` placeholders, then the last-good snapshot (stale-marked past one 6h tick), then live refresh.
- Tree: HEAD `efcd038` (`git rev-parse --short HEAD`); `git log --oneline -8` shows round 6 → round 5 → round 4 (`b70c707`, the live rev) → round 3 → round 2 → f43c623 (starter advance, dropped-source flag, `.env` ignore, partial-source marker) → a2a48d3 → 84442f0. `git status --short` was empty before this file was written. Line endings pinned LF: `git ls-files --eol` shows all 42 tracked files `i/lf w/lf` under `.gitattributes:4` (`* text=auto eol=lf`); the CRLF warning in the brief does not apply to this checkout.
- Sizes (`wc -l`): shipped `public/app.js` 1749, `functions/api/[[path]].js` 740 (via read), `worker/src/index.js` 763, `worker/src/lib.js` 79; tests outweigh shipped code (`api.test.mjs` 2689, `dashboard.test.mjs` 2340, `index.test.js` 2257, `lib.test.js` 118, `tab-icon.test.mjs` 113, `migrations.test.mjs` 126, `deploy.test.mjs` 264).
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (time `2026-09-21T21:56:23Z`, rev `b70c707…`, two commits behind HEAD): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:3.9, oldest_unreviewed_age_h:189.6, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, inflow_paused:true, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. The brief's 08:45 measurement read the same 27/11/19 at 174.4h — nothing moved in ~13h except the age (+15.2h) while cron stayed healthy.

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 189.6h / 7.9d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `last_verdict` null, `hours_since_last_ok_run` 3.9, `inflow_paused` true. Was at 08:45: same 11 unreviewed at 174.4h — six night rounds have shipped against this repository and the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none — no `won`/`lost` row exists (`experiments_by_status:{planned:2}`, `decisions_last_7d:0`), and the money legs read null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter, run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment a human records the first verdict. The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.9 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:587` `vetOpportunity` → `:592` inner → shared `vettedNotes` (`:508-511`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id` (`:599-602`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:524`: `— UNREVIEWED, scores capped until a human vets it`) and a case-insensitive `vetted` search over `worker/src/index.js` + `worker/src/lib.js` hits nothing — with the positive control that the identical search over `functions/api/api.test.mjs` hits 20+ lines, so the absence is real, not a broken matcher. So `vetted_last_7d` 0 with `hours_since_last_ok_run` 3.9 is human-gated-by-design, not a broken automation. Vet-round report as requested: path `public/app.js:587`; cause human-gated-by-design — there is no failing test because nothing is broken, and the passing real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs:2187`, green inside the exit-0 run) drives the real `PATCH` → `GET /api/health` against a stubbed D1 and is the positive proof; a single run of the path (one Vet press) produces exactly unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today. Health already dates the stall without being told: `last_vetted`/`last_verdict` (`functions/api/[[path]].js:657-671`), both null live — and the dashboard now paints it (`verdictRecencyBit`, `public/app.js:359-372`, wired into the pill `:1024-1038`).

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:446-449`, classify skipped `:460-461`, health bit `:700` true live), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change for the verdict itself — the two-minute path (`V`/`K` + auto-advance, one verdict per keypress, token-gated, N+1 prefetch `public/app.js:571`, detail-before-write `:563-585`, cold-boot guard `:570`) already ships with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines (`docs/OWNER-ACTIONS.md:7-20`; note `:7` still says "oldest 7.5 days" while live reads 7.9d — refresh that line when touching the doc). Touch no repo files for this finding. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live health names it: `health_probe_failures:["revenue_week","revenue_lifetime"]`, `health_probe_detail:{revenue_week:"revenue_cents", revenue_lifetime:"revenue_cents"}`, `schema_missing_columns:["revenue_cents","spent_cents"]`, `schema_migration:"disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)"` (`_night/live/aimoney_pages_dev_api_health.html:1`). The probes are the only two `SUM(revenue_cents|spent_cents)` readers (`functions/api/[[path]].js:553-554`); `d1/schema.sql:62-64` declares the columns but is `CREATE TABLE IF NOT EXISTS` (no-op on live); the ALTERs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied by the shared `deploy/apply-d1-migrations.sh:13-28` from both `deploy/deploy.sh:40` and CI `.github/workflows/deploy-worker.yml:33-34` ("Apply D1 migrations in sorted order") — which has never run against live (no repo secrets; fail-fast `:20-26`). The default-off self-migration already ships (`functions/api/[[path]].js:36-77`, `MONEY_PROBE_COLUMNS` `:28`, gate `:58` `String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1"`, ADD-only loop `:64-73`, once-per-isolate `:33-37`, transient-PRAGMA retry `:44-55`); unset leaves the table untouched and names the columns plus the disabled line, exactly as live shows. Migration-ask report: exact variable name `ALLOW_SCHEMA_MIGRATION`; the three results are pinned green in `functions/api/api.test.mjs:1737-1921` (`old schema + ALLOW_SCHEMA_MIGRATION=1 ends with every probed money column and reports numbers` `:1742`; var unset leaves the table unchanged and names the missing columns `:1784-1785`; second run is a clean no-op `:1792-1812`). Exact CI step already in place: `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: every revenue/spend figure is honestly unknown (null) instead of a number; experiment money writes 503 (`missingColumnOf` `:122-126` → `:737`, only `revenue_source` retries `:420-429`/`497-509`) so the owner cannot even record a $ figure until the columns exist; `decisions_last_7d` survives via its COUNT-only probe (`:552`) but the money half of the lane stays dark.

Fix: owner-only, no code. Set `ALLOW_SCHEMA_MIGRATION=1` as a Pages production env var and retry the latest deployment (`docs/OWNER-ACTIONS.md:22-34`); the next health call adds exactly the two `ADD COLUMN` statements and reports numbers on the same call. Second track for `via {source}`: add `CF_API_TOKEN` + `CF_ACCOUNT_ID` repo secrets and redeploy (or run `deploy/deploy.sh` locally) so the revenue-source migration applies (`docs/OWNER-ACTIONS.md:35-41`); until then Wins keep $ but drop `via` with a named toast. Touch no repo files. Size S. Risk low (ADD COLUMN with DEFAULT cannot lose rows; default-off keeps unset behaviour byte-identical; second run is a duplicate-column no-op).

### F3. Live trails HEAD by two commits — rounds 5+6 are committed but not serving

Evidence. `git rev-parse --short HEAD` → `efcd038` (Night round 6); live `/api/health` `rev` → `b70c707b…` (Night round 4), time `2026-09-21T21:56:23Z`. `git log --oneline -8` confirms the order (`efcd038` → `3ebef9a` round 5 → `b70c707` round 4). Cross-check from the served HTML: live `_night/live/aimoney_pages_dev.html:64-67` renders plain `<th title="…">Value</th>` meter columns while repo `public/index.html:64-67` carries `class="meter-col"` (round 5's phone work) — the live bytes predate it. So the verdict-age pill (round 6), the dropped-source toast, and the phone meter hiding are audited here but not observable live.

Why it costs: the owner vets against a surface two rounds stale — including the `never vetted` stall paint that is the point of F1's visibility — and every audit-to-live comparison carries a two-commit skew that wastes the next reader's time.

Fix: deploy, don't code. Pages auto-deploys on push — check the Pages dashboard's latest deployment for `efcd038` and let it finish; then confirm `release.json` revision and `/api/health` `rev` both read `efcd038` (`deploy/verify.sh` live-revision gate `:66-106`). Touch no repo files for this finding. Size S. Risk none (read-only verification; no D1 writes).

### F4. Brief output gap: 19 bare rows, zero net briefs across ~13h and ≥2 cron ticks

Evidence. `bare_without_brief` reads 19 in the brief's 08:45 measurement and 19 again in the 21:56 snapshot — while cron ticks every 6h (`worker/wrangler.toml:9`) and `hours_since_last_ok_run` 3.9 proves a tick completed ~18:00Z. With the backlog at 189.6h (> 48h), both the main oldest-first pick (`worker/src/index.js:595-596`) and the extra pick (`:640-648`) fire per tick — up to 2 briefs per tick, and inflow is paused (0 new rows), so the count can only fall. Two ticks × ≥1 brief each should have moved 19 → ≤17. It did not move at all. Ranked hypotheses, honest about what is unknowable offline: (1) the brief AI calls are failing (`brief:failed` in the run log); (2) the live worker predates the poison-row rotation and is wedged on one poison row (pre-rotation code re-selected the identical row every tick); (3) the rotation itself cannot help across ticks — `briefSkippedIds` is a bounded in-memory array (`:253-261`, "each isolate learns within one tick"), so a fresh isolate re-selects the same deterministic oldest-first poison row every tick, and only the extra pass (one row) can still drain. Cause (1) vs (2)/(3) is readable in one place this audit cannot reach offline: the `error` column of the last runs (`/api/runs`, or the dashboard Research log).

Why it costs: owner time per verdict. A vet without a brief is a guess or a drawer round-trip (`No brief yet`, `public/app.js:230-231`); with 19/27 bare, the "minute a row from a phone" promise (`docs/OWNER-ACTIONS.md:15`) degrades toward the slowest rows first (oldest-first order meets thinnest evidence) — and if the brief pass is wedged rather than slow, the gap never closes no matter how long he waits.

Fix: diagnose before coding — no code change is justified until the runs log names the cause, and the focus rightly bans another visibility widget. The orchestrator's next snapshot round should fetch `/api/runs?limit=5` alongside `/api/health` (reads are public); the owner can read the same in the Research log's Error column (`brief:failed` → AI/outage; clean `ok` with `briefs:0` → selection wedge). If the log shows a wedge, the durable fix is persistent skip state, not a bigger memory array. Touch no repo files for this finding. Size S (one read). Risk none (read-only).

### F5. The experiment modal accepts Revenue ($) on any status — a projection can render as earned lifetime revenue

Evidence. `public/app.js:1401` renders `Revenue ($)` unconditionally and `:1420` ships it as `revenue_cents` for every status (`planned`, `running`, `paused` included — the status select is `:1394-1396` with no coupling to the money fields). Health's lifetime probe sums with no status bound (`functions/api/[[path]].js:554`: `SELECT … SUM(revenue_cents) … FROM experiments`), and the dashboard renders it as earned money (`public/app.js:958`: `` `· ${moneyCents(revenueTotal)} lifetime` ``). So a target figure — or a typo — typed into Revenue on a `planned` row lands in `revenue_total` and the `· $V lifetime` leg as if it arrived. Today the field is empty everywhere (the columns do not even exist live), so no phantom money exists yet; the shape allows it with one keystroke, against the money directive's "never present a projection as a result". The probe side is documented-deliberate (`README.md:75`: "cents from all rows regardless of status (open rows count once recorded)" — mid-run revenue is legitimate), so the probe must not change; the hole is the modal, which never says "received".

Why it costs: trust in the one money number the lane will ever show. The first `$V lifetime` the owner sees must be money received; a label that invites targets next to a probe that sums everything is how a wrong number gets published.

Fix: keep the probe and the API (mid-run revenue stays recordable), but label the modal field `Revenue received ($)` with a one-line hint that it counts toward lifetime revenue on save, in the existing modal builder (`public/app.js:1401`); pin it in `public/dashboard.test.mjs` in the suite's source-shape style (field label + hint present, payload key unchanged) plus a README one-liner beside the money-column docs. Size S. Risk low (label/copy only; request shapes, probes, and guards byte-identical).

### F6. The worker-never-vets invariant has zero test pins

Evidence. "The agent PROPOSES; the human owns the testing workflow" (`worker/src/index.js:4-5`) plus "one human verdict per keypress, no bulk path" (`README.md:71`) is the lane's load-bearing invariant — F1's entire verdict rests on it. Yet no test asserts it: `vetted` (any case) appears nowhere in `worker/src/index.test.js` (empty search; control: the same search hits 20+ lines in `functions/api/api.test.mjs`), and no `describe` in the worker suite (`index.test.js:11-2061`, 40 blocks) guards the invariant. The API side is pinned (`api.test.mjs:2187` real-path Vet test, `:2221` tagless 400, `:2316` killed-guard); the worker side relies on review discipline alone. A future "helpful" automation that clears `UNREVIEWED` in the worker would pass all 504 tests today.

Why it costs: the invariant is what makes every verdict trustworthy and every Kill's post-mortem real. Unpinned, it erodes by accident — one well-meant worker commit, and verdicts stop meaning "a human looked".

Fix: add a static-guard `describe` to `worker/src/index.test.js` in the existing style (`:36-63`): read `worker/src/index.js` as text, assert it is non-trivial (contains `runResearch` — the positive control, so the test cannot pass on an empty file), and assert it contains no `vetted` (case-insensitive) and no `UNREVIEWED`-clearing replace. Full `npm test` green. Size S. Risk none (test-only; no shipped file changes).

### F7. The research worker's serving revision is unobservable — `GET /` reports no rev

Evidence. The worker root answers `{ ok, agent, last_run }` (`worker/src/index.js:701-710`) — no revision key, unlike Pages where `release.json` + health `rev` + the verify gate (`deploy/verify.sh:66-106`) pin exactly what is serving. Combined with F3's class of problem on the worker side (CI `.github/workflows/deploy-worker.yml:38` deploys the worker but has never run for lack of secrets, `:20-26`), the live worker's code vintage is unknowable from any surface: `hours_since_last_ok_run` proves A worker ticks, not WHICH code. That is exactly the missing input to F4's hypotheses (1) vs (2).

Why it costs: every future worker-side anomaly starts with "which code is even running?" asked into the void. One stamped string turns that from archaeology into a glance.

Fix: stamp the deployed SHA into the worker at deploy time and serve it from `GET /` (default `"unknown"` in dev so `wrangler dev` never breaks): extend `deploy/deploy.sh` and `.github/workflows/deploy-worker.yml` with the same stamp step (one helper, like `deploy/apply-d1-migrations.sh`, so the two callers cannot drift), read it in `worker/src/index.js:697-710`, and extend `deploy/verify.sh` with a worker-revision gate mirroring the Pages one. Pin in `deploy/deploy.test.mjs` in the existing static style (both callers stamp, root serves the key, verifier gates it). Size M. Risk low (additive key; root shape otherwise unchanged; no D1 or schedule touch).

## 5. What NOT to change

- The agent never vets or moves status — quoted deliberate: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`) and `README.md:72` (`researching → testing is a human decision … The agent never moves status`). Do not add an automated vet, auto-testing flip, or bulk verdict path — not even for the F1 queue. The brief's rank-(2) asks what the worker could decide without him; the answer here is nothing, until he writes the rule himself.
- One human verdict per keypress, no bulk path — quoted: `README.md:71` (`one human verdict per keypress, no bulk path`). F6 pins the worker half of this; do not implement the other half away.
- Migration stays default-off, ADD-only, triggerless — quoted: `functions/api/[[path]].js:21-23` (`applies exactly the ADD COLUMN statements … one at a time (ADD-only, no endpoint)`). Never add DROP, column rewrite, data rewrite, DELETE, a second trigger, or a URL that migrates. Unset behaviour stays byte-identical to today.
- Lifetime revenue sums all rows — quoted deliberate: `README.md:75` (`cents from all rows regardless of status (open rows count once recorded)`). F5 hardens the modal label only; do not "fix" it by bounding the probe to won/lost.
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change). His one line: set `ALLOW_SCHEMA_MIGRATION=1` as a Pages production env var and retry the latest deployment — money legs fill on the next health call.
- The 11 verdicts are the owner's dashboard presses (need `ADMIN_TOKEN` + per-row judgment), not agent writes. His one line: open Needs-review oldest-first and press V or K on each of the 11 rows.
- The $0 starter and its close are the owner's (Start the seed sprint `d1/seed.sql:96-100` or the drafted census `docs/ZERO-SPEND-STARTER.md:9-42`, run it, Win/Lose with a human-entered $ amount + post-mortem). Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure.
- Deploying rounds 5+6 is the owner's (F3). His one line: live serves `b70c707` while `efcd038` is committed — check the Pages dashboard's latest deployment and let it finish.
- The F4 cause needs live runs data this round cannot reach (no network by design). Orchestrator one line: fetch `/api/runs?limit=5` alongside `/api/health` in the next snapshot round (public read); owner equivalent: open the Research log and read the Error column on the last two ticks.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action.
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment. Never spend money, create accounts, send anything outward, accept terms, solve captchas, fabricate reviews/receipts/testimonials, scrape or message against a platform's terms, or give financial advice/implied return.
- House rules honored by this audit: never read `.env`/secrets/tokens (`.gitignore:4-5` covers `.env`/`.env.*`, pinned by `deploy/deploy.test.mjs:257-258`); no edits to existing files (this findings file only); LF endings pinned (`.gitattributes:4`).

## 6. Proposed next tasks (2–4)

Disposition of the focus-named work (it outranks anything found, so it is answered first): the vet-path round resolves to human-by-design (F1: path `public/app.js:587`, no failing test because nothing is broken — the cause is the absence of a human press, proven by the worker's zero `vetted` strings with a positive control — one Vet press yields exactly unreviewed −1 / `vetted_last_7d` +1 / `last_verdict` = today per `functions/api/api.test.mjs:2187`, and health already dates the stall via `last_vetted`/`last_verdict`, now also painted in the pill). Every other focus-named code item is verified shipped with the suite green: probe isolation + failure naming + `db`-by-answer (`functions/api/[[path]].js:542-598,709`), deploy convergence via one shared script (`.github/workflows/deploy-worker.yml:33-34`, `deploy/apply-d1-migrations.sh:13-28`, convergence suite `d1/migrations.test.mjs:38-70`), the default-off self-migration with its three results (`functions/api/[[path]].js:36-77`, suite `functions/api/api.test.mjs:1737-1921`, variable `ALLOW_SCHEMA_MIGRATION`), the sub-minute human vet path (keys + prefetch + auto-advance + cold-boot guard), the dropped-source flag, partial-source markers, and the $0 starter draft. The unshipped remainder is human/owner work (11 verdicts, the migration switch, the $0 sprint, the deploy, the F4 runs read) — not implementer-completable offline, so it lives in §5 as one line each. The tasks below are the remaining offline-completable items, most valuable first.

### Task 1. Label experiment-modal Revenue as money received so projections cannot pose as lifetime revenue

Owns: `public/app.js`, `public/dashboard.test.mjs` (pin only), `README.md` (one line). Context (F5): the modal takes `Revenue ($)` on any status (`public/app.js:1401,1420`) while health sums every row into `revenue_total` (`functions/api/[[path]].js:554`) by documented design (`README.md:75` — do not touch the probe).

Acceptance: the modal field reads as received-only with a one-line hint that it counts toward lifetime revenue on save; request shapes and payload keys byte-identical; dashboard-suite source-shape cases pin the label + hint; a README one-liner beside the money docs states received-only; full `npm test` green and the tab-icon suite stays green.

### Task 2. Pin worker-never-vets with a static guard test and refresh the owner-action queue age

Owns: `worker/src/index.test.js` (pin only), `docs/OWNER-ACTIONS.md` (one line). Context (F6/F1): the lane's load-bearing invariant — the worker never vets — has no test; and `docs/OWNER-ACTIONS.md:7` hardcodes "oldest 7.5 days" while live reads 7.9d and rising.

Acceptance: a static-guard `describe` in the existing style asserts `worker/src/index.js` is non-trivial (contains `runResearch`, the positive control) and contains no `vetted` (case-insensitive) and no `UNREVIEWED`-clearing replace; the owner-action age line no longer rots (live value or a pointer to `/api/health`); no shipped file changes; full `npm test` green.

### Task 3. Stamp the deployed SHA into the worker root response and gate it in verify.sh

Owns: `worker/src/index.js`, `deploy/deploy.sh`, `.github/workflows/deploy-worker.yml`, `deploy/verify.sh`, `deploy/deploy.test.mjs` (pin only), plus one new stamp helper if needed. Context (F7/F4): `GET /` answers `{ok, agent, last_run}` with no rev, so the live worker's vintage — the missing input to the F4 brief-gap diagnosis — is unknowable.

Acceptance: worker `GET /` carries a `rev` key reading the deployed SHA in CI/manual deploys and `"unknown"` under `wrangler dev`; both deploy callers stamp via one shared step that cannot drift; `verify.sh` fails a mismatched worker rev the way it fails a stale Pages rev; static tests pin all three; no D1, schedule, or probe changes; full `npm test` green.
