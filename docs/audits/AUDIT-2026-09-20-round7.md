# Audit 2026-09-20 round 7 — aimoney (read-only, track B)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r7`. HEAD `a2a48d3` (night round 6); no network, no installs, no edits to existing files. Only this file created at the root. Never read `.env`, secrets or tokens. Standing rules honored: one single-stage shell command per call, no inline code, no redirection. No `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING` at the root (authoritative root `ls`; glob-absence was cross-checked, see §3); `README.md` read first, then the live snapshots, then source.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests (`README.md:3-15`, `worker/src/index.js:4-5`).
- Entry points: `public/index.html` + `public/app.js` (1677 lines, dashboard), `functions/api/[[path]].js` (709 lines, D1 REST API), `worker/src/index.js` (725 lines, research agent) + `worker/src/lib.js` (79 lines, shared pure helpers).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `https://aimoney-research.<account>.workers.dev/`; live rev `d4262cd` trails HEAD `a2a48d3` by three night-round commits (see F3).
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + up to 2 briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:720`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema `:27` + shared migrations `:33-34` + worker `:38`) or manual `deploy/deploy.sh`, gated by `deploy/verify.sh` (public reads only, no credentials).

## 2. End-to-end walk-through

Signal to decision. The hop where it most often goes silent is hop 12.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:78` `redditSignals`, `:106` `githubSignals` via `timedJson` (`:37`, 6s timeout covering the body read). A throwing query increments `failed`; only all-queries-failed names the source in `src_fail` (`:74`, `:102`, `:130`) — partial failures are silent (F7).
2. Store: `:354-359` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:360-363` (`phase:collected`).
3. Sweep: `:367-372` unprocessed signals older than 30d become noise (`stale:N`).
4. Take: `:374-376` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:385-413` via pure `exactUrlTarget` (`:168`); bounded `IN` selects (`:394-400`); a match links as supports (notes newest-kept, no status move). Failed links are skipped and counted (`prepass:N_failed`, `:406-407`), never thrown.
6. Gates, read before classify: `:427-429` bare-without-brief > 10 caps inserts to 1/tick; `:431-432` unreviewed > 10 pauses inflow to 0/tick (`maxNewThisRun = 0`). Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:449` `buildClassifyPrompt(opps, fresh)` (`:272`) + `aiComplete` (`:136`, budget `MAX_AI_CALLS = 4`, `:19`), parsed by `parseJsonLines` (`worker/src/lib.js:64`, NDJSON + `repairJson` for `\_`). Inflow-paused ticks skip classify and still brief (finish line carries `inflow:paused`, `:634`).
8. Verdict loop `:469-537`: strict action enum (`:475`), supports-without-id becomes noise (`:483`), overflow stays unprocessed (`:486`). `new` INSERTs (`:504-507`) with an `UNREVIEWED` note (`:507`) and capped `effectiveScore` (`worker/src/lib.js:30`, cap 6000 `:23`).
9. Verdict flush: `:538` `flushVerdictWrites` (`:291`); failures as `verdict:N_failed`.
10. Briefs: main pass (~`:552-589`, top-scored live-only or oldest-unreviewed-first past 48h, `:553-561`) plus extra pass (`:596-631`) when the oldest unreviewed exceeds 48h (live oldest 183.2h, so both fire), via `buildBriefPrompt` (`:202`), `parseBriefJson` (`:213`), `insertBrief` (`:224`, never stores empty); poison rotation via `briefSkippedIds` (`:249-261`); failures `brief:failed`.
11. Finish: `:634` single `finish("ok", …)` carrying mode/pause/stale/src_fail markers; errors finish `error` plus the `console.error("cron-failed", …)` tail marker (`:642`, also `:654`, `:718`). `scheduled` `:648-654`, `POST /run` `:710-721`.
12. Vet — the ONLY path from unreviewed to vetted, one sentence: a manual dashboard press — `public/app.js:562` `vetOpportunity` → `:538` `fetchDetailForWrite` (token-gated detail fetch, warms N+1) → `:567` inner → `:485` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`:574-577`) with auto-advance (`:582` → `:727`). Starter variant `:616` `vetAndLogStarter` (resume-safe via `:599` `findPlannedStarter`) adds a planned experiment and flips the row to `testing` — but never advances focus (F5). Kill (`:655`, drawer `:1070`) writes `[date killed]` + post-mortem. `V`/`K` keys (`:732+`) with auto-advance: one human verdict per keypress, no bulk path.
13. API writes: `updateOpportunity` (`functions/api/[[path]].js:230`, text bounds, status/range guards, tagless-clear 400 `:264-268` against `VERDICT_TAG` `:90`, `effectiveScore` `:270`); `createExperiment` (`:358`) / `updateExperiment` (`:419`, won/lost require `result` + `post_mortem`, stamps, `outcomeLedgerLine` `:98-103`, cents clamps, `revenue_source` retry `:400-408`/`:475-483` that silently stores `''`, F4).
14. Health: `functions/api/[[path]].js:500-678` — 12 probes (`:520-533`) in one batch (`:534`) with per-probe isolation fallback (`:543-572`, failures named in `health_probe_failures` + column-only `health_probe_detail`), lazy self-migration (`:581-597` via `ensureMoneyColumns` `:36-77`), `last_vetted`/`last_verdict` parsed in JS (`:631-644`) from 500-char tails; payload `:678` (`ok:false` + nulls only on total outage).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 3.6) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (183.2h, +0.4h since the round-6 snapshot). Second: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F2). Third, code-silent: `revenue_source` dropped on unmigrated writes (F4) and partial source failures (F7).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:8`) → exit 0 this session, run once, no repeats. TAP summary: `# tests 442`, `# suites 140`, `# pass 442`, `# fail 0`, `# duration_ms 90783.440482` (Windows baseline in the brief: 90434.5784 — same shape, suite grew with round 6).
- Parse/compile: `python3 -m compileall` has no sources — `find -name *.py` over the clone returns nothing. Positive control: the same `find` for `*.js` lists all 6 shipped/test JS files, so the zero is real. JS parse is covered by the green suite itself (every file imported) plus the deploy parse gate (`dashboard-appjs parse gate` suite, green).
- Dead code: none in shipped source. Every export spot-checked has a shipped importer: `scoreOf` → `effectiveScore` (`worker/src/lib.js:31`), `daysSince` → `listExperiments` (`functions/api/[[path]].js:353`), `firstStepsFirstLine` (`public/app.js:162,216,627`), `startExperiment` (`:648,924,1389`), `findStarterForResume` (`:635`), `openExperimentModal` (`:578,1098,1212,1381`). `appendKeepNewest` (`worker/src/lib.js:51-54`) is test-only by documented design (pinned JS spec of the SQL idiom), not dead.
- Duplicated logic: round-6 closures verified landed — one strip painter `paintStartHere` (`public/app.js:161`, live `:152`, cached `:1613`) and the shared `clamp10` import (`functions/api/[[path]].js:10`, local removed `:79-81`). Remaining mirrors are documented-deliberate: `centsDollars` ↔ dashboard `moneyCents` (`functions/api/[[path]].js:93-94` comment), SQL `substr(notes || ?, -8000)` ↔ `appendKeepNewest`, `VERDICT_TAG` boolean gate (`:90`) vs the health `matchAll` date-capture shapes (`:637-643`, different need: gate vs capture).
- TODO density: zero in shipped source. Regex `TODO|FIXME|HACK|XXX` over the repo hits only prose lines in `docs/audits/*.md`. Control: the same tool+mode matches `UNREVIEWED` dozens of times across `worker/src/index.js` + `public/app.js` + `worker/src/lib.js`, so the zero is real.
- Matcher honesty note (the brief's check-that-matches-nothing rule, applied to this audit): `files_with_matches`+glob returned empty even for `scripts/*`, which `ls` shows is non-empty — so glob-absence was treated as unreliable and every absence claim above was re-verified by a second method (direct `ls` for root docs, `find` with a `.js` positive control, content search with a `UNREVIEWED` positive control).
- Surface rules: all pass, no finding. Light-only (`public/styles.css:4` `color-scheme: light`, ink-on-paper palette); favicon linked with theme-color (`public/index.html:9-10`, byte-identical in the live snapshot); `public/favicon.svg` 284 bytes, 32×32 `$` on `#0d6b3f`; brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suite `public/tab-icon.test.mjs:62-113` (dark-branch, toggle, mark, icon tags) green inside the exit-0 run. First-five-seconds: the static shell (5595 bytes live) renders brand + `Every way to make money with AI, ranked.` + lede with zero JS; counts fill from `…` placeholders, then the last-good snapshot cache, then live refresh.
- Tree: HEAD `a2a48d3`; `git ls-files --eol` shows all 40 tracked files `i/lf w/lf` with `eol=lf` — the CRLF warning in the brief does not apply to this checkout. Sizes (`wc -l`): `public/app.js` 1677, `functions/api/[[path]].js` 709, `worker/src/index.js` 725, `worker/src/lib.js` 79; tests outweigh shipped code (`api.test.mjs` 2527, `dashboard.test.mjs` 1947, `index.test.js` 1464).
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (time `2026-09-21T15:36:06Z`, rev `d4262cd…`, which equals the night-round-3 commit — live trails HEAD `a2a48d3` by three commits, F3): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:3.6, oldest_unreviewed_age_h:183.2, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. Prior snapshot: same 11 unreviewed at 182.8h — the queue has not moved between snapshots; age rises ~1d/d while cron stays healthy.

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 183.2h / 7.6d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `last_verdict` null, `hours_since_last_ok_run` 3.6. Was: same 11 unreviewed at 182.8h — six rounds have shipped against this repository and the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none — no `won`/`lost` row exists (`experiments_by_status:{planned:2}`, `decisions_last_7d:0`), and the money legs read null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter (the seed `Spec-ad sprint: 10 brands, 10 free ads`, `d1/seed.sql:96-100`), run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment a human records the first verdict. The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.6 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:562` `vetOpportunity` → shared `vettedNotes` (`:485-488`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id` (`:574-577`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:507`: `— UNREVIEWED, scores capped until a human vets it`) and every other `UNREVIEWED` site in `index.js` is a create/count/select site (`:18,431-432,504,553,561,605,609`) — zero clearing paths (positive control: the `UNREVIEWED` matcher returns those sites, so the absence of a clearing path is real). So `vetted_last_7d` is 0 with `hours_since_last_ok_run` 3.6 by design, not by failure. Vet-round report as requested: path `public/app.js:562`; cause human-gated-by-design — there is no failing test because nothing is broken, and the passing real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs:2084`, green inside the exit-0 run) is the positive proof; a single run of the path (one Vet press) produces exactly unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today. Health already dates the stall without being told: `last_vetted`/`last_verdict` (`functions/api/[[path]].js:631-644`), both null live.

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:431-432`), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change for the verdict itself — the 2-minute path (`V`/`K` + auto-advance, `public/app.js:727-730`, one verdict per keypress, token-gated) already ships with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines. The one code speedup left on this path is F5 (starter-tap advance). Touch no repo files for this finding. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live: `revenue_last_7d/revenue_total/spent_total` null with `health_probe_failures:[revenue_week,revenue_lifetime]`, `health_probe_detail:{…:revenue_cents}`, `schema_missing_columns:[revenue_cents,spent_cents]`, `schema_migration:disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)`. The two failing probes are the only ones reading money (`functions/api/[[path]].js:527-528`, `SUM(revenue_cents)` / `SUM(revenue_cents/spent_cents)`). `d1/schema.sql:5` is `CREATE TABLE IF NOT EXISTS` (no-op on the existing table); the ADD COLUMNs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql`), applied after `schema.sql` by both CI (`.github/workflows/deploy-worker.yml:27` then `:33-34` `Apply D1 migrations in sorted order` via `./deploy/apply-d1-migrations.sh`) and `deploy/deploy.sh:36` then `:40` — one shared loop + tolerate-list (`deploy/apply-d1-migrations.sh:13-28`, duplicate-column no-op, anything else exits 1). The in-worker fallback is deliberately off: `functions/api/[[path]].js:58` (`if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` → report missing + disabled, change nothing). Migration-ask results, all green this session in the `self-migration switch` suite (`functions/api/api.test.mjs`, 9/9): old schema + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers on the same call; var unset leaves the table unchanged and names the missing columns; second run is a clean no-op (and round 6's retry cases prove a transient PRAGMA failure no longer caches as decided). Exact variable name: `ALLOW_SCHEMA_MIGRATION`. Exact CI step (already in place): `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: earned-to-date is unknown rather than zero, so the money-first question cannot be answered from the project's own data. (The decisions COUNT split, `functions/api/[[path]].js:526`, already saved `decisions_last_7d` = 0 — the money legs are the only remaining nulls.)

Fix: no code change; the path is already correct. The owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var — the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, and re-runs the failed money probes on the same call — or adds the two Cloudflare repo secrets and redeploys so CI applies the migrations. One variable, one decision; see §5 for the exact lines. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F3. Live still serves the round-3 revision — three shipped rounds are committed but not deployed

Evidence. Live `/api/health` reports `rev: d4262cd3b3a…`, which equals the night-round-3 commit `d4262cd` (`git log`: `Night round 3 (aimoney)`), while the clone HEAD is `a2a48d3` (`Night round 6`). Three commits sit between them, all `READY TO SHIP` per the run brief: `812ba25` (round 4: retry-safe starter, calendar-checked verdict tags), `84442f0` (round 5: vet-path honesty gaps), `a2a48d3` (round 6: migration retry, one strip painter, shared clamp10). The round-6 audit already flagged a two-commit lag at its snapshot; the lag has grown to three with no deploy in between.

Why it costs: every fix in rounds 4–6 — including the verdict-tag guard and the self-migration retry that this audit's F2 relies on — is merged but unserved, so the live surface (and the owner's phone) does not reflect the repository. Trust cost, not a code defect: the code is right, it is just not running.

Fix: no code change. The owner merges/deploys the branch (or opens the Pages dashboard and checks the latest deployment status) — one line in §5. The next audit should confirm live rev == HEAD. Touch no repo files. Size S. Risk none (deploy-only; `deploy/verify.sh` gates the rollout on public reads).

### F4. On an unmigrated table, experiment writes silently discard the human-entered revenue source

Evidence. The dashboard sends `revenue_source` on both write paths (`public/app.js:875` Win close, `:1369` experiment modal) and renders it as `via {source}` (`:935`, `:1090`). The API's old-schema tolerance retries once without that column — `functions/api/[[path]].js:400-408` (create) and `:475-483` (update) — and then returns the bare happy-path response (`:416` `201 {id}`, `:484` `200 {id, status}`), identical to a write that kept the source. The caller cannot distinguish "stored" from "dropped". This is the live path today: the live table predates both 2026-09-20 migrations (proven by the missing cents columns in F2), and the self-migration deliberately covers only the cents columns (`README.md:83`), so the next live Win close stores its source as `''` with a success toast. `README.md:83` documents the retry (`stored as ''`) but not the silence — no flag, no toast, no log.

Why it costs: money provenance is the unit of value that makes a closed experiment's money real (amount + date + source), and this step silently drops one third of it at exactly the moment the owner types it in. First Win close on live loses its `via` with a smile.

Fix: keep the retry (it is correct tolerance), but tell the truth about it — return a dropped-field flag (e.g. `dropped: ["revenue_source"]`) in the 200/201 body on the retry path only, and have the dashboard toast it by name next to the existing success toast. Pin with tests against a stubbed D1 missing exactly `revenue_source`: POST and PATCH return success plus the flag; a migrated table returns no flag. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`, `public/app.js`, `public/dashboard.test.mjs` (pin only). Size S. Risk low (additive response key; happy-path bytes unchanged when the column exists).

### F5. "Vet & log starter" never advances review focus — Vet and Kill do

Evidence. After a successful verdict, `vetOpportunityInner` calls `advanceReviewFocus(id, orderBefore)` (`public/app.js:582`) and `killOpportunityInner` calls it (`:679`), so V/K and tap-Vet/tap-Kill all land focused on the next unreviewed row with N+1 prefetched (`focusReviewRow`, `:716-725`). The starter path — `vetAndLogStarter` (`:616-619`) and `vetAndLogStarterInner` (`:621-653`) — captures no `orderBefore` and calls no advance: after the recommended one-tap move (vet + planned starter + flip to `testing`), focus sits on a row that just left the queue and the owner must re-tap into the list for each of the 11 rows. This is the smallest remaining code friction on the queue-clearing path F1 names.

Why it costs: owner minutes on the critical path — the starter tap is the move `docs/OWNER-ACTIONS.md:37-42` prescribes for the money experiment, and it is the only verdict that breaks flow. Eleven rows × re-orient each is the difference between a two-minute clear and a fiddly one on a phone.

Fix: capture `orderBefore` in the starter inner before the PATCHes (same one line as Vet/Kill) and call the existing `advanceReviewFocus` after the post-write refresh; no new helper, no behavior change to the three writes or the resume logic. Pin with the dashboard suite's existing source-shape style (starter inner references `advanceReviewFocus` + `nextReviewIdAfter` order capture) plus the `nextReviewIdAfter` pure cases already covered. Touch `public/app.js`, `public/dashboard.test.mjs` (pin only). Size S. Risk low (focus-only; PATCH shapes, tags, and refresh behavior untouched).

### F6. Two planned experiments, zero running, zero decisions — the money path stalls at planned

Evidence. Live `experiments_by_status:{planned:2}`, `decisions_last_7d:0`; no `running`/`won`/`lost` keys. The seed starter is still planned by name: `d1/seed.sql:96-100` (`Spec-ad sprint: 10 brands, 10 free ads`, `planned`, `metric: replies; pilots closed`, `target: >=3 replies; >=1 pilot at >=$500`). The one-tap moves already exist and are human-pressed: `startExperiment` (`public/app.js:823`, PATCH `running`, API stamps `started_at`), `loseExperiment`/`winExperiment` (`:838+`, human-entered $ → `revenue_cents` + optional spend/source), all token-gated with orphaned cards excluded. The API closure gate holds: won/lost require `result` + `post_mortem` (`functions/api/[[path]].js:446-450`), `ended_at` stamped, outcome ledger appended (`:456-462`, shared `outcomeLedgerLine` `:98-103`).

Why it costs: vetted → experiment → decision is the lane metric, and it reads 0/0/0. Signals seen and proposals added are attempts, not outcomes; without one running row there is no number within a week and no amount/date/source to make real.

Fix: after F1 vets the parent row, the owner presses Start on the $0-capital starter, runs the 10-brand sprint outside the repo, then closes via Win/Lose with a human-entered $ amount and a one-line post-mortem. No code change; the dashboard, API gate, stamps, and ledger already enforce the shape. Touch no repo files. Size S (one human decision + the sprint). Risk none to the repo (token-gated PATCHes; no auto-transitions; never mark decided without evidence).

### F7. A source whose queries mostly fail reads as a healthy tick

Evidence. Each collector counts per-query failures (`failed++`, `worker/src/index.js:70,99,125`) but names the source in `state.src_fail` only when ALL queries fail (`:74` HN 4/4, `:102` Reddit 3/3, `:130` GitHub 2/2), and the run log carries only that all-or-nothing marker (`:634` finish line). A tick where 3 of 4 HN queries throw — rate limits, one bad index shard, a trickling body hitting the 6s `timedJson` abort (`:37-47`) — finishes `ok` with fewer signals and no marker anywhere: not in the run log, not in health, not on the dashboard. The `Promise.allSettled` guard (`:343-349`) covers only a fully-rejected collector, so the same blindness holds one level up.

Why it costs: failure visibility on the machine's input — a half-dead source degrades proposal flow for days while every surface reports green, and the 30d noise sweep (`:367-372`) quietly buries the evidence. Debugging "why so few proposals last week" starts from nothing.

Fix: count per-source ok/total at the collectors and append a compact partial marker (e.g. `src_partial:hn:1/4`) to the finish-line error string next to `src_fail`, keeping the all-failed naming exactly as today so existing log parsers and tests keep matching. Total-outage, partial, and quiet-tick shapes each get a test with stubbed fetch. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk low (log-string-only; no signal, verdict, or brief behavior changes; no new AI spend).

### F8. `.gitignore` does not list `.env` — one broad add could stage local secrets

Evidence. `.gitignore` is 5 lines (`node_modules/`, `.wrangler/`, `.dev.vars`, `*.log`, `.DS_Store`) — `.dev.vars` (wrangler local) is covered, but a plain `.env` is not, even though this repo's whole credential story (`CLOUDFLARE_API_TOKEN`, `ADMIN_TOKEN`, `README.md:36-38,50-52`) lives in environment values an operator may keep in a local `.env`. This audit never probed for or read any such file; the gap stands on its own: any future `git add -A` with a `.env` present stages secrets into history. (House rule honored throughout: no `.env`/secret/token was read here.)

Why it costs: trust catastrophe insurance — a committed token means rotation everywhere (Pages env, worker secret, dashboard `localStorage` copies per `README.md:85-88`) plus history surgery, against a one-line prevention.

Fix: add `.env` (and `.env.*` except any checked-in example, of which there are none) to `.gitignore`, and pin it with a one-assertion test in the deploy suite's style so a future edit cannot silently drop the line. Touch `.gitignore`, `deploy/deploy.test.mjs` (pin only). Size S. Risk none (ignore-only; no tracked file changes, no behavior change).

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, or auto-testing without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless. `functions/api/[[path]].js:16-27` (switch docblock), `:58` (unset → report + change nothing), `:63-76` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate for decided outcomes. Do not default it on, do not add a second trigger, and do not add a migration endpoint: `a URL that alters the schema is a liability even when it is authenticated.`
- Never a data-losing migration here: ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change); unset behaviour is byte-identical to today. His one line: set `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var and reload `/api/health` once — the two money columns appear and the money legs fill on the same call.
- The 11 verdicts are the owner's dashboard presses (needs `ADMIN_TOKEN` + per-row judgment), not agent writes. His one line: open Needs-review oldest-first and press V or K on each of the 11 rows.
- The $0 starter and its close are the owner's (Start the seed sprint, run it, Win/Lose with a human-entered $ amount + post-mortem). The 10 brand sends are his outward action, not the agent's.
- Deploying rounds 4–6 is the owner's (F3). His one line: live serves `d4262cd` (round 3) while `812ba25/84442f0/a2a48d3` are committed — merge/deploy the branch or check the Pages dashboard's latest deployment.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action.
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment from the decision + brief lines. Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure — the inline Win close takes a human-entered amount for exactly this reason.
- Never, whatever the ranking says: no spending, no account or identity creation, no outward sends, no accepting terms, no captcha solving, no fabricated reviews/receipts/testimonials, no scraping or messaging a platform's terms forbid, no financial advice or implied return.
- House rules honored by this audit: never read `.env`/secrets/tokens; no edits to existing files (this findings file only); LF endings verified (`git ls-files --eol`, all 40 tracked files `i/lf w/lf`).

## 6. Proposed next tasks (2-4)

Disposition of the focus-named work (it outranks anything found, so it is answered first): the vet-path round resolves to human-by-design (F1: path `public/app.js:562`, no failing test because nothing is broken, one Vet press yields exactly one verdict, stall already dated by `last_vetted`/`last_verdict`), so its code remainder is the smallest queue-clearing speedup — Task 1. Every other focus-named code item is verified shipped with the suite green: probe isolation + failure naming + `db`-by-answer (`functions/api/[[path]].js:543-572,678`), deploy convergence via one shared script (`.github/workflows/deploy-worker.yml:33-34`, `deploy/apply-d1-migrations.sh:13-28`), the default-off self-migration with its three results (`functions/api/[[path]].js:36-77`, 9/9 suite), and the sub-minute human vet path (keys + prefetch + auto-advance). The unshipped remainder is human/owner work (11 verdicts, the migration switch, the $0 sprint, the deploy) — not implementer-completable offline, so it lives in §5 as one line each. The tasks below are the remaining offline-completable code items, most valuable first.

### Task 1. Starter tap advances review focus like Vet and Kill do

Owns: `public/app.js`, `public/dashboard.test.mjs` (pin only). Context (F5): `vetAndLogStarterInner` (`public/app.js:621-653`) captures no `orderBefore` and never calls `advanceReviewFocus` (`:727-730`), while Vet (`:582`) and Kill (`:679`) both do — the recommended one-tap move breaks flow on every row.

Acceptance: the starter inner captures the pre-verdict order and advances focus to the next unreviewed row after its refresh; the three writes, resume logic, and toasts are byte-identical in behavior; a dashboard-suite pin covers the advance; full `npm test` green.

### Task 2. Flag dropped revenue_source on old-schema experiment writes

Owns: `functions/api/[[path]].js`, `functions/api/api.test.mjs`, `public/app.js`, `public/dashboard.test.mjs` (pin only). Context (F4): the `revenue_source` retry (`functions/api/[[path]].js:400-408,475-483`) returns the bare happy-path 200/201, so the dashboard cannot tell the owner his `via` was stored as `''`.

Acceptance: the retry path — and only the retry path — returns a dropped-field flag naming `revenue_source`; the dashboard toasts it by name on success; stubbed-D1 tests cover POST and PATCH on a table missing exactly that column plus the no-flag migrated case; full `npm test` green.

### Task 3. Ignore .env in .gitignore so local secrets cannot be staged

Owns: `.gitignore`, `deploy/deploy.test.mjs` (pin only). Context (F8): `.gitignore` covers `.dev.vars` but not `.env`, so one broad add with a local `.env` present stages credentials into history.

Acceptance: `.gitignore` lists `.env` (plus `.env.*` unless a checked-in example exists — none does); a deploy-suite test asserts the lines are present; no tracked file changes; full `npm test` green.

### Task 4. Name partial source failures in the worker run log

Owns: `worker/src/index.js`, `worker/src/index.test.js`. Context (F7): per-query failures are counted (`worker/src/index.js:70,99,125`) but only all-failed sources are named (`:74,102,130`), so a 3-of-4 outage finishes `ok` with no marker.

Acceptance: a compact per-source partial marker (ok/total counts) lands on the finish line next to the unchanged `src_fail` naming; total-outage, partial, and quiet-tick shapes each have a stubbed-fetch test; no signal/verdict/brief behavior changes; full `npm test` green.
