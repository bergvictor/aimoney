# Audit 2026-09-20 round 7 — aimoney (read-only)

Clone root: `/root/coding-projects/_workspace/trackb-aimoney-r7`. HEAD `e451b24d8c82611abfca00fdb09f09425b5fdb8a` (`master`); tree clean before this file; no network, no installs, no edits to existing files. Only this file created. Never read `.env`, secrets or tokens.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — ranked AI-money opportunities with research briefs and experiments; the agent proposes, a human vets and tests.
- Entry points: `public/index.html` + `public/app.js` (dashboard), `functions/api/[[path]].js` (D1-backed REST API), `worker/src/index.js` (research agent).
- Live surfaces: `https://aimoney.pages.dev/` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `https://aimoney-research.levitinvlad.workers.dev/`; live `rev` is `29ee528` (HEAD~1, snapshot predates the HEAD build).
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`, collect + triage + briefs) and manual `POST /run` (202, triage-only, `briefs_skipped:true`, `worker/src/index.js:720`).
- Deploys: Pages Git-connected (`public/` + `scripts/stamp-release.sh`) plus CI `.github/workflows/deploy-worker.yml` (schema + shared migrations + worker) or manual `deploy/deploy.sh`, gated by `deploy/verify.sh`.

## 2. End-to-end walk-through

Signal to decision; the hop where it most often goes silent is hop 12.

1. Collect: `worker/src/index.js:49` `hnSignals`, `:78` `redditSignals`, `:106` `githubSignals` via `timedJson` (`:37`, 6s timeout covering the body read). A query that throws marks its source; all-failed names it in `src_fail`.
2. Store: `:354-359` one batched `INSERT OR IGNORE INTO signals`, then heartbeat `:360-363` (`phase:collected`).
3. Sweep: `:367-372` unprocessed signals older than 30d become noise (`stale:N`).
4. Take: `:374-376` oldest-first `LIMIT 6` (`MAX_AI_SIGNALS`, `:15`).
5. Exact-URL pre-pass (no AI): `:385-413` via pure `exactUrlTarget` (`:168`); bounded `IN` selects (`:394-400`) match fresh URLs against opportunity `source_url`s and linked signal URLs; a match links as supports (notes newest-kept, no status move). Failed links are skipped and counted (`prepass:N_failed`, `:385,:407`), never thrown.
6. Gates, read before classify: `:427-429` bare-without-brief > 10 caps inserts to 1/tick; `:431-433` unreviewed > 10 pauses inflow to 0/tick (`maxNewThisRun = 0`). Overflow new-verdicts stay `processed = 0`.
7. Classify (one AI pass): `:444-457` runs only with fresh signals AND `maxNewThisRun > 0`; `buildClassifyPrompt` (`:272`) + `aiComplete` (`:136`, 60s cron / 12s manual, retries 1/0), parsed by `parseJsonLines` (`worker/src/lib.js:62`, NDJSON + `repairJson` for `\_`). Inflow-paused ticks skip classify and still brief (finish line carries `inflow:paused`, `:634`).
8. Verdict loop `:469-537`: first-wins validation, strict action enum, numeric-or-null id. `new` INSERTs (`:496-511`) with an `UNREVIEWED` note (`:507`) and capped `effectiveScore` (`worker/src/lib.js:29`, cap 6000 `:22`); overflow stays unprocessed (`:486`); slug collision links as supports (`:512-524`).
9. Verdict flush: `:538` `flushVerdictWrites` (`:291`, one batch with per-write fallback; failures as `verdict:N_failed`).
10. Briefs: main `:550-589` (top-scored live-only `:566-568`, or oldest-unreviewed-first past 48h `:553-561`) plus extra `:596-631` when the oldest unreviewed exceeds 48h, via `buildBriefPrompt` (`:202`), `parseBriefJson` (`:213`), `insertBrief` (`:224`, never stores empty); poison rotation `:248-265` (`brief:skipped:N`); failures `brief:failed`.
11. Finish: `:634` single `finish("ok", …)` carrying mode/pause/stale/src_fail markers; errors `:636-644` finish `error` plus the `console.error("cron-failed", …)` tail marker (`:642`, also `:654`, `:718`). `scheduled` `:648-656`, `POST /run` `:710-721`.
12. Vet — the path that moves unreviewed to vetted: `public/app.js:481` `vetOpportunity` → `:471` `fetchDetailForWrite` (token-gated detail fetch) → `:486` inner → `:461` `vettedNotes` (clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id`. Starter variant `:511` adds a planned experiment and flips the row to `testing`. Kill (`:547`, drawer `:931`) writes `[date killed]` + post-mortem. `V`/`K` keys (`:623`) with auto-advance (`:618`, `:595`): one human verdict per keypress, no bulk path.
13. API writes: `updateOpportunity` (`functions/api/[[path]].js:224`, text bounds, status/range guards, tagless-clear 400 `:258-261`, `effectiveScore` `:264`); `createExperiment` (`:352`) / `updateExperiment` (`:413`, won/lost require `result` + `post_mortem`, stamps, `outcomeLedgerLine` `:92`, cents clamps, `revenue_source` retry `:393-402`/`:468-477`).
14. Health: `functions/api/[[path]].js:494-669` — 12 probes (`:510-527`) in one batch (`:528`) with per-probe isolation fallback (`:537-566`, failures named in `health_probe_failures` + column-only `health_probe_detail`), lazy self-migration (`:574-591`), `last_vetted`/`last_verdict` parsed in JS (`:622-636`) from 500-char tails (`:526`); payload `:669` (`ok:false` + nulls only on total outage).

Where it most often breaks or goes silent: hop 12, the human Vet press. Nothing automated was ever supposed to vet (F1): cron ticks succeed (`hours_since_last_ok_run` 0.6) while `vetted_last_7d` stays 0, `last_vetted`/`last_verdict` stay null, and the oldest unreviewed ages (180.3h and rising). Second: the money probes return null because the money columns never reached live D1 and the self-migration is disabled (F2).

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:7-8`) → exit 0 this session. Summary from the full TAP tail: `# tests 397`, `# suites 129`, `# pass 397`, `# fail 0`, `# cancelled 0`, `# skipped 0`, `# todo 0`, `# duration_ms 90432.996161`. Baseline `_night/tests-baseline.log:285-292` reads the same 397/0. No `MODULE_TYPELESS_PACKAGE_JSON` warning (HEAD carries `"type": "module"`, `package.json:5`); only the sandbox `UNDICI-EHPA` proxy notice. Zero `not ok` in the 111KB TAP.
- Parse: no `python3 -m compileall` sources exist — glob `**/*.py` matches nothing (positive control: same tool and mode on `**/*.js` matches the 4 shipped JS files `functions/api/[[path]].js`, `public/app.js`, `worker/src/index.js`, `worker/src/lib.js`). JS parse is covered by `npm test`: the `every shipped JS file parses (node --check)` suite plus a broken-syntax positive control (`public/dashboard.test.mjs`), both green inside the exit-0 run.
- Dead code: none in shipped source. Regex `console\.|debugger` over `functions`, `worker/src/index.js`, `worker/src/lib.js`, `public/app.js` hits only the three deliberate `console.error("cron-failed", …)` markers (`worker/src/index.js:642,654,718`) plus one comment mentioning `console.*` (`:640`); no `debugger`. Positive control: the same regex matches 6 harness captures in `worker/src/index.test.js:626-627,631,796-797,806`, so the three is real.
- Duplicated logic: three live cases — Start-here strip HTML built twice (F5), `last_vetted` tail vs outcome appends (F6, shape not text), and two small dups (F8: age buckets, `clamp10`). Everything else is shared: `effectiveScore`/`tokensMatch` (`worker/src/lib.js:29,38`, API import `functions/api/[[path]].js:10`), `isNeedsReview` now reused by `deriveReviewList` (`public/app.js:272-278`, HEAD fix), `vettedNotes`, `fetchDetailForWrite`, brief/classify/insert helpers, `flushVerdictWrites`, `outcomeLedgerLine`, one migration script.
- TODO density: zero in shipped source. Regex `TODO|FIXME|HACK|XXX` over `functions`, `worker/src/index.js`, `worker/src/lib.js`, `public/app.js`, `d1`, `deploy`, `scripts` → no hits. Positive control: the same tool and mode matches `UNREVIEWED` 10+ times across `worker/src/index.js` + `public/app.js` and `ALLOW_SCHEMA_MIGRATION` in `functions/api/[[path]].js:53`, so the zero is real.
- Surface rules: light-only (`public/styles.css:4` `color-scheme: light`; regex `prefers-color-scheme|data-theme` over `public/` hits only the negative assertions in `tab-icon.test.mjs` + `dashboard.test.mjs`, zero shipped hits; control: `color-scheme` matches `styles.css:4`). Favicon linked (`public/index.html:9-10`); `public/favicon.svg` is a hand-written 32×32 `$` on `#0d6b3f` (one line, no fonts, under 2KB); brand mark inline in the header (`public/index.html:16`, same geometry/accent/glyph); guard suite `tab-icon` green inside the exit-0 run. Served `_night/live/aimoney_pages_dev.html` matches `public/index.html` (icon tags, brand mark, `agent: …` placeholder, review `…` placeholder). No surface findings this round.
- Tree: HEAD `e451b24d8c82611abfca00fdb09f09425b5fdb8a` (branch `master`); `.gitattributes:4` pins `eol=lf`. This audit created only `AUDIT-2026-09-20-round7.md` at the root; no existing file was edited.
- Live snapshot read from `_night/live/aimoney_pages_dev_api_health.html:1` (rev `29ee52822f64bd74ed381ef174478f4fe037330f`, HEAD~1 — snapshot at 12:36 UTC predates the HEAD build at the same minute, so the lag is timing, not a stuck deploy): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:0.6, oldest_unreviewed_age_h:180.3, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{revenue_week:revenue_cents, revenue_lifetime:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. Prior snapshot (round6 audit): same 11 unreviewed at 179.9h — the queue has not moved between snapshots; age rises ~1d/d.
- Release marker (build-identity scope): single source of truth is `/release.json` (no `/build.json`, correctly per scope — two version sources would be worse than one). Repo `public/release.json:1` carries the `dev-undeployed` placeholder by design (stamped at build: `scripts/stamp-release.sh:9` full SHA + release; `deploy/deploy.sh:61` same); `deploy/verify.sh:53,62-106` requires JSON with `revision` == deployed commit (HTML/unparsable/placeholder fail immediately, stale retried 6×10s). Live `/release.json` was not fetched for this round (no network; only dashboard + health snapshots provided), so its exact live body is unmeasured here — indirect evidence the live stamp works: `/api/health` `rev` is the full 40-char HEAD~1 SHA, which the handler reads from `release.json` (`functions/api/[[path]].js:661-668`).

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, `vetted_no_experiment` 0, unreviewed 11 (oldest 180.3h / 7.5d, rising ~1d/d), bare-without-brief 19/27, experiments `{planned: 2}`, `last_vetted` null, `last_verdict` null, `hours_since_last_ok_run` 0.6. Was: same 11 unreviewed at 179.9h — seven rounds have shipped against this repository and the queue has not moved once.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by owner time saved and decisions/week, measured the same way. Earned to date: none measured — `revenue_total`, `revenue_last_7d`, `spent_total` all null (unknown, not zero; the live D1 lacks the money columns so the probes cannot run). Last krone arrived: never recorded. Single step between now and the next payment: vet the 11 rows, Start one $0-capital starter (the seed `Spec-ad sprint: 10 brands, 10 free ads`, `d1/seed.sql:96-100`), run it to won/lost with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. Honesty: no projection as result, no rate × volume, no attempt counted as outcome (signals seen ≠ proposals, proposals ≠ vetted, planned ≠ running). The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment a human records the first verdict. The single owner action that moves it, in one line: open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.5 days — a human press is the only path, and it has not happened

Evidence. The path, in one sentence: a manual dashboard button — `public/app.js:481` `vetOpportunity` → shared `vettedNotes` (`:461-463`: `` `${cleanUnreviewed(notes)}\n[${day} vetted] Human vetted; cap lifted.` ``) → `PATCH /api/opportunities/:id`. The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). The worker creates the marker (`worker/src/index.js:507`: `— UNREVIEWED, scores capped until a human vets it`) and the other `UNREVIEWED` hits in `index.js` are create/count/select sites — zero clearing paths (positive control: regex `UNREVIEWED` over `worker/src/index.js` returns the 10 create/count/select sites, so the absence of a clearing path is real). So `vetted_last_7d` is 0 with `hours_since_last_ok_run` 0.6 by design, not by failure: cron triage + briefs run, verdicts are human-gated. A single run of the path is certified by the real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs:1999`, green inside the exit-0 run): one Vet press yields unreviewed −1, `vetted_last_7d` +1, `last_vetted` = today. Health already dates the stall without being told: `last_vetted`/`last_verdict` (`functions/api/[[path]].js:622-636,669`), both null live.

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week, 2 planned stuck), inflow is paused at 0/tick while unreviewed > 10 (`worker/src/index.js:431-433`), and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change — the 2-minute keys (`V`/`K` + auto-advance, `public/app.js:623,618,595`, one verdict per keypress, token-gated, buttons/drawer untouched) already ship with the suite green. The owner opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines. Touch no repo files. Size S (minutes). Risk none (reversible token-gated PATCHes, human presses every verdict, no status auto-moves; every Kill carries its required post-mortem).

### F2. Money legs null — the columns never reached live D1 and the migration is disabled

Evidence. Live: `revenue_last_7d/revenue_total/spent_total` null with `health_probe_failures:[revenue_week,revenue_lifetime]`, `health_probe_detail:{…:revenue_cents}`, `schema_missing_columns:[revenue_cents,spent_cents]`, `schema_migration:disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)`. The two failing probes are the only ones reading money (`functions/api/[[path]].js:521-522`, `SUM(revenue_cents)` / `SUM(revenue_cents/spent_cents)`). `d1/schema.sql:5` is `CREATE TABLE IF NOT EXISTS` (no-op on the existing table); the ADD COLUMNs live in `d1/migrate-2026-09-20-experiment-cents.sql:5-6` (+ `revenue_source` in `d1/migrate-2026-09-20-revenue-source.sql:5`), applied after `schema.sql` by both CI (`.github/workflows/deploy-worker.yml:27` then `:33-34` `Apply D1 migrations in sorted order` via `./deploy/apply-d1-migrations.sh`) and `deploy/deploy.sh:36` then `:40` — one shared loop + tolerate-list (`deploy/apply-d1-migrations.sh:13-28`, duplicate-column no-op, anything else exits 1), pinned by the convergence tests (`d1/migrations.test.mjs`, incl. the withhold-one-file positive control). The in-worker fallback is deliberately off: `functions/api/[[path]].js:53` (`if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` → report missing + disabled, change nothing). Migration-ask results, all measured green this session: old schema + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers; var unset leaves the table unchanged and names the missing columns; second run is a clean no-op (`self-migration switch` suite, 6/6 in `functions/api/api.test.mjs`). Exact variable name: `ALLOW_SCHEMA_MIGRATION`. Exact CI step changed (already in place): `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: earned-to-date is unknown rather than zero, so the money-first question cannot be answered from the project's own data. (The decisions COUNT split, `:520`, already saved `decisions_last_7d` = 0 — the money legs are the only remaining nulls.)

Fix: no code change; the path is already correct. The owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var — the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, and re-runs the failed money probes on the same call — or adds the two Cloudflare repo secrets and redeploys so CI applies the migrations. One variable, one decision; see §5 for the exact lines. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F3. Two planned experiments, zero running, zero decisions — the money path stalls at planned

Evidence. Live `experiments_by_status:{planned:2}`, `decisions_last_7d:0`; no `running`/`won`/`lost` keys. The seed starter is still planned by name: `d1/seed.sql:96-100` (`Spec-ad sprint: 10 brands, 10 free ads`, `planned`, `metric: replies; pilots closed`, `target: >=3 replies; >=1 pilot at >=$500`). The one-tap moves already exist and are human-pressed: `startExperiment` (`public/app.js:714`, PATCH `running`, API stamps `started_at`), `loseExperiment` (`:731`) and `winExperiment` (`:755`, human-entered $ → `revenue_cents` + optional `revenue_source`), all token-gated with orphaned cards excluded. The API closure gate holds: won/lost require `result` + `post_mortem` (`functions/api/[[path]].js:363-368,440-445`), `ended_at` stamped, outcome ledger appended (`:404-409`, shared `outcomeLedgerLine` `:92`).

Why it costs: vetted → experiment → decision is the lane metric, and it reads 0/0/0. Signals seen and proposals added are attempts, not outcomes; without one running row there is no number within a week and no amount/date/source to make real.

Fix: after Task 1 vets the parent row, the owner presses Start on the $0-capital starter (`public/app.js:714`, first candidate named at `:712-713`), runs the 10-brand sprint outside the repo, then closes via Win/Lose with a human-entered $ amount and a one-line post-mortem. No code change; the dashboard, API gate, stamps, and ledger already enforce the shape. Touch no repo files. Size S (one human decision + the sprint). Risk none to the repo (token-gated PATCHes; no auto-transitions; never mark decided without evidence).

### F4. Nineteen of 27 rows have no brief — the evidence gap that makes each vet slower

Evidence. Live `bare_without_brief:19` with `opportunities:27`. Brief capacity is 1 + 1 per cron tick: main `:550-589` (top-scored live-only `:566-568`, or oldest-unreviewed-first past 48h `:553-561`) plus extra `:596-631` when the oldest unreviewed exceeds 48h (live oldest 180.3h, so both fire). Each costs 1 of 4 AI calls (`MAX_AI_CALLS`, `:19`); poison rows rotate via `briefSkippedIds` (`:248-265`) and failures land as `brief:failed`. Inflow is already paused at 0/tick while unreviewed > 10 (`:431-433`), so the bare queue drains 2/tick with no new bare rows arriving.

Why it costs: a vet without a brief is a guess, not a sub-minute decision — the reviewer opens the drawer, reads raw notes, and slows the 11-row clear that unpauses inflow. The brief backlog is the second queue behind the review queue.

Fix: no code change; the 48h oldest-first flip + extra brief already prioritize the review queue, and the pause already stops new bare inflow. The owner vets from the inline decision lines ($/mo range, capital, next action, summary excerpt) while cron drains 2 bare rows per 6h tick; the next audit should confirm `bare_without_brief` falling. Touch no repo files. Size S. Risk none (read-only for the agent; cron already paces within its AI budget).

### F5. Start-here strip is built twice — live render and cached paint must stay in sync by hand

Evidence. `renderStartHere` (`public/app.js:156-165`: `` `<div style="background:var(--green-wash);…` + `<b>Start here today:</b> …` + `Open in drawer` + conditional `Vet`/`Kill`) with handlers `:166-170` (`openDrawer`, `vetOpportunity`, `killOpportunity`) is duplicated by the boot paint (`:1466-1475`: same div, same copy, same three buttons, plus `staleMark`) with handlers `:1476-1480` (same three calls on `cachedTop.id`). The only deliberate differences are the data source (`top` vs `cachedTop`) and the stale mark; copy, order, ids, and handler wiring are byte-parallel.

Why it costs: the next copy/button change lands in one builder and the boot paint diverges until the live refresh wins — wrong actions or stale copy in the owner's first five seconds on every cold load. It also doubles the review burden for the surface he opens most.

Fix: extract one pure builder (e.g. `startHereHtml(top, staleMark)`) plus one binder, called by both `renderStartHere` and `paintLastGood`; keep the stale mark and the cached-id safety (`Vet`/`Kill` already re-fetch detail before writing). Pin with the existing dashboard suites (start-here + last-good cases) and run `npm test` green. Touch `public/app.js`, `public/dashboard.test.mjs` only if a new pin is needed. Size S. Risk low (render-only; PATCH shapes, tag contract, and cache semantics untouched).

### F6. `last_vetted` 500-char tail can miss a vet tag that outcome lines pushed out

Evidence. The verdict-date probe selects only `substr(notes, -500)` per row (`functions/api/[[path]].js:526`: `SELECT substr(notes, -500) AS notes FROM opportunities WHERE notes LIKE '%vetted]%' OR notes LIKE '%killed]%'`), parsed in JS (`:622-636`, `/\[(\d{4}-\d{2}-\d{2}) vetted\]/g` + killed). Outcome closes append after the vet: `outcomeLedgerLine` (`:92-96`: `` `[${day} outcome] Experiment "${…slice(0,120)}" ${status}: ${oneLine.slice(0,200)}${moneyBit}` ``, up to ~400 chars) via `substr(notes || ?, -8000)` (`:404-409` create-close, `:453-455` update-close). Two closes (~800 chars) after a vet push the `[date vetted]` tag outside the 500-char tail, so `last_vetted`/`last_verdict` report an older date or null while `vetted_last_7d` (full-notes `LIKE`, `:523`) still counts the row — count and date disagree.

Why it costs: failure visibility — the stall date the health endpoint was given to show without being told goes stale on exactly the long-lived rows that close experiments, misreading a recent vet as an old stall. Not yet live (0 closes), but the first two Wins trigger it.

Fix: widen the probe tail (e.g. `substr(notes, -2000)`, still bounded; full 8000-char bodies still never cross D1) without touching `vetted_last_7d` semantics or the parse. Add a real-path test: vet tag + two outcome lines (>600 chars after the tag) still reports the vet date on both `last_vetted` and `last_verdict`. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Size S. Risk low (read-only probe; wider tail only adds matches the regex already validates).

### F7. PATCH `null` writes the string `"null"` (or `1`) instead of leaving the field alone

Evidence. `updateOpportunity` (`functions/api/[[path]].js:236-237`: `if (b[f] !== undefined) next[f] = String(b[f]).slice(0, …)`) turns `{title: null}` into `"null"`; `:244` (`clamp10(b[f], cur[f])`) turns `{value: null}` into `1` (`Number(null)` → `0` → clamp to `1`, not `cur`). Same shape in `updateExperiment` (`:425-426` text, `:433` cents `Number(null)||0` → `0`, `:437` source). The dashboard never sends `null` (strings or omitted keys), so the hole is raw-API-only today — but a scripted vet/cleanup that nulls a field corrupts the row instead of skipping it.

Why it costs: data trust — a `"null"` title or a silently reset `value: 1` rewrites the score (`:264` recalculates) and the $/mo display with no error, and the corruption propagates to the cached strip and review lines until a human notices.

Fix: treat `null` like `undefined` (skip the field) for text/numeric PATCH fields in both update paths, or 400 naming the field; keep `skills_needed` array-only and the tagless-clear guard unchanged. Pin in `functions/api/api.test.mjs`: null title/value/cents leave the row untouched (or 400), valid PATCHes unchanged. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Size S. Risk low (dashboard payloads contain no nulls, so no live path changes shape).

### F8. Two small duplications: age buckets and `clamp10` each live in two places

Evidence. `fmtAgeH` (`public/app.js:303-304`: `h < 1 ? " · oldest <1h" : h < 24 ? … : …`) is inlined again in `reviewBacklogBit` (`:336-338`: identical ternary). Separately, `clamp10` is defined twice with identical semantics: `functions/api/[[path]].js:74-78` (`Number` → finite-check → round → clamp) and `worker/src/lib.js:4-7` (`Math.round(Number(v))` → finite-check → clamp), while the API already imports `effectiveScore`/`tokensMatch` from the worker lib (`functions/api/[[path]].js:10`).

Why it costs: maintainability, not money — the next bucket or clamp change must land twice or pill/chip (F8a) and API/worker scoring guards (F8b) diverge silently.

Fix: reuse `fmtAgeH` (or extract the bucket) inside `reviewBacklogBit`, and import `clamp10` from `worker/src/lib.js` in the API instead of the local copy; run `npm test` green (dashboard age + lib clamp suites pin behaviour). Touch `public/app.js`, `functions/api/[[path]].js`, tests only if a new pin is needed. Size S. Risk low (pure helpers; tag, PATCH, and score contracts untouched).

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, or auto-testing without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless. `functions/api/[[path]].js:16-25` (switch docblock), `:53` (unset → report + change nothing), `:59-67` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate (`:35,:69-71`). Do not default it on, do not add a second trigger, and do not add a migration endpoint: `a URL that alters the schema is a liability even when it is authenticated.`
- Never a data-losing migration here: ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- Cloudflare repo secrets are the owner's. The workflow fails fast with a named message until they exist (`.github/workflows/deploy-worker.yml:20-26`). His one line: Settings → Secrets → Actions → add `CF_API_TOKEN` and `CF_ACCOUNT_ID`, then redeploy.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's decision (a live-DB change); unset behaviour is byte-identical to today. His one line: set `ALLOW_SCHEMA_MIGRATION=1` as a Pages env var and reload `/api/health` once — the two money columns appear and the money legs fill on the same call.
- No writes to production D1 from any round: no `wrangler d1 execute … --remote`, no manual migration, no seed. Applying migrations live is the owner's action. Review verdicts and experiment Starts/Wins/Losses below are the owner's dashboard presses, not agent writes.
- Verdicts need a login, a credential, and a human: `ADMIN_TOKEN` (Pages env + worker secret + dashboard `localStorage`) plus per-row judgment from the decision + brief lines. Never mark an experiment won/lost without `result` + `post_mortem` evidence, and never invent a revenue figure — the inline Win close takes a human-entered amount for exactly this reason.
- Never, whatever the ranking says: no spending, no account or identity creation, no outward sends, no accepting terms, no captcha solving, no fabricated reviews/receipts/testimonials, no scraping or messaging a platform's terms forbid, no financial advice or implied return. The spec-ad sprint's 10 brand sends are the owner's outward action, not the agent's.
- House rules honored by this audit: never read `.env`/secrets/tokens; no edits to existing files (this findings file only); CRLF care (whole-line matches — unneeded here since nothing was edited; `.gitattributes:4` pins `eol=lf`).

## 6. Proposed next tasks (2-4)

### Task 1. Vet-or-kill all 11 unreviewed rows via Needs-review V/K; empty the queue

Owns: live dashboard + D1 via the API (no repo files changed). Context (F1 — the focus-named vet ROUND, code half already shipped): the only path from unreviewed to vetted is the manual Vet button (`public/app.js:481` via `vettedNotes` `:461` → `PATCH /api/opportunities/:id`); the worker never vets by design, so no code change can move `vetted_last_7d`. A human opens the Needs-review filter oldest-first and records Vet (or Vet-&-starter) vs Kill per row from the inline decision + brief lines — one keypress per verdict (`V` vets and auto-advances, `K` opens the inline post-mortem, Confirm kills and advances), a couple of minutes with the keys now live.

Acceptance: `/api/health` reads `unreviewed` 0, `oldest_unreviewed_age_h` null, `vetted_last_7d` + dated kills in the window = 11, `last_vetted`/`last_verdict` = today; every Vet carries a `[YYYY-MM-DD vetted]` tag, every Kill a one-line post-mortem in notes; next cron resumes inflow (unreviewed ≤ 10 → cap 2/tick). Requires `ADMIN_TOKEN` and human judgment per row; never mark decided without evidence.

### Task 2. Start the $0 seed starter via dashboard Start; run it toward one decision

Owns: live dashboard + D1 via the API (no repo files changed). Context (F3): with the parent vetted, the seed `Spec-ad sprint: 10 brands, 10 free ads` (`d1/seed.sql:96-100`, $0 capital) is the first candidate (`public/app.js:712-713`). The owner presses Start (PATCH `running`, API stamps `started_at`), runs the sprint outside the repo, then closes via Win (human-entered $ + source) or Lose (one-line post-mortem) so the closure gate, stamps, and outcome ledger record the decision.

Acceptance: `/api/health` shows the experiment `running` then `decisions_last_7d` 1 with `ended_at` set; the parent notes carry the `[date outcome]` ledger line with `$X rev / $Y spent`; `result` + `post_mortem` non-empty; no invented revenue (Win amount is human-entered). Requires `ADMIN_TOKEN` and human judgment; the 10 brand sends are the owner's outward action.

### Task 3. Share one Start-here strip builder between live render and cached paint

Owns: `public/app.js`, `public/dashboard.test.mjs` (only if a new pin is needed). Context (F5): `renderStartHere` (`public/app.js:156-170`) and the boot `paintLastGood` strip (`:1466-1480`) build the same copy, buttons, and handlers by hand; the next edit diverges the cold-load paint from the live strip.

Acceptance: one builder (+ binder) serves both call sites with identical output (stale mark only in the cached path); `npm test` green (397+); cold-boot strip and live strip carry the same buttons and actions.

### Task 4. Widen last_vetted tail so outcome lines cannot hide the vet date + test

Owns: `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Context (F6): the verdict-date probe reads only `substr(notes, -500)` (`:526`), so two outcome appends (~800 chars) after a vet push the `[date vetted]` tag out of the tail while `vetted_last_7d` still counts it.

Acceptance: probe tail widened but still bounded (e.g. 2000), `vetted_last_7d` semantics untouched; new real-path test (vet + two outcomes) reports the vet date on `last_vetted`/`last_verdict`; `npm test` green.
