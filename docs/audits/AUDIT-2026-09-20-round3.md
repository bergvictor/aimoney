# AUDIT 2026-09-20 — aimoney (round 3, overnight lane)

Clone HEAD: `0a49dc5` (round 2: one-click Win, worker inflow cap, revenue source). Live rev (`_night/live/aimoney_pages_dev_api_health.html`): `61d3aac` (round 1) — live is one round behind HEAD.
Lane metric right now (live payload, measured): **0 decisions/week, 0 vetted/week, $0.00 revenue/week, 11 unreviewed (oldest 164h ≈ 6.8d), 19/27 rows without a brief, 2 planned / 0 running experiments, 0 vetted-without-experiment, last ok run 2.3h ago**.
No repo-local `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING.md` (glob search empty); governed by `README.md` plus the session standing rules. Working tree arrived CRLF-dirty on all 32 tracked files (pre-existing checkout artifact: `git diff --stat` 7209+/7209−, `git diff --ignore-cr-at-eol --stat` empty — content untouched, left alone).
Surfaces judgment: live HTML is text-identical to `public/index.html` (`diff --strip-trailing-cr -q` exit 0; 5514 served vs 5635 here = exactly 121 CRLF bytes over 121 lines). Light palette only (`public/styles.css:4` `color-scheme: light`; `prefers-color-scheme|data-theme` appear only inside the guard tests that forbid them), favicon + `theme-color` (`public/index.html:9-10`, `public/favicon.svg` 285 bytes), inline brand mark (`public/index.html:16`), guard suite green. **Zero surface-rule findings this round.**

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — a ranked board of AI-money opportunities with evidence briefs and experiments (`README.md:1-15`).
- Entry points: dashboard `public/index.html` + `public/app.js`; API `functions/api/[[path]].js`; agent `worker/src/index.js` + `worker/src/lib.js`.
- Live surfaces: `https://aimoney.pages.dev`, `/api/health`, research worker (`README.md:27-31`, `deploy/public-surfaces.json:31-44`).
- Scheduled jobs: worker cron every 6h (`worker/wrangler.toml:8-9`); manual triage-only `POST /run` 202 with `briefs_skipped:true` (`worker/src/index.js:540-548`).
- Deploys: `./deploy/deploy.sh` (D1 schema+migrations+seed-if-empty, stamp `release.json`, Pages, worker), `./deploy/verify.sh` gates rollout (≤12h freshness); Pages auto-redeploys on push, worker via `.github/workflows/deploy-worker.yml`.

## 2. End-to-end walk-through

Signal → triage → proposal → brief → vet → experiment → close → money:

1. Collect: cron tick → `runResearch(env,"cron")` (`worker/src/index.js:182` via `scheduled` at `:484-485`) fans out HN/Reddit/GitHub (`:217`, `:48`, `:72`, `:96`), one batched `INSERT OR IGNORE` (`:222-227`).
2. Sweep + take: 30d-stale signals → noise with count (`:235-240`); oldest 6 unprocessed taken (`:242-244`); exact-URL matches link as supports before triage, no AI call (`:250-267` via pure `exactUrlTarget`, `:153-163`). Quiet ticks continue to the brief pass instead of early-returning (`:268-271`).
3. Triage: one Mistral NDJSON call over top-60 list + fresh signals (`:274-294`; budget `MAX_AI_CALLS=4`, `:17`); `parseJsonLines` (`worker/src/lib.js:49-64`); strict validation (`worker/src/index.js:296-315`); `new` inserts a `researching` + `UNREVIEWED` row with humility clamp (`:322-323`) and capped score (`:335`), at most 2 per run with overflow left unprocessed (`:317`, `MAX_NEW_PER_RUN`, `:16`); slug collision links as supports (`:343-355`); `supports` appends `[signal date]` evidence newest-kept (`:356-364`); else noise (`:365-367`).
4. Brief: cron only (`briefDeadline`, `:215`; manual `POST /run` always skips, `:548`, 202 `briefs_skipped:true`); oldest-unreviewed-first past 48h (`:374-388`); one Llama call (`:389-406`); empty/malformed JSON skipped silently (`:407-423`); one extra oldest bare row past 48h (`:429-473`); run closed with a redundant double `finish("ok")` (`:474-475`).
5. Serve: `GET /api/opportunities` with review queue `?unreviewed=1&sort=oldest` (`functions/api/[[path]].js:42-71`, capped `:66-69`); detail + briefs + experiments (`:73-84`); health counters incl. `decisions_last_7d`/`vetted_last_7d`/`revenue_last_7d`/`revenue_total` (`:355-391`); `GET /api/meta` (`:393-399`).
6. Decide (all human): boot `refresh()` (`public/app.js:1049-1068`); review rows carry capital + next action + source + brief summary with inline Vet/Kill (`:164-218`); Vet clears the marker and appends `[date vetted]`, toast offers Log experiment (`:360-378`); Kill takes an inline post-mortem line (`:380-404`); Start-here strip shows Vet/Kill for unreviewed top picks (`:95-130`); drawer + admin zone for rescore/status (`:640-687`, `:712-809`).
7. Experiment: modal log with dollar→cents conversion (`:907-958`, `:945-947`); board one-click Start (`:453-465`, button `:560`), one-click Lose (`:472-489`) and one-click Win with inline $ + line (`:498-515`, buttons `:561`); API create (`functions/api/[[path]].js:236-285`) and update (`:287-340`) enforce result + post-mortem on close (`:310-314`), stamp `started_at`/`ended_at` (`:309,:315`), append a `[$X rev / $Y spent via {source}]` outcome line to the parent (`:320-329`); drawer suggests a ±1 rescore, never auto-applied (`public/app.js:697-710`); zero-decision weeks get a read-only stale nudge that opens the drawer (`:539-549`).
8. Where it most often breaks or goes silent, in order: **(a)** live is one round behind HEAD (`61d3aac` vs `0a49dc5`), so one-click Win, the inflow cap, and revenue source are not live; **(b)** the human vet funnel — Vet → log experiment → flip to testing → Start is four actions across three screens, converting at zero (0 vetted, 0 decisions, oldest 6.8d); **(c)** the stale nudge fires in exactly today's stall and lands in the drawer instead of the board Start; **(d)** 19/27 rows bare while briefs cap at 2/cron-tick and manual runs brief nothing; **(e)** silent drops: the 30d sweep noises old signals (`worker/src/index.js:235-240`), malformed brief JSON is skipped without a marker (`:407-423`), bad-action verdicts mark signals processed with no parent (`:306-309`).

## 3. Health signals measured here

- `npm test` (this Linux checkout, exit 0): **182 pass / 0 fail, 64 suites, duration_ms 1355.02** — identical counts to the Windows baseline (`_night/tests-baseline.log`: 182/64, 483.56ms there; slower here is environment, not regression). Only warnings are the known `MODULE_TYPELESS_PACKAGE_JSON` reparsing notes (x3) and sandbox proxy notes.
- `python3 -m compileall`: N/A — repo contains zero `*.py` files (glob `**/*.py` search returned nothing; JS-only: Pages + Functions + Worker, `node --test` suites).
- `node --check`: exit 0 on `public/app.js`, `worker/src/index.js`, `worker/src/lib.js`. The API router check was refused by the shell-approval adapter (it aborts on the `[[path]]` glob chars, quoted or not); its parse is proven by the green suite, which imports it (`functions/api/api.test.mjs:6`, 20 API suites green).
- TODO density: **0** — regex search for `TODO|FIXME|XXX|HACK` across `public/`, `functions/`, `worker/`, `scripts/`, `deploy/`, `d1/` returns nothing.
- Dead code (all verified present): `BRIEF_DEADLINE_MS` defined but never referenced (`worker/src/index.js:21` vs live `briefDeadline` at `:215`); unused `scoreOf` import (`:140`, no other use in file); redundant double `await finish("ok")` (`:474-475`); `GITHUB_QUERIES.slice(0, 2)` silently drops the third query (`:116` vs `:28`); `appendKeepNewest` has zero production callers (`worker/src/lib.js:36-39`, tests only); five dead toast gates after modal returns (`public/app.js:363,383,456,475,501`, each self-commented as superseded).
- Duplicated logic: `clamp10` in the API (`functions/api/[[path]].js:12-16`) vs `worker/src/lib.js:4-7` (near-identical, not shared); classify prompt text duplicated between the pass (`worker/src/index.js:277-287`) and `/debug-classify` (`:519-528`); brief-prompt + brief-insert duplicated for main (`:393-420`) and extra (`:444-469`) briefs; money formatters `centsDollars` (`functions/api/[[path]].js:20`) vs `moneyCents` (`public/app.js:51`) are an intentional commented mirror; `UNREVIEWED` appears as SQL/string literals (`functions/api/[[path]].js:52,357,361`; `worker/src/index.js:335,338,377,384,431,437`) while the exported `UNREVIEWED_MARKER` (`worker/src/lib.js:21`) is used only inside lib; the vetted tag is string-coupled (`[date vetted]` written at `public/app.js:367`, counted via `LIKE '%vetted]%'` at `functions/api/[[path]].js:369-370`); create-vs-update string bounds differ (create slices, update stores raw: `functions/api/[[path]].js:91-107` vs `:129-133`, `:262-273` vs `:293-297`); worker token compare repeated 3x (`worker/src/index.js:495-497,510-512,541-543`) vs the API constant-time compare (`functions/api/[[path]].js:24-32`).
- Live surfaces: `_night/live/aimoney_pages_dev.html` is text-identical to `public/index.html` (`diff --strip-trailing-cr -q` exit 0; `cmp` differs at byte 16 only — this checkout's CRLF vs served LF). Surface rules all pass, so they expose zero findings: `color-scheme: light` (`public/styles.css:4`), no `prefers-color-scheme`/`data-theme`/theme-toggle in served CSS/JS/HTML (those strings appear only inside test assertions), `<link rel="icon" href="/favicon.svg">` + `<meta name="theme-color">` (`public/index.html:9-10`), inline brand mark matching the favicon accent and `$` glyph (`public/index.html:16`, `public/favicon.svg`, 285 bytes), guard test `public/tab-icon.test.mjs` (5 tests, green).
- Live health (`_night/live/aimoney_pages_dev_api_health.html`, 362 bytes): `{"ok":true,"rev":"61d3aac","db":"up","opportunities":27,"unreviewed":11,"bare_without_brief":19,"experiments_by_status":{"planned":2},"hours_since_last_ok_run":2.3,"oldest_unreviewed_age_h":164,"decisions_last_7d":0,"revenue_last_7d":0,"revenue_total":0,"spent_total":0,"vetted_last_7d":0,"vetted_no_experiment":0,"noise_24h":0}`. Worker is fresh (2.3h); every decision counter is zero; live rev trails HEAD (round-2 code not yet live).
- Note on acceptance: `git status --short` showed every tracked file as modified before I wrote anything (`git diff --ignore-cr-at-eol --stat` is empty — CRLF line endings only, pre-existing). I created only this file and edited nothing.

## 4. Ranked findings (max 8)

Impact order for the lane number (decisions/week, vetted→experiment, backlog drained), following the owner's rank: phone-clear first, worker autonomy second, a $0 experiment with a number third, money-real last. No surface-rule findings: the served page is light-only with icon, brand mark, and a guard test (see §3).

**F1 — Live is one round behind HEAD, so one-click Win, the inflow cap, and revenue source are not live.**
Evidence: live health reads `"rev":"61d3aac"` while clone HEAD is `0a49dc5` (`git log`: "Night round 2 (aimoney): Add one-click Win, worker inflow cap, and revenue source"); the round-2 branches report READY TO SHIP on `night/track_b2-*`, not merged to the live branch. Live therefore lacks the Win button (`public/app.js:561`), the 2-per-run cap (`worker/src/index.js:16-17,317`), and the `revenue_source` column/API (`d1/migrate-2026-09-20-revenue-source.sql:5`, `functions/api/[[path]].js:261,307-308`).
Why it costs money: every stall metric in §3 was measured against a board missing its newest drain tools — the exact buttons that let the owner close an experiment from his phone do not exist on live, and the queue still grows uncapped there.
Fix (operator action, no code): merge the round-2 branch to the live branch, run `./deploy/deploy.sh` (additive migration already tolerated at `deploy/deploy.sh:34-38`, stamps rev) then `./deploy/verify.sh` including the ≤12h freshness gate (live `hours_since_last_ok_run:2.3`, so the gate passes). Touch nothing in the repo. Size S. Risk: low (no pending schema beyond the additive column; seed-if-empty guards live rows).

**F2 — Vet → log → testing → Start is still four actions across three screens; the funnel converts at zero.**
Evidence: `public/app.js:360-378` (Vet PATCHes notes, toast at `:373` offers Log experiment) → `openExperimentModal` (`:907-958`, 9+ fields) → admin status flip (`:726-741` + save `:744-789`) → `startExperiment` one PATCH (`:453-465`, board button `:560`). Four visits per row, three screens (review list, modal, drawer/board). Live: `vetted_last_7d:0`, `vetted_no_experiment:0` only because nothing was vetted at all.
Why it costs money: this is the lane funnel and it converts at zero — 11 unreviewed rows age (oldest 6.8d) while each extra screen between "this looks good" and "test started" sheds the decision.
Fix: add one "Vet & log starter" action on review rows that PATCHes the vet tag, POSTs a `planned` experiment seeded from the row (name/hypothesis/metric prefilled, human-editable), and flips the row to `testing`, then toasts with a Start shortcut. Compose existing endpoints; no API change; update the vet-handoff static tests (`public/dashboard.test.mjs:77-81,313-317`). Touch `public/app.js`, `public/dashboard.test.mjs`. Size M. Risk: low — token-gated like Vet, each write is the same call the human makes today.

**F3 — The stale nudge fires in exactly today's stall state and lands in the slowest path.**
Evidence: when `decisions === 0` the header names the stalest open card (`public/app.js:542-546`) — true right now (`planned:2`, decisions 0) — but the button calls `openDrawer(nudge.opportunity_id, nudge.id)` (`:546`), where the only action is Update (`:670`) → modal, instead of the one-click Start the board already owns (`:560` → `:453-465`). The dashboard suite pins the slow path: `"nudge must open the stalest card drawer"` (`public/dashboard.test.mjs:69`).
Why it costs money: the single nudge designed for this stall adds 3+ taps to the one action that would move `running` off zero. Read-only visibility again, not removed work.
Fix: make the nudge button switch to the Experiments tab and highlight/scroll to the card (or carry its own Start when the card is `planned`); still human-pressed, still no auto-transitions; update the pinned static test. Touch `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk: low.

**F4 — Add / Log-experiment / Run dead-end on a toast when the token is missing; Vet/Kill/Start/Lose/Win open the modal.**
Evidence: `public/app.js:860` (Add), `:908` (experiment modal), `:990` (Run) do `return toast("Enter the admin token first.")`, while `:361,381,454,473,499` do `return openAdminModal(...)`; the dashboard suite pins only the modal half (`public/dashboard.test.mjs:428-435`, count exactly 5).
Why it costs money: the exact path F2 relies on — Vet toast → "Log experiment" (`:373`) — dies with a toast if the token lapsed, stranding a decided row on a phone. Same for the manual research trigger.
Fix: route all three through `openAdminModal` with the same subnote pattern (`:822-838`); extend the dashboard static tests (the count-5 assertion becomes 8). Touch `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk: low — API 401 behavior unchanged.

**F5 — Kill and Lose still require typing on a phone; 11 rows need clearing.**
Evidence: both route through `inlinePostMortem` (`public/app.js:261-299`, `maxLength 200`, empty cancels, row untouched), invoked at `:403` (Kill) and `:488` (Lose). The gate is a non-empty line, so every kill is a typing exercise on glass.
Why it costs money: typing is the per-row bottleneck for draining 11 unreviewed rows from the bottom. Presets keep the gate (a real line is still sent) while turning the common cases into one tap.
Fix: offer 3 one-tap preset reasons beside the input (Kill: duplicate / no buyer / no evidence; Lose: no replies / no conversion / cost > return) plus the existing custom input; a preset submits as the line. API gate untouched. Touch `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk: low — the closure/post-mortem requirement still holds; presets are human-chosen.

**F6 — Agent evidence appends can move the human's vetted metric and evict the vetted tag.**
Evidence: all three agent appends bump `updated_at` to now: the exact-URL pre-pass (`` `UPDATE opportunities SET notes = substr(notes || ?, -8000), updated_at=strftime('%Y-%m-%dT%H:%M:%SZ','now') WHERE id=?` ``, `worker/src/index.js:263`), the slug-collision path (`:351`), and the supports path (`:360-364`). But `vetted_last_7d` counts `notes LIKE '%vetted]%' AND updated_at >= ... '-7 days'` (`functions/api/[[path]].js:369-370`) — so a row vetted a month ago that receives one supporting signal today counts as "vetted this week" with no human involved. Secondarily, the newest-kept 8000-char window (`substr(notes || ?, -8000)`) lets heavy signal traffic push an old `[date vetted]` tag out of notes entirely, silently un-vetting the row in health counts.
Why it costs money: `vetted_last_7d` is half the lane metric ("vetted proposals that become experiments"), and today agent activity — not a human decision — can move it in both directions. The header presents `N vetted this week` (`public/app.js:533`) while the query measures "tagged and touched".
Fix: write the rule down (`README.md`: "agent evidence appends never move human counters — no `updated_at` bump, no status move") and drop `updated_at` from the three agent APPEND statements (keep it on human writes and the outcome ledger at `functions/api/[[path]].js:327`); add an API test pinning a vetted row + signal append staying outside `vetted_last_7d`. Touch `worker/src/index.js`, `worker/src/index.test.js`, `functions/api/api.test.mjs`, `README.md`. Size S. Risk: low-medium — touches SQL in the hot path; keep `sort=updated` behavior covered by existing tests.

**F7 — The concrete $0 experiment exists and sits unpressed: the spec-ad sprint.**
Evidence: `d1/seed.sql:67-71` logs `Spec-ad sprint: 10 brands, 10 free ads` as `planned`, budget `$0 + 10 hours`, metric `replies; pilots closed`, target `>=3 replies; >=1 pilot at >=$500`; `public/app.js:451-452` names it the first Start candidate ("a human still presses it"); live shows `planned:2`, running 0, decisions 0. (The second live `planned` row's identity is not in repo data — seed holds one; it was added live by agent or human.)
Why it costs money: decisions/week cannot move until something runs, and the cheapest possible start — $0, one press — has been idle while the board accumulated visibility. No code is missing for the start itself.
Fix (owner action, proposed concretely): press Start on the spec-ad card this week; ship 10 finished spec ads to 10 real DTC supplement brands built from public assets by day 2; count replies on day 7 (that count IS the week's number); at day 14 close won (≥1 $500 pilot, amount + source entered via F8's path) or lost (reply count as the result line). If the second live `planned` experiment duplicates it, Lose the weaker one first with one line. Files to touch: none — press Start. Size S (one press + 10h of sending). Risk: 10 hours, $0 spend; never mark it decided without the reply/pilot counts.

**F8 — One-click Win records money WITHOUT a source — the cheap path skips what round 2 just built.**
Evidence: `inlineWinClose` renders only an amount input + post-mortem line (`public/app.js:304-354`), and `doWin` PATCHes `{ status:"won", result, post_mortem, revenue_cents }` (`:503-509`) — no `revenue_source`. The modal path sends it (`:928` input, `:947` payload) and both ledger appends render `via {source}` (`functions/api/[[path]].js:277,323-324`), but a phone Win — the path the owner will actually use — lands sourceless: `($X rev / $0.00 spent)` with no `via` bit.
Why it costs money: the first real $500 win closed from a phone renders as un-auditable money with no proof of origin — untrusted and unrepeatable. Lens item 4 (amount + date + source) holds on the slow path and fails on the fast one.
Fix: add an optional one-line source input to the Win inline row (same row, still one screen) and send it as `revenue_source` (≤120 chars, truncate like the modal); empty source renders exactly as today. Extend the Win dashboard tests (existing asserts on the row shape at `public/dashboard.test.mjs:366-375` stay green). Touch `public/app.js`, `public/dashboard.test.mjs`. Size S. Risk: low — additive field, same clamp/validation idioms as the modal.

## 5. What NOT to change

- The scoring formula and the honest cap: `score = 100 * (value * confidence * fit) / (effort + 1)` (`README.md:56`, `worker/src/lib.js:15-16`) with `UNREVIEWED` rows clamped to ≤6000 (`worker/src/lib.js:29-32`). Fix ceilings and friction, not the math.
- "The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)." (`worker/src/index.js:4-5`; also `README.md:72`: "`researching → testing` is a human decision".) F6's rule deliberately moves no status.
- "No auto-transitions — the human still owns every status move." (`public/app.js:541`; drawer rescore "never auto-applied", `:697-699`.) Start/Win/Lose/Vet/Kill stay human-pressed.
- "Killed strategies stay on the board with their post-mortem — that is the point." (`README.md:63-64`.) Never delete killed rows or their notes.
- The experiment closure gate: `won`/`lost` require non-empty `result` + `post_mortem` (`functions/api/[[path]].js:310-314`), `ended_at` stamped (`:315`). F5/F8 send real lines through it; they do not weaken it. Never mark an experiment decided without evidence, never invent a revenue figure.
- Public-read / token-write split with constant-time compare (`functions/api/[[path]].js:24-32`); `ADMIN_TOKEN` rotation stays a human deploy step (`README.md:85-88`: set Pages env + worker secret, redeploy both, re-enter in dashboard). No tokenless writes, no stored bypass.
- Idempotent deploy: schema always, seed only when the table is empty (`deploy/deploy.sh:30-47`). Never reseed over live rows.
- Manual-triage-only + briefs-on-cron: "Manual `Run triage now (briefs on cron)` collects and triages only (`POST /run` 202 carries `briefs_skipped:true`); briefs land on cron ticks" (`README.md:77`; `worker/src/index.js:548`). F1/F6 preserve the manual skip.
- Anything needing a login, credential, payment, or a human decision: Cloudflare deploys, `CLOUDFLARE_API_TOKEN`, sending the spec ads, pricing a pilot, vet/kill calls, closing experiments. This audit changed no code and read no secrets.

## 6. Proposed next tasks (2–4)

Prerequisite (operator, not code): F1 — merge round 2 to the live branch and deploy, so Win, the inflow cap, and revenue source actually reach the board.

### Task 1: One-tap Vet & log starter: vet, seed a planned experiment, flip to testing

- Owned files: `public/app.js`, `public/dashboard.test.mjs`.
- Acceptance: (a) review rows show "Vet & log starter" beside Vet; (b) one tap PATCHes the vet tag, POSTs a `planned` experiment prefilled from the row (human-editable), flips the row to `testing`, toasts with a Start shortcut; (c) token-gated like Vet (modal, not toast); (d) API unchanged — same three calls the human makes today; (e) dashboard tests cover happy-path, token gate, and prefill; (f) `npm test` green.

### Task 2: Win inline row takes an optional revenue source, sent with the close

- Owned files: `public/app.js`, `public/dashboard.test.mjs`.
- Acceptance: (a) Win inline row adds a one-line source input beside $ + post-mortem; (b) close PATCHes `revenue_source` ≤120 chars, truncated like the modal; (c) empty source renders exactly as today (no `via` bit); (d) empty post-mortem line still cancels, row untouched; (e) dashboard tests pin source sent/truncated/empty; (f) `npm test` green.

### Task 3: Stale nudge deep-links to the board card instead of opening the drawer

- Owned files: `public/app.js`, `public/dashboard.test.mjs`.
- Acceptance: (a) the zero-decision nudge button switches to the Experiments tab and highlights/scrolls to the stalest card (or carries its own Start when `planned`); (b) still human-pressed, no auto-transitions; (c) the pinned "nudge must open the drawer" test is updated to the new path; (d) nudge still fires only when `decisions_last_7d` is 0; (e) `npm test` green.

### Task 4: Route Add/Log-experiment/Run token gates to the admin modal

- Owned files: `public/app.js`, `public/dashboard.test.mjs`.
- Acceptance: (a) Add opportunity, Log/Update experiment, and Run triage open the admin modal with a subnote instead of a dead-end toast when the token is missing; (b) API still 401s without the token; (c) dashboard static tests cover all three gates (count-5 assertion becomes 8); (d) `npm test` green; (e) no API change.
