# AUDIT 2026-09-20 round 3 (track B) — aimoney (read-only)

Lane metric: experiments reaching a decision (won/lost with post-mortem) per week, and vetted proposals that become experiments; unreviewed backlog drained.
Live snapshot used: `_night/live/aimoney_pages_dev_api_health.html` (rev 4a9d315, 2026-09-21T07:28:51Z). HEAD here is eadb920.

## 1. What this project is and how it runs

- Purpose: ranked list of AI-money opportunities with research briefs, experiments, and a cron research agent that proposes new rows; a human vets, tests, kills/scales (`README.md:3-16`).
- Entry points: `public/index.html` + `public/app.js` (dashboard); `functions/api/[[path]].js` (REST API on D1); `worker/src/index.js` (research agent: cron + `POST /run`).
- Live surfaces: `https://aimoney.pages.dev` (dashboard), `/api/health`, `/api/opportunities`, `/api/runs`, worker `GET /` status (`deploy/public-surfaces.json`).
- Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`); each tick collects signals, triages (≤6), briefs (1+1 on cron).
- Deploy: Git-connected Pages build stamps `public/release.json` via `scripts/stamp-release.sh:5-8`; `deploy/deploy.sh` applies `d1/schema.sql` (`:33`) + shared `deploy/apply-d1-migrations.sh` (`:37`) + Pages + worker; CI `.github/workflows/deploy-worker.yml` runs schema (`:27`) + the same shared migration script (`:33-34`); `deploy/verify.sh` gates the rollout on live revision (`:62-100`) and the app.js parse gate (`:36-52`).

## 2. End-to-end walk-through

Signal → triage → proposal → review → brief → experiment → decision → money:

1. Cron fires `scheduled` (`worker/src/index.js:598`), `runResearch` (`:281`) reaps runs stuck past 30 min (`:285-290`) and opens a run row (`:291-300`).
2. Collect: `hnSignals` (`:49`), `redditSignals` (`:78`), `githubSignals` (`:106`) in parallel (`:316`), one batched `INSERT OR IGNORE` (`:327-332`), 30d stale sweep (`:341-345`), heartbeat `phase:` mark (`:333-336`).
3. Take oldest 6 unprocessed (`:347-349`); exact-URL pre-pass links duplicates as supports with no AI call (`exactUrlTarget` `:168`, pre-pass `:355-381`).
4. Classify: `buildClassifyPrompt` (`:245`, called `:396`) → `aiComplete` (`:136`, budgeted, racing a timeout) → `parseJsonLines` (`worker/src/lib.js:62`); verdict loop (`:431`) INSERTs `new` rows as `researching` + `UNREVIEWED` with score capped ≤6000 (`:458-473`, `effectiveScore` `worker/src/lib.js:29-32`), or `supports` (`:487-495`) / `noise`; inflow capped at 2/run, 1 while bare-without-brief > 10 (`:411-415`), 0 while unreviewed > 10 (`:420-423`); writes flush via `flushVerdictWrites` with per-write fallback (`:264-280`, called `:500`).
5. Brief: top-scored bare row, or oldest-unreviewed-first past 48h (`:511-526`); one extra oldest-unreviewed bare row past 48h on cron (`:549-581`); shared `buildBriefPrompt`/`parseBriefJson` (`:202-216`) and shared `insertBrief` (`:224-238`); run log carries mode + `src_fail`/`brief:failed`/`verdict:N_failed` (`:584`); D1-dead ticks log `cron-failed` to the tail (`:592`, `:603-605`, `:667-669`).
6. Health: 12 probes, one `env.DB.batch` (`functions/api/[[path]].js:464-478`), per-probe isolation fallback naming `health_probe_failures` + column detail (`:487-516`), self-migration retry of money probes (`:525-541`), payload (`:605`).
7. Review: dashboard Needs-review chip → client-side oldest-first queue (`public/app.js:240-263`); Vet (`:429-449`) clears `UNREVIEWED` and appends `[YYYY-MM-DD vetted]` via `vettedNotes` (`:409-412`), which the `vetted_7d` probe counts (`[[path]].js:473`) and `last_vetted` reports (`:563-575`); Kill requires a one-line post-mortem (`:493-519`); Vet-&-starter (`:457-491`) composes Vet + planned experiment + flip to `testing`.
8. Experiment: `POST /experiments` (`[[path]].js:324`); Start stamps `started_at` (`:401`); Win/Lose require `result` + `post_mortem` (`:402-407`), stamp `ended_at`, append the `$X rev / $Y spent [via source]` outcome-ledger line to the parent (`:412-421`); `decisions_last_7d` counts won/lost closed in 7d (`:470`), revenue probes SUM cents over won/lost (`:471-472`).
9. Where it most often breaks or goes silent: the human step. Cron is alive (`hours_since_last_ok_run` 1.5) but 11 rows sit unreviewed (oldest 175.1 h), 19/27 have no brief, 2 experiments sit `planned` with zero running, and `decisions_last_7d`/`vetted_last_7d` are 0 — nothing has moved through steps 7–8 in 7+ days. Second suspect: the brief pass may be wedged on one poison row with no marker (finding 4). Third: the starter half of step 7 will 503 on live D1 even after the owner flips the migration switch (finding 3).

## 3. Health signals measured here

- Test suite: `npm test` → `node --test` over 7 files, exit code 0. Visible TAP window shows top-level suites ok 1–46+ (all `ok`, zero `not ok` in the visible 40,000 bytes of ~97,000). Failing-path suites are visibly green in that window (health probe isolation, outage honesty, experiment-write 503, self-migration switch, migration convergence with its positive control) — the suite executes real tests, not zero. (The saved-output tail lives outside the workspace and its read was denied, so totals are exit-code 0 + visible window, stated honestly.)
- Compile: N/A `compileall` (JS repo). `node --check public/app.js` exit 0; `node --check worker/src/index.js` exit 0. All source modules load under the suite, including ESM `functions/api/[[path]].js` (one `MODULE_TYPELESS_PACKAGE_JSON` warning, non-failing).
- TODO density: zero in shipped source. Regex search for `TODO|FIXME|HACK|XXX|deprecated` over `public/app.js`, `public/styles.css`, `worker/src`, `functions/api/[[path]].js`, `d1/schema.sql`, `deploy`, `scripts` returns nothing; positive control: the same tool in the same mode returns hits for `color-scheme`, `console\.\w+`, `UNREVIEWED`, and `revenue_cents` — so the zero is real. `console.*` in shipped source is only the three deliberate `console.error("cron-failed", …)` markers (`worker/src/index.js:592`, `:604`, `:668`); no `debugger`.
- Dead code: none found. `appendKeepNewest` (`worker/src/lib.js:49`) is imported only by its own unit test, but it is the documented executable mirror of the live SQL idiom `substr(notes || ?, -8000)` used by every writer — tested spec, not rot.
- Duplicated logic: (1) `centsDollars` (`[[path]].js:82`) vs `moneyCents` (`public/app.js:53`) — intentional documented mirror, keep; (2) `clamp10` in the API (`:74-78`) vs `worker/src/lib.js:4-7` — near-identical, the API imports only `effectiveScore`/`tokensMatch` (`:10`); (3) `inlinePostMortem` (`public/app.js:294-332`) vs `inlineWinClose` (`:338-399`) share input/confirm/cancel/Escape handling (~60 lines) with no shared builder — the largest remaining duplication, cosmetic only.
- Line endings (implementers read this): `file public/app.js` reports LF with no CRLF note in this checkout; prior audits found a mixed worktree elsewhere, so match each file's actual endings, replace whole lines, re-read after every edit.
- Surface rules (all pass, zero findings): `color-scheme: light` (`public/styles.css:4`), no `prefers-color-scheme`/`data-theme`/theme-toggle/`debugger` string in shipped `public/index.html`, `public/app.js`, `public/styles.css`, `public/favicon.svg` (those strings appear only in guard-test assertions — the matcher is proven live, see positive control above); `<link rel="icon" … href="/favicon.svg">` + `<meta name="theme-color" content="#0d6b3f">` (`public/index.html:9-10`); hand-written 32×32 `$`-on-`#0d6b3f` `public/favicon.svg` (<2 KB); inline brand mark reusing the same geometry/accent/glyph (`public/index.html:16`); guard suites green inside `npm test`. Saved live HTML matches the served shell on all markers (`_night/live/aimoney_pages_dev.html:9-10`, `:16`).
- First five seconds (pass): the served shell owns the Start-here strip container above the ledger plus the Needs-review and Zero-spend chips (`public/index.html:42-54`); the strip renders #1-by-score with $/mo, capital, next action, and Vet/Kill when unreviewed (`public/app.js:103-133`); phone rules collapse the masthead under 760px (`public/styles.css:210-215`); first paint reuses the last-good localStorage snapshot with a stale mark past one 6h tick (`public/app.js:1235-1345`).
- Build identity: one source of truth — `/release.json` stamped by `scripts/stamp-release.sh:5-8` on Pages builds and `deploy/deploy.sh:58` on manual deploys, echoed as `rev` in `/api/health`, checked by `deploy/verify.sh:62-100` (JSON parse required; HTML/unparsable/`dev-undeployed` placeholder fail immediately; stale revisions retried bounded 6×10s). The committed `public/release.json:1` is the `dev-undeployed` placeholder by design — do not judge the edge by the committed file. No second source should be added. Caveat: the revision is a 7-char short SHA (finding 7).

Money-first answers (owner directive), in numbers from this project's own data: earned to date **none** — `revenue_total` is null (unknown: migration disabled) and no experiment has ever closed (`experiments_by_status {"planned": 2}`, zero won/lost), so no revenue row can exist; last krone arrived **never**. This lane is a tool the owner uses, not a product he sells — it ranks by decisions/week and vetted→experiment conversion, both currently 0. Single step to the next payment: a human vets one opportunity and starts its zero-spend starter (Vet & log starter → Start). Measured conversion at that step: unmeasured (zero attempts). No projection, no rate×volume figure, no attempt counted as an outcome.

## 4. Ranked findings (max 8)

1. **Nothing automated was ever supposed to vet — `vetted_last_7d: 0` is by design, and the queue now waits on the owner.**
   Evidence: the only path that clears `UNREVIEWED` is a human tapping Vet: `vetOpportunityInner` (`public/app.js:434-449`) PATCHes notes via `vettedNotes` (`:409-412`) into `updateOpportunity` (`functions/api/[[path]].js:207-248`). The worker only ADDS the marker (`worker/src/index.js:469` — `"UNREVIEWED, scores capped until a human vets it"`); no worker or API path removes it, and the file header states "The agent PROPOSES" (`:4-5`). `README.md:72` confirms: "`researching → testing` is a human decision … The agent never moves status." HEAD additionally pauses inflow entirely while unreviewed exceeds 10 (`worker/src/index.js:420-423`), so at 11 the system is deliberately frozen until he acts.
   Why it costs: the lane metric is decisions/week and vetted→experiments — both 0 for 7+ days while five rounds improved machinery; no guard, budget, or query is broken, so no code fix moves the number. Only verdicts do.
   Fix: the owner reviews the 11 oldest-first via the Needs-review chip — each row already shows capital, next action, source, and brief summary with Vet / Vet-&-starter / Kill inline (`public/app.js:167-225`), no drawer round-trip, about a minute a row from a phone. No code files. Size M (human time). Risk: none — verdicts are reversible text.
2. **Live revenue fields are null because the self-migration switch is off — owner action.**
   Evidence: live `{"health_probe_failures":["revenue_week","revenue_lifetime"],"health_probe_detail":{"revenue_week":"revenue_cents","revenue_lifetime":"revenue_cents"},"schema_missing_columns":["revenue_cents","spent_cents"],"schema_migration":"disabled (set ALLOW_SCHEMA_MIGRATION=1 to add missing columns)"}`; gate `if (String(env.ALLOW_SCHEMA_MIGRATION || "") !== "1")` at `functions/api/[[path]].js:53`.
   Why it costs: the money leg of the lane metric is invisible — four fields read unknown instead of honest numbers, and finding 1's starter economics cannot be recorded.
   Fix: owner sets Pages env var `ALLOW_SCHEMA_MIGRATION=1`; the next health call ADDs exactly the two columns from `d1/migrate-2026-09-20-experiment-cents.sql` (`[[path]].js:27-30`) and reports numbers on the same call (`:525-541`). No code change. Size S. Risk: low — ADD COLUMN with DEFAULT, once per isolate, no endpoint.
3. **After the owner flips that switch, "Vet & log starter" still 503s — `revenue_source` is deliberately excluded from the self-migration but mandatory in the write.**
   Evidence: `createExperiment` INSERTs `revenue_source` explicitly (`functions/api/[[path]].js:351-356`) and `updateExperiment` UPDATEs it (`:423-430`); the self-migration covers only probe-read cents columns by design (`README.md:83`: "write-only columns are never auto-migrated"), and any `no such column` on those paths answers 503 (`[[path]].js:92-96`, `:633`). Live D1 almost certainly lacks `revenue_source` too (same-day migration, CI never applied it — `.github/workflows/deploy-worker.yml:33-34` cannot run without secrets). Effect on the queue-clearing path: the Vet PATCH succeeds, the starter POST fails, the flip to `testing` is skipped (`public/app.js:469-490` awaits in order), and the owner sees "Starter failed: missing column: revenue_source".
   Why it costs: the exact one-tap flow that clears finding 1 completes only one-third on live D1 — vetted but experiment-less, which is the `vetted_no_experiment` trap the header warns about.
   Fix: in `createExperiment`/`updateExperiment`, when the write fails naming exactly `revenue_source` (reuse the existing narrow `missingColumnOf` match), retry the statement once without that column — semantics identical to its `DEFAULT ''`. Keep the 503 for any other column. Add tests driving the real POST/PATCH paths against an old-schema stub asserting 201/200 with `revenue_source` reported as `""`. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Size S. Risk: low — no schema change, no data rewrite, failure mode unchanged for genuinely broken writes.
4. **The brief pass can wedge silently on one poison row — the plausible mechanism behind bare-without-brief stuck at 19.**
   Evidence: bare-row selection is deterministic — top-scored, or oldest-unreviewed-first past 48h (`worker/src/index.js:511-526`, extra pass `:549-563`) — with no memory of prior failure. A brief that parses but lacks `summary`/`first_steps` makes `insertBrief` return false (`:224-238`), which the caller treats as a silent skip: `if (await insertBrief(…)) { state.briefs++; }` (`:538-540`, same `:575-577`) sets no `brief:failed` marker (`briefFailed` is only set when parse or write THROWS, `:541`). Next tick selects the identical row. Result: `ok` runs, a `brief:oldest-first` mode in the log, `briefs: 0`, no alarm — exactly the live signature (bare 19 static across snapshots, `hours_since_last_ok_run` 1.5). Note the `aiComplete` throw path (`:531-535` sits outside the try) would fail the whole run instead, so the silent wedge specifically implicates a deterministically keyless/empty brief for one row.
   Why it costs: unbriefed rows cannot be decided in under a minute from a phone; 19 rows stay unactionable and finding 1 never clears even with the owner willing.
   Fix: when `insertBrief` returns false, log the row id in the run error (e.g. `brief:skipped:<id>`) and rotate selection past ids skipped in the last N runs (kept in the run log or a bounded in-memory set — no schema change); add a test driving two ticks with stubbed AI returning `"{}"` asserting the second tick briefs a DIFFERENT bare row and the first id is named. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size M. Risk: low — brief order only, no status moves.
5. **Revenue recorded on an open experiment is invisible — every money probe filters to won/lost.**
   Evidence: the experiment modal accepts Revenue/Spend/Source on ANY status (`public/app.js:1071-1073`, sent `:1090-1092`) and board cards display it regardless of status (`:700`: `moneyCents(e.revenue_cents)` rev `via source`), but the weekly probe (`functions/api/[[path]].js:471`) and the lifetime probe (`:472`) both SUM only `WHERE status IN ('won','lost')`.
   Why it costs: money-path silent drop — a running experiment already earning, faithfully recorded via Update, contributes $0 to `revenue_last_7d`, `revenue_total`, and `spent_total` until the day it closes. The board and the health totals disagree by construction.
   Fix: drop the status filter on the lifetime totals probe only (recorded cents are recorded cents; the lifetime label stays true), keep the weekly probe bound to rows closed in-window via `ended_at`, and update the one-line probe semantics in `README.md:75`. Add a test with revenue on a `running` row asserting `revenue_total` includes it while `revenue_last_7d` does not. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`, `README.md`. Size S. Risk: low-medium — changes a reported number's definition; the test pins the new contract.
6. **The live edge is one round stale: it serves round-1 rev while HEAD is round-2.**
   Evidence: saved health `"rev":"4a9d315"` vs `git rev-parse --short HEAD` → `eadb920`; `git log --oneline` shows `eadb920` (dashboard syntax repair + app.js parse gate + pause-inflow-past-10) on top of `4a9d315`. The served shell matches HEAD's markers but the edge lacks the newest worker/API behavior.
   Why it costs: every round is judged by the live surface; a stale edge reads as failed work, and `deploy/verify.sh:62-100` would fail its live-revision gate against HEAD right now.
   Fix: check the Pages deployment for `eadb920` (build log for the Git-connected project), let the queued build finish or re-run the deploy, then run `deploy/verify.sh eadb920`. Do not add a second version source. Not this lane (needs deploy credentials). Size S. Risk: low.
7. **Build identity is a 7-char short SHA, not the full 40-char commit the fleet standard requires.**
   Evidence: `scripts/stamp-release.sh:5-6` (`cut -c1-7`), `deploy/deploy.sh:23` (`git rev-parse --short HEAD`), live `"rev":"4a9d315"`. `release.json` is correctly the single source of truth (no `/build.json` second source should be added) and `verify.sh` correctly requires JSON-not-HTML — but a short SHA is a weaker identity than the specified full `revision`.
   Why it costs: a short revision can theoretically collide or mismatch across roles (verify compares short-to-short consistently today, so nothing is broken — this is hardening, not a fire).
   Fix: stamp the full 40-char SHA as `revision` (keep it the only version source) and have `verify.sh` accept the expected short SHA as a prefix of the served full revision; extend the deploy test to pin a 40-char `revision` in stamped output. Touch `scripts/stamp-release.sh`, `deploy/deploy.sh`, `deploy/verify.sh`, `deploy/deploy.test.mjs`. Size S. Risk: low — old edges serving short SHAs still prefix-match during rollout.
8. **`last_vetted` costs a full-notes scan of every vetted row on every health call — unbounded growth.**
   Evidence: probe 11 selects full `notes` from all vetted rows (`functions/api/[[path]].js:476`: `SELECT notes FROM opportunities WHERE notes LIKE '%vetted]%'`) and parses tags in JS (`:563-575`). Each notes body runs to 8000 chars; every dashboard load and every `verify.sh` freshness poll pays the scan, and it grows with each vetting.
   Why it costs: waste, not breakage — today a handful of rows, but the cost scales with exactly the success finding 1 asks for, and D1 bills the bytes.
   Fix: bound the scan without a schema change — tags are appended at the END (`vettedNotes`, `public/app.js:409-412`), so select `substr(notes, -512)` instead of full bodies; add a test with an 8000-char notes body asserting the tag is still found (plus a positive control: a tag buried past the window is missed, documenting the bound). Alternatively memoize per isolate like `cachedHealthRev` (`[[path]].js:14`). Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Size S. Risk: low.

## 5. What NOT to change

- Vetting stays human: "The agent never moves status" (`README.md:72`); "it never moves status to testing/scaling/killed" (`worker/src/index.js:5`). Do not automate vetting, and do not invent verdicts to make the queue look worked.
- `ALLOW_SCHEMA_MIGRATION=1` is the owner's switch: "Default-off: unset changes nothing" (`functions/api/[[path]].js:23`); ADD-only, once per isolate, no endpoint (`:25-32`, `:58-72`). One line for him: Pages → Settings → Environment variables → add `ALLOW_SCHEMA_MIGRATION=1`, redeploy.
- `CF_API_TOKEN` / `CF_ACCOUNT_ID` repo secrets are his to add (CI `deploy-worker.yml:20-26` fails fast without them; migrations `:33-34` never run). One line: repo Settings → Secrets → Actions → add both, re-run Deploy research worker.
- Promoting HEAD to the live edge (finding 6) needs deploy credentials and is not this lane's; never enter or store a credential here.
- Never spend money, create accounts, enter payment details, sign up for anything, or accept terms — all his, per the brief's hard boundaries. Never start an experiment that incurs cost or makes an outward-facing commitment; planning is the work, running is his decision.
- Never touch the production database directly (`wrangler d1 execute --remote`); write through the app's own paths. Applying migrations to live D1 is his action.
- `revenue_source` is deliberately NOT auto-migrated: "write-only columns are never auto-migrated" (`README.md:83`); experiment writes 503 naming only that column by design (`functions/api/[[path]].js:86-96`). Finding 3 tolerates its absence; it does not migrate it. Do not widen the self-migration beyond the probe-read cents columns and do not add a migration endpoint.
- Never invent a revenue figure or mark an experiment decided without evidence; never present projections as results; never multiply an unverified rate by a volume.
- The committed `dev-undeployed` placeholder in `public/release.json:1` is by design (the stamp step replaces it at build time); `verify.sh` fails a live placeholder. Do not hardcode a revision.

## 6. Proposed next tasks

Pipeline report for this round: reviewed 0, rejected 0 (no commonest reason), briefs written 0, no best candidate named from live data — all pipeline work is task 1 below.
Unreviewed is still 11 because this lane was read-only (findings file only, no network, no writes), so no verdict could be recorded.

1. **Vet-or-kill the 11 unreviewed rows oldest-first via Needs-review chip (≈11 min)**
   Owned files: none (live data via dashboard only; verdicts in rows' own notes/tags, no new schema, no new file). Acceptance: `/api/health` shows `unreviewed` 0 (or each remaining row has a recorded concrete blocker), `vetted_last_7d` equals rows vetted, `last_vetted` is today; every verdict carries its reason in the row (vet tag or `[date killed]` + post-mortem); report counts reviewed/rejected + commonest rejection reason. Use Vet & log starter for rows worth testing so each vet links straight to its experiment.
2. **Make experiment writes tolerate a missing revenue_source column (unblocks Vet-&-starter)**
   Owned files: `functions/api/[[path]].js`, `functions/api/api.test.mjs`. Acceptance: POST/PATCH `/experiments` against an old-schema stub (no `revenue_source`) return 201/200 with `revenue_source` effectively `""`; any other missing column still 503s naming only it; full `npm test` green; whole-line edits verified by re-read.
3. **Name skipped brief rows and rotate past them so one poison row cannot wedge the pass**
   Owned files: `worker/src/index.js`, `worker/src/index.test.js`. Acceptance: two-tick stubbed-AI test returning `"{}"` shows the second tick briefing a different bare row with the first id named in the run error; no new schema; full `npm test` green; whole-line edits verified by re-read.
4. **Include open-row revenue in lifetime totals; keep weekly bound to closed rows**
   Owned files: `functions/api/[[path]].js`, `functions/api/api.test.mjs`, `README.md`. Acceptance: revenue on a `running` row appears in `revenue_total`/`spent_total` but not in `revenue_last_7d`; `README.md` probe semantics updated in one line; full `npm test` green.
