# AUDIT-2026-09-20-round3 (aimoney) — read-only audit, 2026-09-21

Scope: clean clone at HEAD `af7f6d4`, no network, no installs, no edits to existing files.
Live numbers below are from the runner-fetched snapshots in `_night/live/` (health fetched
2026-09-21T13:43:11Z at rev `2fa64a9`, one round behind this clone). Lane: aimoney —
experiments decided/week, vetted proposals becoming experiments, unreviewed backlog drained.

## 1. What this project is and how it runs

1. AIMoney Lab: a ranked ledger of AI-money opportunities with research briefs, experiments, and a cron research agent — a tool the owner uses to find/test money-makers, not a product he sells.
2. Entry points: static dashboard `public/app.js`, Pages API `functions/api/[[path]].js` (D1), worker `worker/src/index.js` (cron + `POST /run`).
3. Live surfaces: `https://aimoney.pages.dev`, `/api/health`, `/api/opportunities`, `/api/runs`, worker `/` status; D1 `aimoney`, Workers AI (worker only).
4. Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:8-9`); manual triage via dashboard button → `POST /run` (202, briefs skipped).
5. Deploys: Pages auto-deploys from Git (`scripts/stamp-release.sh` stamps `release.json`); worker + D1 schema/migrations via `deploy/deploy.sh` or `.github/workflows/deploy-worker.yml`; verified by `deploy/verify.sh`.

## 2. End-to-end walk-through

Signal → proposal: cron `scheduled` (`worker/src/index.js:648`) → parallel HN/Reddit/GitHub collect (`:49,:78,:106`, 6s body-covering timeout `:37-47`) → one batched signal INSERT (`:354-359`) → 30d stale sweep to noise (`:368-372`) → take 6 oldest unprocessed (`:374-376`) → exact-URL supports pre-pass, no AI call (`:386-413`, `exactUrlTarget` `:168-178`) → inflow gates: bare-without-brief > 10 caps inserts to 1 (`:427-429`), unreviewed > 10 pauses inflow to 0 (`:431-433`) → one classify AI call unless paused (`:444-457`, `buildClassifyPrompt` `:272`, NDJSON `parseJsonLines` in `worker/src/lib.js:64`) → verdict writes batched with per-write fallback (`:469-538`, `flushVerdictWrites` `:291`) → new rows enter `researching` with `UNREVIEWED`, score capped ≤6000 (`:491-511`) → 1–2 bare rows briefed per cron tick, oldest-unreviewed-first past 48h (`:550-631`, `buildBriefPrompt` `:202`, `insertBrief` `:224`) → run row finished with mode/counters (`:634`).

Review → money: `GET /api/opportunities` (`functions/api/[[path]].js:130`, capped scores, `needs_review` bit, brief excerpts) → dashboard ledger (`public/app.js:174`) and Needs-review filter (`:277`, oldest-first, `:224`) → human Vet/Kill (`public/app.js:493/:559`, `vettedNotes` `:473`) → `PATCH /api/opportunities/:id` (`functions/api/[[path]].js:224`, tagless-clear 400 guard `:258-262`) → health counts `vetted_last_7d` + `last_vetted`/`last_verdict` (`:523,:623-636`) → Vet & log starter composes vet + planned experiment + flip to `testing` (`public/app.js:523`) → Start stamps `started_at` (`:726`, API `:439`) → Win/Lose inline close, closure gate result + post-mortem (`:743/:770`, API `:440-444`), outcome-ledger `$X rev / $Y spent` line appended to parent notes (API `:450-456`) → health `decisions_last_7d` + revenue sums (`:520-522`) → dashboard week line (`public/app.js:787-825`).

Where it breaks or goes silent (honest ranking): (a) the human verdict step — nothing moves without owner taps; the 7-day stall with fresh ticks is this, by design; (b) money columns missing in live D1 → revenue probes read null (named via `health_probe_failures` + `health_probe_detail`, no longer silent); (c) manual `POST /run` runs in `waitUntil`, cancelled 30s after the response (`worker/src/index.js:7-11,:717-720`), and can strand `running` rows until the 30-min reaper (`:312-317`) — the dashboard shows them raw (finding F5); (d) AI/source failures degrade to named markers (`src_fail:*`, `brief:failed`, `verdict:N_failed` on `:634`), never silence.

## 3. Health signals measured here

- Test suite: `npm test` (`node --test` over the 7 files in `package.json:8`) → exit 0. TAP stream voluminous; recount via dot reporter (same 7 files, single command) → 531 dots, exit 0, 0 failures. Count reported, not assumed.
- Syntax: `node --check public/app.js`, `node --check worker/src/index.js`, `node --check functions/api/[[path]].js` → exit 0 each (only sandbox `EnvHttpProxyAgent` warning noise).
- `compileall`: N/A — zero `*.py` files in repo (glob `**/*.py` → no hits; positive control `**/*.js` → 6 files).
- Dead code: `appendKeepNewest` (`worker/src/lib.js:51`) has zero shipped importers (definition + `lib.test.js` only; the comment admits it — see §5); `/run`'s `briefs_skipped`/`reason` keys are unread by the dashboard (`public/app.js:1300` hardcodes the toast; key appears only in `worker/`, `README.md`, old audits). All other shared helpers (`effectiveScore`, `tokensMatch`, `parseJsonLines`, `repairJson`) are imported.
- Duplicated logic: `clamp10` (`functions/api/[[path]].js:74` vs `worker/src/lib.js:4`); slugify (`worker/src/lib.js:9` vs inline `API:193`, slightly different regexes); money format (`API:88 centsDollars` vs `public/app.js:91 moneyCents`, a deliberate mirror per comment); `inlineWinClose` vs `inlinePostMortem` (`public/app.js:343-464`, ~120 lines overlap) — finding F7.
- TODO density: regex `TODO|FIXME|HACK|XXX` over the repo → 5 hits, all prose in `docs/audits/*.md`, zero in shipped source. Positive control: same tool+mode matches `UNREVIEWED` 10+ times (`functions/api/api.test.mjs:31` ff.), so the zero is real.
- Surface rules: `prefers-color-scheme|data-theme|theme-toggle` over `public/app.js` → no hits (positive control `color-scheme` → `public/styles.css:4` `light`, plus `public/tab-icon.test.mjs:62-81` guard); `styles.css` has no dark query (only `prefers-reduced-motion` `:210`, `max-width` `:213`); favicon link + `theme-color` (`public/index.html:9-10`) and inline brand mark (`:16`, accent/glyph matching `favicon.svg`) present; live HTML snapshot is 5595 bytes, same as repo `index.html`.
- Live snapshot (health @ `2fa64a9`): `db up`, opportunities 27, unreviewed 11, bare_without_brief 19, `experiments_by_status {"planned":2}`, `hours_since_last_ok_run` 1.7, `oldest_unreviewed_age_h` 181.4, `decisions_last_7d` 0, `vetted_last_7d` 0, `last_vetted`/`last_verdict` null, revenue trio null, failures `[revenue_week, revenue_lifetime]` → `revenue_cents`, `schema_missing_columns [revenue_cents, spent_cents]` + disabled line. Isolation + migration-switch + verdict-date work from prior rounds is confirmed live.
- Git: HEAD `af7f6d4`, `git status --short` clean before this file; `.gitattributes:4` pins `eol=lf`, so the CRLF hazard in the brief is contained (and this audit edits nothing).

## 4. Ranked findings (max 8)

### F1. Money ledger: earned none, never; the next payment needs the $0 starter run to a pilot
- Evidence: live health `revenue_total`/`revenue_last_7d`/`spent_total` all null AND `decisions_last_7d` 0 with `experiments_by_status {"planned":2}` (zero `running`/`won`/`lost`) — even a migrated DB would sum $0. Seed starter `d1/seed.sql:98` (`Spec-ad sprint: 10 brands, 10 free ads`, `$0 + 10 hours`, `>=1 pilot at >=$500`); one-tap Start `public/app.js:726`; Win records human-entered $ `public/app.js:770`.
- Why it costs money: this is the lane metric at zero — 0 vetted/week, 0 decisions/week, 2 planned → 0 running (0% start rate), oldest unreviewed 181.4h (7.6d). This project is a tool the owner uses to find and test money-makers, not a product he sells; it earns nothing itself and ranks by decisions/week and backlog drain.
- Fix (no code; owner actions in order): set `ALLOW_SCHEMA_MIGRATION=1` so a $ figure can be recorded/reported (F3); vet ≥1 queue row to reopen inflow (F2); press Start on the $0 seed starter, run the 10 free spec ads inside a week, close Win with the pilot $ amount (or Lose with post-mortem). Never enter a $ figure without evidence. Size: — (no change). Risk: none.

### F2. Nothing automated was ever supposed to vet — the 7-day stall is the designed human bottleneck, and one verdict reopens the funnel
- Evidence: path in one sentence — the unreviewed→vetted path is a human-only Vet PATCH (`public/app.js:493 vetOpportunity()` → `PATCH /api/opportunities/:id`, `functions/api/[[path]].js:224`), and no cron tick, worker route, or scheduled pass vets anything (`worker/src/index.js:4-5`: "The agent PROPOSES; the human owns the testing workflow"; `README.md:71-72`). Fresh ticks + zero verdicts is the designed paused-tick shape: live unreviewed 11 > 10 → inserts 0, classify skipped (`worker/src/index.js:431-433,:445`), still briefs, logs `inflow:paused` (`:634`); pinned by `worker/src/index.test.js:1205` (unreviewed 11: 0 classify calls, ≥1 brief, pause named). The human path itself works: `functions/api/api.test.mjs:1999` drives PATCH→detail→health against stubbed D1 (unreviewed 2→1, vetted 0→1, `last_vetted` today). Health already dates verdicts (`functions/api/[[path]].js:623-636`; live `last_vetted`/`last_verdict` null = stall visible).
- Why it costs money: every prior round improved machinery while the queue sat still; while unreviewed stays above 10 the worker inserts nothing, so the funnel is shut until a human acts.
- Fix (no code; this is the brief's allowed good answer): owner opens Priority tab → Needs-review chip (or agent pill), presses one verdict per row — `V` vets, `K` opens inline Kill post-mortem (`public/app.js:635-646`, auto-advances `:630-633`), ~1 min/row from a phone, no drawer needed (decision + brief lines inline, `:205-222`). Clearing a single row (11→10) reopens inflow on the next cron tick. Do not automate verdicts: bulk/auto vetting violates the deliberate one-verdict-per-keypress design (`public/app.js:590-597`) and the honesty rule against marking decided without evidence. Size: — (no change). Risk: none.

### F3. Four money fields null: `revenue_cents`/`spent_cents` never reached live D1 and the self-migration switch is OFF
- Evidence: live `health_probe_failures [revenue_week, revenue_lifetime]` → `revenue_cents`, `schema_missing_columns [revenue_cents, spent_cents]` + `disabled (set ALLOW_SCHEMA_MIGRATION=1 …)`; probes `functions/api/[[path]].js:521-522`; switch `ensureMoneyColumns` `:34-72` (PRAGMA compare of probe columns only `:40-52`, default-off `:53-56`, once per isolate `:35`, ADD-only DDL `:27-30` matching `d1/migrate-2026-09-20-experiment-cents.sql:5-6`); deploy convergence already shipped (`deploy/apply-d1-migrations.sh`, `.github/workflows/deploy-worker.yml:33-37`, fail-fast secrets guard `:20-26`).
- Why it costs money/users/trust: every revenue/spend figure reads unknown instead of a number, and the decisions COUNT split (`:520`) is what keeps the lane metric alive during the outage. Code is done; the database is one variable flip behind it.
- Fix (no code): owner adds `ALLOW_SCHEMA_MIGRATION=1` to the Pages production env vars and retries the latest deployment; the next health call adds exactly the two `ADD COLUMN` statements and reports numbers on the same call. No second trigger, no endpoint, never default-on (brief boundary; current code complies). Size: — (no change). Risk: none (ADD COLUMN with DEFAULT cannot lose rows).

### F4. CI worker/D1 path fails loudly without secrets — worker deploys and D1 migrations never run from CI until the owner adds them
- Evidence: `.github/workflows/deploy-worker.yml:20-26` fails fast with named `::error::CF_API_TOKEN` / `::error::CF_ACCOUNT_ID` when empty, before schema (`:27`) and shared migrations (`:33-37`); `deploy/deploy.sh:10-12` requires the same values locally. Whether this repository has those secrets is unverifiable offline (the brief states it has none); the code fact is that without them every CI run stops at the guard.
- Why it costs money: schema changes (F3's columns included) can never reach live D1 from CI, and the research worker never redeploys there either — the cron keeps running the last good worker while the code moves on.
- Fix (no code): owner adds `CF_API_TOKEN` and `CF_ACCOUNT_ID` under the repository's Settings → Secrets → Actions (values never enter version control). Never create, enter, or store a credential from a round. Size: — (no change). Risk: none.

### F5. A manual triage run that overruns 30s strands a `running` row the dashboard shows raw until the 30-min reaper
- Evidence: `POST /run` returns 202 and runs in `waitUntil` (`worker/src/index.js:717-720`), which the file's own comment says is cancelled 30s after the response (`:7-11`); stuck rows are reaped by the next run after `STUCK_RUN_MINUTES = 30` (`:24,:312-317`); the dashboard renders `status` verbatim with no age check (`public/app.js:889-901`).
- Why it costs users/trust: the owner presses "Run triage now", the pass dies silently mid-flight, and the Research log shows `running` for up to 30 min with no hint it is dead — a failure-visibility gap in exactly the surface he watches.
- Fix: in `public/app.js` add a pure `isStaleRunning(run, now)` helper (status `running` + `started_at` older than 30 min) and render a read-only `stale?` badge next to such rows in `renderRuns`; pin with `public/dashboard.test.mjs` cases (fresh running unbadged, 31-min running badged, finished rows untouched). No status writes — the worker reaper keeps owning transitions. Size S. Risk: low (read-only presentation + pure helper).

### F6. The `last_verdict` probe scans every vetted/killed row on each health call with no LIMIT
- Evidence: `functions/api/[[path]].js:526` — `SELECT substr(notes, -500) … WHERE … LIKE '%vetted]%' OR … LIKE '%killed]%'`, no `LIMIT`; it runs on every `/api/health`, which the dashboard refetches on nearly every action (`public/app.js:1512-1531`). Cost today is ~zero (no vetted rows live), per-row bytes are bounded (500 chars), but row count grows with verdict history forever.
- Why it costs later: at thousands of verdicts every dashboard tap pays a multi-megabyte D1 scan for a single max-date — a slow, silent degradation of the surface the owner uses most.
- Fix: touch `functions/api/[[path]].js` probe 11 to `ORDER BY updated_at DESC LIMIT 2000` (verdict tags are appended on touch, so the newest tag rides a recently-touched row) and extend `functions/api/api.test.mjs` with a LIMIT+1-rows case pinning that the newest tag still reports. Keep the JS max-parse (`:625-636`) unchanged. Size S. Risk: low (bounded read, pinned by test).

### F7. Duplicated pure helpers and near-identical inline-row builders (polish, rank last)
- Evidence: `clamp10` twice (`functions/api/[[path]].js:74-78` vs `worker/src/lib.js:4-7`); slugify twice (`lib.js:9-11` vs inline `API:193`, different regexes); money format twice (`API:88` vs `public/app.js:91`, admitted mirror); `inlineWinClose` vs `inlinePostMortem` (`public/app.js:343-464`, same input/Confirm/Cancel/Escape skeleton, ~120 lines overlap).
- Why it costs: every copy is a future divergence (the slug regexes already differ); zero money impact today — pure tidiness, ranked last per the money directive.
- Fix: import the shared `clamp10`/`slugify` from `worker/src/lib.js` into the API (or accept the API-local copies and delete nothing); extract one `inlineRow(container, {inputs, …})` builder in `public/app.js` used by both Win and post-mortem paths; extend the existing `lib`/`dashboard` suites to pin byte-identical behavior. Size S. Risk: low (pure code, heavily tested).

## 5. What NOT to change

- Human owns every verdict — no auto/bulk vetting, no auto-transitions: "The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)" (`worker/src/index.js:4-5`); "A human admin vets each row" + "`researching → testing` is a human decision" (`README.md:71-72`); "Nothing here vets without a keypress: no timer, no loop over the queue, no bulk call" (`public/app.js:590-597`). Proposing auto-vet would violate design and the honesty rules.
- Migration switch stays default-off, single trigger, no endpoint: "Default-off: unset changes nothing" (`functions/api/[[path]].js:23-24`); ADD-only DDL (`:27-30`); "no endpoint" (self-migration test `functions/api/api.test.mjs`, `ships exactly the two ADD COLUMN statements … no endpoint`). Never default-on, never a second trigger, never a migration URL.
- Orphaned-experiment close succeeding without a ledger line is deliberate (test `closing an orphaned experiment still succeeds without a parent row`); `appendKeepNewest` is a "Pinned JS spec of that SQL idiom (no shipped importer; the lib suite pins the rule)" (`worker/src/lib.js:48-54`) — leave it.
- `ADMIN_TOKEN` rotation is manual by design ("No expiry; rotate manually when shared or leaked", `README.md:85-88`); anything needing login, credential, payment, or human judgment is out of round scope.
- Owner one-liners (his actions, not tasks): clear the 11-row Needs-review queue with V/K (~1 min/row, phone OK — one verdict reopens inflow); set `ALLOW_SCHEMA_MIGRATION=1` in Pages production env + retry deployment; add `CF_API_TOKEN`/`CF_ACCOUNT_ID` repo secrets so CI can deploy the worker and apply D1 migrations; press Start on the $0 `Spec-ad sprint` experiment and drive it to Win/Lose with a post-mortem inside a week.

## 6. Proposed next tasks (2–4)

- [ ] Task 1: Prefetch next review-row detail so each V/K verdict costs one round trip
  - Owned files: `public/app.js`, `public/dashboard.test.mjs`.
  - Why first: the focus's vet round resolves to its allowed good answer (F2: human-only by design) — "the smallest thing that would let the owner clear eleven rows in a couple of minutes." Per-tap verdicts already exist; the remaining per-verdict cost is two round trips (detail GET `public/app.js:483-491` + PATCH `:505-507`).
  - Acceptance: focusing/verdicting row N prefetches row N+1 detail; Vet/Kill reuse the cache when fresh and fetch on miss; failed PATCH keeps the row visible with the existing `<Action> failed` toast (no silent drop); new dashboard tests pin prefetch, cache reuse, and invalidation on refresh; `npm test` green.

- [ ] Task 2: Flag research runs stuck in running past 30 min as stale in the log
  - Owned files: `public/app.js`, `public/dashboard.test.mjs`.
  - Acceptance: `running` rows with `started_at` older than 30 min render a read-only `stale?` badge (F5); fresh `running` and finished rows render exactly as today; pure helper covered by dashboard tests; `npm test` green.

- [ ] Task 3: Bound the last-verdict health probe scan with a LIMIT
  - Owned files: `functions/api/[[path]].js`, `functions/api/api.test.mjs`.
  - Acceptance: probe 11 (`functions/api/[[path]].js:526`) carries `ORDER BY updated_at DESC LIMIT 2000`; a new API test with LIMIT+1 verdict rows pins the newest tag still reported on both `last_vetted` and `last_verdict`; all other health keys byte-identical; `npm test` green.
