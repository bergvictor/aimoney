# AUDIT 2026-09-20 — aimoney (round 3, overnight lane: WASTE)

Clone HEAD: `a4fdbcc` (round 2: targeted refresh, batched health, strip list excerpt).
Live rev (`_night/live/aimoney_pages_dev_api_health.html`): `d1aa266` — live trails HEAD by two commits (`68ab209`, `a4fdbcc`).
Lane metric right now (live payload, measured): **0 decisions/week, 0 vetted/week, $0.00 revenue (week and lifetime), 11 unreviewed (oldest 166.3h ≈ 6.9d), 19/27 rows without a brief, 2 planned / 0 running experiments, last ok run 4.6h ago**.
No repo-local `AGENTS.md`/`CLAUDE.md`/`CONTRIBUTING.md` (root listing); governed by `README.md` plus the session standing rules.
Working tree arrived dirty on all 32 tracked files (pre-existing CRLF checkout artifact: `git diff --ignore-cr-at-eol --stat` empty — content untouched, left alone).
Surfaces judgment: live HTML is text-identical to `public/index.html` (`diff --strip-trailing-cr -q` exit 0; plain `diff -q` differs on CRLF only). Light palette only (`public/styles.css:4` `color-scheme: light`; no `prefers-color-scheme`/`data-theme`/theme-toggle in served HTML/CSS/JS), favicon + `theme-color` (`public/index.html:9-10`, hand-written `public/favicon.svg`), inline brand mark (`public/index.html:16`), guard suite green (`public/tab-icon.test.mjs`, 5/5). **Zero surface-rule findings this round.** Served `/app.js` and `/styles.css` bytes were not re-fetched (no network per task rules); the HTML shell matches source.

## 1. What this project is and how it runs

- Purpose: AIMoney Lab — a ranked board of AI-money opportunities with evidence briefs and experiments, re-optimized as results land (`README.md:1-15`).
- Entry points: dashboard `public/index.html` + `public/app.js`; API `functions/api/[[path]].js`; agent `worker/src/index.js` + `worker/src/lib.js`.
- Live surfaces: `https://aimoney.pages.dev`, `/api/health`, research worker (`README.md:27-31`, `deploy/public-surfaces.json:31-44`).
- Scheduled jobs: worker cron every 6h (`worker/wrangler.toml:8-9`); manual triage-only `POST /run` 202 with `briefs_skipped:true` (`worker/src/index.js:543-551`).
- Deploys: `./deploy/deploy.sh` (D1 schema+migrations+seed-if-empty, stamp `release.json`, Pages, worker), `./deploy/verify.sh` gates rollout (≤12h freshness); Pages auto-redeploys on push, worker via `.github/workflows/deploy-worker.yml`.

## 2. End-to-end walk-through

Signal → triage → proposal → brief → vet → experiment → close → money:

1. Collect: cron tick → `runResearch(env,"cron")` (`worker/src/index.js:182` via `scheduled` at `:487-489`) fans out HN/Reddit/GitHub (`:217`, `:48-70`, `:72-94`, `:96-117`), one batched `INSERT OR IGNORE` (`:222-227`).
2. Sweep + take: 30d-stale signals → noise with count (`:235-240`); oldest 6 unprocessed taken (`:242-244`); exact-URL matches link as supports before triage, no AI call (`:250-267` via pure `exactUrlTarget`, `:153-163`). Quiet ticks continue to the brief pass instead of early-returning (`:268-271`).
3. Triage: top-60 list fetched (`:274-276`), one Mistral NDJSON call over list + fresh signals (`:277-294`; budget `MAX_AI_CALLS=4`, `:17`); `parseJsonLines` (`worker/src/lib.js:49-64`); strict validation (`worker/src/index.js:296-315`); `new` inserts a `researching` + `UNREVIEWED` row with humility clamp (`:322-323`) and capped score (`:335`), at most 2 per run with overflow left unprocessed (`:317`, `MAX_NEW_PER_RUN`, `:16`); slug collision links as supports (`:343-355`); `supports` appends `[signal date]` evidence newest-kept (`:356-364`); else noise (`:365-367`).
4. Brief: cron only (`briefDeadline`, `:215`; manual `POST /run` always skips, `:551`, 202 `briefs_skipped:true`); oldest-unreviewed-first past 48h (`:374-388`); one Llama call (`:394-411`); empty/malformed JSON skipped silently (`:412-428`); one extra oldest bare row past 48h (`:434-475`); single run-log write carrying the brief mode (`:478`).
5. Serve: `GET /api/opportunities` with full rows + 3 correlated brief subqueries (`functions/api/[[path]].js:46-75`); detail + briefs + experiments (`:77-88`); batched 10-statement health incl. `decisions_last_7d`/`vetted_last_7d`/`revenue_last_7d`/`revenue_total` (`:359-417`); `GET /api/meta` (`:418-424`).
6. Decide (all human): boot `refresh()` → `refreshTargets` (`public/app.js:1177`, `:1142-1176`); review queue derived client-side (`:225-226`, `:228-245`); review rows carry capital + next action + source + brief summary with inline Vet/Vet-&-starter/Kill (`:161-219`); Vet clears the marker and appends `[date vetted]`, toast offers Log experiment (`:388-406`); Vet-&-starter vets + POSTs a prefilled planned experiment + flips to testing with a Start shortcut (`:414-444`); Kill takes an inline post-mortem line (`:446-470`); Start-here strip reads the list excerpt with Vet/Kill for unreviewed top picks (`:103-127`); drawer + admin zone for rescore/status (`:720-767`, `:792-889`).
7. Experiment: modal log with dollar→cents conversion (`:987-1038`); board one-click Start (`:531-543`), one-click Lose (`:550-567`) and one-click Win with inline $ + source + line (`:576-593`); API create (`functions/api/[[path]].js:240-289`) and update (`:291-344`) enforce result + post-mortem on close (`:314-318`), stamp `started_at`/`ended_at` (`:313`,`:319`), append a `($X rev / $Y spent via {source})` outcome line to the parent on the won/lost transition (`:324-333`); drawer suggests a ±1 rescore, never auto-applied (`public/app.js:780-790`); zero-decision weeks get a read-only stale nudge that deep-links to the board card (`:620-629`, `:517-524`).
8. Where it most often breaks or goes silent, in order: **(a)** live trails HEAD by the two rounds that cut per-tap cost (`d1aa266` vs `a4fdbcc`), so every measured stall metric was taken against the wasteful code; **(b)** the human funnel converts at zero (0 vetted, 0 decisions, oldest 6.9d) while each tap re-downloads the world (F1); **(c)** the $0 spec-ad sprint (`d1/seed.sql:66-71`, `planned`, `$0 + 10 hours`, target `>=3 replies; >=1 pilot at >=$500`) sits unpressed since seed — a human still presses Start (`public/app.js:529-530`); **(d)** 19/27 rows bare while briefs cap at 2/cron-tick and manual runs brief nothing; **(e)** silent drops: the 30d sweep noises old signals (`worker/src/index.js:235-240`), malformed brief JSON is skipped without a marker (`:412-428`), bad-action verdicts mark signals processed with no parent (`:306-309`).

## 3. Health signals measured here

- `npm test` (this Linux checkout, exit 0): **212 pass / 0 fail, 74 suites** — per file: `lib.test.js` 14/7 (`duration_ms 154.91`), `index.test.js` 37/14 (`228.30`), `api.test.mjs` 69/21 (`2239.91`), `tab-icon.test.mjs` 5/1 (`114.93`), `dashboard.test.mjs` 87/31 (`249.58`). Matches the `a4fdbcc` commit message (212 pass). The Windows baseline (`duration_ms 317.9701`) is a different environment, not a comparable total. Only warnings are the known `MODULE_TYPELESS_PACKAGE_JSON` reparsing notes and sandbox proxy notes.
- `python3 -m compileall`: N/A — repo contains zero `*.py` files (glob `**/*.py` search returned nothing; JS-only: Pages + Functions + Worker, `node --test` suites).
- `node --check`: exit 0 on `public/app.js`, `worker/src/index.js`, `worker/src/lib.js`. The API router's parse is proven by the green suite that imports it (`functions/api/api.test.mjs:6`, 69/69 green).
- TODO density: **0** — regex search for `TODO|FIXME|XXX|HACK` across `public/`, `functions/`, `worker/`, `scripts/`, `deploy/`, `d1/` returns nothing.
- Dead code (all verified present at HEAD): `BRIEF_DEADLINE_MS` defined but never referenced (`worker/src/index.js:21`, sole occurrence; live logic uses `briefDeadline` at `:215`); unused `scoreOf` import (`:140`, sole occurrence in file); five unreachable toast gates after modal returns (`public/app.js:391,449,534,553,579`, each self-commented "supersedes the toast gate" at `:390,448,533,552,578`); `GITHUB_QUERIES` third entry `"ai-side-project"` (`:28`) never fetched (`:116` slices `(0, 2)`); `appendKeepNewest` has zero production callers (`worker/src/lib.js:36-39`, tests only).
- Duplicated logic: `clamp10` in the API (`functions/api/[[path]].js:16-20`) vs `worker/src/lib.js:4-7` (near-identical, not shared); classify prompt text duplicated between the pass (`worker/src/index.js:277-287`) and `/debug-classify` (`:522-531`); brief-prompt + brief-insert duplicated for main (`:399-427`) and extra (`:442-471`) briefs; `inlinePostMortem` (`public/app.js:276-314`) vs `inlineWinClose` (`:320-382`) share ~50 lines of row/input/cancel/Escape shape; money formatters `centsDollars` (`functions/api/[[path]].js:24`) vs `moneyCents` (`public/app.js:53`) are an intentional commented mirror; `UNREVIEWED` appears as SQL/string literals (`functions/api/[[path]].js:56,365,371-372`; `worker/src/index.js:335,338,381,389,439`) while the exported `UNREVIEWED_MARKER` (`worker/src/lib.js:21`) is used only inside lib; the vetted tag is string-coupled (`[date vetted]` written at `public/app.js:395`, counted via `LIKE '%vetted]%'` at `functions/api/[[path]].js:371-372`); create-vs-update string bounds differ (create slices, update stores raw: `functions/api/[[path]].js:96-110` vs `:134-137`); worker token compare repeated 3x (`worker/src/index.js:498-500,513-515,544-546`) vs the API constant-time compare (`functions/api/[[path]].js:28-36`); three separate `#board` click listeners for Start/Lose/Win (`public/app.js:1043-1066`).
- Live surfaces: `_night/live/aimoney_pages_dev.html` (5514 bytes) text-identical to `public/index.html` modulo checkout CRLF; surface rules all pass, so they expose zero findings. Live health (364 bytes): `{"ok":true,"rev":"d1aa266","db":"up","opportunities":27,"unreviewed":11,"bare_without_brief":19,"experiments_by_status":{"planned":2},"hours_since_last_ok_run":4.6,"oldest_unreviewed_age_h":166.3,"decisions_last_7d":0,"revenue_last_7d":0,"revenue_total":0,"spent_total":0,"vetted_last_7d":0,"vetted_no_experiment":0,"noise_24h":0}`. Worker fresh (4.6h); every decision counter zero.
- Note on acceptance: `git status --short` showed every tracked file as modified before I wrote anything (`git diff --ignore-cr-at-eol --stat` is empty — CRLF line endings only, pre-existing). I created only this file and edited nothing.

## 4. Ranked findings (max 8)

Impact order for the lane number (decisions/week, vetted→experiment, backlog drained), phone-clear first, then worker waste. No surface-rule findings: the served page is light-only with icon, brand mark, and a guard test (see §3).

**F1 — Live trails HEAD by the two waste-cutting rounds, so every tap still pays the old cost.**
Evidence: live health reads `"rev":"d1aa266"` while clone HEAD is `a4fdbcc`; `git show --stat` confirms the gap is exactly `68ab209` (client-side review list, meta once, single run-finish write) + `a4fdbcc` (targeted refresh, batched health, strip list excerpt). Live therefore still issues the second review-list fetch, re-fetches `/api/meta` per refresh, full-refreshes on every action, and runs 13 sequential health selects (`functions/api/[[path]].js:363-375` is the batched replacement).
Why it costs money: every stall metric in §3 was measured against the wasteful code — each phone decision on live pays ~6 API calls where HEAD pays 2–3, and the under-a-minute clear starts each tap in a slower hole.
Fix (operator action, no code): merge the two rounds to the live branch, run `./deploy/deploy.sh` (additive migrations tolerated, seed-if-empty guards live rows) then `./deploy/verify.sh` including the ≤12h freshness gate (live `hours_since_last_ok_run:4.6`, so the gate passes). Touch no repo files. Size S. Risk: low (no pending schema beyond applied additive columns).

**F2 — Every list fetch ships up to 200 full notes bodies plus full first_steps text to render one bit and one line.**
Evidence: `SELECT o.*, …` (`functions/api/[[path]].js:60-69`) returns full `notes` (writers cap at 8000 chars) and the `brief_first_steps` subquery (`:63`) returns the full up-to-8KB `first_steps`, while the only list-state consumers are two `UNREVIEWED` bit-checks (`public/app.js:110,226`), one first-line split (`:93-94`), and three write-path appends (`:395,419,456`); the drawer re-fetches detail anyway (`:720-724`). The dashboard requests `limit=200` (`:1157`), so worst case is ~200 × 16KB per tap to render zero of those bytes in list views.
Why it costs money: the phone path pays this on every decision tap (vet/kill/save all refetch the list) — D1 egress, phone data, and parse time for text nobody reads there. It is the single largest per-tap waste left after F1.
Fix: drop `notes` from the list select and add a `needs_review` bit (`notes LIKE '%UNREVIEWED%'`, reusing the `:56` idiom); `substr()` the first_steps excerpt to ~300 chars in SQL. Vet/Vet-&-starter/Kill `GET` the detail endpoint first for the full notes they append to — one extra fetch on the rare write path instead of megabytes on every read. Touch `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`, `public/dashboard.test.mjs`. Size M. Risk: medium — touches the vet path; keep the `limit=200` review fallback and the `[date vetted]`/`[date killed]` tag formats byte-identical.

**F3 — Verdict phase does up to ~12 sequential D1 writes; the collect phase proved batching cuts 9s to 0.5s.**
Evidence: each verdict awaits its own writes inside the loop — invalid-action (`worker/src/index.js:307`), new-row signal link (`:341-342`), slug-collision pair (`:350-351`), supports pair (`:358-364`), noise (`:366`) — while collect uses `ONE batched round-trip instead of ~24 sequential inserts (9s -> 0.5s)` (`:222-227`).
Why it costs money: those sequential round-trips burn the 30s `waitUntil` budget the whole pass is engineered for (`:7-11`); a killed run loses proposals and briefs, and the 11-deep backlog never drains from the top.
Fix: accumulate the deterministic signal-UPDATEs and notes-appends during validation and fire one `env.DB.batch` at loop end; keep the slug-collision existence lookup (`:347`) inline since it branches. No verdict semantics change, no status moves. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size M. Risk: medium — batch failure modes differ from per-statement (round 2's health batching already accepted this tradeoff with a test).

**F4 — The exact-URL pre-pass loads two unbounded full-table URL sets to match at most 6 URLs.**
Evidence: every run with fresh signals loads all opportunity URLs (`worker/src/index.js:251-253`) and all ever-linked signal URLs (`:254-256`) to feed `exactUrlTarget` (`:153-163`), but `fresh` holds at most `MAX_AI_SIGNALS=6` rows (`:242-244`). The signals table grows forever (the sweep marks, never deletes, `:235-240`), so these two scans grow every 6h.
Why it costs money: per-run D1 reads grow without bound inside the 30s budget, to match a handful of strings — pure O(table) work for an O(6) question.
Fix: match in SQL instead of in memory — `SELECT id, source_url FROM opportunities WHERE source_url IN (…≤6 fresh URLs…)` and the same for linked signals — so both scans stay bounded no matter how large the tables get. Pure-function `exactUrlTarget` and its tests stay as the matcher. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk: low — same matches, bounded reads.

**F5 — Quiet ticks fetch the top-60 list and build the classify prompt, then throw both away.**
Evidence: the `opps` query (`worker/src/index.js:274-276`) and `classifyPrompt` build (`:277-287`) run unconditionally, but verdicts are `!fresh.length ? [] : parseJsonLines(…)` (`:290-294`) — on a quiet tick the 60 rows and the prompt string are built and discarded. Quiet ticks are the common case (the brief-on-quiet path at `:268-271` exists because of them).
Why it costs money: one D1 read plus prompt construction per quiet tick with zero possible benefit — the textbook discarded-result call. It also spends wall-clock inside the 30s budget before the brief pass that actually matters on those ticks.
Fix: move the `opps` fetch and `classifyPrompt` build inside `if (fresh.length)`, next to the existing `mark("classify-ai")` guard (`:288`). Behavior identical on every path; quiet ticks skip straight to the oldest-unreviewed check. Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk: low.

**F6 — Up to 72 signals collected and inserted per run, 6 triaged; judged-noise rows live forever.**
Evidence: 9 source fetches (4 HN + 3 Reddit + 2 GitHub, `worker/src/index.js:25-28,116`) × `MAX_SIGNALS_PER_SOURCE=8` (`:14`) feed one batch insert (`:222-227`), but triage takes `MAX_AI_SIGNALS=6` (`:15`, `:242-244`); the 30d sweep marks leftovers processed (`:235-240`) yet no statement ever deletes a signal row, so processed noise accumulates permanently and feeds F4's scans.
Why it costs money: repeated fetch + insert work every 6h for rows that queue until the sweep noises them, plus unbounded table growth taxing every future scan. The queue drains at neither end by design.
Fix (rule written down, nothing load-bearing lost): cap collection closer to use (per-source 8→4 still yields up to 36 candidates for 6 slots, keeping a dedupe buffer) and add a retention delete — `processed = 1 AND opportunity_id IS NULL AND created_at` older than 90d — whose only foregone consumer is an audit trail past `noise_24h`'s 24h window (`functions/api/[[path]].js:373`); linked-evidence rows are never touched. Document the rule in `README.md` next to the sweep rule. Touch `worker/src/index.js`, `worker/src/index.test.js`, `README.md`. Size S. Risk: low-medium — it is a DELETE, so gate it on all three predicates and pin them in a test.

**F7 — Up to five heartbeat UPDATEs per run buy progress text nobody watches live.**
Evidence: `mark` is an awaited D1 write (`worker/src/index.js:228-231`), called at `:241` (always), `:288` (fresh only), `:295` (always), `:398` and `:446` (briefs only) — yet stuck-run detection keys off the `started_at` cutoff (`:184-191`), not heartbeats, and the dashboard fetches runs only on its own refresh (`public/app.js:1159`), never polling mid-run.
Why it costs money: up to five sequential round-trips per run inside the 30s budget for `phase:…` text with no live reader — the same budget F3 is starving.
Fix: keep one heartbeat after collect (proves the run is alive and carries the stale count) and drop the rest; the run row is still opened once and closed once by `finish` (`:478`). Touch `worker/src/index.js`, `worker/src/index.test.js`. Size S. Risk: low — mid-run phase text is the only loss, and no surface polls for it.

**F8 — Dead code still shipped: a deadline that enforces nothing, an unused import, five dead gates, a dead query.**
Evidence: `const BRIEF_DEADLINE_MS = 18000; // skip the brief pass past this elapsed time` (`worker/src/index.js:21`, sole occurrence — the live gate is `briefDeadline` at `:215`); `scoreOf` in the import at `:140` (sole occurrence); five `if (!state.token) return toast(…)` lines (`public/app.js:391,449,534,553,579`) each unreachable behind its `return openAdminModal(…)` and self-commented as superseded (`:390,448,533,552,578`); `"ai-side-project"` in `GITHUB_QUERIES` (`worker/src/index.js:28`) excluded by `.slice(0, 2)` (`:116`).
Why it costs money and trust: a reader (or a future worker) will believe the 18s deadline is enforced and the toast gates can fire — false confidence in safety rails that do not exist. The dead query string promises coverage the fetcher silently drops.
Fix: delete the constant, the `scoreOf` import name, and the five dead toast lines; either drop `.slice(0, 2)` to fetch all three GitHub queries or delete the third string so config matches behavior. (`appendKeepNewest` at `worker/src/lib.js:36-39` is tests-only but harmless as the documented newest-kept spec — leave it.) Touch `worker/src/index.js`, `public/app.js`, and `public/dashboard.test.mjs` only if a static guard counts the removed lines. Size S. Risk: low.

## 5. What NOT to change

- The scoring formula and the honest cap: `score = 100 * (value * confidence * fit) / (effort + 1)` (`README.md:56`, `worker/src/lib.js:15-16`) with `UNREVIEWED` rows clamped to ≤6000 (`worker/src/lib.js:29-32`). Cut bytes and round-trips, not the math.
- "The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)." (`worker/src/index.js:4-5`; also `README.md:72`: "`researching → testing` is a human decision".) F6's retention rule moves no status.
- "No auto-transitions — the human still owns every status move." (`public/app.js:517`.) Start/Win/Lose/Vet/Kill stay human-pressed, including the spec-ad sprint (`public/app.js:529-530`: "a human still presses it").
- "Killed strategies stay on the board with their post-mortem — that is the point." (`README.md:63-64`.) Never delete killed rows or their notes; F6 deletes only unlinked noise signals.
- The experiment closure gate: `won`/`lost` require non-empty `result` + `post_mortem` (`functions/api/[[path]].js:314-318`), `ended_at` stamped (`:319`). Never mark an experiment decided without evidence, never invent a revenue figure.
- Public-read / token-write split with constant-time compare (`functions/api/[[path]].js:28-36`); `ADMIN_TOKEN` rotation stays a human deploy step (`README.md:85-88`). No tokenless writes, no stored bypass.
- Idempotent deploy: schema always, seed only when the table is empty (`deploy/deploy.sh:30-47`). Never reseed over live rows.
- Manual-triage-only + briefs-on-cron (`README.md:77`; `worker/src/index.js:551`). F3/F5/F7 preserve the manual skip.
- Anything needing a login, credential, payment, or a human decision: Cloudflare deploys, `CLOUDFLARE_API_TOKEN`, sending the spec ads, pricing a pilot, vet/kill calls, closing experiments. This audit changed no code and read no secrets.

## 6. Proposed next tasks (2–4)

Prerequisite (operator, not code): F1 — merge `68ab209` + `a4fdbcc` to the live branch and deploy, so the already-built waste cuts actually reach the board.

### Task 1: Skip discarded classify fetch; bound URL pre-pass to fresh URLs

- Owned files: `worker/src/index.js`, `worker/src/index.test.js`.
- Acceptance: (a) quiet ticks issue no top-60 list query and build no classify prompt (F5); (b) the pre-pass matches via bounded `IN (…≤6…)` selects instead of two full-table URL loads (F4); (c) classify verdicts, exact-URL links, AI budget, status rules, and manual brief-skip all unchanged; (d) new tests pin the skip and the bounded queries; (e) `npm test` green.

### Task 2: List ships needs_review bit instead of full notes; vet fetches detail first

- Owned files: `functions/api/[[path]].js`, `public/app.js`, `functions/api/api.test.mjs`, `public/dashboard.test.mjs`.
- Acceptance: (a) list rows carry `needs_review` (same `LIKE '%UNREVIEWED%'` idiom) and no full `notes`; first_steps excerpt `substr`'d in SQL; (b) review derive, chip count/age/tooltip, strip, and ledger render identically; (c) Vet/Vet-&-starter/Kill fetch detail first and append byte-identical `[date vetted]`/`[date killed]` tags; (d) API tests pin the new shape, dashboard tests pin the detail-before-write order; (e) `npm test` green.

### Task 3: Batch verdict-phase D1 writes; keep one run heartbeat

- Owned files: `worker/src/index.js`, `worker/src/index.test.js`.
- Acceptance: (a) per-verdict signal/note writes go out as one `env.DB.batch` (slug-collision lookup stays inline); (b) at most one mid-run heartbeat (post-collect) plus the single finish write; (c) verdict outcomes, inflow cap, AI budget, and run-log final row unchanged; (d) new tests pin one batch + one heartbeat + byte-identical finish; (e) `npm test` green.

### Task 4: Retain processed-noise signals 90d, then delete; sweep dead code

- Owned files: `worker/src/index.js`, `worker/src/index.test.js`, `public/app.js`, `README.md`.
- Acceptance: (a) retention deletes only `processed = 1 AND opportunity_id IS NULL AND created_at` older than 90d; linked-evidence and unprocessed rows untouched; (b) `README.md` documents the rule beside the 30d-sweep rule; (c) `BRIEF_DEADLINE_MS`, the `scoreOf` import name, the five dead toast gates, and the GitHub slice-or-string mismatch are gone; (d) static-guard suites updated only where they counted removed lines; (e) `npm test` green.
