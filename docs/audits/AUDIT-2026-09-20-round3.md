# AUDIT-2026-09-20-round3 (aimoney) — read-only audit, 2026-09-21

Scope: clean clone at HEAD `b8ae5b5`, no network, no installs, no edits to existing files;
only this file created at the root (`git status --short` was empty before it). No `AGENTS.md`/
`CLAUDE.md`/`CONTRIBUTING` at the root (content search for those names returned nothing);
`README.md` read first, then the live snapshots in `_night/live/`, then source. Shell used
only for single-stage commands, no inline code, no redirection. Never read `.env`/secrets.

## 1. What this project is and how it runs

1. AIMoney Lab: a ranked ledger of AI-money opportunities with research briefs, experiments, and a cron research agent — a tool the owner uses to find and test money-makers, not a product he sells.
2. Entry points: static dashboard `public/app.js` (1687 lines), Pages API `functions/api/[[path]].js` (735 lines, D1), worker `worker/src/index.js` (733 lines) + shared `worker/src/lib.js` (79 lines).
3. Live surfaces: `https://aimoney.pages.dev` (+ `/api/health`, `/api/opportunities`, `/api/runs`), worker `/` status; D1 `aimoney`, Workers AI (worker only). Live rev `f43c623` = HEAD~1 (one-commit lag, F4).
4. Scheduled jobs: worker cron `0 */6 * * *` (`worker/wrangler.toml:9`); manual triage via dashboard button → `POST /run` (202, briefs skipped, `worker/src/index.js:718-729`).
5. Deploys: Pages auto-deploys from Git (`scripts/stamp-release.sh` stamps `release.json`); worker + D1 schema/migrations via `deploy/deploy.sh` or `.github/workflows/deploy-worker.yml` (both through `deploy/apply-d1-migrations.sh`); gated by `deploy/verify.sh` (public reads only).

## 2. End-to-end walk-through

Signal → proposal: cron `scheduled` (`worker/src/index.js:656`) → parallel HN/Reddit/GitHub collect (`:49, :81, :110`, 6s body-covering timeout `:37-47`) → one batched signal INSERT (`:359-364`) → 30d stale sweep to noise (`:373-377`) → take 6 oldest unprocessed (`:379-381`) → exact-URL supports pre-pass, no AI (`:391-418`, pure `exactUrlTarget` `:173`) → inflow gates: bare-without-brief > 10 caps inserts to 1 (`:431-434`), unreviewed > 10 pauses inflow to 0 and skips classify (`:435-438, :449-462`, logged `inflow:paused`) → one classify AI call (`buildClassifyPrompt` `:277`, NDJSON `parseJsonLines` in `worker/src/lib.js:64`) → verdict writes batched with per-write fallback (`:474-543`, `flushVerdictWrites` `:296`) → new rows enter `researching` with `UNREVIEWED`, score capped ≤6000 (`:501-512`) → 1–2 bare rows briefed per cron tick, oldest-unreviewed-first past 48h (`:555-639`, `buildBriefPrompt` `:207`, `insertBrief` `:229`, poison rotation `:253-270`) → single finish write with mode/counters (`:642`).

Review → money: `GET /api/opportunities` (`functions/api/[[path]].js:143`, capped scores, `needs_review` bit, brief excerpts) → dashboard ledger (`public/app.js:184`) and Needs-review filter (`:234`, oldest-first, per-row Vet / Vet-&-starter / Kill, `V`/`K` keys `:688-760` + N+1 prefetch `:524`) → human Vet (`:562`, `vettedNotes` `:485`: clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`functions/api/[[path]].js:237`, tagless-clear 400 `:271-275`, killed-clear post-mortem 400 `:282-288`) → health counts `vetted_last_7d` + `last_vetted`/`last_verdict` (`:559, :657-671`) → Vet-&-starter composes vet + planned experiment + flip to `testing` (`public/app.js:616`, resume-safe `:599`) → Start stamps `started_at` (`:825`, API `:467`) → Win/Lose inline close, closure gate result + post-mortem (API `:468-473`), outcome-ledger `$X rev / $Y spent [via source]` line appended to parent notes (API `:478-484`, shared `outcomeLedgerLine` `:103-108`) → health `decisions_last_7d` + revenue sums (`:552-554`) → dashboard week line (`public/app.js:898-933`).

Where it breaks or goes silent, ranked: (a) the human verdict step — nothing automated was ever supposed to vet (F1), so the 7.8-day stall with healthy ticks is the design, not a failure; (b) every experiment write 503s on live until the migration switch flips (F2) — the Starter POST, Start, Win, Lose, and modal save all fail today, so the experiment lane is write-blocked, not just unmeasured; (c) a hung manual `/run` POST never times out and strands the button (F3); (d) live trails HEAD by one commit, so the brief-failure and kill-guard fixes are merged but unserved (F4).

## 3. Health signals measured here

- Test suite: `npm test` (the 7 files in `package.json:8`, unmodified) → exit 0 this session: `# tests 459, # suites 145, # pass 459, # fail 0, # duration_ms 90606.34`. Runner baseline `_night/tests-baseline.log`: same 459/145/0 at 90235.77ms — same shape, no drift.
- Syntax: `node --check` on `public/app.js`, `worker/src/index.js`, `functions/api/[[path]].js` → exit 0 each (only the sandbox `EnvHttpProxyAgent` warning).
- `compileall`: N/A — zero `*.py` files (glob search over the clone; positive control: the identical search shape for `*.js` in `worker/src` returns all 4 files, so the zero is real).
- Dead code: two no-op rails in the worker — the empty `if (!fresh.length)` block (`worker/src/index.js:419-422`, comment only) and the redundant `!fresh.length ? [] :` inside `if (fresh.length)` (`:456`); the `btn-run` handler parses the 202 body (`public/app.js:1432`) but only reads `error`, so `briefs_skipped`/`reason` are a dead contract (folded into F3). `appendKeepNewest` (`worker/src/lib.js:51`) is test-only by documented design, not dead.
- Duplicated logic: `slugify` twice with different regexes (F6); `centsDollars` ↔ `moneyCents` is a documented-deliberate mirror (`functions/api/[[path]].js:97-99`); `VERDICT_TAG` gate vs health `matchAll` capture shapes differ by need (gate vs capture); `inlineWinClose` vs `inlinePostMortem` (`public/app.js:355-469`) share the input/Confirm/Cancel/Escape skeleton (~115 lines overlap) — polish, no finding.
- TODO density: zero in shipped source. Regex `TODO|FIXME|HACK|XXX` over functions/worker/public/d1/deploy/scripts hits nothing; positive control: the same tool+mode matches `UNREVIEWED` 10+ times across the same tree, so the zero is real. `console.*` appears only as the three deliberate `cron-failed` tail markers (`worker/src/index.js:650,662,726`) plus test stubs.
- Surface rules: all pass, no finding. Light-only (`public/styles.css:4` `color-scheme: light`, ink-on-paper palette, no dark query/toggle); favicon linked with theme-color (`public/index.html:9-10`) and `public/favicon.svg` (under 2 KB, `$` on `#0d6b3f`); inline brand mark in the header (`:16`, same geometry/accent/glyph); guard suite `public/tab-icon.test.mjs` green inside the exit-0 run. First five seconds: the static shell renders brand + headline + agent pill with zero JS; the pill already carries `N to review · oldest Xd`.
- Tree: HEAD `b8ae5b5`; `git ls-files --eol` shows all 41 tracked files `i/lf w/lf` — the CRLF warning in the brief does not apply to this checkout. Sizes (`wc -l`): `public/app.js` 1687, `functions/api/[[path]].js` 735, `worker/src/index.js` 733, `worker/src/lib.js` 79; tests outweigh shipped code (`api.test.mjs` 2608, `dashboard.test.mjs` 1987, `index.test.js` 1695).
- Live snapshot (`_night/live/aimoney_pages_dev_api_health.html:1`, time `2026-09-21T19:47:41Z`, rev `f43c623…` = HEAD~1): `ok:true, db:up, opportunities:27, unreviewed:11, bare_without_brief:19, experiments_by_status:{planned:2}, hours_since_last_ok_run:1.8, oldest_unreviewed_age_h:187.4, decisions_last_7d:0, revenue_last_7d:null, revenue_total:null, spent_total:null, vetted_last_7d:0, last_vetted:null, last_verdict:null, vetted_no_experiment:0, noise_24h:0, health_probe_failures:[revenue_week,revenue_lifetime], health_probe_detail:{…:revenue_cents}, schema_missing_columns:[revenue_cents,spent_cents], schema_migration:disabled…`. The queue has not moved between any two snapshots tonight; its age rises ~1d/d while cron stays healthy.

## 4. Ranked findings (max 8)

Lane metric (aimoney): experiments reaching a decision (won/lost + post-mortem) per week, and vetted proposals becoming experiments; unreviewed backlog drained. Live now: `decisions_last_7d` 0, `vetted_last_7d` 0, unreviewed 11 (oldest 187.4h / 7.8d), bare-without-brief 19/27, experiments `{planned:2}`, `last_vetted`/`last_verdict` null, `hours_since_last_ok_run` 1.8.

Money-first (owner directive), in this project's own numbers. This project is a tool the owner uses to rank and test AI-money ideas, not a product he sells — rank by decisions/week and backlog drain, measured the same way. Earned to date: none — no `won`/`lost` row has ever existed (`experiments_by_status:{planned:2}`, `decisions_last_7d:0`), and the money legs read null (unknown, not zero). Last krone arrived: never recorded. Single step between now and the next payment: flip the migration switch (F2), vet the queue (F1), Start the $0 `Spec-ad sprint` starter (`d1/seed.sql:96-100`), run it inside a week, close Win with a human-entered $ amount + post-mortem. Measured conversion at that step: 0/11 vetted (0%), 0/2 planned started (0%), 0 decisions/week. No projection as result, no rate × volume, no attempt counted as outcome. The money number: was 0 decisions/week and 0 vetted/week, is 0 and 0 now, and moves next the moment the switch flips and the first verdict lands. The single owner action that moves it, in one line: set `ALLOW_SCHEMA_MIGRATION=1` on Pages production, then open Needs-review oldest-first and press V or K on each of the 11 rows.

### F1. Eleven unreviewed, oldest 7.8 days — a human press is the only path, and it has not happened (THE ROUND)

Evidence. The path, in one sentence: a manual dashboard press — `public/app.js:562` `vetOpportunity` → shared `vettedNotes` (`:485-487`: clears `UNREVIEWED`, appends `[YYYY-MM-DD vetted]`) → `PATCH /api/opportunities/:id` (`:574-577`). The worker never vets by design: `worker/src/index.js:4-5` (`The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed)`), `README.md:72` (`researching → testing is a human decision … The agent never moves status`). Every other `UNREVIEWED` site in `index.js` is a create/count/select site — zero clearing paths (positive control: the `UNREVIEWED` matcher returns those sites, so the absence of a clearing path is real). So `vetted_last_7d` 0 with `hours_since_last_ok_run` 1.8 is the designed paused-tick shape (unreviewed 11 > 10 → inserts 0, classify skipped, `worker/src/index.js:435-438`, still briefs), not a failure — there is no failing test because nothing is broken. The passing real-path test `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs:2115`, green in this session's exit-0 run) drives the shipped `vettedNotes` through the real PATCH handler and the real `/api/health` on a stubbed D1: a single run of the path produces exactly unreviewed −1, `vetted_last_7d` +1, `last_vetted`/`last_verdict` = today. Health already dates the stall without being told (`functions/api/[[path]].js:657-671`; both null live).

Why it costs: the lane metric is frozen at every stage (0 vetted/week → 0 started → 0 decisions/week), inflow is paused at 0/tick while unreviewed > 10, and 19/27 rows lack the brief that makes a sub-minute vet possible.

Fix: no code change for the verdict itself — the two-minute path (`V`/`K` + auto-advance, N+1 prefetch, per-row buttons, inline decision + brief lines, all green) already ships. The owner opens Needs-review oldest-first and records one verdict per row from a phone. Touch no repo files. Size S (minutes). Risk none (reversible token-gated PATCHes, one human verdict per keypress, every Kill carries its required post-mortem).

### F2. The experiment lane is write-blocked on live until the migration switch flips — not just unmeasured

Evidence. Live D1 lacks `revenue_cents`/`spent_cents` (proven by `health_probe_detail`, §3). Every experiment write references those columns: create (`functions/api/[[path]].js:414-419`) and update (`:491-496`, so even `PATCH {status:"running"}` rewrites them). The old-schema retry covers only `revenue_source` (`:420-430`, `:497-509` — `if (missingColumnOf(e) !== "revenue_source") throw e`), and the router catch answers 503 naming the column (`:731-733`). So on live today the Vet-&-starter POST, Start, Win, Lose, and modal save ALL fail with `missing column: revenue_cents` (dashboard toasts `Starter failed: …` / `Start failed: …` / `Save failed: …`, `public/app.js:654,834,1388`) — the vet half of a starter tap sticks while the experiment half dies. The 2 live `planned` rows predate the money-column writes (seed `d1/seed.sql:96` inserts without money columns). The self-migration is deliberately off (`functions/api/[[path]].js:58`) and the deploy convergence is already correct (CI `.github/workflows/deploy-worker.yml:27` then `:33-34` via the shared `deploy/apply-d1-migrations.sh`, mirrored by `deploy/deploy.sh:36,40`; second run is a duplicate-column no-op, anything else exits 1). Migration-ask results, green this session in the `self-migration switch` suite (9/9): old schema + `ALLOW_SCHEMA_MIGRATION=1` ends with every probed money column and reports numbers on the same call; var unset leaves the table unchanged and names the missing columns; second run is a clean no-op. Exact variable name: `ALLOW_SCHEMA_MIGRATION`. Exact CI step: `.github/workflows/deploy-worker.yml:33-34`.

Why it costs: the switch doesn't just fill numbers — it unblocks the entire experiment lane. Until it flips, the owner cannot log, start, or close a single experiment, so the money number cannot move.

Fix: no code change (tolerating missing cents columns on writes would silently drop money — the 503 is the honest answer). The owner sets `ALLOW_SCHEMA_MIGRATION=1` as a Pages production env var; the next `/api/health` PRAGMAs `experiments`, applies exactly the two ADD COLUMNs one at a time, re-runs the failed money probes on the same call, and unblocks writes. Touch no repo files. Size S. Risk none (ADD COLUMN with DEFAULT cannot lose a row; unset behaviour is byte-identical to today).

### F3. The manual triage POST bypasses the dashboard's timeout and ignores the 202 disclosure

Evidence. The `btn-run` handler uses a bare `fetch` (`public/app.js:1429-1431`), the only write path that does not go through `api()` (`:29-64`, 15s reads / 25s writes, `:19-27`). If the worker hangs, the `await` never settles: the button stays disabled reading `Triaging…` (`:1425-1427` run before the `await`, re-enable `:1439-1440` after it) until the browser gives up — no toast, no recovery except reload. It also parses the 202 body (`:1432`) but only reads `error`, so the `briefs_skipped:true` + `reason` disclosure the worker ships (`worker/src/index.js:728`, pinned by `index.test.js` `manual run disclosure`) is parsed and discarded; the toast (`:1434`) hardcodes the same sentence the server already sends.

Why it costs: failure visibility on the owner's own trigger — the one button he presses to force progress is the one request that can hang silently, and its honest server disclosure never reaches him.

Fix: route the POST through `api()` (absolute URLs pass straight into `fetch`, auth + timeout + error contract come free), restoring the button label in a `finally`, and render the returned `reason` in the success toast instead of the hardcoded sentence. Pin with dashboard tests in the existing timeout style: a hung POST rejects within the write timeout with the button re-enabled; a 202 surfaces the server `reason`; 401 and missing-`worker_url` paths unchanged. Touch `public/app.js`, `public/dashboard.test.mjs` (pin only). Size S. Risk low (one handler; no API/worker change; timeout path already proven by the suite).

### F4. Live trails HEAD by one commit — the brief-failure and kill-guard fixes are merged but unserved

Evidence. Live `/api/health` reports `rev f43c623…`, the parent of HEAD `b8ae5b5` (`git log --oneline -8`). `git show --stat b8ae5b5` deltas: (1) a main-brief AI throw degrades to `brief:failed` + ok (`worker/src/index.js:578-596`, `try` around `aiComplete`) — on live it still errors a tick whose triage already flushed; (2) a killed `UNREVIEWED` clear with a bare tag 400s naming the post-mortem (`functions/api/[[path]].js:282-288`) — on live it still passes and the row exits the queue with a tag and no post-mortem; (3) dropped-source toast wording + `docs/OWNER-ACTIONS.md` §2 second track — copy only.

Why it costs: trust, not code — the repository is right and the owner's phone does not reflect it. The kill-guard gap is the sharp edge: a bare-tag kill on live today leaves a killed row with no post-mortem, the one artefact killed rows exist to keep.

Fix: no code change. The owner deploys HEAD (or checks the Pages dashboard for the queued build); the next audit confirms live rev == HEAD. Touch no repo files. Size S. Risk none (deploy-only; `deploy/verify.sh` gates the rollout on public reads).

### F5. Experiment-write 503s name the column but not the remedy the owner needs

Evidence. Read-path failures already get the one-liner: health attaches `schema_missing_columns` + `schema_migration: disabled (set ALLOW_SCHEMA_MIGRATION=1 …)` and the dashboard banner names probes, columns, and the fix (`README.md:79`). Write-path failures do not: every experiment-write catch toasts the raw `e.message` (`public/app.js:654,834,854,886,1388`), so on live today the owner sees `Starter failed: missing column: revenue_cents` with no hint that one env var fixes it — at exactly the moment F2 blocks him.

Why it costs: owner minutes on the critical path — an honest-but-unactionable error where the actionable sentence already exists one surface over.

Fix: add one pure helper mapping a `missing column: revenue_cents|spent_cents` error to the existing switch one-liner, and use it in the five experiment-write catches (starter, Start, Lose, Win, modal save); every other error renders byte-identical. Pin with dashboard tests: both cents columns map, `revenue_source` and non-column errors pass through untouched. Touch `public/app.js`, `public/dashboard.test.mjs` (pin only). Size S. Risk low (toast text only; no write-shape change).

### F6. Manual-add slugs diverge from the shared slugify (leading/trailing dashes kept)

Evidence. `createOpportunity` inlines its own slug (`functions/api/[[path]].js:206`: `toLowerCase().replace(/[^a-z0-9-]+/g, "-").slice(0, 80)`) instead of the shared `slugify` (`worker/src/lib.js:9-11`: same replacement over `[^a-z0-9]+`, then trims leading/trailing dashes). Manual `Foo!` stores `foo-`; the identical agent title stores `foo` — near-duplicate slugs for the same title, defeating the slug-collision link-as-supports intent (`worker/src/index.js:517-529`) for manual rows. (`clamp10` was already deduped to the shared import, `functions/api/[[path]].js:10` — this is the remaining copy.)

Why it costs: future divergence, near-zero today — polish, ranked last with F7.

Fix: import the shared `slugify` in the API (same one-line pattern as `clamp10`) and pin with API tests that manual adds match agent slugs byte-for-byte including dash-trimming. Touch `functions/api/[[path]].js`, `functions/api/api.test.mjs` (pin only). Size S. Risk low (pure function, heavily tested).

### F7. Two no-op worker rails and one redundant API wrap (polish, rank last)

Evidence. `worker/src/index.js:419-422` is an `if (!fresh.length)` block containing only a comment; `:456` re-tests `!fresh.length ? [] :` inside a block already guarded by `if (fresh.length)`; `functions/api/[[path]].js:675-678` wraps `decisionsWeekRow`/`revenueWeekRow`/`lifetimeRow` into single-key objects only to unwrap them at `:704`.

Why it costs: nothing today — reading friction only, ranked last per the money directive.

Fix: delete the empty block, drop the redundant ternary (keep `parseJsonLines(…)`), read the probe rows directly in the payload. No test changes needed beyond the suite staying green. Touch `worker/src/index.js`, `functions/api/[[path]].js`. Size S. Risk low (no behaviour change; suite pins every surrounding shape).

## 5. What NOT to change

- The agent never vets, by documented design. `worker/src/index.js:4-5`: `The agent PROPOSES; the human owns the testing workflow (it never moves status to testing/scaling/killed).` `README.md:72`: `researching → testing is a human decision … The agent never moves status.` Do not add auto-vet, auto-kill, auto-testing, or bulk verdicts without an owner-written rule, reversibility, and a real-path test.
- The self-migration stays default-off, ADD-only, triggerless. `functions/api/[[path]].js:16-27` (switch docblock), `:58` (unset → report + change nothing), `:63-76` (one ADD at a time, duplicate-column tolerated, anything else stays missing), once per isolate for decided outcomes. Do not default it on, do not add a second trigger, never a migration endpoint: `a URL that alters the schema is a liability even when it is authenticated.` ADD only — `No DROP, no ALTER of an existing column, no data rewrite, no DELETE.`
- Never invent a revenue figure and never mark an experiment decided without evidence; won/lost keep requiring `result` + `post_mortem` (API `:468-473`), kills keep requiring the post-mortem line (API `:282-288`).
- Deliberate as documented: `appendKeepNewest` is a `Pinned JS spec of that SQL idiom (no shipped importer; the lib suite pins the rule)` (`worker/src/lib.js:48-54`); closing an orphaned experiment succeeds without a ledger line (test `closing an orphaned experiment still succeeds without a parent row`); `ADMIN_TOKEN` rotation is manual (`No expiry; rotate manually when shared or leaked`, `README.md:88`).
- Anything needing a login, credential, payment, or human judgment is out of round scope. Owner one-liners (his actions, not tasks): press one verdict per row on the 11 Needs-review rows oldest-first (`V` vets, `K` kills, ~1 min/row from a phone — clearing one row reopens inflow); set `ALLOW_SCHEMA_MIGRATION=1` in Pages production env + retry the deployment (unblocks experiment writes AND fills the money legs); add `CF_API_TOKEN` + `CF_ACCOUNT_ID` to this repository's Settings → Secrets → Actions so CI can deploy the worker and apply D1 migrations (unverifiable offline; without them every CI run stops at the fail-fast guard, `.github/workflows/deploy-worker.yml:20-26`); press Start on the $0 `Spec-ad sprint` and drive it to Win/Lose with a post-mortem inside a week; deploy HEAD so live matches the repo (F4).

## 6. Proposed next tasks (2–4)

- [ ] Task 1: Re-run the real-path Vet test; report one-run unreviewed/vetted delta
  - Owned files: `functions/api/api.test.mjs` (read; no code change expected).
  - Why first: this is the focus's vet round, worded for the implementer (F1): the path is `public/app.js:562`, the cause is human-gated-by-design (no automation ever vets), and the real-path pin already exists. Clearing live rows is NOT the task — it needs network + live D1 and belongs to the owner (§5), and a task that cannot be finished is not a task.
  - Acceptance: `npm test` green with `Vet PATCH clears UNREVIEWED and health counts +1 vetted with last_vetted today` (`functions/api/api.test.mjs:2115`) passing; report quotes the single-run delta (unreviewed 2→1, vetted 0→1, `last_vetted`/`last_verdict` = today); negative proof included — no worker path clears `UNREVIEWED`, cited with the positive control that the matcher does find the create/count/select sites; if the pin is red, fix the path, never weaken the test.

- [ ] Task 2: Route manual /run POST through timed api(); surface 202 reason
  - Owned files: `public/app.js`, `public/dashboard.test.mjs`.
  - Acceptance: the `btn-run` handler (`public/app.js:1421-1441`) goes through `api()` so a hung POST rejects within the 25s write timeout with the button re-enabled and its label restored (no reload needed); the success toast renders the server's 202 `reason` instead of the hardcoded sentence; 401 and missing-`worker_url` paths unchanged; new dashboard tests pin the timeout, the reason surfacing, and the unchanged guards; `npm test` green.

- [ ] Task 3: Map experiment-write 503s on money columns to the switch hint
  - Owned files: `public/app.js`, `public/dashboard.test.mjs`.
  - Acceptance: a `missing column: revenue_cents|spent_cents` failure in any of the five experiment-write catches (starter, Start, Lose, Win, modal save) toasts the existing `ALLOW_SCHEMA_MIGRATION=1` one-liner alongside the error; `revenue_source` and all non-column errors render byte-identical; new dashboard tests pin both cents columns plus a pass-through case; `npm test` green.
